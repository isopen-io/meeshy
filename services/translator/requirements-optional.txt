# =============================================================================
# OPTIONAL/LEGACY DEPENDENCIES - Translator Service
# =============================================================================
# These packages have special requirements and are NOT installed by default.
# Install manually based on your needs.
#
# NOTE: Core TTS/STT features use:
#   - STT: faster-whisper (mandatory, Python 3.12 compatible)
#   - TTS: chatterbox-tts (mandatory default, Python 3.12 compatible)
# =============================================================================

# ─────────────────────────────────────────────────────────────────────────────
# XTTS v2 (Coqui TTS) - LEGACY TTS Model
# ─────────────────────────────────────────────────────────────────────────────
# ⚠️ CONSTRAINTS:
#   ❌ Coqui Public License - NO commercial use allowed
#   ❌ Requires Python < 3.12 (incompatible with Python 3.12+)
#   ❌ Coqui closed in 2024, model no longer maintained
#
# Installation (Python 3.11 only):
#   pip install TTS>=0.22.0
#
# Usage: Set TTS_MODEL=xtts-v2 in environment
# ─────────────────────────────────────────────────────────────────────────────
TTS>=0.22.0

# ─────────────────────────────────────────────────────────────────────────────
# SPEAKER DIARIZATION - Identification des locuteurs
# ─────────────────────────────────────────────────────────────────────────────
# ℹ️ NOTE: Les dépendances de diarisation (pyannote.audio, scikit-learn) sont
#   maintenant INCLUSES dans requirements.txt (installation par défaut)
#
# La diarisation est activée automatiquement:
#   ENABLE_DIARIZATION=true (par défaut dans .env)
#   HF_TOKEN=your_token (optionnel mais recommandé pour pyannote.audio)
#
# Voir requirements.txt pour les dépendances installées automatiquement.
# ─────────────────────────────────────────────────────────────────────────────

# ─────────────────────────────────────────────────────────────────────────────
# OpenVoice V2 - Advanced Voice Cloning (Optional)
# ─────────────────────────────────────────────────────────────────────────────
# ⚠️ CONSTRAINTS:
#   ❌ Requires Python 3.9 or 3.10 (dependency PyAV incompatible with 3.11+)
#   ✅ Optional: Voice cloning works with Chatterbox Multilingual if unavailable
#
# Installation automatique:
#   ./install-openvoice.sh
#
# Installation manuelle (Python 3.9-3.10 only):
#   pip install git+https://github.com/myshell-ai/OpenVoice.git
#
# ℹ️ Si OpenVoice indisponible:
#   Le service utilisera Chatterbox Multilingual pour le clonage vocal
#   - Support de 23 langues
#   - Clonage vocal natif de haute qualité
#   - Aucune configuration supplémentaire requise
# ─────────────────────────────────────────────────────────────────────────────
