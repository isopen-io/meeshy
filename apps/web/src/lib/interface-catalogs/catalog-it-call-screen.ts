/**
 * LE PARTAGE D'ÉCRAN PENDANT UN APPEL (#8063) — tranche du catalogue,
 * RÉPANDUE par `catalog-it.ts` comme `catalog-it-call-devices.ts` : le
 * bouton Écran de l'écran d'appel, la pastille de celui qui partage et la
 * bannière de celui qui regarde (`call-screen.tsx`).
 */
const itCallScreen = {
  'call.screen.share': 'Condividi schermo',
  'call.screen.stop': 'Interrompi condivisione schermo',
  'call.screen.sharing': 'Stai condividendo lo schermo',
  'call.screen.peerSharing': '{name} sta condividendo lo schermo',
} as const;

export default itCallScreen;
