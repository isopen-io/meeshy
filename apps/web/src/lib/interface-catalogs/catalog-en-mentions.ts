import type { MentionCatalogSlice } from './catalog-fr-mentions';

/** The composer's mention list (#7826) — see `catalog-fr-mentions.ts`. */
const enMentions = {
  'composer.mention.suggestions': 'Mention suggestions',
  'composer.mention.empty': 'No one found',
  'composer.mention.count': 'Mention suggestions: {count}',
  'composer.mention.badge.friend': 'Contact',
} satisfies MentionCatalogSlice;

export default enMentions;
