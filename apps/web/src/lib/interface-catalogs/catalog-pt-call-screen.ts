/**
 * LE PARTAGE D'ÉCRAN PENDANT UN APPEL (#8063) — tranche du catalogue,
 * RÉPANDUE par `catalog-pt.ts` comme `catalog-pt-call-devices.ts` : le
 * bouton Écran de l'écran d'appel, la pastille de celui qui partage et la
 * bannière de celui qui regarde (`call-screen.tsx`).
 */
const ptCallScreen = {
  'call.screen.share': 'Partilhar ecrã',
  'call.screen.stop': 'Parar partilha de ecrã',
  'call.screen.sharing': 'Está a partilhar o seu ecrã',
  'call.screen.peerSharing': '{name} está a partilhar o ecrã',
} as const;

export default ptCallScreen;
