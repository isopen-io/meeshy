/**
 * LE CHAMP DE MOT DE PASSE — tranche du catalogue `catalog-fr.ts`, extraite pour
 * tenir le budget de taille (CLAUDE.md) : le catalogue la RÉPAND. Le bouton œil
 * de `components/password-input.tsx` (#8054).
 */
const frPassword = {
  'password.show': 'Afficher le mot de passe',
  'password.hide': 'Masquer le mot de passe',
} as const;

export default frPassword;
