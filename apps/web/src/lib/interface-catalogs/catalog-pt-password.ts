/**
 * LE CHAMP DE MOT DE PASSE — tranche du catalogue `catalog-pt.ts`, extraite pour
 * tenir le budget de taille (CLAUDE.md) : le catalogue la RÉPAND. Le bouton œil
 * de `components/password-input.tsx` (#8054).
 */
const ptPassword = {
  'password.show': 'Mostrar a senha',
  'password.hide': 'Ocultar a senha',
};

export default ptPassword;
