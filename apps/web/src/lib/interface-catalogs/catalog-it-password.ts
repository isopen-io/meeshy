/**
 * LE CHAMP DE MOT DE PASSE — tranche du catalogue `catalog-it.ts`, extraite pour
 * tenir le budget de taille (CLAUDE.md) : le catalogue la RÉPAND. Le bouton œil
 * de `components/password-input.tsx` (#8054).
 */
const itPassword = {
  'password.show': 'Mostra la password',
  'password.hide': 'Nascondi la password',
};

export default itPassword;
