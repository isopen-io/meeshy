/**
 * LE CHAMP DE MOT DE PASSE — tranche du catalogue `catalog-en.ts`, extraite pour
 * tenir le budget de taille (CLAUDE.md) : le catalogue la RÉPAND. Le bouton œil
 * de `components/password-input.tsx` (#8054).
 */
const enPassword = {
  'password.show': 'Show password',
  'password.hide': 'Hide password',
};

export default enPassword;
