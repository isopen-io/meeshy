// Real v1 page for `/story/[postId]` — a story is a Post (type STORY); renders
// the same post-detail screen (reads `postId` from the route segment). NOT a
// rewrite to /v2.
export { default } from '@/app/feeds/post/[postId]/page';
