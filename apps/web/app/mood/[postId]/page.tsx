// Real v1 page for `/mood/[postId]` — a mood/status is a Post (type STATUS);
// renders the same post-detail screen (reads `postId` from the route segment).
// NOT a rewrite to /v2.
export { default } from '@/app/feeds/post/[postId]/page';
