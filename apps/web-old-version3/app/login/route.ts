import { PORTE_DE_CONNEXION } from '@/app/authentification/porte';

/** `/login` — « Connexion ». La porte vit dans `app/authentification/porte.ts`. */
export const GET = PORTE_DE_CONNEXION.GET;
export const POST = PORTE_DE_CONNEXION.POST;
