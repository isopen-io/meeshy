# Matchfile for Meeshy iOS Code Signing
# Uses SSH deploy key for Git access in CI

git_url(ENV["MATCH_GIT_URL"] || "git@github.com:isopen-io/meeshy-certificates.git")

storage_mode("git")

type("appstore")

app_identifier(["me.meeshy.app"])

team_id(ENV["FASTLANE_TEAM_ID"] || "D72UK7R5RE")
