import { PORTE_D_INSCRIPTION } from '@/app/authentification/porte';

/** `/signup` — « Créer un compte ». La porte vit dans `app/authentification/porte.ts`. */
export const GET = PORTE_D_INSCRIPTION.GET;
export const POST = PORTE_D_INSCRIPTION.POST;
