package me.meeshy.core.datastore

/**
 * Preference storage — `DataStore` + `EncryptedSharedPreferences` wrappers,
 * per-user namespaced (ARCHITECTURE.md §2; ADR-013).
 *
 * Populated in Phase 3: app/theme/language preferences and the local-first
 * user-preference repositories.
 */
internal object DataStoreModulePlaceholder
