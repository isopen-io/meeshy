package me.meeshy.core.database

/**
 * Room database — the single source of truth (ARCHITECTURE.md §3, §4; ADR-004).
 *
 * Populated in Phase 3: entities, DAOs, FTS5 search, migrations, the SWR
 * `cacheFirstFlow` backing store, and the lane-partitioned `outbox` table.
 * SQLCipher whole-DB encryption per ADR-013.
 */
internal object DatabaseModulePlaceholder
