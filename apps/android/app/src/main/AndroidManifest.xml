<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Audio/video calls (WebRTC media capture) -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />
    <!-- Feed post composer: "add my location" attachment (device fix, no map/geocoding yet). -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- Android 15+ : détection « l'écran de l'app est enregistré » pendant un
         appel → alerte privacy du pair (call:screen-capture-detected). Permission
         normale (aucun prompt), inerte sous l'API 35. -->
    <uses-permission android:name="android.permission.DETECT_SCREEN_RECORDING" />
    <!-- A ringing call raises a full-screen intent (FCM incoming-call notification). -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />

    <application
        android:name=".MeeshyApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Meeshy">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.Meeshy.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Deep link: meeshy://conversations/{id} — autoVerify retire : un
                 scheme custom ne peut pas etre verifie (source de l'erreur lint
                 AppLinkUrlError), la verification appartient au filtre https. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="meeshy" />
            </intent-filter>
            <!-- App Link : le lien du mail magic link (https://meeshy.me/auth/magic-link?token=)
                 ouvre l'app directement. La verification s'appuie sur
                 apps/web/public/.well-known/assetlinks.json, qui doit contenir le
                 SHA-256 de la cle de signature RELEASE. Procedure (obtenir + poser
                 l'empreinte, verifier) : apps/android/docs/magic-link-app-links.md -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="meeshy.me" android:pathPrefix="/auth/magic-link" />
            </intent-filter>
            <!-- Deep link: meeshy://chat/{conversationId} -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="meeshy" android:host="chat" />
            </intent-filter>
            <!-- Deep link: meeshy://profile/{userId} -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="meeshy" android:host="profile" />
            </intent-filter>
        </activity>

        <!-- FCM Push -->
        <service
            android:name=".push.MeeshyFcmService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Bouton « Refuser » de la notification d'appel (CallStyle) -->
        <receiver
            android:name=".push.DeclineCallReceiver"
            android:exported="false" />

        <!-- Home-screen widget: unread-count scaffold (feature-parity.md
             "Home-screen widgets" — first sub-slice). Parity target: iOS
             MeeshyWidgets.UnreadCountWidget. -->
        <receiver
            android:name=".widget.UnreadCountWidgetReceiver"
            android:exported="false"
            android:label="@string/widget_unread_count_title">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/unread_count_widget_info" />
        </receiver>

        <!-- Home-screen widget: recent conversations (feature-parity.md
             "Home-screen widgets" — second sub-slice, after unread-count).
             Parity target: iOS MeeshyWidgets.RecentConversationsWidget. -->
        <receiver
            android:name=".widget.RecentConversationsWidgetReceiver"
            android:exported="false"
            android:label="@string/widget_recent_conversations_title">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/recent_conversations_widget_info" />
        </receiver>

        <!-- WorkManager initialization -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <!-- Shares exported GDPR data files from the cache dir (feature-parity §L). -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
