## auth-session
QUICK WINS:
- APIClient 401 sur endpoints isRefreshOrAuth : mapper vers MeeshyError.auth(.invalidCredentials) en gardant le message serveur + retirer l'appel handleUnauthorized — corrige d'un coup le message « Session expirée » sur mauvais mot de passe ET mauvais code 2FA (APIClient.swift:535-538)
- MeeshyApp.swift:281 : remplacer `catch APIError.serverError` par `catch MeeshyError.server(let code, _) where (400..<500).contains(code)` — stoppe le replay infini des actions settings en 4xx
- SessionManager.clearSessions : accepter un userId explicite capturé avant `currentUser = nil` (ou wiper dans AuthManager.logout) — les clés E2EE sortent réellement du Keychain
- handleAppLevelDeepLink : guard `!authManager.isAuthenticated` (ou logout complet préalable) avant validateMagicLink — ferme la bascule de compte sans teardown
- DeepLinkRouter.handleCustomScheme : ajouter l'alias `case "c"` à côté de "conversation"
- AuthManager.logout : capturer token/sessionToken avant le wipe et les passer à performServerLogoutWithRetries
- checkExistingSession : borner le refresh proactif (race timeout ~4s) pour que le splash ne dépende jamais d'un URLSession 60s
- AuthManager.swift:563 : `AuthService.shared.me()` → `self.authService.me()`
NOTES: Audité en lecture seule : AuthManager/AuthService/AuthModels/APIClient/MeeshyError/KeychainManager/SessionSnapshotStore (SDK), MeeshyApp/LoginView/OnboardingFlowView/RegistrationViewModel/EmailVerificationViewModel/JoinFlow/GuestConversationContainer/AnonymousSessionStore/E2ESessionManager/DeepLinkRouter/RootView/ActiveSessionsViewModel (app), avec vérification croisée gateway (auth/login.ts confirme 401 « Identifiants invalides » et 401 2FA ; magic-link répond 400 donc non affecté par le bug sessionExpired). Points sains vérifiés : restauration cold start cache-first instantanée (user Keychain décodé et isAuthenticated=true AVANT tout réseau, corruption gérée par drop+log) ; revalidation stale-while-revalidate de /auth/me avec distinction .auth vs erreurs transitoires ; requireReauthentication préserve le saved account ; teardown logout doublement couvert (AuthManager.logout + onChange MeeshyApp) hors défaut E2EE cité ; refreshSession sérialisé via tokenRefreshTask (pas de refresh storm) ; deep links invités correctement gatés par hasCheckedSession ; anonymousSessionToken correctement prioritisé sous authToken et nettoyé au deinit gardé par hasStarted ; X-Session-Token OK. Le défaut P0 (magic link en étant connecté) est aggravé par le fait que CacheCoordinator n'est pas namespacé par userId (admis en commentaire MeeshyApp:615). Non retenus (jugés intentionnels) : suppression du saved account au logout explicite, activeGuestSession=nil sans leaveAnonymousSession au login, fail-open des checks de disponibilité pseudo/email en cas d'erreur réseau.

## conversations-list
QUICK WINS:
- Inverser/supprimer le guard isCacheValid de handleForegroundReturn (1 ligne) pour que les stories se rafraîchissent au retour foreground après long background.
- Recherche : remplacer c.name par c.displayName dans filterConversations (1 ligne) pour retrouver les conversations renommées.
- Reset des champs compagnons (senderName, attachments, isBlurred/isViewOnce/expiresAt) dans le handler conversationUpdated quand lastMessageId change — évite les previews « Vue unique »/mauvais auteur fantômes.
- Ajouter loadState == .idle au gate skeleton + un état « Aucun résultat » quand searchText est non vide (supprime le flash du CTA de création au cold start).
- Logger + exposer l'erreur dans NewConversationViewModel.performSearch (2 lignes).
- Purger le plumbing mort B1 si le backend ne ship pas lastMessageTranslations : le paramètre preferredContentLanguages traverse 3 structs et 2 Equatable pour un résultat toujours identique au preview brut.
NOTES: Architecture globalement solide : cache-first réel (fresh/stale/expired/empty distingués partout dans le VM), pagination par curseur avec garde anti-boucle persistée, mutations per-user optimistes via ConversationStore + outbox, rows Equatable avec renderFingerprint, badge unread câblé bout-en-bout (unread-updated → cache → conversationsDidChange → reloadFromCache). Le trio de P1 est concentré sur (1) le Prisme de la preview jamais alimenté (machinerie complète mais zéro producteur de données), (2) les champs compagnons du dernier message non resynchronisés sur les bumps temps réel, (3) le pull-to-refresh qui détruit le disque avant le fetch. Point de vigilance non compté comme défaut faute de repro déterministe : double écrivain sur le blob cache \"list\" (schedulePersist du VM, débouncé 200 ms, vs updates ciblés du ConversationSyncEngine) — la protection recentlyCreatedAt ne couvre que les rows insérées par le VM lui-même ; une prepend du sync engine peut théoriquement être clobbée par un persist VM capturé avant elle. Vérifié sans défaut : résolution couleur d'accent (accentColor partout dans le contexte conversation, sémantiques error/success seules exceptions), présence via PresenceState central (pas de redéclaration de couleurs), typing multi-membres avec source per-user, adaptiveOnChange utilisé systématiquement, deinit nonisolated propres, discovery des nouvelles conversations à 3 chemins dédupliqués (conversation:new / notification:new legacy / CONVERSATION_UPDATED).

## messaging-ui
QUICK WINS:
- Déplacer `messageStore.loadOlder(before:)` avant le fetch réseau (ou l'ajouter au catch) dans loadOlderMessages — 3 lignes, restaure la pagination offline.
- Ajouter `lhs.mentionDisplayNames == rhs.mentionDisplayNames` au `==` de ThemedMessageBubble.
- Remplacer les 4 `"fr"` codés en dur / `?? "fr"` (lignes 2328, 2469, 2688, 2730 de ConversationViewModel) par `Self.composeLanguage(for:preferred:)`.
- Supprimer le fallback `?? viewModel.messages.first` du full reaction picker (retourner sans ouvrir).
- Copier `preferredTranslation?.translatedContent ?? msg.content` dans les deux actions Copier.
- Supprimer failedMessageBar (mort) et brancher removeFailedMessage sur le delete d'un message .failed sans serverId.
- Remplacer `@ObservedObject theme = ThemeManager.shared` d'AudioPlayerView par un paramètre isDark ou @Environment(colorScheme).
NOTES: Audit en lecture seule (aucune modification, aucun test exécuté). Qualité générale élevée : le cœur du Prisme est correct (preferredTranslation retourne nil si l'original matche la langue préférée — jamais translations.first ; resolveEffectiveContent retombe sur l'original, BubbleContentBuilder.swift:237-242), le cache-first à l'ouverture est exemplaire (loadMessages gère .fresh/.stale/.expired/.empty cas par cas, skeleton uniquement sur cache vide — ConversationView.swift:995), l'accentColor vient bien de conversation.accentColor (ConversationView.swift:349-351), et l'envoi optimiste texte (GRDB + outbox + dedup clientMessageId + socket-first) est robuste. Les défauts les plus graves se concentrent sur les CHEMINS DE REPLI (retry manuel, offline, échec REST) qui n'ont manifestement pas la même couverture que le happy path : retryMessage (P1) et la pagination offline (P1) sont les deux correctifs prioritaires. Le retryMessage P1 est partiellement atténué par le fait que la plupart des échecs média passent par l'outbox durable (rejeu automatique avec attachments préservés) — le chemin cassé est le retry MANUEL après épuisement (barre orange BubbleFailedRetryBar), qui est précisément le moment où l'utilisateur agit.

## social-feed
QUICK WINS:
- FeedPostCard.effectiveContent : retomber sur post.displayContent (résolution Prisme B2 déjà correcte) et supprimer la dérivation currentDisplayLangCode par translations.keys.first — corrige le P1 Prisme en ~10 lignes
- FeedModels.FeedPost : ajouter repostCount/bookmarkCount/shareCount/viewCount/postOpenCount/impressionCount/qualifiedViewCount/playCount + isBookmarkedByMe/isRepostedByMe aux CodingKeys avec decodeIfPresent (motif déjà appliqué à FeedMedia.translatedAudios)
- FeedView.sharePostWithLink : déplacer le bump de postShareDelta APRÈS le succès REST (copier le motif ProfileUserPostsList.share:213-214)
- FeedViewModel handler comment:added : mapper media/effectFlags/currentUserReactions + resolveCommentTranslation (copier le handler de PostDetailViewModel.subscribeToSocket)
- Supprimer (ou câbler) les 4 méthodes durables mortes : FeedViewModel.likePost, FeedViewModel.bookmarkPost, PostDetailViewModel.likePost, et le paramètre onSendComment jamais invoqué de CommentsSheetView
- FeedViewModel.clearTranslationOverride : remplacer par posts[index].resolved(preferredLanguages:)
NOTES: Audit en lecture seule du feed social iOS (apps/ios/Meeshy + packages/MeeshySDK), toutes les citations vérifiées dans le code réel, plus une contre-vérification gateway (postIncludes.ts take:3) pour le défaut de la feuille de commentaires. Constats transverses : (1) trois implémentations parallèles du like coexistent (toggle socket+REST dans FeedView/PostDetailView/ReelsViewModel, chemin OfflineQueue durable dans les ViewModels — mort, et REST simple dans ProfileUserPostsViewModel) — la promesse offline du feed n'est tenue que pour la création de post et le commentaire top-level du détail ; (2) le Codable de FeedPost est la cause racine de plusieurs symptômes cache-first (compteurs à 0, flags perdus) car la fenêtre .fresh de 5 min ne déclenche AUCUNE revalidation ; (3) points sains vérifiés : ReelsViewModel (optimistic+rollback corrects, dédup du share), ProfileUserPostsViewModel (overrides réconciliés serveur), mergePreservingRealtimeHead, seeding likedIds/currentUserReactions, bannière de profil (CachedAsyncImage + fallback gradient OK), résolution Prisme canonique resolved()/resolveTranslation/resolveCommentTranslation conforme (jamais translations.first). Les compteurs vues/impressions sont correctement séparés (viewCount dédupliqué non affiché, postOpenCount œil, impressionCount chart) avec throttle 1h persistant côté profil ; le flush d'impressions ne marque recorded qu'en cas de succès (retry OK) dans les deux surfaces.

## stories
QUICK WINS:
- Réparer mergingTextObjectTranslations en 3 arguments : passer viewedAt/updatedAt/impressionCount dans le re-init (StoryModels.swift:1879) + test round-trip pinnant tous les champs de StoryItem
- Ajouter `.adaptiveOnChange(of: currentStory?.commentCount)` dans StoryViewerView.swift:618, miroir exact de celui des réactions trois lignes au-dessus
- Logger la branche visuelle sautée dans runStoryUpload (StoryViewModel.swift:1392) en parité avec le log audio « clip will be uploaded but unplayable » existant ligne 1408
- Catch APIError.serverError(409) dans StoryInteractionService.react et rollback de l'emoji/compteur optimiste dans triggerStoryReaction (snapshot avant append)
- Router sendComment de story par OfflineQueue (.createComment) — le payload et l'OutboxDispatcher existent déjà (copier FeedViewModel.swift:727)
- Supprimer les chemins morts : publishStory/publishStorySingle, handlePublishTap+StubOnlinePublisher+StoryVisibility, et le fullScreenCover repost-composer jamais présentable (binding jamais assigné)
NOTES: Audit lecture seule du domaine Stories iOS (apps/ios/Meeshy + packages/MeeshySDK). Points VÉRIFIÉS CONFORMES (non défauts) : garde file:// reader-only correctement gatée sur resolver != nil (StoryMediaLayer.swift:336 avec fileExists) et fallback resolver côté StoryBackgroundLayer:608-624 ; loop background-only respecté (StoryMediaLayer:461 « loop: mode != .play », renderBackground looping:true pour le bg) ; canvas statique en fin de playback (advancePlayheadIfActive → stopPlayback, StoryCanvasUIView+Playback.swift:270-276) ; Prisme Linguistique conforme partout (preferredContentLanguages ordonné system>regional>custom>deviceLocale>fr, resolvedContent sans fallback translations.first, chaîne complète passée au reader, override d'exploration éphémère) ; realtime des nouvelles stories câblé (storyCreated → insertOrMergeStoryGroups, subscribe depuis RootView:559 et iPadRootView:137, garde anti-double-sink) ; cache-first tray avec skeleton uniquement sur cache vide + SWR delta R8 ; sanitizer file:// appelé dans PostService.createStory:221 ; publication RAW sans bake (aucun appel prepareExport dans runStoryUpload) ; expiry 21h aligné client/serveur ; présence via provider sans @ObservedObject singleton dans les leaf cells du tray ; story:viewed émis au seul author room (pas de faux « vu »). Les défauts les plus graves concernent les ÉCRITURES du viewer (réactions/commentaires : pas d'OfflineQueue, pas de rollback, 409 reachable avalé) — en contraste net avec markViewed (outbox durable R6) et avec le pipeline de publish (write-ahead E5), qui sont eux exemplaires. COMMUNITY : sélectionnable au composer (PostVisibility.composerSelectableCases) et transmis verbatim via visibility String — pas de blocage iOS constaté ; seule l'enum morte StoryVisibility du chemin timeline l'omet.

## reels-video
QUICK WINS:
- SharedAVPlayerManager : déplacer le nil de attachmentId hors de cleanup() (ou API load(urlString:attachmentId:)) — 1 ligne, ressuscite tout le tracking watched vidéo
- ConversationMediaGalleryView:648 : poser videoManager.isMuted = false (ou l'intention voulue) avant play() — corrige les vidéos muettes après feed
- ReelsViewModel.fetch : ajouter un Logger.error dans le catch et ne plus poser hasMore=false sur erreur réseau
- VideoTransportControls:152 : String(localized:) sur les 2 labels mute
- ReelsPlayerView.onDisappear : ajouter un SharedAVPlayerManager.stop() conditionnel (activeURL ∈ reels) pour couvrir la fuite du player à la fermeture
- AudioPlayerView:821/825 : remplacer .onChange brut par adaptiveOnChange (helper déjà dans le package)
NOTES: Audit lecture seule de apps/ios (ReelsPlayerView, ReelFeed*, ReelsViewModel, ReelRepostEmbedCell, VideoAvailabilityResolver, ConversationMediaGalleryView, BackgroundTransitionCoordinator) et MeeshySDK/MeeshyUI (SharedAVPlayerManager, PlaybackCoordinator, MediaSessionCoordinator, MediaThermalPolicy, AudioPlayerView, AdaptiveVerticalPager, VideoTransportControls). Points sains vérifiés : optimistic like/bookmark avec rollback corrects (ReelsViewModel), cache-first seed du pager (feed cache partagé), call-awareness complète (gates isCallActive + resume F3 sur fin d'appel), pattern .equatable() sur ReelFeedCard/ReelPoster/ReelImageBackdrop, boucle vidéo réaffirmée après load() (fix scrub documenté), PiP delegate correctement retenu, deinit AudioPlaybackManager libère la session (refcount). Le défaut iOS 18 (élection onPreferenceChange) est étayé par la documentation du même package (MeeshyRefreshableScroll:100 + ScrollOffsetTracking) qui contourne exactement cette régression pour le header mais pas pour l'élection réel — non exécuté sur simulateur dans cet audit. Le défaut duckOthers est structurel (session .playback+duckOthers activée par load() même muet, jamais désactivée par pause() ni par prepareForBackground quand isAnyPlaying) ; à confirmer à l'oreille avec musique de fond. Hors périmètre laissé de côté : StoryVideoPlayerView/StoryMediaLoader (domaine story), AudioFullscreenView (survolé : onFullscreen câblé depuis les réels via .fromFeed, stop/unregister sur disparition OK).

## calls-ux
QUICK WINS:
- Remplacer FloatingCallPillView.formattedDuration par callManager.formattedDuration (1 ligne, corrige l'affichage > 1 h)
- Capsule().fill(MeeshyColors.success) au lieu de Color.green sur le bouton Réessayer (CallView:1480)
- rejectSupersededPendingCall → appeler le helper emitCallReject(callId:) au lieu de MessageSocketManager.emitCallEnd direct (raison correcte + déferral socket-down gratuits)
- Localiser le toast d'échec toggleVideo avec deux clés activer/désactiver (CallManager:2232)
- Ajouter l'icône record.circle (screen capture) au badge durée de videoCallLayout et à compactAudioCallHeader — même pattern que isRemoteQualityDegraded
- Gate Reduce Motion sur le pulse du live-call message (BubbleCallNoticeView:148), pattern P2-iOS-9 déjà en place partout ailleurs
- Capturer le callId dans lastOutgoingContext et le comparer dans canRetryCall pour tuer le retry vers la mauvaise personne
NOTES: Domaine massivement audité en amont (P1-16, P2-iOS-9/10/11, watchdog 0x8BADF00D isolé via CallPresentationLayer dans RootView:58, sinks tous .receive(on: DispatchQueue.main) après le fix SIGTRAP ≤1175, adaptiveOnChange partout, surfaces Light toutes épinglées .dark : CallView:255, FloatingCallPillView:115, CallWaitingBannerView:121, CallEffectsOverlay:89). Les défauts restants sont donc surtout des angles morts d'intégration : (1) le couple retry/ended est le plus grave — contexte de rappel jamais invalidé (mauvaise cible possible) + état ended invisible en mode minimisé + modal 12 s sans sortie ; (2) l'indicateur privacy screen-capture est mort en vidéo ; (3) petites dérives SSOT (durée pill, ThemeManager dans une cellule Equatable). CallsViewModel est un exemplaire cache-first (CacheFirstLoader, pas de spinner sur cache, garde de génération anti-clobber). CallRetryPolicy lui-même est pur et correct — le bug est dans son gating côté CallManager. PiPCallController propre (teardown stoppe le PiP orphelin, reset fps/mute inter-appels). Non vérifié à l'exécution : tous les constats sont statiques sur code lu, aucun build lancé (lecture seule).

## media-pipeline
QUICK WINS:
- ImageFullscreen : ajouter autoLoad/bypass du policy gate à CachedAsyncImage et l'activer en fullscreen — corrige le spinner infini en une dizaine de lignes
- Supprimer apps/ios/Meeshy/Features/Main/Services/AttachmentSendService.swift (0 call-site) avant que son bug vidéo→store images ne soit rebranché
- MediaCompressor : ajouter `case "image/heic", "image/heif": return "heic"` à CompressedImageResult.fileExtension (ou transcoder en JPEG pour l'interop web)
- sendMessageWithAttachments : envelopper les Data(contentsOf:) des lignes 144/324 dans Task.detached (le helper readFileBytes existe déjà dans le service mort — le récupérer)
- prefetchAdjacentPages (BubbleCarouselView) : ajouter le même gate MediaDownloadPolicyEngine que ConversationMediaHandler
- cacheImageForPreview : insertion NSCache synchrone + garde maxCacheableDecodedBytes (aligner sur cacheIfWithinBudget)
- startDownloadFlow : honorer le retour false de registerInFlightDownload (annuler le byteTask et awaiter le download déjà enregistré)
NOTES: Périmètre audité en lecture seule : upload (AttachmentPreparationService, MediaCompressor, TusUploadManager + checkpoints, ConversationView+AttachmentHandlers, AttachmentUploader), download local-first (DiskCacheStore, CacheCoordinator, CachedAsyncImage/ProgressiveCachedImage, AttachmentDownloader, Video/AudioAvailabilityResolver, MediaDownloadPolicyEngine + prefs + NetworkConditionMonitor, ConversationMediaHandler), affichage (BubbleStandardLayout+Media, BubbleAttachmentView, carousel, ImageViewerView/ImageFullscreen), sauvegarde (PhotoLibraryManager, MediaFileSaver, MediaSaveCoordinator/FlowHost). Points sains vérifiés : PHPhotoLibrary résout l'album AVANT performChanges (deadlock historique corrigé, logs présents) ; adoption optimiste file://→https au niveau SDK (MessagePersistenceActor.adoptSDKLevel) route vers le bon store typé ; TusUploadManager.seedMediaCache route par MIME correctement et est idempotent ; dédup réseau par funnel in-flight dans DiskCacheStore (hors la race P3 signalée) ; offline write-ahead audio/visuel via OfflineQueue avec gestion disque-plein toastée côté envoi ; éviction budget auto (E1) + pins sidecar corrects. Réserves non classées en défauts faute de preuve d'impact : DiskCacheStore.save échoue silencieusement (log seul) sur disque plein — dégradation silencieuse en re-téléchargements, pas de surfacing utilisateur ; VideoAvailabilityResolver utilise .task(id: attachment.id) là où AudioAvailabilityResolver utilise .task(id: attachment.fileUrl) — la bascule file://→https à la réconciliation ne re-résout pas la disponibilité vidéo (masqué en pratique par l'adoption cache) ; CachedPlayIcon poll le cache vidéo toutes les 1,5 s tant que visible. Buildé sur lecture de code uniquement — aucun build/test exécuté (audit lecture seule).

## offline-instant
QUICK WINS:
- Rebrancher RootView.swift:359 et iPadRootView+Panels.swift:122 sur le RequestsTab/RequestsViewModel conforme (cache-first + outbox déjà écrits) au lieu de FriendRequestListView
- Remplacer l'état local de BlockedUsersView (Réglages) par le BlockedViewModel existant — même écran, déjà CacheFirstLoader + loadState offline
- MessageForwardDetailView.loadConversations : copier tel quel le bloc cache-first de ForwardPickerSheet.swift:225-241 (même store, même clé 'list')
- Seeder MessageReactionsDetailView.reactionGroups depuis message.reactions avant le fetch et ne plus vider dans le catch
- Brancher DiscoverViewModel.sendRequest / UserProfileSheet / ConnectionActionView sur OfflineQueue.enqueue(.sendFriendRequest,…) — le dispatcher (OutboxDispatcher.swift:45) et les labels SyncPill existent déjà, il ne manque que l'enqueue
- Ajouter la règle SwiftLint annoncée dans CacheResult.swift (ban .value en UI) si absente — aujourd'hui la protection ne repose que sur @available(deprecated)
NOTES: L'architecture Instant App est globalement TRÈS conforme sur les surfaces principales : FeedViewModel, PostDetailViewModel, StoryViewModel (SWR + delta refresh), ConversationViewModel (GRDB snapshot atomique + revalidation silencieuse + réconciliation outbox), ConversationListViewModel, BookmarksViewModel, ProfileUserPostsList, ReelsViewModel (seed + cold start cache), NotificationListView, ParticipantService, et tous les ViewModels Contacts (Requests/Blocked/Calls/Discover/ContactsList) suivent le switch 4-cas CacheResult, l'optimistic snapshot→local→réseau→rollback et l'outbox durable. Le flush FIFO est prouvé (OutboxFlusher.swift:165 `.order(Column(\"createdAt\").asc)`), le réveil offline→online est câblé sur NetworkConditionMonitor (OutboxRetryScheduler.startObservingNetworkReconnect, armé dans MeeshyApp), et le cold start borne le splash (floor 1s / socket 1.5s / ceiling 5s avec fast-path offline via NetworkMonitor.isOffline). Aucun usage du `.value` déprécié de CacheResult n'existe dans l'app ; les `snapshot()` trouvés sont des lectures sync/aggregation légitimes documentées. Les violations se concentrent sur les surfaces secondaires/dupliquées : vieux écrans sous Features/Main/Views (FriendRequestListView, BlockedUsersView, ThreadView) doublonnant des implémentations conformes, sheets de détail message (forward/reactions), et les chemins d'envoi 'partage/transfert' qui contournent l'outbox. Le cluster le plus révélateur : OutboxKinds .sendFriendRequest/.createConversation entièrement implémentés côté dispatch mais jamais enqueués — le pipeline offline a été construit puis jamais branché sur les surfaces d'action.

## settings-profile
QUICK WINS:
- AttachmentUploader.swift:105 — décoder `fileUrl` au lieu de `url` (1 ligne) : répare instantanément le changement d'avatar via EditProfileView.
- ProfileView.swift:803 — corriger l'endpoint SettingsAction en `/users/me` (1 ligne) pour que les édits profil offline atteignent enfin le serveur.
- MeeshyApp.swift:281, UserProfileViewModel.swift:80, SecurityView.swift:957/1006/1034 — remplacer `catch APIError.serverError` par `catch MeeshyError.server` (le type réellement lancé par APIClient) : débloque la purge des 4xx, l'état isBlockedByTarget et le message « Code incorrect ou expiré ».
- UserProfileViewModel.swift:43 — initialiser isBlocked depuis blockService.isBlocked(userId:) (la source canonique déjà injectée).
- NotificationSettingsView.swift:199-218 — remplacer les 2 TextField DnD par des DatePicker .hourAndMinute.
- PrivacySettingsView — griser/retirer les 5 toggles non câblés (pattern déjà utilisé pour la section Chiffrement du même fichier, lignes 139-151).
NOTES: Constat transversal 1 — DEUX pipelines concurrents pour l'édition de profil : EditProfileView/EditProfileViewModel (OfflineQueue .updateProfile → PATCH /users/me, avatar cassé deux fois : decode `url` vs `fileUrl`, puis clé `avatar` rejetée par le Zod `.strict()`) et ProfileView.saveProfile (UserService direct + SettingsActionQueue vers un endpoint inexistant). Chaque pipeline est cassé différemment ; violation Single Source of Truth qui a directement produit les P0. Constat 2 — le type APIError (APIClient.swift:148) n'est JAMAIS lancé par le client HTTP réel (qui lance MeeshyError) mais est pattern-matché dans au moins 4 écrans du domaine : classe entière de branches d'erreur mortes. Constat 3 — préférences de traduction (`translateToSystemLanguage`, `translateToRegionalLanguage`, `useCustomDestination`, `autoTranslateEnabled`) : AUCUNE surface iOS (0 occurrence dans apps/ios et MeeshySDK hors champ passif MeeshyUser.autoTranslateEnabled) — l'utilisateur iOS ne peut pas contrôler ces réglages pourtant modélisés dans Prisma et exposés web ; gap de parité, pas un bug prouvable. Constat 4 — champs privacy iOS (allowCallsFromNonContacts, blockScreenshots, hideProfileFromSearch, shareUsageData) absents du `PrivacyPreferencesDTO` gateway (types.ts:118-131) mais présents dans le Zod schema partagé (privacy.ts) — la persistance marche, seule l'application des règles manque. Constat 5 — MediaDownloadSettingsView utilise MediaDownloadPreferencesStore (UserDefaults device-local, jamais synchronisé ni purgé au logout) tandis que DocumentPreferences.autoDownloadEnabled synchronisé serveur n'a aucun consommateur iOS (`shouldAutoDownloadMedia` : 0 appelant) — dead API sans impact visible. Vérifications négatives : ChangePasswordView gère correctement MeeshyError en fallback ; DeleteAccountView est cohérent (phrase « SUPPRIMER MON COMPTE » hardcodée des deux côtés, gateway delete-account.ts:94) ; le flux 2FA et email/phone change utilisent les bons endpoints ; ProfileView/UserProfileViewModel respectent bien le pattern CacheResult .fresh/.stale/.expired/.empty pour stats et profils.

## perf-rerender
QUICK WINS:
- Supprimer iPadRootView.swift:47 (@ObservedObject networkMonitor jamais lu) — 1 ligne, gain immédiat sur tout re-render racine iPad
- Remplacer les 8 .onChange bruts SDK par .adaptiveOnChange (mécanique, aucune sémantique à changer)
- ReelFeedVideoSurface/ReelVideoView : remplacer @ObservedObject manager par onReceive($activeURL.removeDuplicates()) + $player → stoppe le réveil 5 Hz de toutes les cartes réel pendant la lecture
- ConversationView : envelopper ConversationListViewModel dans le ConversationListVMOwner déjà existant (RootView.swift:118) au lieu de @EnvironmentObject direct
- ProfilePostRow/ProfilePostsEmpty : passer les couleurs theme en let primitifs (pattern FeedPostCard déjà en place dans le même repo)
NOTES: Audit lecture seule du 2026-07-20 sur apps/ios/Meeshy + packages/MeeshySDK. Points positifs vérifiés (non-défauts) : le fix watchdog 0x8BADF00D tient (CallPresentationLayer isole CallManager hors de RootView/iPadRootView.body, RootView.swift:44-60) ; HeaderCallButtonsView isole correctement l'observation CallManager dans un sous-arbre minuscule ; ConversationRowItem et FeedPostCard ont des gates Equatable complets + .equatable() ; CachedAsyncImage/ProgressiveCachedImage downsamplent via targetSize et le cap 1200px par défaut ; AudioForegroundReaderOverlay documente et throttle son observation playhead à 30 Hz (quantum) ; les gardes des dot-timers évitent les mutations d'état inutiles à vide. Le fil rouge des défauts : SharedAVPlayerManager mélange dans un seul ObservableObject l'identité du player (change rarement) et currentTime 5 Hz — quatre familles de vues qui n'affichent pas le temps l'observent en entier (feed réels, pager réels, galerie ×2 niveaux) ; c'est la généralisation du pattern déjà corrigé pour CallManager. Hors domaine mais relevé en passant : CachedAsyncImage/ProgressiveCachedImage (SDK MeeshyUI/Primitives) cascadent MeeshyConfig.resolveMediaURL + CacheCoordinator + MediaDownloadPolicy (NetworkConditionMonitor.shared + MediaDownloadPreferencesStore.shared) — le tableau de placement de packages/MeeshySDK/CLAUDE.md classe explicitement les 'View wrappers cascadant cache + policy + downloader' côté APP ; à transmettre à l'agent du domaine SDK Purity.

## quality-crashes
QUICK WINS:
- EmbeddableVideoResolver.make() : ajouter `$0.isASCII` au filtre du videoId (1 ligne) — élimine le vecteur de crash iOS 16 sans toucher aux 3 force unwraps
- AudioPlayerManager.swift:185 et UserProfileSheet.swift:298/311/327 + PostsTab:65 : remplacer les `catch {}` vides par `catch { Logger.xxx.error(...) }` (mécanique, ~5 sites)
- MeeshyApp.swift:322 : do/catch + log sur bootRecovery (le pattern existe déjà 20 lignes plus bas pour le flusher)
- ConversationView.swift:631 : ajouter `.environmentObject(statusViewModel)` sur ForwardPickerSheet — alignement 1 ligne sur la convention ThreadView/SharePickerView
- AppDatabase.inMemoryWriter : logger l'échec de runMigrations (le logger local existe déjà dans makeWriter)
NOTES: Codebase globalement très durci sur ce domaine — vérifié sans défaut : sinks CallManager tous en .receive(on: .main) (leçon SIGTRAP build 1175 appliquée, commentaire l.3824), publishers Socket.IO émis sur main (handleQueue défaut) donc les sinks de ConversationSyncEngine/FeedSocketHandler qui wrappent en Task sont sains, force unwraps OfflineQueue (`existing!`) prouvés non-nil par le switch sur `existing?.kind`, AppDatabase avec récupération corruption complète, NetworkMonitor hop main explicite, PresenceManager nonisolated(unsafe) cohérent (didSet toujours main), regexes try! toutes statiques et mention-regex dynamique correctement échappée+try?, AudioMixer hostTime déjà durci contre l'overflow UInt64 (commit 841a528a), injections @EnvironmentObject des covers RootView/iPadRootView cohérentes avec les besoins déclarés des sous-vues. Le point n°1 (URL force unwrap) est le seul crash déclenchable à distance trouvé ; il ne touche que le runtime iOS 16.x (parser URL strict pré-iOS 17), d'où P1 plutôt que P0. Volume try? global élevé (531 app + 447 SDK) mais majoritairement sur des écritures cache best-effort documentées ; seuls les sites à perte de données utilisateur ou dead-end UX sont rapportés.

## i18n-a11y
QUICK WINS:
- Relancer l'extraction String Catalog sur la cible app et committer le catalogue resynchronisé — débloque d'un coup la traduction des 1664 clés absentes (prioriser les 107 clés a11y et notifications.action.*)
- Traduire en batch les 207 clés fr-only state=new du catalogue MeeshyUI (audio.editor.*, presence.*, conversation.settings.*) — mécanique, les defaultValue français sont la source
- Ajouter un Localizable.xcstrings à la cible MeeshyWidgets et localiser les 8 littéraux anglais de LiveActivities.swift (Mute/End/View/Active Call/Sent/Delivered/Read/Failed)
- MessageTextRenderer.buildText : remplacer les 4 Font.system(size:) par le mapping textStyle de MeeshyFont.relative — le texte des messages suivra Dynamic Type
- Ajouter les accessibilityLabel manquants sur les 4 boutons de DocumentViewerView et les boutons xmark de MessageDetailSheet (pattern déjà en place dans CallView/BubbleFooter)
- Corriger la valeur en de notification.audio_voice_message.body (« 🎵 Voice message ») et répliquer la clé dans les 6 autres langues de l'extension
NOTES: Méthode : parsing JSON des 4 catalogues xcstrings (clés, localizations par langue, extractionState), cross-référence par script des clés String(localized:) réellement utilisées dans le code vs présentes au catalogue, lecture des sites d'appel cités. Points positifs vérifiés : aucune récidive du markup inflect ^[…](inflect:true) (les 3 sites purgés par PR #2165 restent propres, seules des mentions en commentaires subsistent) ; flux d'appel (CallView, IncomingCallView) et bulle de message (BubbleFooter, BubbleDeliveryCheck, BubbleCallNoticeView) bien couverts en accessibilityLabel ; aucun `translations.first` aveugle (tous des first(where:) prédiqués, Prisme respecté) ; MeeshyUI bien synchronisé côté extraction (5 clés absentes seulement, feature timeline récente). Le défaut dominant est systémique : le catalogue de la cible app est figé (~40 % de clés stale, 68 % des clés du code absentes) — l'expérience non francophone de la cible app repose sur les defaultValue. Nuance sur P1-1 : les clés absentes donnent un rendu patchwork (defaultValue FR pour les vues, EN pour les actions de notification), pas un crash. ChatBubble.swift (MeeshyUI, bundle: .main lignes 31-32) est du code mort (aucun call site) — non compté comme défaut."

## sdk-purity
QUICK WINS:
- GlobalSearchView.swift:728 — remplacer formatTimeAgo par RelativeTimeFormatter.shortString(for:) (1 ligne, corrige l'anglais codé en dur)
- ParticipantsView.swift:550 — remplacer .formatted(.relative(...)) par RelativeTimeFormatter.longString(for:)
- StoryModels.swift:1957 — StatusEntry.timeAgo → RelativeTimeFormatter.longString(for: createdAt) ; localiser timeRemaining ("expired")
- ClipInspector.swift:227 — déléguer à TransportBar.formatTime(seconds:)
- UserPreferencesManager.swift:149-154 — supprimer le chemin auto-download mort (shouldAutoDownloadMedia / shouldAutoDownload(fileSizeMB:))
- Extraire les 3 tables supportedLanguages identiques (MessageDetailSheet:441, MessageTranscriptionDetailView:24, MessageLanguageDetailView:33) vers LanguageDisplay SDK
NOTES: Vérifications qui n'ont PAS donné de défaut (pour éviter les faux positifs en aval) : (1) normalizeLanguageCode n'est PAS dupliqué côté app — ConversationLanguagePreferences.normalize délègue à MeeshyUser.normalizeLanguageCode (ConversationLanguagePreferences.swift:138-140), le « miroir 3 sites » documenté ne concerne que TS↔Swift ; (2) la résolution Prisme StoryModels.resolvedContent retombe bien sur le contenu ORIGINAL, jamais translations.first ; (3) présence : aucun mapping couleur redéclaré hors PresenceStyle/PresenceModels ; (4) ImageVariantSelector et MediaDownloadPolicyEngine sont bien des moteurs purs (placement SDK conforme) ; (5) HapticFeedback, DynamicColorGenerator, ThumbHash : une seule implémentation, pas de dup app. Points d'arbitrage à connaître : le composer story complet (StoryComposerViewModel + 20 extensions) vit dans MeeshyUI avec des appels AuthManager.shared/CacheCoordinator.shared — placement probablement arbitré (WS1-WS6 livrés) mais en tension littérale avec le tableau « ViewModels → APP » ; je ne l'ai pas compté comme défaut. MediaDownloadPreferencesStore est dans MeeshyUI/Networking alors que le tableau le classe côté SDK core — incohérence de cible mineure, non comptée. NotificationToastManager (suppression de toast selon activeConversationId) est documenté comme SDK-owned dans les deux CLAUDE.md — non compté. Le défaut #1 (CachedAsyncImage) est le plus important : c'est le même pattern que le précédent VideoAvailabilityResolver rollbacké (commit 83e55297c) et le fichier encode des décisions par surface produit (Feed vs Conversation) dans les doc-comments du paramètre autoLoad.

## tests-health
QUICK WINS:
- Passer les guards P2PWebRTCClient*Tests du pattern silencieux (throw XCTSkip) au pattern loud (XCTFail puis skip) déjà utilisé par CallManagerTests:6112 — 2 fichiers, ~4 lignes
- Ajouter -skip-testing des 3 suites de perf dans ios-tests.yml (elles tournent aujourd'hui à chaque run CI contrairement à leur en-tête)
- Supprimer les 4 mocks morts (MockKeychainService, MockCacheService, MockMediaCache, MockProfileCache), MeeshySDKPlaceholderTests et BubbleExpandableTextUITests (jamais compilé) + son entrée NON_PHASE_SUITES dans meeshy.sh:1193
- Retirer « apps/ios/MeeshyIntents » (supprimé 2026-06-24) des sourceRoots de LocalizationConsistencyTests:31
- Granter photos-add au simulateur CI (simctl privacy) pour que CameraCaptureSaveRuntimeTests cesse de skipper le guard du crash caméra
- Réactiver StoryCanvasSnapshotTests avec assertSnapshot — l'infra swift-snapshot-testing et les baselines 18.2 existent depuis les suites Timeline
NOTES: Constat d'ensemble : la volumétrie est bonne (~281 fichiers app / ~581 fichiers SDK, ~3800 + ~4700 fonctions de test) et les flux critiques envoi message / offline queue / présence sont réellement couverts (MessageSendFlowTests optimistic+rollback, ConversationViewModelOfflineQueueTests 11 cas, OfflineQueueTests/OutboxFlusherTests/OfflineQueueReconnectIntegrationTests côté SDK, PresenceManagerTests + UserPresenceStateTests + PresenceStyleTests sur les 3 mappings). Le trou de couverture le plus dangereux est AUTH : AuthService.login est testé côté SDK (MockAPIClient), mais l'orchestration AuthManager de l'app n'est couverte que par des tests tautologiques sur mock (AuthServiceTests app) + un test réseau réel skippable (ZZEndStateConnectedSessionTests, skip silencieux sans DEMO_USER — donc jamais exécuté en CI GitHub), et la sérialisation du refresh concurrent est skippée depuis 2 mois. 18 sites XCTSkipIf(true) au total ; les skips « manuels » honnêtes (AudioMixerTests interruption, StoryExporterCacheTests perf device, StoryOfflineQueueTests file-protection, StoryViewerView_PrefetchTimerIntegrationTests seam @State) sont documentés et acceptables, contrairement aux 4 « Covered by Phase 4 XCUITest suite » qui référencent une cible inexistante. Isolation : conforme (SWIFT_DEFAULT_ACTOR_ISOLATION: nonisolated sur le bundle MeeshyTests dans project.yml, coreSwiftSettings sans defaultIsolation(MainActor) pour les cibles de test SDK ; @MainActor opt-in par classe). Les mocks actifs conforment tous à leurs protocoles *Providing (drift impossible à la compile) — les seuls mocks sans protocole sont les 4 morts. Écart CI/local notable : la CI (ios-tests.yml) exécute MeeshyTests en un seul xcodebuild sans le phasage 1/2/3 de meeshy.sh — l'ordre alphabétique fait tourner ZZ en dernier par accident de nommage, pas par construction.

