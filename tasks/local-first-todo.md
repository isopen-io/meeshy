# iOS Local-First — Backlog (vérifié par preuves, HEAD 51ca65281)

Méthodologie par tâche : analyse → auto-review → preuves verbatim → vérif edge-cases (Opus) → **TDD (RED→GREEN→REFACTOR)** → vérif cache end-to-end (frontend↔backend coordonnés) → build/tests verts.

Backlog issu du workflow `ios-local-first-backlog` (31 agents, 16 candidats vérifiés → 7 confirmés + 12 découverts → 17 tâches). 9 candidats réfutés/déjà-corrigés écartés.

## Clusters systémiques
1. **Désync reconnect/lifecycle** : teardown background zéroie `hadPreviousConnection`/`joinedConversations`/`activeConversationId` → `didReconnect` ne fire jamais au foreground → backfill + reçus jamais flushés.
2. **Gap recovery message mort** : `ReconnectionGapDetector` jamais câblé + endpoint gateway sans watermark `after`. ✅ RÉSOLU (T8 endpoint `?after=` + T9 `syncMissedMessages` réécrit en forward-paging par watermark ; détecteur mort supprimé).
3. **Fuites cross-compte au logout** : `reset()` omet 4 stores prefs + `clearOutbox()` ne purge pas la table `messages`.

## Tâches

### P0 — corruption / perte / fuite visible
- [x] **T1** (S, sdk) — Préserver `hadPreviousConnection` à travers le teardown background → `didReconnect` fire au resume. Débloque T2, T9. ✅ `suspendTransport()` (transport-only, préserve le flag) extrait ; `disconnect` = suspend + clear flag ; `prepareForBackground`/`forceReconnect` = suspend. Seam testable `handleConnectionEstablished()`. TDD 4 tests (RED 2/4 → GREEN 4/4) + régression 148/0 sur 11 suites lifecycle.
- [x] **T3** (S, sdk) — Ne plus réinitialiser `lastFetchedAt` (horloge fraîcheur L2) sur flush de mutation locale (`GRDBCacheStore.flushKeyToL2`). ✅ Préserve `existingMeta?.lastFetchedAt ?? now` (comme writeCursorToL2). TDD 3 tests (RED 2/3 → GREEN 3/3) + régression 84/0 sur 9 suites cache. Débloque T4.
- [x] **T4** (M, sdk) — Flusher les entrées L1 dirty avant éviction LRU dans `touchKey` (+ purger dirtyKeys fuités). Dépend T3. ✅ Invariant "aucune éviction ne drop une L1 dirty sans flush" : helper `flushDirtyKeyForEviction` partagé par `touchKey`+`evictL1` ; `flushDirtyKeys` GC les clés mortes + clear `firstDirtyAt` sur vide. TDD 2 tests (RED 2/2 → GREEN 2/2) + régression 61/0.
- [x] **T5** (S, sdk) — Purger les 4 stores de préférences (categories/userTags/userPreferences/conversationPreferences) au logout (`CacheCoordinator.reset`). ✅ Le leak réel = L1 RAM (singleton) + persistTask dirty re-flush, pas L2 (déjà vidé par deleteAllL2 des autres stores). 4 `invalidateAll()` ajoutés à `reset()` ET `invalidateAll()`. TDD 2 tests (RED 2/2 → GREEN 2/2) + régression CacheCoordinator 22/0.
- [x] **T6** (S, sdk) — Purger les tables GRDB `messages` au logout (étendre `clearOutbox`). ✅ Nouveau `clearAllMessagesForLogout()` purge les 7 tables (messages+translations+transcriptions+audio+attachments+pending_ids+outbox, aucune FK, transaction atomique) ; hook `wireOutboxLogoutHook` re-câblé. TDD (stub → RED 7/7 → GREEN) + régression persistence 49/0 + app build 38s OK. **Tous les P0 terminés.**

### P1 — sync correctness + efficacité haute valeur
- [x] **T2** (M, sdk) — Préserver/restaurer `joinedConversations` + `activeConversationId` à travers suspend/resume. Dépend T1. ✅ `suspendTransport` préserve les rooms (resume re-join via seam `roomsToRejoinOnConnect()` active-first) ; `disconnect` (logout) les efface. `joinRoom()` mort supprimé (ConversationSocketHandler). TDD 3 tests (RED 1/3 → GREEN 3/3) + régr 64/0 + app build OK.
- [x] **T7** (M, sdk+app) — Tagger la réaction de l'utilisateur courant avec son `userId` (pas le `participantId` de l'auteur) en ingestion REST. ✅ Bug réel localisé : `MessagePersistenceActor.upsertFromAPIMessages` taguait la réaction du user courant avec `api.senderId` (participantId de l'**auteur**) → après reload cache/REST (source de vérité local-first, read = passthrough), l'highlight « j'ai réagi » disparaissait. `toMessage` était correct (`currentUserId`). Fix single-source-of-truth : helper pur partagé `MeeshyReaction.reconstructFromSummary(messageId:reactionSummary:currentUserReactions:currentUserId:)` (dédupe les 2 blocs, empêche la re-divergence) ; l'actor reçoit `currentUserId` (état injecté via `DependencyContainer.wireCurrentUserHook` ⇐ `AuthManager.$currentUser`, seed+sync login/switch/logout). End-to-end : gateway fournit déjà `currentUserReactions`+`reactionSummary` (0 changement backend, pas d'over-fetch). TDD : test pur helper + test intégration `upsertFromAPIMessages` (RED `author_participant_id`≠`me_user_id` → GREEN). Régr MessagePersistenceActorTests + APIMessageToMessageTests verts + app build 72s. **Débloque T13.**
- [x] **T8** (S, backend) — Param watermark `since`/`after` sur `GET /conversations/:id/messages`. ✅ Helper pur `buildAfterWatermarkClause(after)` (gt Date, garde Invalid Date) + `afterMode` câblé : `createdAt > after`, ordre **asc** (backfill contigu), skip COUNT + skip offset-pagination. `MessagesQuery.after?` ajouté. TDD 4 tests jest (RED 1/4 → GREEN 4/4, ts-jest typecheck OK). Débloque T9.
- [x] **T9** (M, sdk+app) — Câbler gap recovery message au reconnect via watermark `after` (remplacer `ReconnectionGapDetector`). Dépend T1, T8. ✅ Le chemin LIVE était déjà câblé (`ConversationSocketHandler.subscribeToReconnect` → `delegate.syncMissedMessages()`) mais **bogué** : `list(offset:0,limit:30)` ne récupérait que les 30 derniers → un gap > 30 msgs jamais comblé. Fix = `syncMissedMessages` réécrit en **forward-paging par watermark** : `after = max(createdAt local) − 1ms` (tie-backoff anti strict `>`), boucle `listAfter` jusqu'à page < pageSize (100) ou cap 1000. Nouveau `MessageServiceProviding.listAfter(after:Date)` (consomme T8 `?after=` ISO8601 fractional, ordre asc) + overload backward-compat + 3 mocks étendus. **Acteur mort `ReconnectionGapDetector` SUPPRIMÉ** (jamais instancié, redondant — single source of truth) ; `AsyncSemaphore` partait avec (0 autre usage). TDD : SDK `testListAfterCallsWithCorrectEndpoint` (GREEN) + 3 tests app (RED 2/3 → GREEN 3/3 : watermark, multi-page gap-fill, empty-guard). Régr MessageServiceTests 15/0, ConversationSyncEngine+CacheCoordinator verts, ConversationViewModelTests full vert, app build 62s OK.
- [x] **T10** (S, app) — Flusher les mutations offline non-message au reconnect réseau. ✅ Découverte : l'outbox modélise/route/enqueue déjà 14 kinds non-message (`OutboxKind`, `OutboxDispatcher`, enqueuers câblés) — le vrai bug était que le flush n'était déclenché que sur lifecycle (boot, background→foreground) + timer backoff, **jamais au reconnect réseau** : une mutation enqueueée offline court-circuite le gate `isNetworkReachable` → `flush()` retourne `nil` → `OutboxRetryScheduler.schedule(at: nil)` **annule** le timer → elle reste `pending` jusqu'à un évènement incident. (Le commentaire « OutboxRetryScheduler re-fires on NWPath transitions » dans `OutboxFlusher`+`OutboxFlusherBandwidthGateTests` était un contrat documenté mais **jamais implémenté** — maintenant vrai.) Fix reuse-first : `OutboxRetryScheduler.startObservingNetworkReconnect(conditionPublisher:flush:)` s'abonne à `NetworkConditionMonitor.$condition` (même source que le gate du flusher), flush sur transition offline→online (pattern `SettingsActionQueue.observeConnection`), wiré une fois au boot `MeeshyApp`. TDD 2 tests (positif flush + négatif online→online no-flush, ce dernier garde le `dropFirst`). Build app 68s. **Débloque T11+T14.**
- [~] **T10b** (M, app) — Router via l'outbox les call sites qui bypassent encore. ✅ **`FeedViewModel.likePost` FAIT** (`a` venir commit) : injecté `offlineQueue: OfflineQueueing` (défaut `.shared`, comme PostDetailViewModel/EditProfileViewModel), `likePost` → `enqueue(.toggleLikePost)` (optimiste + GRDB sync + rollback sur échec enqueue), `toggleLikeComment` basculé sur l'injecté (cohérence/testabilité). TDD : test enqueue + test obsolète `test_likePost_failure_rollsBackIsLikedAndCount` mis à jour (échec via outbox au lieu d'API). Pattern réutilisé : PostDetailViewModel.likePost. ✅ **`FeedViewModel.sendComment` FAIT** (commit suivant) : refactor optimiste (commentaire local cmid + `enqueue(.createComment)` + rollback, mirror PostDetailViewModel.sendComment ; le commentaire serveur réconcilie via `comment:added`/refresh). 3 tests sendComment mis à jour aux sémantiques outbox. **Reste T10c — DIFFÉRÉ (faible valeur / non « parfaitement maîtrisable »)** : `DiscoverViewModel.sendRequest`/`ConnectionActionView.sendRequest` (friend request offline = cas **rare** ; `cache.didSendRequest` dépend du `request.id` **serveur** → router via outbox impose une réconciliation d'id optimiste + coalescing du cancel-avant-confirm, risque > valeur ; + ConnectionActionView est SDK-UI → SDK-purity) ; `ProfileView` legacy (`UserService.updateProfile` direct — **redondant**, EditProfileViewModel queue déjà ; à dé-doublonner, pas à re-router). Reprendre seulement si un cas d'usage offline-friend-request réel émerge.
- [x] **T11** (M, sdk+app) — Router edit/delete offline via l'outbox (câbler `enqueueEdit`/`enqueueDelete`). Dépend T10. ✅ Bug : `editMessage`/`deleteMessage` appelaient `messageService.edit/delete` **directement** (online-only) → offline, la mutation était perdue (rollback). L'infra outbox (`OutboxKind.editMessage/.deleteMessage`, `OfflineEditPayload`/`OfflineDeletePayload`, `enqueueEdit`/`enqueueDelete` avec coalescing, `dispatchEditMessage`/`dispatchDeleteMessage`) était **complète mais dormante** (aucun call site ne l'invoquait). Fix reuse-first : `OfflineMessageQueueing` (protocole SDK) étendu de `enqueueEdit`/`enqueueDelete` (OfflineQueue conforme déjà) ; VM gate `!networkMonitor.isOnline` → enqueue + return (pattern `sendMessage`). **Clé de coalescing** : `payload.clientMessageId = messageId` (id local = cid → éditer/supprimer un message encore non-envoyé offline coalesce/annule son send pending ; `payload.messageId = serverId(for:)` = cible REST ; dispatch legacy sans header mutation-id → pas de conflit dédup). TDD 2 tests RED→GREEN (`FakeOfflineMessageQueue` étendu) + régr ConvVMOfflineQueueTests+ConvVMTests verts + build 54s. **Note migration** : `ConversationCommandHandler.editMessage`/`deleteMessage` (forks morts du VM-split, non appelés par les vues) gardent l'ancien chemin direct — à fixer si/quand la migration VM-split les câble. **Vérif faite malgré blocage** : build app bloqué ~5 min par refactor MeeshyUI non-committé d'un agent concurrent ; SDK vérifié en isolation (`scheme MeeshySDK`), app vérifiée après recompilation de MeeshyUI.
- [x] **T12** (S, sdk) — Re-fetch ciblé des pages intérieures perdues dans `fullSync` (liste partielle silencieuse). ✅ `droppedIndices` re-fetchés avant `saveSorted` (merge dédupe par id) ; `succeeded=recoveredAll` (un échec transitoire récupéré ne flag plus incomplet). TDD 1 test (RED 150≠250 → GREEN 250) + régr ConversationSyncEngine 21/0.

### P2 — efficacité / robustesse
- [x] **T13** (S, sdk) — Ne pas clobber `reactionsJson` muté localement avec un snapshot REST périmé. Dépend T7. ✅ Bug : `upsertFromAPIMessages` faisait `existing.reactionsJson = reactionsJson` (snapshot REST) inconditionnellement → une réaction optimiste (appendReaction écrit la table messages + enqueue `.sendReaction`) non-syncée était revertie par tout refresh REST jusqu'au prochain sync. Fix : query `pendingReactionMessageIds` (outbox `.sendReaction` pending, décodés via `ReactionOutboxPayload.messageId`) une fois par batch, puis guard `if !pendingReactionMessageIds.contains(api.id) { reactionsJson/count = serveur }`. TDD : 2 tests discriminants `MessagePersistenceActorTests` (préserve-si-pending + clobber-si-rien-pending) GREEN. Vérif débloquée après réparation du build MeeshyUITests cassé par l'agent (`c1d11400c`).
- [x] **T14** (S, sdk) — GC des lignes outbox terminales (moitié GC de la tâche). Dépend T10. ✅ Les lignes `.exhausted` (après maxAttempts) n'étaient JAMAIS supprimées sauf au logout → croissance non-bornée de l'outbox. Fix : `MessagePersistenceActor.purgeExhaustedOlderThan(days: 7)` (supprime `.exhausted` plus vieilles que la fenêtre de rétention ; rétention bornée plutôt que delete-on-exhaust pour qu'un échec récent reste visible/retriable une semaine) + trigger one-shot au boot dans `start()`. TDD 1 test (old-exhausted purgé / recent-exhausted + old-pending gardés) GREEN. Réutilise le pattern des purges existantes (`clearOutbox`/`clearAllMessagesForLogout`).
- [x] **T14b** (S, sdk+app) — Surfacer les lignes outbox `.exhausted` dans le SyncPill. ✅ 3 changements : (1) `OfflineQueue.refreshPendingUIItems` filtre += `.exhausted` → les échecs permanents atteignent le SyncPill ; (2) `SyncPillViewModel.derive` traite `.exhausted` comme `.failed` (échec nécessitant action) ; (3) `ConnectionBanner` dot `.error` pour `.exhausted` (cohérence visuelle). Borné par la GC T14 (7j). TDD : test filtre SDK (`OfflineQueuePendingUIItemsPublisherTests`) + test derive app pur (`SyncPillViewModelDeriveTests`) GREEN + régression SyncPill + build app 50s.
- [x] **T15** (M, backend) — ETag/304 sur `GET /conversations` et `/conversations/:id/messages` (demi-gateway). ✅ Aucun header de cache auparavant (juste `Cache-Control: private, no-cache`). Helper pur réutilisable `utils/etag.ts` : `computeETag` (strong = `"sha256(JSON.stringify(payload))"`, déterministe → capture exactement les bytes logiques envoyés y compris isOnline/unread, donc **un 304 ne peut jamais servir du périmé**) + `ifNoneMatchMatches` (RFC 7232 : valeur unique / liste / `*` / array) + `sendWithETag(request,reply,payload)` (set ETag+Cache-Control, 304 body-less si match → true). Wiré aux 2 seuls chemins d'envoi GET (`core.ts:550`, `messages.ts:1200`) ; compose avec les modes delta `updatedSince`/`after`/`before`/`around` (ETag du résultat filtré). `no-cache` conservé (revalidation systématique = toujours frais, 304 = économise seulement le body = « ne pas rapatrier inutilement »). Inoffensif pour clients existants (ils ignorent l'ETag). Réutilise le précédent `attachments/download.ts`. TDD 12 tests jest (`etag.test.ts`, 100% couverture) + `tsc --noEmit` 0 erreur. **Découpé** : le **demi-client iOS** (envoyer If-None-Match + gérer 304 + stockage ETag) = T15b (plus lourd, SDK).
- [~] **T15b** (S→M, sdk) — Raffinement demi-client iOS du ETag/304. **(b) FAIT 2026-07-02** : iter-4 (travail parallèle) avait activé un URLCache dédié (`meeshy_http_cache`, 10 MB mem + 50 MB disque) dans `APIClient.init` mais AUCUN site ne le purgeait → bodies REST du compte A (conversations, messages) au repos sur disque après logout (violation invariant 9). Fix : `APIClient.clearHTTPCache()` + double purge dans `AuthManager.logout()` (début AVANT le guard — couvre le chemin sans-session ; fin après `CacheCoordinator.reset` — ferme la course store-disque-bufferisé URLCache qui peut atterrir après la 1re purge et ressusciter l'entrée). TDD : RED 2/2 prouvé (seed URLCache → logout → entrée toujours présente) → GREEN 2/2 (`AuthManagerLogoutHTTPCachePurgeTests`, seed `.allowedInMemoryOnly` déterministe — le store disque bufferisé est un internal Foundation, pas notre contrat) + régression auth/logout/APIClient 11/11. Restent (a) vérif device du comportement URLCache sur réponses authentifiées et (c) skip re-processing sur 304 explicite (optionnels, cœur acquis). **MAJ analyse** : `APIClient` utilise `URLRequest(url:)` SANS cachePolicy → **`.useProtocolCachePolicy` + `URLCache.shared` par défaut** → post-T15 la revalidation 304 est **probablement déjà transparente** (URLCache stocke + envoie If-None-Match + sur 304 retourne le body caché). Donc l'économie réseau cœur est sans doute déjà acquise gratuitement ; le code « 304 as error » (l.417-476) ne se déclenche que si l'app voit un 304 brut (URLCache bypassé/plein). **Reste à faire pour T15b** : (a) **vérifier sur device** que URLCache cache bien ces réponses authentifiées (`private, no-cache` + ETag) et sert un 304 transparent ; (b) **purger `URLCache.shared` au logout** (cross-compte/privacy — T5/T6 ont purgé GRDB+prefs mais PAS URLCache ; le `no-cache`+revalidation empêche le stale-serve mais les bodies de A persistent sur disque) ; (c) optionnel : handling 304 explicite pour skip le re-processing du sync engine. Priorité abaissée (cœur déjà acquis).

### P3 — over-fetch
- [x] **T16** (S, backend) — Trimmer les champs `sender.user` inutilisés du select GET messages. ✅ **Découverte schema** : `messageSenderSchema` (response schema) ne déclare que `{id,username,firstName,lastName,displayName,avatar,isMeeshyer}` → fast-json-stringify **strippe le nested `sender.user` entier** + `systemLanguage`/`role`. Donc `sender.user.{systemLanguage,role}` = pur over-fetch DB (jamais sérialisés), et `firstName`/`lastName` (sérialisés top-level via overlay) **lus par aucun client** (vérif cross-client). Trim : `firstName,lastName,systemLanguage,role` retirés du `sender.user.select` (extrait en const `messageSenderUserSelect as const`) + overlays firstName/lastName retirés ; gardés `id,username,displayName,avatar` (l'overlay en dérive les top-level que les clients lisent). **Spec corrigée** : « + projeter isOnline » **abandonné** — aucun client ne lit le isOnline du sender message (APIMessageSender ne le déclare même pas, et le schema le stripperait) → l'ajouter serait de l'over-fetch, pas un fix. TDD 2 tests jest + tsc 0 erreur.
- [x] **T17** (S, backend) — Trimmer le select participants de la LISTE conversations. ✅ **Spec corrigée par la vérif cross-client** : le libellé disait « permissions/language » mais `language` est **lu par le web** (`apps/web/utils/user.ts:212`, langue du titre de conv) → seul `permissions` (objet ~20 booléens, jamais lu par aucun client iOS/web dans la liste, ×5 participants ×N convs) est trimmé ; `language` gardé. Select extrait en const exportée `conversationListParticipantSelect` (`as const`, typage Prisma préservé) → trim explicite + garde-fou anti-régression. L'endpoint DÉTAIL `GET /conversations/:id` garde permissions (include non filtré). TDD 4 tests jest + tsc 0 erreur.

## Découvertes annexes (à backloguer après vérif)
- `SocialSocketManager` : même pattern `disconnect()` zéroie le flag de reconnect (lignes 333/411/418/424/429) — sibling de T1 ; vérifier si son `didReconnect` pilote du sync-critique.
- `resumeFromBackground()` guard sur `APIClient.shared.authToken != nil` → no-op pour les sessions **anonymes** (session token, pas authToken) → pas de reconnect socket au resume pour anonymes. Pré-existant, hors T1.

## Wave 2 — Round 2 (ré-analyse adversariale 2026-06-01, 4 hunters)

Backlog frais issu d'un sweep (cache SWR / reconnect-realtime / optimistic-rollback / delta-sync), cross-vérifié dans le code actuel, dédupliqué du livré. Priorisé + noté maîtrisabilité.

### P1 — correctness
- [x] **R1** (P1, sdk) — `SocialSocketManager` zéroyait `hadPreviousConnection` au background. ✅ `suspendTransport()` extrait (préserve le flag), utilisé dans `prepareForBackground`/`forceReconnect` ; `disconnect()` (logout) seul clear le flag. Miroir exact de T1.
- [~] **R2** (P1) — **R2-core (SDK) FAIT** : `SocialSocketManager.didReconnect` publisher + seam `handleConnectionEstablished()` (fire sur reconnect), `.connect` handler câblé. 4 tests `SocialSocketReconnectLifecycleTests` (cold/reconnect/disconnect/forceReconnect) GREEN + build. **Reste R2-app** : observer `SocialSocketManager.didReconnect` côté app (FeedViewModel/FeedSyncEngine) → backfill posts/réactions manqués au reconnect (défini par le futur SocialSyncEngine ; medium, app).
- [~] **R3** (P1, sdk) — **VÉRIFIÉ** : `saveSorted` (l.1043) ne `throw` PAS, il **swallow** l'erreur `cache.conversations.save` (catch+log) → `syncSinceLastCheckpoint` atteint TOUJOURS `lastSyncTimestamp = Date()` même si le save a silencieusement échoué → conversations non-persistées + watermark avancé → **mises à jour perdues** (jamais re-fetchées). Fix : `saveSorted` retourne `@discardableResult Bool`, watermark avancé seulement si `saved` (+ capturer `syncTime` avant le fetch pour couvrir la fenêtre). **DÉFÉRÉ (test-seam)** : `ConversationSyncEngine` prend un `CacheCoordinator` concret (pas de protocole) → forcer un échec GRDB en test = pas faisable sans abstraction de fault-injection (gros refactor, pas un petit chantier). À reprendre avec un seam de cache mockable.
- [x] **R6/R7** (P1, app) — `FeedViewModel.likePost`+`sendComment` rollback sur `.exhausted`. ✅ Helper `observeOutcome(cmid:rollback:toast:)` (miroir RequestsViewModel, via injected `offlineQueue`) + `restoreLike` snapshot partagé sync-refusal/async-exhausted. 3 tests (`test_likePost_rollsBack_whenOutcomeExhausted`, `..._doesNotRollBack_whenOutcomeApplied`, `test_sendComment_rollsBack_whenOutcomeExhausted`) + helper `waitForContinuation`. Suite FeedViewModelTests verte (aucune régression).
- [x] **R4/R5** (P1, app) — `PostDetailViewModel.likePost`+`sendComment` rollback sur `.exhausted`. ✅ Même `observeOutcome(cmid:rollback:toast:)` + `restoreLike` snapshot. 3 tests (`test_likePost_rollsBack_whenOutcomeExhausted`, `..._doesNotRollBack_whenOutcomeApplied`, `test_sendComment_rollsBack_whenOutcomeExhausted`) + `waitForContinuation`. Suite PostDetailViewModelTests verte. ⇒ **Groupe R4-R7 (rollback optimistic `.exhausted`) ÉPUISÉ.**

### P2 — efficiency / robustesse
- [x] **R8** (P2, sdk, trivial) — ✅ `DiskCacheStore.data(for:)` l.229 + `image(for:)` l.438 : `result.value?.first` (déprécié) → `result.snapshot()?.first`. Behavior-identical (`.value` appelle littéralement `snapshot()`). DiskCacheStoreTests verts, SDK build OK, warnings dépréciation supprimés.
- [N/A] **R9** (P2, app) — **NON-ISSUE vérifié (preuve, pas de fix)**. Prémisse fausse ×2 : (1) `reactionSynced` EST observé (`ConversationSyncEngine:635` → `handleReactionSynced` merge réel dans le cache) ; (2) l'event `reaction:sync` est **mort des deux côtés** — la gateway ne le broadcast jamais (seulement un ack-request inutilisé `callback(...)` dans ReactionHandler), et le SDK ne l'émet jamais → `socket.on("reaction:sync")` ne fire jamais. La réconciliation réactions fonctionne déjà : temps-réel via `reaction:added`/`reaction:removed` (observés par `ConversationSocketHandler` live ET le cache SyncEngine), et gap-offline via le refresh messages — chaque add/remove appelle `updateMessageReactionSummary` → `message.update` → Prisma bump `Message.updatedAt @updatedAt` (schema l.70), et l'endpoint messages porte TOUJOURS `reactionSummary`/`reactionCount` → delta-sync re-fetch les réactions manquées au reconnect. Câbler l'event mort dans la live view = mauvais fix + dead-code. _(Cleanup possible mais faible valeur : supprimer le listener/publisher/handler morts touche le protocole public SDK + 2 mocks + SyncEngine + tests → différé.)_
- [N/A] **R10** (P2, app) — **NON-ISSUE vérifié (preuve)**. Comme R9 : event `conversation:online-stats` **mort** — la gateway ne l'émet JAMAIS (aucune référence runtime à `CONVERSATION_ONLINE_STATS` hors la constante events.ts + une signature de type ; aucun `emit`/`.to()`). Le SDK le décode (`socket.on` + publisher + decode-tests) mais aucun consumer live + aucune UI de online-count. Vestigial (plumbing scaffoldé côté event-constant + decode SDK, jamais câblé côté emit). Pas un bug de sync. ⚠️ **Pattern Round-2** : les items « event décodé mais jamais observé » (R9, R10) = events morts vestigiaux, PAS des gaps. La ré-analyse les a sur-signalés.
- [ ] **R11** (P2, app) — `ConversationListViewModel` conversationUpdated : métadonnées (title/avatar/…) mises à jour in-memory AVANT le persist async → kill entre les deux = stale au cold start. Hoister le `CacheCoordinator.conversations.save` immédiatement.
- [N/A] **R12** (P2, app) — **NON-ISSUE vérifié (déjà correctement géré)**. `PresenceManager` câble `MessageSocketManager.didReconnect` (T1) → `PresenceService.refreshKnownUsers()` → REST `GET /users/presence?ids=` → `ingestRefresh` repeuple depuis la vérité serveur (in-flight dedup, cap 200 ids). De plus la presence map n'est PAS wipée aux drops brefs (préservée), `.away` ne kicke qu'après 5 min d'inactivité réelle, recalc timer 60s. Pas de flip massif `.away`. **3e P2 sur-signalé (R9/R10/R12 tous non-issues).**
- [ ] **R13** (P2, app) — `FeedViewModel.likeComment` (470-482) : enqueue fire-and-forget SANS optimistic ni rollback → latence apparente + perte si kill. Ajouter optimistic like-count + observeOutcome.
- [ ] **T15b/R14** (P2, sdk) — `APIClient` ne gère pas 304/If-None-Match (ETag T15 inexploité) — déjà backloggé T15b.

### P3 — polish
- [ ] **R15** (P3, sdk-isolatable) — `ConversationSyncEngine` : clamper `lastSyncTimestamp` à `Date()` (jamais un watermark futur) pour résister à un saut d'horloge NTP/DST arrière.
- [ ] **R16** (P3, app, reuse) — Le helper `observeOutcome(cmid:rollback:toast:)` existe désormais en 5 copies privées quasi-identiques (RequestsViewModel, UserProfileViewModel, EditProfileViewModel `(cmid:snapshot:)`, FeedViewModel, PostDetailViewModel). Extraire un seul `protocol OutboxOutcomeObserving { var offlineQueue: OfflineQueueing { get } }` + extension default `observeOutcome` (app-side, pas SDK — c'est de l'orchestration UX). Refactor à isofonction, couvert par les tests existants des 5 VMs. À faire en chantier dédié (touche 5 fichiers).

**État Round-2 (post-R8/R12)** : livrés R1/R2-core, R4-R7, R8 ; déféré R3 (test-seam) ; N/A vérifiés R9/R10/R12 (events morts vestigiaux / déjà géré — 3/4 des P2 « unobserved » sur-signalés par la ré-analyse). Reste réel mais bas/larger : R11 (kill-window 200ms rename, mineur+cosmétique, self-corrigé, timing-param peu TDD-able), R13 (UIKit medium, durabilité déjà OK), R14/T15b (ETag client, plus gros), R15 (clamp clock-skew polish), R16 (extraire observeOutcome, refactor 5 fichiers). **Prochain : 3e RÉ-ANALYSE** — le filon Round-2 haute-valeur est tari ; régénérer un backlog frais ciblé sur des zones NON encore auditées : OutboxFlusher retry/backoff/maxAttempts, conflict-resolution edits/deletes offline, attachment-upload offline-queue durabilité, GRDB migration/corruption recovery, watermark/delta edge-cases côté gateway, coalescing outbox multi-mutations même entité.

## Wave 2 — Round 3 (3e ré-analyse, 2026-06-01)

Audit évidence-first de 4 sous-systèmes NON couverts par Round-1/2 (4 subagents general-purpose, file:line cités, SOLID vs BUG distingués). **Filon bien plus riche que Round-2** (zones réellement non auditées). Areas SOLID confirmées : backoff/maxAttempts/dead-letter outbox, FIFO, coalescing message/edit/delete/reaction (.pending), crash-replay idempotency (unique `(conversationId, clientMessageId)` + MutationLog cmid), tombstone cold-start, eviction flush-dirty-first, L1/L2 coherence, encryption-on-read, reconciliation attachment + double-send REST/socket.

### P1 — data divergence / lost mutation (haute valeur, faire d'abord)
- [x] **S1** (P1, sdk) — ✅ **Concurrent flushers double-dispatch** — CORRIGÉ. Seam `claimPending(_:) -> Bool` (UPDATE conditionnel `WHERE id=? AND status='pending'` via `updateAll`, retourne `count==1`) ; `processRecord` `guard await claimPending(record) else { return }` avant dispatch. GRDB sérialise les writes → 2 flushers concurrents = 2 claims séquentiels, un seul gagne. 2 tests : `claimPending_rejectsSecondClaim` (déterministe, le 2e claim d'une row déjà inflight → false) + `concurrentFlushers_dispatchSharedRowExactlyOnce`. Régression FIFO/backoff/exhausted OK. Détail original : 5+ sites construisent un `OutboxFlusher` neuf (MeeshyApp:264, BackgroundTransitionCoordinator:126, BackgroundTaskManager:123, OutboxRetryScheduler timer+reconnect OutboxDispatcher:848/865). `processRecord` flip `.inflight` via `update(db)` inconditionnel (OutboxFlusher.swift:155-157), PAS `WHERE status='pending'`. Exposition réelle : double-dispatch `reaction` → 2e renvoie 409 → branche `.serverError(409)` (OutboxDispatcher.swift:759) → `retryExhausted` → **rollback de la réaction optimiste que l'user a posée avec succès**. Fix : claim conditionnel `UPDATE outbox SET status='inflight' WHERE id=? AND status='pending'` + check `changesCount==1`, skip si 0. Test : 2 flushers / 1 pool in-memory, dispatcher lent, assert dispatch 1×/row.
- [x] **S2** (P1, sdk) — ✅ **Edit/delete offline en attente écrasés par un refresh REST** — CORRIGÉ. Guards `pendingEditMessageIds`/`pendingDeleteMessageIds` ajoutés dans `upsertFromAPIMessages` (miroir T13) : skip overwrite content/isEdited/editedAt si edit pending, skip deletedAt si delete pending, skip content si l'un ou l'autre. 3 tests (preservesOptimisticEdit, preservesOptimisticDelete, takesServerContent_whenNoEditPending) — RED confirmé (content revertait "edited"→"original", delete un-deletait) → GREEN. T13 réaction sans régression. Reste détail original : `upsertFromAPIMessages` a le guard `pendingReactionMessageIds` (T13, MessagePersistenceActor.swift:1043/1315) mais écrase inconditionnellement `existing.content`=api.content (:1297), `existing.editedAt=nil` (:1304), `existing.deletedAt=api.deletedAt` (:1305). Un edit/delete optimiste pending (outbox non drainé) est reverti par tout refresh REST (revalidation SWR, reconnect refresh, loadOlder overlap). Fix : `pendingEditMessageIds`/`pendingDeleteMessageIds` (query OutboxRecord `.editMessage`/`.deleteMessage` `.pending`, décoder payload→messageId), skip l'overwrite content/editedAt/deletedAt si présent. **Miroir exact du guard réaction T13.** Test : MessagePersistenceActorTests (infra existante :644/:1040).
- [x] **S3** (P1, app) — ✅ **Edit/delete offline exhausted ne rollback jamais** — CORRIGÉ. Switch `retryExhausted` extrait en méthode awaitable `handleRetryExhausted(_:)` (logique send/reaction préservée verbatim) + cases `.editMessage` (restore le contenu pré-edit depuis `EditHistoryStore.revisions().last.content` + `removeHistory`, miroir online rollback) et `.deleteMessage` (`markUndeleted`, miroir online). 2 tests (undeletesLocally, restoresOriginalContent) + seed helper. Suite ConversationViewModelOfflineQueueTests verte. Détail original : `ConversationViewModel` retryExhausted switch (:969-1015) gère `.sendMessage`/`.sendReaction`, `default: break` (:1007) drop edit/delete. Le commentaire prétend « dedicated ViewModels » mais edit/delete naissent dans ConversationViewModel même (pas d'autre subscriber `retryExhausted`). Conséquence : delete exhausted → message reste blanked localement à jamais (vivant ailleurs) ; edit exhausted → contenu édité jamais envoyé reste local + révision EditHistoryStore fantôme. Fix : cases `.editMessage`/`.deleteMessage` → `markUndeleted(localId:)` / restore contenu serveur. Test : fire `retryExhausted(kind:.deleteMessage)`, assert markUndeleted.
- [x] **S4** (P1, sdk) — ✅ **Corruption SQLite catchée mais jamais récupérée** — CORRIGÉ. `makeWriter` restructuré : ouvre+migre+récupère en interne via seam `openOrRecover(at:)` — sur échec open/migration (SQLITE_CORRUPT/NOTADB) supprime le fichier+WAL/SHM, retry 1×, sinon in-memory (`inMemoryWriter` migre aussi l'éphémère). Migration retirée de `init`. 2 tests : corrupt→recréé on-disk migré (ephemeral false, `cache_entries` présent) + valid→réutilisé (sentinel survit, pas de wipe). Note : assert initial sur `conversations` faux (table droppée par v4_drop_legacy_tables → schéma actuel = cache_entries/translation_cache/tus_upload_checkpoint). Détail original : `AppDatabase.makeWriter()` ne catch que l'échec à `DatabasePool(path:)` (:77-85), mais `SQLITE_CORRUPT` survient à la 1re query = dans `runMigrations` → catch (:21-27) log « continuing with ephemeral » mais NE crée PAS de fallback in-memory → pool corrompu retenu toute la session + à travers les relaunches (jamais supprimé). L2 cache mort, reads tombent en réseau à jamais. Fix : sur `SQLITE_CORRUPT`/`SQLITE_NOTADB` supprimer le fichier + retry `makeWriter()` 1×, sinon `DatabaseQueue` in-memory + `isEphemeral=true` (rendre `databaseWriter`/`isEphemeral` réassignables). Test : injecter un .sqlite pré-corrompu, assert round-trip save/load réussit.

### Vérification adversariale S5-S13 (workflow `wf_56378a71-90b`, 9 agents, 2026-06-01)
**3 réfutés, 2 partiels, 4 réels** — encore ~⅓ sur-signalés (cohérent Round-2). La vérif-avant-code a évité 3 faux fixes.

### P2 — robustesse
- [N/A] **S5** (P2) — **RÉFUTÉ**. La séquence createComment→deleteComment est **inatteignable** : aucun code prod n'enqueue `.deleteComment` (grep exhaustif — `DeleteCommentPayload`/`dispatchDeleteComment`/`PostService.deleteComment` = scaffolding mort, seul `FeedPersistenceActor.deleteComment` GRDB-local live via socket `comment:deleted`). Sous-claims 404-swallow (:459) + no-coalesce vrais mais inertes. Optionnel : supprimer le scaffolding mort (faible valeur).
- [x] **S6** (P2, sdk) — ✅ **CORRIGÉ (downscopé au vrai défaut)**. Les 2 sites `.failed` (bootRecovery audio-missing OfflineQueue:1692 + enqueueAudios copy-error :1148) → `.exhausted` (terminal + GC-éligible). enqueueAudios ajoute cleanup des copies partielles + emit retryExhausted (path live, VM résout la bulle) ; bootRecovery = status seul (fichiers déjà manquants → cleanup moot, boot sans subscriber → emit into-void). 2 tests (bootRecovery file-missing→exhausted via enqueueAudios+delete, copyFailure→exhausted+throws) RED (`.failed`/`.pending`) → GREEN, suite OfflineQueueTests verte. **Le défaut « non-retryable » de la prémisse était faux** (retryItem marche) ; vrai défaut = leaked-row jamais GC'd + SyncPill across sessions, maintenant fixé. Détail original : Faits structurels confirmés (`.failed` à OfflineQueue:1692/1148, jamais flush/GC, reste dans SyncPill across sessions). MAIS « non-retryable » FAUX : `retryItem`/`retryByClientMessageId` (:578/:620, sans filtre status) marchent + câblés UI (BubbleStandardLayout:912). **Vrai défaut = mauvais status** : `.failed`→`.exhausted` (terminal, GC-éligible MessagePersistenceActor:162) + cleanupLocalFiles + emit retryExhausted. TDD sdk-scheme propre (configure pool → bootRecovery fichier-audio-manquant → assert `.exhausted` + GC-éligible). small.
- [~] **S7** (REAL, app) — **IMMÉDIAT FAIT** (`bc477699a`). Bulle `.sending` fantôme→`.failed` retryable via `markOptimisticMediaFailed`. **Reste S7b/S8 = durabilité offline media (SPEC ci-dessous).**

#### SPEC S7b/S8 — durabilité offline media (parité audio/texte). Design vérifié 2026-06-01.
Contexte vérifié : audio offline EST durable (`enqueueAudios` write-ahead sous `Documents/pending-audio/` + replay dispatcher TUS `OutboxDispatcher:547-622`). Visuel ne l'est PAS. Le replay dispatcher hardcode `mimeType:"audio/mp4"` (:577) → généralisation mime nécessaire. `OfflineQueueItem` Codable synthétisé (champs `let`) → un nouveau champ optionnel est **additif/backward-compat** (vieilles rows décodent nil).
**Ordre d'atterrissage (le caller en DERNIER pour éviter tout mis-dispatch)** : ST1 (enqueueMedia, sans caller) → ST3 (dispatcher gère localMediaPaths) → ST2 (vue appelle enqueueMedia).
- [x] **ST1 (sdk)** : ✅ FAIT. (a) `OfflineQueueItem.localMediaPaths: [String]?` additif (threadé dans les 2 inits + CodingKeys + decoder/encoder manuels — backward-compat via decodeIfPresent). (b) `pendingMediaDirectoryName="pending-media"` + `pendingMediaRelativePath(for:index:ext:)` (extension préservée) + `absoluteMediaPath(forStored:)` (délègue à absoluteAudioPath générique). (c) `enqueueMedia(...)` + `EnqueueMediaResult`/`EnqueueMediaError`, miroir enqueueAudios (Phase A write + Phase B copie Documents/pending-media/, copy-error→`.exhausted`+cleanup+emit cf S6). Test `_persistsLocalMediaPaths_andRelocatesFiles_preservingExtension` GREEN, régression audio/queue OK. _Sans caller → safe._
- [x] **ST3 (app)** : ✅ FAIT. `OutboxDispatcher.dispatchSendMessage` — bloc `localMediaPaths` après le bloc audio : upload TUS via `OfflineQueue.absoluteMediaPath` + mime via **`MimeTypeResolver.mimeType(forExtension:)`** (util SDK existante RÉUTILISÉE — pas de helper neuf ; gère jpg/mp4/mov/heic) → `sendWithAttachmentsAsync` + reconcile (miroir audio exact). `cleanupLocalFiles` (free func OutboxFlusher:42) étendu pour sweeper `localMediaPaths` (anti-fuite). Test SDK `test_cleanupLocalFiles_sweepsLocalMediaPaths` RED→GREEN ; app build vert (72s). NB le replay wiring lui-même n'est pas unit-testable (dispatcher non-injectable TusUploadManager/MessageSocketManager.shared inline — posture pré-existante = bloc audio idem) ; testable = cleanupLocalFiles + MimeTypeResolver (déjà testé). Idempotence gateway (conversationId,clientMessageId) couvre le double-send.
- [x] **ST2 (app)** : ✅ FAIT. `ConversationView+AttachmentHandlers` — branche offline `.visual` (miroir audio) : `if send.group.kind == .visual, isOffline` → `enqueueMedia(sourceMediaURLs:kinds:...)` (kind via `AttachmentKind(mimeType: MimeTypeResolver.mimeType(forURL:)).rawValue`) + `continue` (skip TUS). La bulle reste `.sending` durable (pas `.failed`). Le caller en DERNIER → S7b/S8 complet. App compile (vérifié via app-test build). Fix : `pairs.map(\.url)` keypath-tuple = erreur Swift → `pairs.map { $0.url }`.
- [x] **S8** : ✅ résolu par ST1-3 (row durable sous Documents/pending-media/ → OutboxFlusher rejoue au boot → uploadFile même sha256 key → resume TUS in-session auto). Relocaliser sous Documents/ (pas tmp purgée iOS) est la clé, fait par enqueueMedia.

**✅ S7b/S8 COMPLET (ST1 `ca1fa88d8` + ST3 `fe29a2ec6` + ST2 ci-dessous)** : photo/vidéo offline désormais DURABLE end-to-end (parité audio/texte) — survit au kill, auto-resend au flush, fichiers sweepés au terme, mime par extension. Reste smoke device (envoyer photo offline → kill → relancer → vérifier auto-send au retour réseau).
- [ ] **S8** (REAL, medium, both, dépend S7) — **CONFIRMÉ**. Kill mid-upload vidéo non rejoué : TUS resume OK in-session (TusUploadManager:189-195) mais aucun replay boot pour media chat (`allCheckpoints()` test-only). Fix = S7 (row durable sous Documents/ — pas tmp, purgée par iOS) → dispatcher re-upload via uploadFile (même sha256 key → resume auto). Dedup gateway (conversationId,clientMessageId) protège le double-send.
- [N/A] **S9** (P2) — **RÉFUTÉ**. Fenêtre `.inflight` gatée out : edit/delete n'enqueue que OFFLINE (ConversationViewModel:2628/2485), send ne devient `.inflight` que ONLINE (flusher gardé `isNetworkReachable` OutboxDispatcher:804). Élargir le filtre à `.inflight` serait **INCORRECT** (un in-flight ne peut être rappelé ; online edit/delete supersede déjà via REST — c'est le bon design). Fenêtre sub-runloop théorique (2 NWPathMonitor distincts) négligeable.

### P3 — polish
- [⏸] **S10** (REAL, medium, sdk-scheme) — **DIFFÉRÉ (interaction observeOutcome)**. toggleLikePost/Comment ne coalescent pas → like→unlike→like = 3 rows → fan-out redondant (PAS lost-update, P3). Fix = coalescer toggles opposés même (kind, postId/commentId) miroir state-machine réaction (:1511-1523). **⚠️ COMPLEXITÉ découverte** : la coalescing supprime/annule la row → mais `FeedViewModel.likePost`/`PostDetailViewModel` (R6/R4) `observeOutcome(cmid)` écoutent `outcomeStream(cmid)` (alimenté par `onOutcome` du flusher, PAS par `retryDropped` comme la réaction). Si on coalesce, l'observeOutcome(cmid) **hang à jamais** (stream jamais alimenté/fini). Le fix correct doit AUSSI résoudre l'outcomeStream du cmid droppé/annulé (emit .applied + finish) — plomberie supplémentaire. P3 + cette interaction → différé après S7b. (Bonus séparé : comment-like comments.ts:246-293 sans withMutationLog re-broadcast à chaque POST — defense-in-depth gateway.)
- [x] **S11** (REAL, small, app) — ✅ **CORRIGÉ**. `LocallyHiddenMessagesStore.migrate(from:to:)` (remove old + insert new si présent, persiste ; no-op sinon) + appelé dans `persistMessagesUsingServerIds` pour chaque (temp,server)∈pendingServerIds (couvre les 3 sites reconcile). 3 tests : migrate→déplace/no-op (store-level, suite DI) + VM end-to-end (hide temp + pendingServerIds → persist → isHidden(server)&&!isHidden(temp)). GREEN. Détail original : hide(temp id) → l'id swap vers serverId à l'ack (`toMessage` = serverId ?? localId, MessageRecord+ToMessage:94) → filtre `messages.filter{$0.id}` (:461) rate → réapparaît (in-memory + cold-start). Fix : `LocallyHiddenMessagesStore.migrate(from:to:)` + l'appeler dans `persistMessagesUsingServerIds` (:371) pour chaque (temp,server) ∈ pendingServerIds (couvre les 3 sites reconcile :1984/:2059/:953). Test store-level propre (DI UserDefaults).
- [N/A] **S12** (P3) — **RÉFUTÉ**. Le flush background EST gardé : `BackgroundTransitionCoordinator.enterBackground()` (MeeshyApp:391, inconditionnel) acquiert `beginBackgroundTask` synchrone (:37) puis flush le MÊME CacheCoordinator (:54) sous l'umbrella → endBackgroundTask (:83). Le hook SDK didEnterBackground non-wrappé est un secondaire redondant (flushDirtyKeys idempotent). + willResignActive flush encore plus tôt. Pas un bug live (au plus durcissement SDK-consumer hypothétique).
- [⏸] **S13** (P3, defer) — **PARTIEL inerte**. L'attachment-drop dans `retryMessage` (:2141, pas d'attachmentIds) est réel MAIS son seul caller `failedMessageBar` (ConversationView+MessageRow:500/517) est **dead UI jamais rendu** (liste UIKit `.failed` = icône sans tap) → zéro perte user actuelle. Le retry live (outbox) préserve déjà les attachments. Différer jusqu'à câblage d'une UI de retry (+ le fix proposé est narrower : ne marche que pour attachments déjà TUS-uploadés).

**État Round-3** : livrés S1-S4, S6, S11, S7-immédiat (8 fixes). N/A réfutés : S5/S9/S12. Différés : S13 (dead-UI), S10 (interaction observeOutcome, P3). **Reste S7b+S8** = durabilité media offline (medium, app+sdk — le plus gros chantier restant : enqueueMedia + branche offline visuelle + dispatcher TUS replay). **Prochain : S7b/S8** (chantier durabilité media — découper en sous-tâches : (1) enqueueMedia SDK + test, (2) branche offline `.visual` qui relocalise + enqueue, (3) dispatcher TUS replay video/image + test). Méthodo : preuve(RED)→TDD→vérif→commit isolé `git add` par fichier (exclure StoryTextLayer/StoryComposer de l'agent parallèle).

## Wave 2 — Round 4 (4e ré-analyse découverte, workflow `wr9xowh87`, 2026-06-01)
4 agents évidence-first sur zones NON auditées (Round 1-3 couvraient socket-events, outbox, watermark, presence, edit/delete, attachment durabilité, GRDB, hidden-id, optimistic rollback). **4/4 subsystems avec issues, 4 findings (P1×1, P2×2, P3×1), 0 réfuté.** ⚠️ Findings de découverte — vérifier chacun via RED avant fix (discipline maintenue malgré 0 refute).

- [x] **U1** (P1, medium, both) — ✅ **CORRIGÉ end-to-end (FE+BE coordonné, 3 sous-commits 2026-06-02)**. ST1 `f98aff686` (BE) : `broadcastPostCreated` forwarde `request.clientMutationId` dans le payload `post:created` (`PostCreatedEventData.clientMutationId?` + handler test 44/44). ST2 `d5a8cba6b` (FE) : `SocketPostCreatedData` gagne `clientMutationId` (+ public init) ; le publisher `postCreated` porte le payload complet ; `FeedViewModel.subscribeToSocketEvents` reconcilie par cmid (echo matchant un post optimiste id==cmid → remplace en place cmid→server id, préserve isLiked, pas de doublon ; echo sans match = insert normal). ST3 `b2b66ae89` (FE) : `FeedViewModel.createPost` **text-only POST** route via l'outbox (post optimiste id=cmid + `enqueue(.createPost)` + rollback sur refus/`.exhausted`) ; `CreatePostPayload` gagne `originalLanguage` (additif) + `dispatchCreatePost` le forwarde (zéro régression Prisme). Scope honoré : media/audio/transcription gardent le chemin direct (U1b futur) ; gate `type=="POST"` (gateway n'echo le cmid que sur la branche POST). Boucle complète : offline → optimiste → outbox → reconnect → POST idempotent (withMutationLog) → echo {post,cmid} → reconcile (pas de doublon). Tests : FeedViewModelTests 60/60 (+4 createPost +2 reconcile), SDK MutationPayloads/SocialSocketAdditional verts, gateway 44/44 + tsc clean. _DONE._ Description originale ↓\
  ~~**createPost offline silencieusement perdu**~~. `FeedViewModel.createPost` (:435-465) appelle `postService.create` direct ; `catch { publishError = ... }` ; AUCUN enqueue → post tapé perdu offline (pattern S7). La machinery `.createPost` est PRÉ-BÂTIE et morte côté caller : `OutboxKind.createPost` (OutboxRecord:46), `CreatePostPayload` (MutationPayloads:213), `dispatchCreatePost` (OutboxDispatcher:348-379, POST /posts + X-Client-Mutation-Id), SyncPill label. Fix : router le createPost **text-only** via l'outbox (cmid + CreatePostPayload + enqueue(.createPost) + insert optimiste temp + reconcile temp→server au socket post:created, miroir S11) + rollback sur enqueue-fail (miroir likePost). **Scope honnête** : CreatePostPayload ne porte que content/attachmentIds/visibility → audioUrl/transcription/media-non-uploadés PAS représentables → text-only ; posts media/audio = write-ahead S7b-style séparé (U1b futur). Test FeedViewModelTests via MockOfflineQueue : assert enqueueCalls.kind==.createPost + rollback. **⚠️ SCOPE VÉRIFIÉ = FE+BE COUPLÉ (medium, pas « connect caller »)** : `dispatchCreatePost` ne reconcilie PAS (POST + log seulement), et le handler `postCreated` (FeedViewModel:697-707) dédup par **server id seulement** (`posts.contains{$0.id==feedPost.id}`). `emitToFriends` (SocialEventsHandler) **INCLUT l'auteur** (`targetIds=[...friendIds, authorId]`, « Inclure l'auteur pour feedback immédiat ») → l'auteur offline reçoit son propre post:created (server id) qui NE matche PAS le temp post (cmid) → **DOUBLON persistant** (jusqu'au prochain reload). Donc la durabilité FE seule échange « post perdu » contre « post dupliqué » (régression) → durabilité + reconcile sont **couplés**. Fix complet coordonné : (BE) `broadcastPostCreated`/payload post:created inclut `clientMutationId` ; (FE) `SocketPostCreatedData`+handler reconcilient le temp post (id==cmid) → remplace par server id au lieu d'insérer. À faire en chantier FE+BE dédié décomposé. _Reconcile = integration._
- [x] **U2** (P2, sdk) — ✅ **CORRIGÉ**. `NotificationCoordinator` : `currentUserIdProvider` injecté (défaut AuthManager) + `handleReadStatusUpdated(_:)` extrait + abonné à `MessageSocketManager.shared.readStatusUpdated` ; gate **`type=="read" && eventUser==currentUser`** (miroir SyncEngine — PAS sur 'received', sinon flicker) → `markConversationRead`. 3 tests (currentUser-read reset / 'received' no-reset / other-user no-reset) GREEN, régression OK. Détail original : `NotificationCoordinator.applyConversationUnread` (:175-181) n'a PAS de gate open-conversation (contrairement à `ConversationSyncEngine.handleUnreadUpdated` :918-924 qui force 0 si conv ouverte) + n'est jamais reset sur le read path (markAsRead ne touche pas NotificationCoordinator ; markConversationRead appelé 1× à l'open seulement). → badge grimpe pendant que l'user lit. Fix (recommandé) : `NotificationCoordinator` s'abonne à `MessageSocketManager.shared.readStatusUpdated` (subscribeToSocketEvents :214) → `markConversationRead(event.conversationId)` si type=='read' && user==currentUser (miroir SyncEngine.handleReadStatusUpdated:948). Test NotificationCoordinatorTests : applyConversationUnread(3) → publish ReadStatusUpdateEvent(read, currentUser) → assert total==0 (+ négatif : type 'received' ne reset pas).
- [x] **U3** (P2, sdk) — ✅ **CORRIGÉ**. Guard `pendingOptimisticProfile` dans AuthManager via fonction pure `resolveServerUserWithOptimistic(server, pending) -> (user, clearedPending)` (merge optimiste sur le user serveur + self-clear quand le serveur reflète l'edit). Câblée dans `updateUserAfterRevalidation`:648 + `applySession`:578 ; set dans `applyLocalProfileChanges` (+ saveUserToKeychain cold-start) ; cleared dans `restoreLocalProfileSnapshot` + `logout()` (anti-leak cross-user). **Additif = zéro changement du chemin non-edit** (pending nil → server inchangé). 3 tests pure (stale→garde optimiste / reflète→clear / nil→inchangé) + régression auth (profile-mutation + refresh + rotation + logout) verte. Détail original : `EditProfileViewModel` applique optimiste + rollback sur `.exhausted` seulement ; mais `AuthManager.updateUserAfterRevalidation` (:647-648 `currentUser=user`) + `applySession` (:578) écrasent INCONDITIONNELLEMENT (revalidation boot me() :503-518 + refresh sur tout 401 :532-535). + `applyLocalProfileChanges` (:809-826) n'écrit PAS le keychain → cold-start hydrate l'ancien profil. **DESIGN VÉRIFIÉ (prêt à implémenter, sensible=auth → next tick) :** approche **self-contained AuthManager, additive (zéro risque non-edit)** + **testable via fonction pure**. Touch points : (1) `private var pendingOptimisticProfile: ProfileSnapshot?`. (2) `static func resolveServerUserWithOptimistic(_ server:, pending:) -> (user, clearedPending: Bool)` — pure : pending nil→(server,false) ; server reflète déjà l'optimiste (3 champs ==) →(server,true=clear, l'edit a propagé) ; sinon →(merge via withProfileChanges, false). (3) `applyLocalProfileChanges` : set pending = optimistic snapshot + `saveUserToKeychain(updated, userId: updated.id)` (cold-start). (4) `restoreLocalProfileSnapshot` : `pendingOptimisticProfile=nil` + keychain (rollback). (5) `updateUserAfterRevalidation`:648 + `applySession`:578 : `let r = Self.resolveServerUserWithOptimistic(user, pending:); currentUser=r.user; if r.clearedPending { pendingOptimisticProfile=nil }`. (6) `logout()`:362 : `pendingOptimisticProfile=nil` (sinon LEAK cross-user → merge l'edit de l'ancien user au nouveau). **GOTCHAS** : pas d'AuthManagerTests existant (créer) ; `updateUserAfterRevalidation` est `private` (non-testable direct → tester la fonction pure `resolveServerUserWithOptimistic`, callable via @testable car internal static) ; `MeeshyUser(id:username:displayName:)` init dispo pour les tests ; withProfileChanges coalesce (param ?? self) → clear-to-nil d'un champ = limite pré-existante (cas commun set-valeur OK). **RÉSIDU** : cold-start-revalidation-clobber si edit pas encore propagé (pending in-mem=nil après relaunch) → transitoire, self-heal au flush outbox ; keychain-persist couvre l'affichage initial. Tests : resolveServerUserWithOptimistic pure (pending≠server→garde optimiste ; ==→clear ; nil→server inchangé).
- [x] **U4** (P3, sdk) — ✅ **CORRIGÉ**. `requestTranslation` bufferise désormais INCONDITIONNELLEMENT (avant : émettait + return si connecté → rien à rejouer si drop pendant le traitement). Émet toujours si connecté (gate passé de `socket?.status` à `isConnected` settable → rend le cas connecté testable ; le buffer backstop toute différence de gate). flushBufferedTranslationRequests rejoue au reconnect (idempotent serveur, TTL 60s+dedup+cap). Test `_whenConnected_stillBuffers` (isConnected=true → assert bufferisé) + 4 tests existants verts. Détail original : `MessageSocketManager.requestTranslation` (:1304-1317) émet `translation:request` immédiatement si connecté + ne buffer QUE dans la branche déconnectée → si le socket blip pendant Whisper/NLLB, le `message:translation` broadcast (fire-and-forget) est droppé ; reconnect ne re-demande pas (flushBufferedTranslationRequests rejoue seulement les bufferisés-déconnectés ; syncMissedMessages ne re-fetch que les messages plus récents). Self-heal seulement en quittant/rouvrant la conv. Fix : **toujours** bufferiser la requête (en plus d'émettre si connecté) — TTL 60s + dedup + cap 50 bornent déjà ; flushBufferedTranslationRequests rejoue au reconnect ; requête idempotente serveur. Test TranslationRequestBufferTests : socket connecté + requestTranslation → assert debug_pendingTranslationRequests contient l'entrée.

**Ordre révisé (après scoping U1)** : U1 s'avère **FE+BE-couplé medium** (durabilité + reconcile-by-cmid inséparables car `emitToFriends` inclut l'auteur → doublon). Donc : **U2** (small sdk-scheme clean, badge gate) → **U4** (small sdk-scheme clean, translation buffer) → **U3** (medium sdk-scheme, profile guard) → **U1** (chantier FE+BE coordonné dédié, décomposé : BE cmid echo + FE outbox-route+optimistic+reconcile). Lead avec les wins masterables, U1 en chantier coordonné. Méthodo : preuve(RED confirme le bug)→TDD→vérif→commit isolé `git add` par fichier.

## Wave 2 — Round 5 (5e ré-analyse découverte, workflow `wf_63d4e282-1ab`, 2026-06-02)

7 sous-systèmes NON encore audités balayés évidence-first (Explore) + vérif adversariale par finding : **15 findings → 2 confirmés / 13 réfutés**. Sur-signalement ~⅔ encore (cohérent Round 2-4). Plusieurs « fixes proposés » réfutés auraient **régressé** (scroll-anchor sur collection-view inversée auto-ancrée ; read-cursor déjà server-authoritative ; post-update media diff lisant l'état post-mutation ; SyncEngine stomp-race sur le cache "list"). Réfutés notables (tous vérifiés morts/déjà-gardés/spec-périmée) : scroll-anchor (dead code post-migration UIKit), read-cursor cross-device (cursor serveur canonique + recompute frais), thumbnail/media invalidation ×3 (caches **content-addressed** par SHA256(URL) → URL immuable = invalidation inutile ; UpdatePostSchema n'autorise pas le changement de media URL), call-missed (timer ring local 45s socket-indépendant), typing-TTL (safety-timer 15s borne déjà), conversation-metadata ×3 (chemin optimiste mort/`.updateConversation` jamais enqueué + cache "list" déjà persisté par le VM).

### Confirmés
- [x] **D2** (P2, ios, divergence) — ✅ **CORRIGÉ** `76f8f4003`. `GlobalSearchViewModel` LRU message-query (5 entrées, 2min TTL) sans invalidation socket → après rename/avatar/suppression d'une conversation, re-taper la même requête dans la TTL servait un `conversationName` périmé (titre de ligne) ou une ligne morte. Fix : inject `MessageSocketProviding`+`SocialSocketProviding` (miroir ConversationListViewModel) + clear coarse du cache sur `conversation:updated`/`conversation:deleted` (`.receive(on:.main)`, publishers off-main). RED prouvé (delta 1 vs 2) → GREEN. 23/23 tests, +2 (helper `drainMainQueue` déterministe, pas de Task.sleep).
- [ ] **D1** (P2, sdk, data-loss) — **story draft perdu au crash/suspend OS**. `StoryComposerView.saveDraft()` (StoryComposerView.swift:2167, persiste via `StoryDraftStore.shared` GRDB) n'est appelé QUE depuis 2 chemins user-initiés : bouton menu « Sauvegarder le brouillon » (:796) + dialog confirm « Sauvegarder » (:629→saveDraftAndDismiss:2153). `onDisappear` (:379) ne sauve PAS. Zéro `.onChange/.adaptiveOnChange→saveDraft` (contraste : ConversationView:816-823 a 7 triggers réactifs `persistDraft`). Aucun presenter (StoryTrayView/StoryViewerView/UnifiedPostComposer) ne sauve le draft sur scenePhase `.background`. → suspend/force-kill/crash avant un Save explicite = slides/effects/media/stickers perdus silencieusement. **⚠️ DESIGN VÉRIFIÉ : fix = save sur scenePhase `.background`/`.inactive` (pattern MeeshyVideoEditorView.swift:66 + AdaptiveOnChange.swift), PAS `onDisappear`** — car le chemin discard (`cancelAndDismiss`/`handleDismiss` empty → `clearAllDrafts()`:2214) fire AUSSI `onDisappear` → un save dans onDisappear re-persisterait un brouillon qu'on vient de jeter (régression). **⚠️ BLOQUÉ : `StoryComposerView.swift` est activement modifié par l'agent story-drawing parallèle (worktree partagé)** → chantier coordonné à faire quand l'agent a fini OU via worktree dédié, sinon conflit. TDD : observer scenePhase → assert `StoryDraftStore.save` appelé ; négatif : discard ne re-sauve pas.

## Suivi U1b — durabilité offline des posts media (suite de U1)

- [x] **U1b** (P1, both) — ✅ **TERMINÉ END-TO-END (2026-06-02)**. ST1 `1b450116b` (dispatcher : `dispatchCreatePost` upload TUS de `CreatePostPayload.localMediaPaths` au reconnect + cleanup) → ST2a `a589b42b7` (SDK : `OfflineQueue.enqueuePostMedia` write-ahead, helper `copyPendingMediaFiles` extrait/réutilisé) → ST2b core `05cabb6c0` (`FeedViewModel.createOfflineMediaPost` : post optimiste id=cmid + preview FeedMedia local + `enqueuePostMedia` via protocole + rollback ; `EnqueueMediaResult` public init + `OfflineQueueing.enqueuePostMedia` + MockOfflineQueue ; FeedViewModelTests 63/63) → ST2b wiring `28e6811d4` (2 gates View `publishPost`/`publishPostWithAttachments` offline → createOfflineMediaPost, garde `audioURL==nil`). Boucle : offline → optimiste → outbox → reconnect → TUS+POST → cmid echo reconcile (pas de doublon). Scope : visual media (photo/vidéo) ; audio offline = futur. ✅ **Vérif (a) FERMÉE (2026-06-02)** : l'agent parallèle a committé son travail StrokeWidthMapping → test bundle SDK débloqué → tests SDK `enqueuePostMedia` + `enqueueMedia` (régression helper extrait, OK) + `cleanupLocalFiles` + `CreatePostPayload` roundtrip TOUS VERTS. ⚠️ Reste (b) **smoke device** (View wiring non-unit-testable, miroir S7b/S8 : post media offline survit au kill + s'envoie au reconnect sans doublon) — nécessite device utilisateur.

## Wave 2 — Round 6 (6e ré-analyse découverte, workflow `wf_6f8ef3cd-535`, 2026-06-02)

6 sous-systèmes non audités (friend/block, notifications, settings, downloads, forwarding, membership) : **16 findings → 6 confirmés / 10 réfutés**. Réfutés solides : notif counts sync déjà cross-device (notification:counts → NotificationCoordinator), read/deleted = dead events délibérés ; download all-or-nothing (aucune persistance partielle, network-drop passe par catch, self-heal au re-tap) ; forward attachments copiés server-side (copyForwardedAttachments) ; ConnectionActionView/BlockedViewModel direct = catch gère fort (pas de perte silencieuse) + SDK purity.

### Confirmés — cluster A : gateway participant emits (divergence temps réel, contention-free) ✅ LIVRÉ `e633047f3`
- [x] **R6-1** (P1, gateway, divergence) — ✅ **CORRIGÉ** `e633047f3`. POST émet `CONVERSATION_JOINED {conversationId,userId}` vers `ROOMS.conversation(id)` après create (miroir role-update emit). io-guarded. iOS réagit (ParticipantsView invalidate+reload, SyncEngine invalidate).
- [x] **R6-2** (P2, gateway, divergence) — ✅ **CORRIGÉ** `e633047f3`. DELETE émet `CONVERSATION_PARTICIPANT_LEFT {conversationId,userId,displayName,leftAt}` (displayName capturé via findFirst avant inactive ; leftAt partagé DB+emit). iOS réagit (ParticipantsView drop, ConversationListViewModel count−1, SyncEngine invalidate). participants.test.ts 82/82 (+2 RED→GREEN +graceful no-io).
- [x] ~~**R6-1**~~ (orig) — **POST /conversations/:id/participants n'émet AUCUN socket** (participants.ts:~317 `prisma.participant.create` sans `io.to(...).emit`). L'endpoint role-update (:626-635) émet PARTICIPANT_ROLE_UPDATED → infra existe. iOS POST via AddParticipantSheet:324 ; iOS s'abonne à `conversation:joined` + invalide cache participants (ParticipantsView:128-137, ConversationSyncEngine:691-696). Sans broadcast → liste membres des AUTRES devices périmée jusqu'à reload manuel. Fix : émettre vers `ROOMS.conversation(id)` après create (vérifier event exact + payload + incrément count côté iOS). _Le device émetteur self-reload via le sheet → pas de double-load._
- [ ] **R6-2** (P2, gateway, divergence, masterable) — **DELETE /conversations/:id/participants/:userId n'émet AUCUN socket** (participants.ts:~446 `prisma.participant.updateMany` sans emit). leave.ts:73 émet CONVERSATION_PARTICIPANT_LEFT → infra existe. iOS DELETE via ParticipantsView:583 ; iOS s'abonne à `conversation:participant-left` (ParticipantsView, ConversationListViewModel:714 décrément count, ConversationSyncEngine:698 invalide). Fix : émettre **CONVERSATION_PARTICIPANT_LEFT** (`conversation:participant-left`, PAS `conversation:left` qui est self-only ack) vers la room (parité leave.ts + feed le count).

### Confirmés — cluster B : friend/block outbox-wiring (data-loss offline, caveats)
- [ ] **R6-3** (P2, ios, data-loss) — **NÉCESSITE DESIGN (pas un mirror trivial)**. **DiscoverViewModel.sendRequest** (:118-129) appelle `friendService.sendFriendRequest` direct, pas d'outbox → demande d'ami perdue offline/crash. `.sendFriendRequest` kind+payload+dispatcher EXISTENT mais ZÉRO caller (dead-but-ready). ⚠️ **PROBLÈME requestId ASYMÉTRIE (vérifié 2026-06-02, raison de ne PAS rusher)** : le chemin actuel est network-first → succès = `cache.didSendRequest(to:requestId: request.id)` avec l'id SERVEUR, et `cancelSent` rappelle `deleteRequest(requestId:)` avec cet id. Un enqueue offline n'a PAS d'id serveur → un optimiste `.pendingSent(requestId: cmid)` porterait un id bidon → un cancel ultérieur fait `deleteRequest(cmid)` → 404, ET il n'y a PAS de reconcile (OutboxOutcome.applied(cmid:) ne porte que le cmid, pas le résultat serveur). C'est exactement l'asymétrie qui a fait RÉFUTER le sibling ConnectionActionView. Contrairement à RequestsViewModel accept/reject (qui opèrent sur un requestId serveur DÉJÀ existant), le SEND n'a pas d'id avant le réseau. **Fix correct = design d'un reconcile cmid→server-id** : soit (a) un event socket `friend-request:created` que FriendshipCache consomme pour remplacer le cmid par l'id réel, soit (b) un refresh relationship au reconnect qui écrase le cmid, soit (c) le dispatcher met à jour FriendshipCache avec l'id serveur. Sans ça : régression UX online OU cancel-404. NON-optimiste actuellement (catch montre erreur, PAS de perte silencieuse) → défaut = perte d'intent / pas de durabilité offline (P2, pas corruption). ⚠️ 2 call sites SDK (ConnectionActionView, UserProfileSheet) = SDK purity, EXCLUS. → **Différé : chantier design dédié (vérifier d'abord si un event/refresh de reconcile existe déjà), pas un quick mirror.**
- [ ] **R6-4** (P2, ios, data-loss, masterable) — **ConversationListView block/unblock** (:449 swipe, :626 dialog) + **ConversationInfoSheet.blockOtherUser** (:1241) + autres (ConversationListView+Overlays:190, BlockedUsersView:240) appellent `BlockService` direct, pas d'outbox → block perdu offline. Miroir UserProfileViewModel:137-171 (`.blockUser`/`.unblockUser` + observeOutcome). ⚠️ CAVEAT : UserProfileViewModel ne met PAS à jour `BlockService.shared.blockedUserIds` (son commentaire ment) ; le swipe label lit `BlockService.isBlocked` → le fix DOIT aussi MAJ `blockedUserIds` optimiste + rollback sinon label périmé. ⚠️ ConversationInfoSheet = View struct → router via un petit VM (MVVM). Plusieurs call sites → chantier groupé.

### Confirmé — cluster C : différé
- [ ] **R6-5** (P3, ios, divergence, NON-masterable) — `UserPreferencesManager.fetchFromBackend` (:122) appelle `applyRemote` sans checker l'outbox pending → un fetch online (ré-entrée SettingsView dans la fenêtre debounce+roundtrip) peut clobber un toggle de préférence optimiste. ⚠️ Fix proposé CASSÉ (`pendingItems` = miroir messages, pas de .payload pour décoder UpdateSettingsPayload) → exige une NOUVELLE query outbox-table (filter kind==updateSettings). Probabilité faible (online-only, fenêtre étroite). DIFFÉRÉ (P3 + non-masterable + offline N/A car getAllPreferences throw offline).

## Review log
_(rempli au fil des tâches)_
