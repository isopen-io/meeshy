# Confidentialité de la présence — 2026-08-25

Plan : `docs/superpowers/plans/2026-08-25-presence-privacy.md`. Worktree `../v2_meeshy-presence`, branche `feat/presence-privacy-2026-08-25`.

Directive : hors amitié, ni état en ligne ni dernière connexion ; seule l'activité dans une conversation (m'écrire / répondre) révèle que je suis en ligne ; ADMIN et au-dessus voient toujours.

- [x] Inventaire (gateway, iOS, web+Android) — 3 agents, aucune modification client requise
- [x] W1 loi + service + `GET /users/presence` + décision
- [x] W2a conversations · W2b messages/appels/liens · W2c communautés · W2d socket · W2e stories/admin — rendus 2026-08-25 soir (rapports `scratchpad/w2*-report.md`) ; + W2f stats (hors plan)
- [ ] W3 : `resolvePrefsOnly` SUPPRIMÉE + garde `presence-visibility-viewer-aware-guard.test.ts` [x] · CLAUDE.md § présence [x] · shared 109/2627 vert [x] · W3a `onlineOnly` fuite par sélection [x] (128/128, mutation 6 rouges) · W3b repli « entrée absente » en un site [x] (`presenceFor` / `presenceMissingEntryPolicy` dans `presence-gate.ts` ; core ×4, search, messages ; 5 rouges prouvés ; suivi `participants.ts` ×3 sites FAIT par le lead — 134/134, témoin rouge « inscrit ABSENT ⇒ caché USER ») · W3c fuite par ORDONNANCEMENT [x] (`mayOrderByRawPresence` + `servedOnlineFirst` dans `presence-gate.ts` ; fill restreint `joinedAt` seul ; `/users/search` sans clé de présence pour un non-admin, page stabilisée sur la présence servie ; 7 rouges par mutation) · revue opus workflow #1 [x] (84 agents, 8,1 M jetons : 28 confirmés/4 plausibles/6 réfutés — réfutés = déjà corrigés par W3a/W3c pendant la revue) → 8 correctifs F1-F8 [ ] · suite gateway complète AVANT la vague F [x] (880 suites / 19 987 tests verts, 22 min) · suite gateway complète APRÈS F1-F12 [x] (par tranches en avant-plan : 880 suites / 20 057 tests verts ; shared 109/2629 + tsc 0 ; web 793/14 650) · iOS `MeeshyTests` complet [ ]
- [x] Commit `37dbd85711` (111 fichiers, +7994/−1597, par chemins explicites) · PR **#3523** → `main` (2026-08-26 02:45) ; CI en cours

## Suivis
- web `useUserStore` : `lastActiveAt: null` reçu ne purge pas l'ancien horodatage (comportement préexistant) — à durcir si le produit veut l'oubli immédiat
- Android : `typing:start` ne force pas la présence (parité web/iOS) — hors directive

## Sites trouvés HORS plan pendant W2 (2026-08-25, soir)
- [x] **W2f** `conversation:stats.onlineUsers` — liste d'IDENTITÉS en ligne (id, username, prénom, nom) calculée par `ConversationStatsService.computeOnlineUsers` et émise par socket à chaque `conversation:join` (`ConversationHandler.sendConversationStatsToSocket`) → projection par destinataire via `resolveForTargets(viewer)`, cache inchangé (trouvé par la revue W2c)
- Décisions à graver (revue) : sur `/links/:identifier` les MEMBRES inscrits restent `isOnline:false` pour tous, ADMIN+ compris (plus strict que la loi, aucune fuite) ; sur l'éventail socket un ADMIN+ BLOQUÉ est exclu (`.except`) alors que REST/snapshot lui rendent FULL — divergence pré-existante ; retard ≤ 60 s d'une promotion de rôle sur le canal socket (cache admins)
- Candidat W3 : le bypass ADMIN+ pour une CIBLE ANONYME est écrit en ligne dans 3 sites (`users/presence.ts`, `messages.ts`, `links/retrieval.ts`) → helper unique du service
- À trancher en revue (W2a-2) : `GET …/messages` (liste, épinglés, recherche) ne CHARGE aucune présence (`messageSelect.sender` / `messageSenderUserSelect` sans `isOnline`/`lastActiveAt`, `userMinimalSchema` tronque `lastActiveAt`) — la porte par destinataire y coûte jusqu'à 4 requêtes + prefs par page sur le chemin le plus chaud, pour des valeurs jamais servies. Alternative : garde de source « le select de l'expéditeur ne porte pas de présence » + porte retirée de ce chemin
- Sémantique unifiée (W2a-2) : entrée ABSENTE de la carte ⇒ masquée, sauf viewer ADMIN+ (`PresenceMissingEntryPolicy` + `applyPresenceVisibilityAsOffline`) ; avant, `?.showOnline === false` laissait passer `undefined` ⇒ révélé
- Doutes W3a : (1) CLOS — `onlineOnly` n a qu UN consommateur, `bubble-stream-page.tsx:475` (web, première page seule, jamais paginée) ; iOS et Android ne l envoient pas ; `use-contacts-data` filtre côté client sur un `isOnline` déjà gaté ; (2) `acceptedFriendIds` + `resolveForTargets` relisent l'amitié deux fois par appel non-admin (fusion = service, suivi) ; (3) `servedOnline` s'applique aussi au privilégié (seule différence : une ligne désactivée-mais-`isOnline` sort de la page)
- Fuite par ORDONNANCEMENT (W3a, doute 4) → W3c : même famille que la sélection ; leçon 288

## Revue workflow #1 (2026-08-26, 00:xx) — constats dédoublonnés → correctifs
- F1 communautés : `POST /communities/:id/conversations/:conversationId` — `additionalProperties: true` + `include` participants ⇒ `Participant.isOnline/lastActiveAt` (ligne) + `sessionTokenHash`/`anonymousSession` partent BRUTS pendant que `gateRow` ne masque que `row.user` (BLOCKER) ; `gateCoMemberPresence` sans `deactivatedAt` (fail-open)
- F2 liens + messages : `links/retrieval.ts:251` `lastActiveAt: joinedAt` part À CÔTÉ d'`isOnline` gaté ⇒ pastille web quand même (leçon 275/284 : ce qui part à côté) ; `messages.ts:384` repli inline → `presenceFor`
- F3 **troisième site hors plan** : `GET /affiliate/stats` sert `referredUser.isOnline` BRUT à tout parrain (`additionalProperties: true`) — l'affiliation n'est pas une amitié
- F4 `GET /admin/users?lastActiveAfter=&sortBy=lastActiveAt` : fuite par SÉLECTION/ORDRE ouverte à MODERATOR/AUDIT ; `sortBy` sans liste blanche
- F5 `GET /mentions/suggestions` : co-participants CLASSÉS et TRONQUÉS (10) par `lastActiveAt` — l'ordre dit la présence
- F6 socket : `_broadcastUserStatus` ignore `deactivatedAt` (désactivation ne révoque pas les sockets)
- F7 shared : `serializeConversationParticipant` fail-OPEN sur `presence` absente (doc contradictoire à 6 lignes ; témoin shared qui ÉPINGLE le défaut) ; `services/gateway/CLAUDE.md:421` prescrit encore `resolvePrefsOnly` ; docs périmées (`postIncludes.ts`, `presence-gate.ts:106`, `preferences.ts:655`) ; garde de source sans contrôle POSITIF ; `PublicUser.lastActiveAt: Date` non nullable vs `null` écrit
- F8 web : `useUserStore.mergeParticipants` REJETTE une charge masquée (`lastActiveAt: null` ⇒ `incomingTime = 0`) et garde l'ancienne présence ⇒ pastille survit ≤ 5 min — « aucune modification client » était FAUX
- [x] F3 rendu : `isOnline: true` retiré du `select` de `referredUser` (56/56, mutation rouge). Suivis : web `types/contacts.ts` type `referredUser.isOnline: boolean` devient mensonger (`undefined` à l'exécution, `AffiliatesTab` + `ParticipantPresenceIndicator` rendent « hors ligne » sans crash) → aligner le type dans un lot web ; `email` du filleul servi au parrain = contrat produit web (`AffiliatesTab.tsx:99-103`), décision distincte ; `convertAffiliateVisit` crée une amitié AUTO-ACCEPTÉE ⇒ la pastille du filleul peut rester visible par le chemin AMI (légitime)
- [x] F4 rendu : `presenceGatedFilters(query, canViewPresence)` + liste blanche `USER_SORT_KEYS` exhaustive par le type (169/169, 8 rouges par mutation). Suivis hors lot : `analytics.ts:54,195-206,301` agrégats `user.count` sur `lastActiveAt` pour AUDIT/ANALYST (sans identité — hors directive, à trancher) ; `reports.ts:98` + `report.service.ts:93-95` `orderBy[sortBy]` sans liste blanche (même forme, sans présence)
- [x] F6 rendu : `deactivatedAt` lu + `return` avant toute émission (405/405 manager, mutation prouvée) ; snapshot déjà juste par la loi (témoin ajouté). **Cause racine, candidat F9** : `user-management.service.updateStatus` pose `deactivatedAt` SANS révoquer les sockets (`disconnectRevokedSessions` n'est appelé que par `revoke-all-sessions` et un autre site) — un désactivé connecté reçoit ses fils, émet `typing:start` (= vert chez les clients), et `_presenceViewer` ne lit pas `deactivatedAt` du LECTEUR ; réactivation sans transition émise
- [x] F5 rendu (agent coupé par la limite de session APRÈS restauration du mutant — prouvé `cmp` identique au correctif ; rapport reconstitué par le lead) : tri des suggestions par `participantStats` (activité DANS la conversation), `lastActiveAt` hors `select` ; 20 suites/410 verts ; mutant ⇒ 8 rouges
- Limite de session 01:0x → six agents coupés (F1 mi-implémentation après RED 13/52 ; F2 patché, gates non rendues ; F7 8 fichiers écrits, non vérifiés ; F8 store + 9 témoins verts 40/40, mutation/tsc/rapport manquants ; F9 mi-chemin). Relance à 01:20 en mode finalisation + vérification de complétude par le lead (rapport présent + gates rejouées)
- [x] F2 rendu et VÉRIFIÉ COMPLET (rapport, 2 mutations ⇒ 4 rouges chacune, 33 suites/611, tsc 0) : `links/retrieval.ts` `lastActiveAt` sous le même prédicat qu'`isOnline` (anonymes) et `null` pour les membres inscrits (plus strict que la loi, ADMIN compris — à confirmer produit) ; `messages.ts` repli inline → `presenceFor`
- [x] F7 rendu et VÉRIFIÉ COMPLET (rapport, M1 fabrique fail-closed ⇒ 3 rouges, M2 garde sans balayage ⇒ 1 rouge ; shared tsc 0 / vitest 109/2629 / build OK ; gateway 6 suites 454/454, tsc 0) : `serializeConversationParticipant` fail-closed ; `services/gateway/CLAUDE.md` § présence réécrit ; docs `postIncludes`/`presence-gate`/`preferences` ; garde à 3 racines + contrôles positifs ; `PresenceAsOffline<T>` + `PublicUser.lastActiveAt: Date | null`
- [x] F1 rendu et VÉRIFIÉ COMPLET (rapport, mutation (a) schéma rouvert ⇒ 2 rouges, (b) ligne brute ⇒ 3 rouges ; 15 suites/352, tsc 0) : POST `/communities/:id/conversations/:conversationId` schéma FERMÉ (`communityConversationSchema`, site unique `serialization.ts`) ; `gateRow` masque la LIGNE Participant + `user` ; `sessionTokenHash`/`anonymousSession`/IP ne partent plus ; `deactivatedAt` obligatoire dans `CoMemberUser`
- [x] F8 rendu et VÉRIFIÉ COMPLET (rapport, 3 mutations ⇒ 3/2/2 rouges ; store 40/40, stream-socket 10/10, presence.service 51/51 ; tsc web 1194=1194 hors fichiers) : `mergeParticipants` applique toujours une charge masquée ; `updateUserStatus` — la CLÉ gouverne ; hook `use-stream-socket` déjà juste (+1 témoin). **D1 → F10** : `ConversationLayout.tsx` re-sème le store depuis le cache React Query — une charge DATÉE de cache ranime une pastille par-dessus un masque `null` jusqu'au refetch
- [x] F9 rendu et VÉRIFIÉ COMPLET (rapport, 2 mutations ⇒ 2/1 rouges ; 9 suites/313, tsc 0) : DEUX écrivains runtime de `deactivatedAt` (`UserManagementService.updateStatus` et `routes/admin/roles.ts:260` — le geste écrit deux fois) révoquent les sockets via `disconnectRevokedSessions` (injection `UserManagementServiceDeps.revokeSessions`, résolue à l'appel) ; handshake socket + REST refusent déjà un inactif. **F9 bis** : `deleteUser` (`isActive:false`), `delete-account.ts:296`, `MaintenanceService.ts:702` ne coupent rien
- [x] F10 rendu et VÉRIFIÉ COMPLET (rapport, mutation ⇒ 3 rouges ; 4 suites web/106 ; tsc 1194 = socle) : `mergeParticipants(…, { presence: 'keep-existing' })` pour le re-semis depuis le cache RQ (`ConversationLayout`) ; 4 autres écrivains classés VIVANTS. **F11 iOS** : `PresenceManager.seed(from:)` écrit `presenceMap` sans condition depuis la liste cache-first, carte PERSISTÉE sur disque et rechargée au démarrage. Android : aucun détenteur d'état présence nommé (à inventorier). Web à classer : `ConversationSettings.tsx:299`, `UserProfileContent.tsx:355-379`, `conversation-participants-drawer.tsx:206-215` rendent la présence depuis leur payload sans le store
- Classification web (lead) : `ConversationSettings.tsx:299` = charge REST fraîche gatée (vivant) ; `conversation-participants-drawer.tsx` = écrit le store depuis un fetch frais (vivant) ; `UserProfileContent.tsx:114-120` = sur `user:status` masqué, GARDE `prevUser.lastActiveAt` et fabrique `new Date()` si en ligne → **F12** (défaut F8 en miniature). Android : aucun détenteur d'état ni cache Room de présence — rendu direct des modèles gatés, rien à faire
- [x] F9 bis rendu et VÉRIFIÉ COMPLET (13 suites/324, mutation ⇒ 2 rouges) : `deleteUser`, `routes/me/delete-account.ts` (`delete-now`), `MaintenanceService.processAccountDeletionRequests` (désactivation EN LOT) révoquent les sockets ; `reason: 'logout_all_devices'` faute d'`account_deleted` dans l'énum shared (suivi). **Câblage du setter posé par le lead** dans `MeeshySocketIOManager` (témoins rouge→vert, 433/433, tsc 0)
- [x] F12 rendu et VÉRIFIÉ COMPLET (10/10, mutation ⇒ 1 rouge, tsc socle) : `UserProfileContent.onUserStatus` — la valeur REÇUE gouverne, plus de `new Date()` fabriqué ; libellé « En ligne » sans horodatage = question produit
- [x] F11 iOS rendu et VÉRIFIÉ COMPLET (rapport, mutation ⇒ 4 rouges, 7 suites/97 verts sur simu dédié) : `PresenceManager.seed(from:)` = un `merge` « connu ⇒ garde, inconnu ⇒ reçoit » ; garde anti-stale SDK vérifiée (rien à changer) ; pbxproj +4 lignes (fichier de test seul). Suivi : un masque (`lastActiveAt:nil`) n'est pas persisté ; Android lit la présence REST directement depuis les modèles (même famille si roster servi d'un cache daté — non vérifié)
- iOS `MeeshyTests` complet : run local INVALIDE (critère : 103 relances du hôte de test « unexpected exit, crash, or test timeout » dans des classes sans lien — CallTranscriptionService, AudioRecorderManager, BubbleContentMatrix, AuthService… — pendant qu'une session voisine faisait tourner `xcodebuild test -enableCodeCoverage` sur un autre simulateur ; charge 600, ~64 Mo libres). 1840 cas passés / 0 échec avant arrêt. Gate valide retenue : 7 classes touchant `PresenceManager` = 97/97 (F11) + CI de la PR. À rejouer sur machine calme si exigé
