
## LENTIL — LENTILLE 4 — Synchroniser TOUS ses contacts, pas seulement 200 (ni 2000, ni 5000) — 4 constats, 10 conformes, 4 questions

Chaîne complète vérifiée fichier par fichier. Lecture device (CNContactStore, sans predicate → tous conteneurs/comptes) : OK, lit tout. Filtre (au moins 1 identifiant) : OK. Puis DOUBLE plafond dur à 2000 : client (`ContactSyncService.prefix(maxContactsPerSync)`) ET gateway (`normalizeContacts`/`MAX_CONTACTS_PER_SYNC`), en UN SEUL POST, sans pagination côté SDK (`DirectorySyncRequest` n'a aucun champ de lot). Le gateway a déjà un garde-fou testé (downgrade replace→merge si le LOT REÇU dépasse 2000) mais il ne se déclenche JAMAIS via l'app iOS, qui tronque AVANT d'envoyer. Or les DEUX seuls sites d'appel iOS (`PhonebookViewModel`, `DiscoverViewModel`) utilisent exclusivement `mode: .replace` — donc pour tout utilisateur avec >2000 fiches exploitables, CHAQUE synchronisation supprime silencieusement du répertoire persisté les contacts au-delà de la fenêtre de 2000 retenue par `CNContactStore.enumerateContacts` (ordre non garanti), sans qu'aucun signal (log, toast, `removedCount`) n'atteigne l'utilisateur — le champ existe côté réponse mais n'est lu nulle part côté iOS. Côté lecture, le plafond à 200 a déjà été levé (2026-08-21) mais `DirectoryPaging.maxPages=25×200` réintroduit un filet dur à 5000, documenté en décision comme « Répertoire sans plafond » — actuellement inatteignable (le `replace` maintient le répertoire ≤2000) mais deviendrait un vrai défaut le jour où le plafond d'écriture est levé sans lever ce filet en même temps. `bodyLimit` (50 Mo global) et le rate-limit ne sont PAS le goulot — les constantes `MAX_CONTACTS_PER_SYNC` le sont. `web` et `android` n'implémentent PAS cette fonctionnalité aujourd'hui (zéro occurrence de `contacts/sync`).

| id | sév | safe | verdict | titre | fichiers |
|---|---|---|---|---|---|
| F1 | defect | ✓ | confirmed | Sync en mode replace + troncature client à 2000 = suppression silencieuse et récurrente des contacts au-delà d | ContactSyncService.swift:75, ContactSyncService.swift:131, PhonebookViewModel.swift:172 |
| F2 | risk | ✗ | confirmed | L'ordre d'énumération de CNContactStore n'est pas fixé — la fenêtre de 2000 fiches retenue peut varier d'une s | ContactSyncService.swift:186-200 |
| F3 | risk | ✗ | refuted | La pagination de lecture, déjà déplafonnée du 200, réintroduit un filet dur à 5000 — actuellement mort, mais c | PhonebookViewModel.swift:289, PhonebookViewModel.swift:292, decisions.md:385 |
| F4 | proposal | ✗ | — | Proposition — contrat minimal de lots pour synchroniser un carnet de taille arbitraire, réutilisant lastSynced | ContactSyncService.swift, ContactDirectoryModels.swift, ContactDirectoryService.swift |

Verdicts non confirmés :
- **F3 refuted** — Les faits cités sont exacts (`PhonebookViewModel.swift:289` `static let pageSize = 200`, `:292` `static let maxPages = 25`, commentaire `:290-291` « Filet contre un serveur qui répondrait toujours hasMore : 25 pages de 200 = 5 000 contacts, au-delà du plus gros carnet d'adresses » ; `apps/ios/decisions.md:385` « **Répertoire sans plafond** : … filet `maxPages` = 25 × 200 » ; `PhonebookViewModelTests.swift:511-513` `test_listAll_isCappedByMaxPages` avec `maxPages: 3`). Mais aucun de ces faits ne 

Correctifs corrigés par le sceptique :
- **F2** — Deux pas indépendants, sans arbitrage de fuite (il n'y en a pas).

Pas 1 — SÛR MAINTENANT, purement additif, aucun champ nouveau exposé (userSchema filtre déjà le corps) : dans services/gateway/src/routes/users/profile.ts, faire passer PATCH /users/me/avatar (select lignes 360-383) et PATCH /users/me/banner (459-482) par le même chemin que PATCH /users/me — supprimer le `select` manuel et rendre `user: formatUserResponse(updatedUser, permissions)` comme à la ligne 283, ou à défaut compléter les 

## 2c — S — 2c — Socket social (posts, commentaires, likes/reposts, stories, présence, notifications, communauté — 3 constats, 8 conformes, 3 questions

SocialSocketManager (iOS SDK) est bien aligné sur packages/shared/types/socketio-events.ts : chaque SERVER_EVENTS post:*/comment:*/story:*/status:* déclaré côté gateway a son socket.on() côté iOS avec un décodeur conforme, les absences volontaires (post:reaction-sync, comment:reaction-sync) sont documentées et correctes, et la fuite déjà connue de post:reposted non typé est bien filtrée sur le chemin socket EN DIRECT (FeedViewModel, ProfileUserPostsList). Deux défauts confirmés et un risque restent : (1) trois évènements temps réel de commentaire (comment:updated dans FeedCommentsSheet, et plus largement comment:deleted/comment:media-updated/comment:translation-updated dans les deux vues) mettent à jour l'état en mémoire mais PAS le cache CacheCoordinator.shared.comments — alors qu'un doc-comment du fichier affirme explicitement l'inverse, et que PostDetailViewModel persiste bien comment:updated ; une réouverture dans la fenêtre de fraîcheur (2 min) rejoue le contenu périmé. (2) le bandeau système au premier plan ne supprime la bannière que pour une conversation activement lue (activeConversationId) : postId est extrait et loggé dans AppDelegate mais jamais transmis au résolveur, qui n'a pas de paramètre équivalent — un utilisateur en train de lire un post reçoit quand même une bannière redondante si le socket est temporairement down. (3, risque) un repost de type STORY est persisté sans filtre dans la table GRDB feed_posts par FeedSocketHandler, alors que le chemin socket live l'exclut (belongsToStoryTray) ; le secours pagination hors-ligne de FeedViewModel lit cette même table sans filtre et pourrait réintroduire la story dans le fil pendant une navigation hors-ligne.

| id | sév | safe | verdict | titre | fichiers |
|---|---|---|---|---|---|
| F1-comment-cache-staleness | defect | ✓ | confirmed | Les mutations de commentaire reçues à distance ne réécrivent pas le cache — contredit le doc-comment du fichie | FeedCommentsSheet.swift, PostDetailViewModel.swift |
| F2-notification-banner-postid-dropped | defect | ✓ | refuted | La bannière système au premier plan n'est jamais supprimée pour un POST activement consulté — postId extrait p | AppDelegate.swift, NotificationPresentationResolver.swift |
| F3-story-repost-leaks-into-offline-feed-fallback | risk | ✓ | confirmed | Un repost de type STORY est persisté sans filtre en GRDB et peut réapparaître dans le fil via le secours de pa | FeedSocketHandler.swift, FeedStore.swift, FeedViewModel.swift |

Verdicts non confirmés :
- **F2-notification-banner-postid-dropped refuted** — Le FAIT est exact — `AppDelegate.swift:599` extrait `postId`, `602` le logge, et l'appel `605-612` ne passe que `conversationId`/`activeConversationId` ; `NotificationPresentationResolver.options` (22-30) n'a pas de paramètre post. Mais la PRÉMISSE du constat — « bannière système redondante » — est fausse, et la garde proposée retirerait de l'information au lieu d'en retirer du bruit.
Preuve contraire : la garde n'aurait d'effet que lorsque le socket est DOWN. `NotificationPresentationResolver.s

Correctifs corrigés par le sceptique :
- **F1-comment-cache-staleness** — Le correctif proposé est bon mais doit être RESSERRÉ sur un point, sinon il régresse : il persiste `liveComments ?? post.comments`. Or un sink peut tirer AVANT que `.task { loadFullCommentsIfNeeded() }` n'ait rempli `liveComments` (il est encore `nil`) : on écrirait alors les 3 commentaires embarqués du feed par-dessus une page de 20 déjà écrite par `PostDetailViewModel`/`FeedViewModel`. Reprendre exactement la garde que `submitCommentEdit` applique déjà (`FeedCommentsSheet.swift:497-501`) : ne 
- **F2-notification-banner-postid-dropped** — Rien à corriger en l'état. Si la suppression de bannière pour le post consulté est voulue, c'est une PROPOSITION produit, pas un correctif : elle n'est acceptable que couplée au pendant du traitement conversation, c'est-à-dire un rafraîchissement forcé des commentaires du post dans le même `willPresent` (miroir de `ensureMessages(for:force:)`), faute de quoi le commentaire arrivé pendant la coupure est perdu sans aucun signal. À défaut, l'amélioration utile et sans risque est l'inverse : que la 
- **F3-story-repost-leaks-into-offline-feed-fallback** — Le correctif proposé est déjà minimal et correct (`guard !data.repost.belongsToStoryTray else { return }` en tête du sink `postReposted`, `FeedSocketHandler.swift:67-71`). Deux précisions à porter dans le lot, sans l'élargir : nommer la suite réelle à étendre — `apps/ios/MeeshyTests/Integration/FeedSocketPersistenceScopeTests.swift` (et non un `FeedSocketHandlerTests` qui n'existe pas), qui dispose déjà du montage `DatabaseQueue` + `FeedPersistenceActor` + `MockSocialSocket` et du lecteur `feedA

## 3b — M — 3b — Modification effective : Conversation, Message, Communauté (endpoints, interfaces, cohérence de — 3 constats, 8 conformes, 3 questions

WT_OK confirmé. Chaîne écran→ViewModel→SDK→route→Prisma→réponse→cache→socket auditée sur les 3 entités.

Entité | Écran | Endpoint | Champs | Verdict
Conversation | ConversationSettingsView (via ConversationInfoSheet) | PUT/PATCH /conversations/:id | title, description, avatar, banner, defaultWriteRole, isAnnouncementChannel, slowModeSeconds, autoTranslateEnabled | Écriture+socket OK ; MAIS la ConversationView déjà ouverte affiche l'ancien header (defect)
Message (édition) | ConversationViewModel.editMessage | PUT /messages/:messageId | content | Écriture+broadcast+mentions+retraduction OK ; MAIS le cache client des traductions n'est pas invalidé (defect Prisme)
Communauté | CommunitySettingsView (sheet interne CommunityDetailView) | PUT /communities/:id | name, identifier, description, isPrivate, avatar, banner | already_ok, se rafraîchit correctement
Communauté (2e chemin) | Route .communitySettings (Router.swift) | jamais atteint | - | onOpenSettings n'est jamais appelé → code mort qui porte le même bug que la Conversation (risk, actuellement inoffensif)

Verdict global : les ROUTES gateway sont saines et bien durcies (déjà auditées/corrigées par des cycles précédents, tests dédiés). Les deux défauts trouvés sont côté CLIENT iOS : la confirmation serveur revient correcte mais n'atteint pas l'affichage courant — cache non patché au sens de la consigne.

| id | sév | safe | verdict | titre | fichiers |
|---|---|---|---|---|---|
| conv-settings-stale-open-view | defect | ✓ | confirmed | Éditer une conversation (titre/desc/avatar/banniere/permissions) laisse la conversation déjà ouverte affichée  | ConversationInfoSheet.swift:113-119, ConversationView.swift:287, ConversationSettingsView.swift:128-133 |
| message-edit-stale-translation-cache | defect | ✓ | confirmed | Éditer un message (le sien ou reçu d'un pair) ne purge pas le cache client des traductions — la bulle/le parta | ConversationSocketHandler.swift:578-597, ConversationViewModel.swift:3643-3680, ConversationViewModel.swift:4454-4484 |
| community-settings-dead-route-duplicate-contract | risk | ✓ | confirmed | Le chemin .communitySettings (Router) porte le même bug de callback jeté que la Conversation, mais n'est jamai | RootView.swift:367-393, iPadRootView+Panels.swift:44-69, CommunityDetailView.swift:114-135,173,378 |

Correctifs corrigés par le sceptique :
- **conv-settings-stale-open-view** — Le `fix` proposé est juste dans son intention mais sa variante « override local » est décrite sans nommer une régression réelle : renommer/étiqueter autrement le paramètre `conversation:` de `ConversationView` FERAIT ROUGIR une garde de source, `apps/ios/MeeshyTests/Unit/Focal/FocalBetaPreviewNavigationSourceGuardTests.swift:99`, qui cherche la chaîne littérale `"ConversationView(conversation: conv, previewMode: true, onOpenFullConversation:"` dans RootView.swift. Correctif minimal corrigé, sans
- **message-edit-stale-translation-cache** — Le `fix` proposé est INSUFFISANT et se réfuterait lui-même au premier cycle d'hydratation : `hydrateTranslationsFromCache` (`ConversationViewModel.swift:4262`) sélectionne précisément `let uncoveredIds = msgIds.filter { messageTranslations[$0] == nil || messageTranslations[$0]?.isEmpty == true }` puis relit CacheCoordinator (`:4234-4256`) et les `TranslationRecord` GRDB (`:4264-4291`). Vider seulement le dictionnaire en mémoire RANGE donc le message édité parmi les « non couverts » et RÉINJECTE 
- **community-settings-dead-route-duplicate-contract** — La phrase `regression_risk: "Nul si suppression pure de code mort inatteignable"` et le `tests: "Aucun test ne couvre .communitySettings"` sont FAUX : deux suites existantes référencent la route — `apps/ios/MeeshyTests/Unit/Navigation/RouterTests.swift:71-73` (`test_isHub_communitySettings_returnsFalse`) et `:304-310` (`test_push_communitySettings_addsToPath`) — et `apps/ios/Meeshy/Features/Main/Services/AnalyticsManager.swift:54` (`case .communitySettings: return "CommunitySettings"`) doit rest

## 3a — M — 3a — Modification effective : PROFIL, CONTACT, LIEN — 2 constats, 6 conformes, 3 questions

Chaîne PROFIL suivie bout en bout (iOS → SDK → gateway → Prisma → réponse → `AuthManager.currentUser`), preuve par lecture sans build.
Deux défauts INDÉPENDANTS et cumulatifs :
(1) `ProfileView.applyingProfileEdits` (l'apply optimiste) omet 7 paramètres du constructeur `MeeshyUser`, qui retombent au défaut `nil` au lieu d'être reportés — fenêtre visible jusqu'à la reconnexion en offline.
(2) Les réponses gateway du profil-SOI (`formatUserResponse`, PATCH /users/me + /avatar + /banner, GET /auth/me) sont plus pauvres que `MeeshyUser` ; comme ces 4 sites remplacent `currentUser` EN BLOC, toute sauvegarde réussie efface `isAnonymous`/`blockedUserIds`/`deviceLocale`/thumbHashes/profil vocal, SANS refetch réparateur (GET /auth/me partage le même formateur tronqué).
Conséquence prouvée : `isAnonymous ?? true` (2 sites) fait passer un utilisateur INSCRIT pour anonyme dans le résolveur Lentille (perte du mode `.summary`) après CHAQUE sauvegarde de profil, jusqu'au prochain login. Le contournement déjà écrit pour `voicePublic` seul (`applyLocalVoicePublicChange`) prouve que l'équipe a déjà buté sur ce mécanisme pour un champ, sans le généraliser.
CONTACT (email/téléphone, blocage) et LIEN (toggle actif) : chaînes vérifiées endpoint/méthode/schéma cohérents, RAS. Répertoire (DirectoryContact) : aucune édition serveur n'existe (sync/list/clear seulement) — la prémisse « fiche éditable » du contexte ne s'applique pas ici.

| id | sév | safe | verdict | titre | fichiers |
|---|---|---|---|---|---|
| F1 | defect | ✓ | confirmed | L'apply optimiste du profil efface 7 champs au lieu de les reporter (nil au lieu de self.x) | ProfileView.swift:944-987 |
| F2 | defect | ✗ | confirmed | Les réponses gateway du profil-soi sont plus pauvres que MeeshyUser, et 4 sites iOS remplacent currentUser EN  | types.ts:43-75,96-133, profile.ts:283,360-383,459-482, magic-link.ts:71 |

Verdicts non confirmés :
- **F3 refuted** — Les faits cités sont exacts (`PhonebookViewModel.swift:289` `static let pageSize = 200`, `:292` `static let maxPages = 25`, commentaire `:290-291` « Filet contre un serveur qui répondrait toujours hasMore : 25 pages de 200 = 5 000 contacts, au-delà du plus gros carnet d'adresses » ; `apps/ios/decisions.md:385` « **Répertoire sans plafond** : … filet `maxPages` = 25 × 200 » ; `PhonebookViewModelTests.swift:511-513` `test_listAll_isCappedByMaxPages` avec `maxPages: 3`). Mais aucun de ces faits ne 

Correctifs corrigés par le sceptique :
- **F2** — Deux pas indépendants, sans arbitrage de fuite (il n'y en a pas).

Pas 1 — SÛR MAINTENANT, purement additif, aucun champ nouveau exposé (userSchema filtre déjà le corps) : dans services/gateway/src/routes/users/profile.ts, faire passer PATCH /users/me/avatar (select lignes 360-383) et PATCH /users/me/banner (459-482) par le même chemin que PATCH /users/me — supprimer le `select` manuel et rendre `user: formatUserResponse(updatedUser, permissions)` comme à la ligne 283, ou à défaut compléter les 

## Lentil — Lentille 5 — recevoir tous les signaux d'appel et rejoindre un appel en cours, y compris après un re — 4 constats, 14 conformes, 5 questions

Verdict : la RÉCEPTION des signaux est solide ; la REPRISE après relance ne l'est qu'à moitié.
Couverts (preuve des deux côtés) : S4 (VoIP→CallKit→`joinCallRoomReliably` + buffer d'offres gateway 150 s), S5 (socket vivant en arrière-plan → CallKit via `applicationState != .active`), S6 (call-waiting, supersession, `clearPendingIncomingCall`), S7 (rejoin de room ACK-aware sur `didReconnect` + grâce 30 s×4 côté serveur), S8 (`prepareForShutdown` + `rehydrateActiveCalls` + réhydratation du ring), S10 (dédup `shouldProcessRemoteEnd`, whitelist `call:error`), S11 (les DEUX `call:initiated` sont composés par le serveur, mêmes champs), S12 (offre bufferée serveur + `pendingRemoteOffer` client), S13 (freshness `GET /calls/:id` + `call_cancel`/`call_answered_elsewhere`).
Partiels / non couverts :
• S1/S2 (force-quit, crash) — l'app ne détecte un appel ENCORE ACTIF que si l'utilisateur rouvre LA conversation. `call:check-active` ne rejoue que `initiated|ringing` < 60 s ; `GET /calls/active`, la route de reprise dédiée du gateway, n'a AUCUN consommateur iOS. → L5-F1 (defect).
• S3 (redémarrage du téléphone) — le token VoIP est bien réenregistré (AppDelegate, inconditionnel), mais la fenêtre de grâce serveur (30 s sans socket) est toujours plus courte qu'un reboot : l'appel 1:1 est mort avant le retour. → L5-F4 (proposal).
• S6 bis — `reportIncomingVoIPCall` mute `callUsesCallKit`/`lastCallWasOutgoing` AVANT sa garde « busy » : une 2ᵉ sonnerie VoIP corrompt l'état de l'appel EN COURS (session audio jamais désactivée, `CXEndCallAction` sur un UUID inconnu). → L5-F2 (defect, correctif sûr).
• S9 (groupe) — le gateway accepte désormais les conversations `group` (cap 9999) mais iOS reste mono-pair et masque toute affordance hors `direct`. → L5-F3 (proposal).
• S14 — une fois REJOINT, la pilule globale couvre bien « je suis ailleurs » ; c'est l'affordance de REPRISE qui manque (F1).

| id | sév | safe | verdict | titre | fichiers |
|---|---|---|---|---|---|
| L5-F1 | defect | ✗ | refuted | Aucune détection GLOBALE d'un appel encore actif après relance — `GET /calls/active` (route de crash-recovery  | ActiveCallService.swift, calls.ts, CallEventsHandler.ts |
| L5-F2 | defect | ✓ | confirmed | `reportIncomingVoIPCall` mute l'état de l'appel EN COURS avant sa garde « busy » — une 2ᵉ sonnerie VoIP corrom | CallManager.swift, CallEventsHandler.ts |
| L5-F3 | proposal | ✗ | — | S9 — rejoindre un appel de GROUPE est impossible sur iOS alors que le gateway l'accepte désormais | ConversationView+Header.swift, ConversationViewModel.swift, CallService.ts |
| L5-F4 | proposal | ✗ | — | S3 — la fenêtre de grâce serveur (30 s sans socket) est toujours plus courte qu'un redémarrage de téléphone :  | CallEventsHandler.ts, AppDelegate.swift |

Verdicts non confirmés :
- **L5-F1 refuted** — Les faits BRUTS sont exacts (j'ai relu : `services/gateway/src/routes/calls.ts:1071` `fastify.get('/calls/active'` et son `sendNotFound(reply, 'NO_ACTIVE_CALL')` à la ligne 1146 ; `ActiveCallService.swift:6/23` n'expose que `activeCall(conversationId:)` ; `CallEventsHandler.ts:2387-2389` limite bien le replay à `status: { in: [initiated, ringing] }` + `startedAt: { gte: ringingWindowStart }` (1374 : `Date.now() - 60_000`) + `initiatorId: { not: userId }` ; `FloatingCallPillView.swift:118` gate s

## LENTIL — LENTILLE 1 — Défilement du fil de messages sans effet (apps/ios : MessageListViewController + Focal/ — 8 constats, 17 conformes, 5 questions

Verdict : le fil est PROPRE en mode `.bubbles` — aucun effet par rangée piloté par le défilement (fond animé éteint, `animatingDifferences: false` partout, auto-scroll interdit pendant geste/momentum). Deux réserves de fluidité y subsistent (report §4.7ter non appliqué aux bulles).
`.focal` EST atteignable (clamp du 2026-08-18 levé par `renderDecision`, addendum du 2026-08-21) mais reste derrière le programme bêta (défaut OFF ⇒ loi rend `.bubbles`). Les effets de défilement se concentrent donc dans les modes plats (Script/Focal) et en Rivière.
Défaut central (L1-01) : la reconfiguration de la rangée élue AU TICK d'élection n'est autorisée que par l'invariant « rien ne change de hauteur » (`apps/ios/decisions.md:328`, doc de champ `MessageListViewController.swift:264`). Cet invariant est FAUX : `FocalRow.swift:532` plafonne le texte à 360 caractères en focus contre 512 sinon. Tout message > 360 caractères change donc de hauteur en plein momentum — exactement la famille de bogues que `decisions.md` liste deux fois en « alternatives rejetées », et que `MessageListLayout` ne compense pas (compensation nulle pour une cellule DANS la fenêtre).
Trois gaspillages par frame mesurables et sans risque produit : sur-réserve `focalOverscan` de 0,3 hauteur d'écran payée pour une compaction qui n'est PLUS appliquée (`poses`/`apply`/`showFocusCard` n'ont aucun appelant de production) ; construction des géométries AVANT la garde d'armement ; `indexPathsForVisibleItems` recalculé 2 à 3 fois par frame.
Deux constats relèvent d'un ARBITRAGE PRODUIT, pas d'un correctif : le révélé opacité des heures + coches sur chaque rangée plate, et la carte/chips/détails du message élu qui se déplacent de rangée en rangée pendant le geste. Ce sont des effets pendant le défilement au sens strict de la directive, mais chacun naît d'une directive utilisateur explicite (2026-08-22 / 2026-08-24) et Focal n'existe plus sans le second — je les PROPOSE, je ne les retire pas.
Enfin : deux des trois effets TOLÉRÉS n'existent pas aujourd'hui (aucun envol composeur→bulle ; aucune animation d'entrée de message, interdite par la directive ROULEAU du 2026-08-18). Seul (c), `Message.effectFlags`, est en place et aucun correctif proposé ne le touche.

| id | sév | safe | verdict | titre | fichiers |
|---|---|---|---|---|---|
| L1-01 | defect | ✗ | confirmed | Focal : la rangée élue change de HAUTEUR en plein défilement — le plafond de texte dépend du focus, ce qui inv | FocalRow.swift, MessageListViewController.swift, decisions.md |
| L1-02 | risk | ✓ | confirmed | Mode BULLES (le mode nominal) : les reconfigurations de cellules visibles ne sont pas différées pendant le ges | MessageListViewController.swift, MessageListLayout.swift |
| L1-03 | improvement | ✓ | — | Focal : 0,3 hauteur d'écran de cellules pré-réalisées par frame sont payées pour une compaction qui n'est plus | MessageListViewController.swift, FocalScrollPerspective.swift, MessageListLayout.swift |
| L1-04 | improvement | ✓ | — | Focal : travail mort à chaque frame de défilement — géométries construites avant la garde d'armement, et balay | MessageListViewController.swift, FocalScrollPerspective.swift |
| L1-05 | improvement | ✓ | — | Tous modes : `indexPathsForVisibleItems` est reconstruit deux à trois fois par frame de défilement | MessageListViewController.swift |
| L1-06 | proposal | ✗ | — | Script et Focal : les heures et les coches de CHAQUE rangée apparaissent et s'effacent en fondu au rythme du d | FocalTimestampRevealState.swift, FocalMetaRow.swift, MessageListViewController.swift |
| L1-07 | proposal | ✗ | — | Focal : la carte accent, ses chips et les détails du message élu se déplacent de rangée en rangée pendant le g | MessageListViewController.swift, FocalRow.swift |
| L1-08 | risk | ✗ | confirmed | Rivière : chaque frame de défilement republie les cadres de toutes les bulles visibles et réévalue le corps de | RiverBubbleView.swift, RiverStreamHost.swift |

Correctifs corrigés par le sceptique :
- **L1-01** — Le site est bon (FocalRow.swift:532) mais le correctif ne doit PAS être présenté comme un choix entre deux constantes : l'option `FocalMetrics.Focus.maxCharacters` (360 partout) tronque plus tôt TOUTES les rangées plates, y compris au repos et en mode Script — un changement visible hors directive. Correctif minimal : remplacer le ternaire par la constante historique, `truncateLimit: BubbleExpandableText.truncateLimit`, ce qui laisse bit-à-bit inchangé le rendu de toutes les rangées non élues (c'
- **L1-02** — Le geste de code proposé est juste (retirer `readingMode != .bubbles` en `:1771` et `:2155`, garder les conditions de mouvement), mais `safe_now: true` est FAUX et doit passer à false : le report change un comportement VISIBLE du mode nominal. Régression à nommer avant de la livrer — le chemin global `:1771` ne porte pas que des traductions : `itemsToReconfigure` est bâti sur `changeVersion` (`:1743-1746`), donc en bulles les coches de remise/lecture, les éditions, les réactions et l'empreinte d
- **L1-08** — Deux corrections à l'esquisse, qui la rendraient sinon fausse ou insuffisante. (1) « dont aucun traqueur n'existe » est inexact : le jumeau VERTICAL existe déjà dans le SDK — `packages/MeeshySDK/Sources/MeeshyUI/Navigation/ScrollOffsetTracking.swift:40` `func trackScrollContentOffset(_ onChange: @escaping (CGFloat) -> Void)` (iOS 18+, `onScrollGeometryChange` sur `contentOffset.y + contentInsets.top`), consommé par une dizaine de sites ; seul son CÂBLAGE manque dans `RiverStreamHost`. Le lot est

## LENTIL — LENTILLE 6 — Appel audio/vidéo stable sous mauvais réseau (iOS + web + gateway), lecture seule dans  — 7 constats, 21 conformes, 4 questions

Verdict : la directive est CONTREDITE par la couche de survie, sur les DEUX clients, et le reste de la chaîne est déjà conforme.
Ce qui est déjà juste (et vaut d'être dit) : l'échelle adaptative iOS ne touche JAMAIS la piste (WebRTCService.swift:464-486, plancher 360p/15fps/100 kbps) ; le récepteur iOS ne détruit la vue distante QUE sur `call:media-toggled` — sans lui, `RTCMTLVideoView` GARDE la dernière image (WebRTCVideoView.swift:27-43) ; l'état `.reconnecting` conserve les vues et n'affiche qu'une pastille discrète (CallView.swift:884, 955) ; la pastille « Réseau faible (contact) » existe déjà des deux côtés (CallView.swift:976/1117, use-remote-call-alerts.ts:112) ; l'audio est protégé (DTX/FEC/RED, plancher 16 kbps) ; la passerelle tolère 90 s de grâce socket et 120 s de heartbeat, et n'a AUCUN timeout qui tuerait un appel pendant 10-30 s de coupure.
Le défaut est concentré en un point : quand le lien reste `.poor`/`.critical` 6 s, `VideoSurvivalController` ne dégrade pas l'encodeur — il DÉTRUIT l'émission (arrêt de capture, `sender.track = nil`, `.recvOnly`, renégociation SDP) puis ANNONCE la coupure au pair avec l'événement même de la coupure caméra volontaire. Résultat exact : le pair remplace la dernière image par un avatar « Caméra désactivée » — l'inverse littéral de la directive, et un mensonge (personne n'a coupé sa caméra). Le web fait la même chose, avec les mêmes seuils 6 s/10 s.
Le correctif est petit et déjà prévu par la spec d'origine (`docs/superpowers/specs/2026-05-10-calls-sota-redesign-design.md:827` : « active=false », un réglage d'encodage, pas un retrait de piste) : garder `VideoSurvivalPolicy` telle quelle (pure, 26 tests verts) et changer l'ACTION — plancher `maxFramerate` 1-2 fps + `minVideoBitrate` via `applyVideoEncoding` (aucune renégociation, aucune perte de la chaîne de références donc aucune keyframe à forcer), et ne plus émettre `call:media-toggled`.
Attention : les deux moitiés ne se déploient pas séparément. Ne plus prévenir le pair TANT QUE la piste est retirée serait PIRE (image figée définitive, sans explication). Et `isVideoSuspended` est lu par quatre sites qui l'interprètent aujourd'hui comme « la piste est partie » (CallManager.swift:3653, 3734, 4903, CallView.swift:807) : geler sans les corriger casse la reprise après un hold CallKit.
Reste hors survie : le budget de reconnexion iOS plafonne l'appel à ~30 s de coupure (3 × 10 s) alors que le serveur en tolère 90 — un tunnel de 40 s raccroche.

| id | sév | safe | verdict | titre | fichiers |
|---|---|---|---|---|---|
| L6-1 | defect | ✗ | confirmed | La couche de survie iOS COUPE la vidéo sortante (arrêt de capture + piste retirée + renégociation) au lieu de  | VideoSurvivalController.swift, CallManager.swift, P2PWebRTCClient.swift |
| L6-2 | defect | ✗ | confirmed | La suspension réseau est annoncée au pair par `call:media-toggled(video,false)` — indiscernable d'une coupure  | CallManager.swift, CallView.swift, CallParticipantVisual.swift |
| L6-3 | defect | ✗ | confirmed | Le web coupe sa vidéo exactement de la même façon (mêmes seuils 6 s/10 s) — corriger iOS seul créerait une asy | VideoCallInterface.tsx, use-webrtc-p2p.ts, webrtc-service.ts |
| L6-4 | risk | ✗ | confirmed | Le budget de reconnexion iOS raccroche l'appel après ~30 s de coupure, alors que la passerelle en tolère 90 à  | CallManager.swift, WebRTCTypes.swift, CallEventsHandler.ts |
| L6-5 | improvement | ✗ | — | La vidéo sortante partage la priorité réseau `.high` de l'audio — rien ne la déclasse dans le pacer quand le l | P2PWebRTCClient.swift |
| L6-6 | improvement | ✗ | — | `degradationPreference` reste `.maintainFramerate` jusqu'au plancher — au moment où la directive demande de fi | P2PWebRTCClient.swift, WebRTCService.swift |
| L6-7 | proposal | ✗ | — | Tableau des seuils et durées qui gouvernent la dégradation — valeurs actuelles et cibles | WebRTCTypes.swift, P2PWebRTCClient.swift, CallManager.swift |

Correctifs corrigés par le sceptique :
- **L6-1** — MÊME intention, trois corrections au correctif proposé. (1) Les « deux passe-plats qui n'appellent QUE client.applyVideoEncoding » seraient ÉCRASÉS en silence : `applyVideoQuality` est ré-appliqué à chaque changement de palier (WebRTCService.swift:460) ET à chaque transition thermique (WebRTCService.swift:445 `if thermalChanged { applyVideoQuality(currentQualityLevel) }`). Un passage `.critical → .poor` pendant un gel remettrait 15 fps et dégèlerait sans que personne ne le décide. Le gel doit do
- **L6-2** — Supprimer CallManager.swift:6369-6371 NE SUFFIT PAS : le même signal repart par un second site que le constat ne cite pas. CallManager.swift:4902-4906 recompose `let effectiveVideoOn = !self.isVideoSuspended && !isVideoSuspendedByCaptureInterruption && !isVideoSuspendedByHold` puis émet `emitCallToggleVideo(callId:enabled: effectiveVideoOn)` à CHAQUE reconnexion socket — pendant un gel réseau (le moment où une reconnexion socket est justement probable), le pair recevrait donc `media-toggled(vide
- **L6-3** — Le `fix` proposé recrée exactement le conflit que le dépôt documente déjà. (1) Appliquer un palier `frozen` CALL-WIDE le fait écraser par `usePerPeerVideoTier` : use-per-peer-video-tier.ts:63-68 réapplique `deriveVideoTier(stats.level)` par pair à chaque tick, et sa déduplication ne connaît que son propre `lastTierRef` — dès qu'un pair passe `poor → fair` (tier `medium`) pendant que la machine de survie reste suspendue (l'hystérésis n'exige `good`/`excellent` que pour reprendre), le gel saute sa
- **L6-4** — Le correctif à un seul chiffre est le bon, mais son inventaire est incomplet et sa justification doit être bornée. (a) La section `tests` omet le témoin qui rougira le premier : WebRTCTypesTests.swift:599-602 `func test_maxReconnectAttempts_is3()` avec `XCTAssertEqual(QualityThresholds.maxReconnectAttempts, 3, ...)` — il doit être mis à jour dans le même commit, sinon la gate rougit sur un simple changement de constante. (b) La justification « la passerelle tolère 90 s » n'est vraie que pour une

## 2b — C — 2b — Couverture des features temps-réel dans chaque mode de lecture (Script / Focal / Rivière / Résu — 8 constats, 11 conformes, 5 questions

Verdict : la chaîne temps réel est SAINE jusqu'à l'état (aucun filtre par mode dans `ConversationStateStore`, tous les événements écrivent en GRDB puis remontent par `viewModel.messages`) ; c'est au RENDU que la couverture diverge, et trois défauts touchent TOUS les modes à la fois.

Modes réellement atteignables : `.bubbles` (défaut hors bêta), `.script`, `.focal` (le clamp du retrait 2026-08-18 est LEVÉ — `ReadingModeController.renderDecision`), `.summary`, `.river` (bêta + éligibilité). Script et Focal sont la MÊME rangée par construction (`usesFlatRow`, `FocalRow` ne lit jamais `density`) : une seule colonne à auditer pour les deux, et elle couvre la matrice partagée F01–F15.

Les trois défauts transverses, par ordre de portée :
1. La traduction qui arrive APRÈS coup ne bascule JAMAIS la bulle — `translationResolutionCache` mémorise « pas de traduction » et l'écrivain socket ne l'invalide pas. Contredit `behaviour-matrix.json` F06, et frappe le mode par DÉFAUT (`.bubbles`), pas seulement la bêta. Le témoin F06 existe mais garde la SOURCE du cross-fade, pas le fait qu'il ait lieu : vert par omission.
2. Le Résumé Vivant est un instantané figé au montage — son `@StateObject` est construit une fois et son VM n'a aucune voie d'entrée. Quand il est le mode d'OUVERTURE (> 25 non-lus), il naît sur `messages == []` et reste au squelette. La Rivière a exactement ce remède (recalcul sur empreinte + reprise de la première géométrie peuplée) ; le Résumé ne l'a pas.
3. La liste UIKit reste MONTÉE sous les panes opaques Rivière/Résumé : `willDisplay` alimente le suivi de lecture, donc des accusés partent pour des messages que le lecteur n'a jamais vus — l'inverse exact de l'invariant écrit au site d'appel.

Reste : la Rivière ne rend que texte + citation + avis système (lot R-4 ouvert et honnêtement documenté), et ni la Rivière ni le Résumé ne montrent la frappe — la pastille `ConnectionBanner` EXCLUT explicitement la conversation ouverte, donc il n'y a aucun repli.

| id | sév | safe | verdict | titre | fichiers |
|---|---|---|---|---|---|
| 2b-1 | defect | ✓ | confirmed | La traduction arrivée après coup ne bascule jamais la bulle : le cache de résolution du Prisme est empoisonné  | ConversationViewModel.swift, ConversationSocketHandler.swift, MessageListViewController.swift |
| 2b-2 | defect | ✓ | confirmed | Le Résumé Vivant naît VIDE quand il est le mode d'OUVERTURE : la première population de messages n'est jamais  | ConversationView.swift, LivingSummaryHost.swift, LivingSummaryViewModel.swift |
| 2b-3 | risk | ✗ | confirmed | Aucun événement temps réel n'atteint le Résumé Vivant une fois monté : nouveau message, édition, suppression,  | LivingSummaryHost.swift, LivingSummaryViewModel.swift, ConversationView.swift |
| 2b-4 | defect | ✗ | confirmed | Des accusés de lecture partent pour des messages jamais affichés : la liste UIKit reste montée et « vue » sous | ConversationView.swift, MessageListViewController.swift, RiverConversationHost.swift |
| 2b-5 | defect | ✗ | confirmed | La bulle Rivière ne rend que du texte : média, vocal, réactions, accusés, « modifié », éphémère/vue unique, ef | RiverBubbleView.swift, RiverConversationMapping.swift, ConversationView.swift |
| 2b-6 | risk | ✗ | confirmed | Un message supprimé pour tous DISPARAÎT en Rivière, là où le Fil garde une rangée fantôme | RiverConversationMapping.swift, BubbleContentBuilder.swift, behaviour-matrix.json |
| 2b-7 | defect | ✗ | confirmed | La frappe n'a AUCUN rendu en Rivière ni en Résumé, et la pastille de repli exclut explicitement la conversatio | MessageListViewController.swift, ConnectionBanner.swift, RiverStreamHost.swift |
| 2b-8 | defect | ✗ | confirmed | Les sessions de position en direct sont reçues par trois handlers socket et lues par AUCUNE vue, dans aucun mo | ConversationSocketHandler.swift, ConversationViewModel.swift, ConversationStateStore.swift |

Correctifs corrigés par le sceptique :
- **2b-2** — Deux corrections. (1) La garde `viewModel.showsSkeleton` proposée est FAUSSE : `showsSkeleton` vaut `digest.messageCount == 0 && faceRamp.isEmpty && agentSummary == nil` (`LivingSummaryViewModel.swift:46-49`), et `refreshAgentEnrichment()` est lancé par `.task` au montage (`LivingSummaryView.swift:56`) ; si la réponse agent arrive AVANT la première population GRDB (cas nominal pour un inscrit sur base froide), `showsSkeleton` passe à `false` et l'adoption est refusée pour toujours — le correctif
- **2b-4** — Le `fix` proposé (gate dans le `didSet` de `readingMode`, `:288-300`) RATE le cas nominal et introduirait un faux sentiment de sécurité : ce `didSet` sort sur `guard oldValue != readingMode, isViewLoaded`, et le commentaire de `syncFocalOverscan` (`:532-537`) dit explicitement que « le mode arrive AVANT le chargement de la vue — `readingMode.didSet` sort alors sur `isViewLoaded` ». Or l'ouverture DIRECTE en `.summary` (décision auto > 25 non-lus) ou en `.river` est précisément le scénario où les
- **2b-5** — Ne pas ouvrir un lot concurrent de R-4. Le geste minimal et immédiatement utile est documentaire + garde : (1) amender l'énoncé R-4 de `tasks/riviere-r137-montage.md:69-73` avec les quatre lignes qu'il ne nomme pas (accusés, « modifié », effets, mentions) ; (2) écrire `apps/ios/MeeshyTests/Unit/Riviere/RiverRealtimeMatrixTests.swift` sur le modèle de la garde d'exhaustivité `FocalRealtimeMatrixTests.swift:487-497` — c'est elle, et elle seule, qui empêchera la prochaine ligne de manquer en silenc
- **2b-8** — Le `fix` (« décision produit requise ») reste juste, mais il doit être posé sur la chaîne ENTIÈRE, pas sur le seul côté réception : la question à arbitrer est « la position en direct est-elle une fonctionnalité de ce cycle ? ». Si NON, le retrait d'un bloc porte sur cinq sites, pas trois : les trois abonnements (`ConversationSocketHandler.swift:1185-1226`), l'exigence de protocole (`:28`), les deux `@Published` (`ConversationViewModel.swift:336`, `ConversationStateStore.swift:34`), les trois faç

## LENTIL — LENTILLE 8 — Esthétique et expérience : réduire le nombre d'interactions, remonter les features impo — 10 constats, 16 conformes, 5 questions

Carte des parcours depuis l'accueil connecté (liste de conversations, boutons flottants Flux/Avatar) — verdicts :
FLUIDE (1–2 gestes) : rejoindre un appel en cours (pilule permanente, RootView.swift:95 → 1 tap) · escalade audio→vidéo EN appel (CallView.swift:1692, 1 tap) · démarrer un appel (menu du header, 2 taps) · répondre à un message (swipe, 1 geste) · réagir (appui long + emoji, 2) · Prisme voir l'original/autre langue (drapeaux + bouton translate DANS le pied de bulle, BubbleStandardLayout.swift:1154-1190, 1 tap) · rechercher dans la conversation (1 tap) · recherche globale (1 tap) · créer une story (tray, 1) · nouvelle conversation (« + » du header, 1) · épingler/muter/marquer non lue une conversation (appui long + tap, 2) · appels manqués (avatar + « Appels », 2) · partager un lien de conversation (2) · photo (2) · profil (appui long avatar, 1).
ENFOUI (≥3 gestes) : épingler/partager/transférer un MESSAGE (appui long → « Plus… » → item, 3 — arbitrage produit assumé, MessageActionResolver.swift:199-208) · envoyer un VOCAL dans le fil (2 gestes avant de parler, alors que la variante minimisée du MÊME composant offre le micro en 1) · bloquer un utilisateur depuis la liste (4) · synchroniser ses contacts (~5, la puce « Repertoire » n'est atteignable qu'après deux écrans) · les 9 accès rapides (story/mood/post/inviter/lien) ne vivent qu'en QUEUE de liste, donc derrière un défilement complet · changer sa LANGUE PRINCIPALE (le cœur du Prisme) : 5 gestes en mode édition du profil.
INTROUVABLE : auto-traduction — l'unique interrupteur (ConversationSettingsView.swift:328-336) est gardé par `canManageMembers && !isDirect` : inaccessible en conversation directe et à tout membre non-admin, sans équivalent au niveau utilisateur · modes de lecture Script/Rivière : la puce est parfaite (1 tap = cycle, appui long = menu) mais le drapeau bêta est OFF par défaut (décision produit, non remise en cause ici).
Verdict global : la navigation de conversation est excellente et le Prisme de LECTURE est à un tap ; ce qui manque, ce sont les COMMANDES du Prisme (langue principale, auto-traduction) et le micro. 3 défauts esthétiques objectifs (libellés français en dur, filtres Contacts non localisés, bouton d'envoi hors accent), 5 propositions, 2 améliorations sans effet visible.

| id | sév | safe | verdict | titre | fichiers |
|---|---|---|---|---|---|
| L8-D1 | defect | ✓ | confirmed | Neuf libellés français EN DUR dans les menus contextuels d'avatar — invisibles aux deux gardes de localisation | ConversationView+Header.swift, MeeshyAvatar.swift, UserIdentityBar.swift |
| L8-D2 | defect | ✓ | confirmed | Les puces de filtre de l'annuaire Contacts affichent `ContactFilter.rawValue` — français non accentué, non loc | ContactsListTab.swift, ContactsShared.swift, RequestsTab.swift |
| L8-D3 | defect | ✗ | confirmed | Le bouton d'envoi — le contrôle le plus visible du fil — ignore la couleur d'accent de la conversation que tou | UniversalComposerBar.swift, ConversationView+Composer.swift |
| L8-P1 | proposal | ✗ | — | Changer sa langue principale — le réglage qui GOUVERNE le Prisme — coûte 5 gestes et vit dans le mode édition  | ProfileView.swift, SettingsView.swift |
| L8-P2 | proposal | ✗ | — | L'auto-traduction n'a AUCUN interrupteur atteignable en conversation directe ni pour un membre non-admin | ConversationSettingsView.swift, ConversationInfoSheet.swift, ConversationPreferencesTab.swift |
| L8-P3 | proposal | ✗ | — | Envoyer un vocal coûte 2 gestes avant de parler, alors que la variante minimisée du MÊME composeur offre le mi | UniversalComposerBar.swift, UniversalComposerBar+Attachments.swift |
| L8-P4 | proposal | ✗ | — | Les neuf accès rapides (story, mood, post, inviter, lien) ne sont atteignables qu'en QUEUE de liste, donc aprè | ConversationListQuickActions.swift, ConversationListView.swift, ConversationListView+Overlays.swift |
| L8-P5 | proposal | ✗ | — | La tuile « Voir mes contacts sur Meeshy » n'ouvre pas le répertoire téléphonique qu'elle promet — il reste une | ConversationListView.swift, ContactsListTab.swift, Router.swift |
| L8-I1 | improvement | ✓ | — | Hex bruts dupliquant des jetons MeeshyColors existants dans un composant de conversation | ConversationPreferencesTab.swift |
| L8-I2 | improvement | ✗ | — | Le titre de l'écran d'accueil est un littéral anglais/français mixte, non localisé | ConversationListView+Overlays.swift |

Correctifs corrigés par le sceptique :
- **L8-D1** — Le `fix` proposé est trop large sur deux points et porte un risque non nommé. (1) Côté APP (ConversationView+Header.swift:426/430/434) il n'y a AUCUNE clé à créer : le catalogue `apps/ios/Meeshy/Localizable.xcstrings` porte déjà `Voir le profil`, `Conversation` et `Envoyer un message` traduites dans les 6 langues non-source (vérifié). Écrire `String(localized: "Voir le profil", bundle: .main)` (idem pour les deux autres) suffit et n'ajoute rien au catalogue — inventer `avatar.menu.view_profile` 
- **L8-D2** — Le fix est bon mais incomplet sur UN point : il ne corrige que le libellé visible et laisse la fuite VoiceOver à moitié ouverte. Correctif minimal : ajouter dans ContactsShared.swift, sous `enum ContactFilter`, une `var title: String` calquée mot pour mot sur `PeopleTab.title` (ContactsShared.swift:24-34) — `String(localized: "contacts.list.filter.all|online|offline|phonebook|affiliates", defaultValue: "Tous|En ligne|Hors ligne|Répertoire|Affiliés", bundle: .main)` — puis remplacer `filter.rawVa
- **L8-D3** — Le fix proposé porte une régression que l'auteur n'a pas nommée, et qui frappe hors conversation. Les TROIS autres hôtes du composeur ne passent PAS `secondaryColor` (vérifié : `grep secondaryColor` rend zéro sur PostDetailView.swift, StoryViewerView+Canvas.swift, FeedCommentsSheet.swift) — il retombe donc sur son défaut :58 `MeeshyColors.indigo600Hex`. Poser `[Color(hex: accentColor), Color(hex: secondaryColor)]` y produirait un dégradé HYBRIDE : accent du post/de la story vers l'indigo de marq

## LENTIL — LENTILLE 7 — Réduire drastiquement la consommation de données iOS (audit lecture seule du worktree / — 15 constats, 23 conformes, 6 questions

Verdict : la roadmap de mai est largement SOLDÉE côté serveur et côté HTTP iOS (perMessageDeflate, Traefik compress, ETag/304, URLCache, WebSocket, watermark `after=`, pull-to-refresh qui ne purge plus les médias, audio 64 kbps, participants `take: 5`). Ce qui reste est ailleurs, et pèse beaucoup plus lourd que ce que le doc de mai chiffrait.

Trois défauts dominent, tous du même genre : **un mécanisme d'économie existe, et le chemin qui consomme ne le branche pas.**
1. Le gateway compresse les trames WebSocket depuis le sprint bande passante ; **le client iOS n'a jamais passé `.compress`**, donc `SocketEngine.compress` reste `false`, Starscream n'annonce pas `permessage-deflate` et **aucune trame temps réel iOS n'est compressée**. QW1 — le quick win n°1 de tout l'audit — n'a jamais atteint iOS. Une ligne.
2. `MediaDownloadPolicyEngine` + l'écran Réglages « Vidéo : Wi-Fi uniquement » existent, sont testés, et **le prefetch des STORIES et du FEED ne les consulte pas** : vidéos et audio téléchargés en entier sur cellulaire (et en Low Data Mode). Le correctif jumeau a déjà été fait pour le carrousel de bulles en 2026-07-21 — le motif est écrit, testé, à recopier.
3. `?languages=` (filtre Prisme serveur, texte ET audio) est livré des deux côtés depuis juin ; **aucun appelant iOS ne le passe**, alors que les TROIS chemins de lecture jettent déjà toute traduction hors Prisme.

Ordre de grandeur cumulé pour un profil mobile actif : **8–35 Mo/jour** évitables, dont l'écrasante majorité (stories + feed) sur le seul cellulaire.

| id | sév | safe | verdict | titre | fichiers |
|---|---|---|---|---|---|
| BW-IOS-01 | defect | ✓ | confirmed | iOS ne négocie PAS `permessage-deflate` : le `perMessageDeflate` du gateway est inerte pour toutes les trames  | MessageSocketManager.swift, SocialSocketManager.swift, MeeshySocketIOManager.ts |
| BW-IOS-02 | defect | ✓ | confirmed | Le préchargement des STORIES télécharge vidéos et audio en entier sans consulter `MediaDownloadPolicyEngine` — | StoryViewModel.swift, MediaDownloadSettingsView.swift, BubbleStandardLayout+Media.swift |
| BW-IOS-03 | defect | ✓ | confirmed | Le préchargement du FEED ignore la même politique et télécharge intégralement l'audio de posts jamais joués | FeedViewModel.swift |
| BW-IOS-04 | improvement | ✓ | — | `?languages=` (filtre Prisme serveur) livré depuis juin des DEUX côtés, mais aucun appelant iOS ne le passe —  | ConversationViewModel.swift, ConversationSyncEngine.swift, MessageService.swift |
| BW-IOS-05 | risk | ✓ | confirmed | `markAsReceived` REST émis à CHAQUE `message:new` entrant, sans coalescing, alors que le gateway a déjà marqué | ConversationSyncEngine.swift, MessageHandler.ts |
| BW-IOS-06 | improvement | ✓ | — | Le pull-to-refresh purge TOUT le cache messages, ce qui réarme derrière lui le prefetch réseau des 20 conversa | ConversationListViewModel.swift, GRDBCacheStore.swift |
| BW-GW-01 | improvement | ✓ | — | `speakerAnalysis` et les champs OCR/vision/qualité vocale sont sérialisés dans chaque liste de messages ; iOS  | api-schemas.ts, messages.ts, MessageModels.swift |
| BW-IOS-07 | improvement | ✓ | — | Deuxième battement applicatif `heartbeat` sur la socket sociale — NU, sans écouteur d'ack, doublon exact de ce | SocialSocketManager.swift, AuthHandler.ts |
| BW-GW-02 | proposal | ✗ | — | Le gateway collecte les QUATRE rangs de Prisme de CHAQUE participant comme langues cibles ; chaque traduction  | MessageTranslationService.ts, MeeshySocketIOManager.ts |
| BW-GW-03 | proposal | ✗ | — | Pas de variantes responsive pour les avatars (512 px unique) ni pour `PostMedia` — `ImageVariantSelector` n'a  | ImageProcessingService.ts, schema.prisma, ImageVariantSelector.swift |
| BW-IOS-08 | proposal | ✗ | — | `prefetchTopConversationMessages` — 20 requêtes parallèles de 20 messages, avec réponses et traductions complè | ConversationListViewModel.swift |
| BW-IOS-09 | improvement | ✓ | — | Le préchargement d'arrière-plan (`BGProcessingTask`) tire 10 conversations toutes les ~30 min sans aucune gard | BackgroundTaskManager.swift, ConversationSyncEngine.swift |
| BW-IOS-10 | improvement | ✓ | — | Tous les téléchargements média passent par `URLSession.shared` — hors du pool, du pinning et de HTTP/3 d'`APIC | DiskCacheStore.swift, APIClient.swift |
| BW-GW-04 | improvement | ✗ | — | `cacheKey` et `cached` — détails d'implémentation serveur — voyagent dans chaque `message:translation` | buildTranslationEvent.ts, socketio-events.ts |
| BW-IOS-11 | proposal | ✗ | — | FirebaseAnalytics + Crashlytics + Performance embarqués ; Performance instrumente automatiquement TOUTES les r | project.yml |

Correctifs corrigés par le sceptique :
- **BW-IOS-01** — Identique au correctif proposé, avec la SEULE correction du site : la troisième construction est `packages/MeeshySDK/Sources/MeeshySDK/Sockets/SocialSocketManager.swift:537` (la ligne 368 citée est une déclaration de propriété `private var manager: SocketManager?`, pas la config). Ajouter `.compress` aux trois listes d'options : `MessageSocketManager.swift:2220`, `MessageSocketManager.swift:2260`, `SocialSocketManager.swift:537`. Rien d'autre. Vérifier au moins un appel réel (audio + SDP/ICE pas
- **BW-IOS-03** — Correctif proposé JUSTE mais incomplet sur un point que j'ai vérifié : la branche `.video` SANS `thumbnailUrl` (`FeedViewModel.swift:1648-1654`) appelle `StoryMediaLoader.shared.videoThumbnail(url: resolved)`, qui fait `StoryMediaDecoder.firstFrame(of: url, ...)` sur l'URL DISTANTE (`packages/MeeshySDK/Sources/MeeshyUI/Story/StoryMediaLoader.swift:134-143`) — donc des lectures réseau sur le MP4 (moov + premier GOP), pas une image. La ranger sous `allowImage` (défaut `.wifiAndGoodCellular`) laiss
- **BW-IOS-05** — Correctif proposé conservé tel quel (coalescing par conversationId dans `ConversationSyncEngine`, fenêtre 500 ms–1 s sous `stateQueue`, purge dans le chemin de `stopSocketRelay()`). Une seule précision à porter dans le lot : dimensionner la fenêtre en regard du quota partagé de 30 req/min (`services/gateway/src/routes/message-read-status.ts:70-73`), que `mark-read` partage avec `mark-as-received` — c'est ce quota, pas seulement les octets, qui rend la coalescence nécessaire.

## 2a — I — 2a — Inventaire d'alignement des événements Socket.IO (messagerie + social), frontend/backend, mécan — 6 constats, 9 conformes, 3 questions

121 SERVER_EVENTS / 58 CLIENT_EVENTS dans packages/shared/types/socketio-events.ts. Zéro dérive de NOM : les 105 `.on("…")` et ~40 `emit("…")` littéraux iOS matchent tous exactement une valeur SERVER_EVENTS/CLIENT_EVENTS (aucun typo, aucun underscore/tiret erroné). 16 SERVER_EVENTS non écoutés par iOS : 3 sont RESERVED (aucun émetteur nulle part, non-défaut), 5 sont des non-problèmes vérifiés (AUTHENTICATED, LINK_MESSAGE_NEW dual-émis, USER_PREFERENCES_COMMUNITY_REORDERED feature absente d'iOS, AGENT_ADMIN_EVENT admin-only, PENDING_MESSAGES_DELIVERED plomberie web-only). Restent 5 défauts réels prouvés + 1 gap symétrique aux 3 clients déjà auto-documenté dans la source. Payloads vérifiés conformes et bien gardés : message:new, reaction:added/removed, typing:*, message:attachment-updated, conversation:unread-updated (bridge 3 états), conversation:participant-joined/left (memberCount absolu), read-status:updated (résolution d'acteur — documentée et correcte). Partie B (client→serveur) : message:edit/delete, reaction:add/remove passent par REST côté iOS mais la diffusion serveur résultante est bien écoutée — pas un défaut de contrat.

| id | sév | safe | verdict | titre | fichiers |
|---|---|---|---|---|---|
| auth-session-revoked-unheard | defect | ✗ | confirmed | `auth:session-revoked` n'a AUCUN écouteur iOS — la révocation forcée (changement de mdp, revoke-all-devices, a | MessageSocketManager.swift, disconnectRevokedSessions.ts, connection.service.ts |
| friend-request-cancelled-unheard | defect | ✗ | confirmed | `friend-request:cancelled` — seul signal temps réel existant (pas de Notification persistée), zéro écouteur iO | NotificationService.ts, presence.service.ts, MessageSocketManager.swift |
| friend-request-rejected-silently-dropped | defect | ✓ | refuted | `friend-request:rejected` : ni écouteur typé, ni prise en charge du legacy notification:new (type friend_rejec | NotificationModels.swift, NotificationToastManager.swift, socketio-events.ts |
| notification-bulk-events-unheard | defect | ✗ | confirmed | `notification:read-bulk` / `notification:deleted-bulk` — zéro écouteur iOS, la synchro multi-appareils du tout | NotificationService.ts, notification-socketio.singleton.ts, lessons.md |
| call-initiated-conversation-title-ignored | defect | ✗ | refuted | `call:initiated.conversationType`/`conversationTitle` toujours peuplés par la gateway, jamais lus par iOS — Ca | CallEventsHandler.ts, CallNotification.tsx, MessageSocketManager.swift |
| story-created-client-mutation-id-not-wired | improvement | ✗ | — | `story:created.clientMutationId` posé par la gateway en parité avec post:created, mais déclaré par AUCUN des 3 | post.ts, SocialSocketManager.swift |

Verdicts non confirmés :
- **friend-request-rejected-silently-dropped refuted** — La prémisse centrale est FAUSSE, et le correctif serait du code MORT appelant la MAUVAISE méthode. (1) `grep -rn "friend_rejected" services/gateway/src packages/shared apps/web packages/MeeshySDK/Sources apps/ios` → ZÉRO occurrence dans tout le dépôt. Aucun `notification:new{type:'friend_rejected'}` n'existe. (2) La « legacy system notification » du doc-comment (`socketio-events.ts:349-352`) est littéralement une notification de type `system` : `services/gateway/src/routes/friends.ts:528` appell
- **call-initiated-conversation-title-ignored refuted** — Les FAITS cités sont exacts — je les ai tous relus — mais la qualification `defect` est réfutée : il n'y a ni contrat violé, ni directive contredite. (1) Faits vérifiés : `packages/shared/types/video-call.ts:429-445` documente la distinction direct/groupe ; `CallEventsHandler.ts:2436-2437` et `:2570-2571` peuplent les deux champs ; `CallOfferData` (`MessageSocketManager.swift:1271-1288`) ne les déclare pas ; `CallManager.swift:4639` compose `callerName = event.initiator.displayName ?? event.init

Correctifs corrigés par le sceptique :
- **auth-session-revoked-unheard** — Correction du fix : NE PAS réutiliser `AuthManager.shared.handleUnauthorized()` (la ligne voisine de `auth:token-expired`, MessageSocketManager.swift:3598-3603) — elle lance `refreshSession(force: true)`, et `/auth/refresh` rend un JWT neuf sans vérifier la session (magic-link.ts:210) : le correctif RE-ARMERAIT la session révoquée pour 24 h de plus. Minimal : (1) `AuthManager.swift` — ajouter une méthode publique étroite `handleSessionRevoked()` qui fait `guard let userId = activeUserId` puis ap
- **friend-request-cancelled-unheard** — Le fix proposé est correct dans sa direction mais INCOMPLET sur deux points, et il invente une méthode qui existe déjà. (1) Aucune « méthode de retrait miroir de didReceiveRequest » à écrire : `FriendshipCache.didRejectRequest(from:)` (`packages/MeeshySDK/Sources/MeeshySDK/Cache/FriendshipCache.swift:321`) retire déjà de `_receivedPending`, exactement le miroir demandé. La charge ne porte que `{friendRequestId, cancelledBy}`, et `cancelledBy` est par construction l'AUTRE partie : appeler `didCan
- **friend-request-rejected-silently-dropped** — Le manque RÉEL, bien plus étroit, est l'absence d'écouteur de l'événement TYPÉ `friend-request:rejected` (émis par `services/gateway/src/routes/friends.ts:534` avec `{ senderId, friendRequestId, rejecterId }`). Si l'équipe veut le fermer : écouter cet événement dans `MessageSocketManager` et appeler `FriendshipCache.shared.didCancelRequest(to: rejecterId)` (`Cache/FriendshipCache.swift:306` — c'est `_sentPending` qu'il faut vider chez le destinataire de l'événement), suivi de `await FriendshipCa
- **notification-bulk-events-unheard** — Le fix surestime le travail : le prédicat de portée EXISTE déjà côté iOS et est testé. `NotificationReadScope` (`packages/MeeshySDK/Sources/MeeshySDK/Notifications/NotificationCachePatch.swift:10-19`) porte `.all` / `.conversation(id:)` / `.post(id:)` / `.types([String])` et `NotificationCachePatch.markingRead(_:scope:)` (l.36) l'applique ; la suite `packages/MeeshySDK/Tests/MeeshySDKTests/Notifications/NotificationCachePatchTests.swift` le couvre. Le correctif minimal est donc du CÂBLAGE, pas u
- **call-initiated-conversation-title-ignored** — À porter en `proposal`, et alors réduit : (1) ajouter `conversationType: String?` / `conversationTitle: String?` à `CallOfferData` (`MessageSocketManager.swift:1271-1288`) — optionnels, mêmes noms que le fil, calqués sur le précédent `type`/`participants` déjà optionnels pour compat ascendante ; (2) les faire transiter jusqu'à `reportIncomingVoIPCall` et composer `localizedCallerName` SEULEMENT quand `conversationType == "group"`, en retombant à l'identique sur le nom seul sinon (non-régression 