# Plan UI/UX — Itération 1 — 2026-06-08

## Objectif
Corriger les lacunes d'i18n, d'accessibilité et de feedback identifiées dans l'analyse `2026-06-08-iteration-1.md`.

## Tâches

### ✅ Web
- [x] Ajouter clé `conversationHeader.unreadInOtherConversations` dans en/fr/es/pt `conversations.json`
- [x] `ConversationHeader.tsx` : supprimer fallback français hardcodé + `role="status"` sur le badge
- [x] `MessageBubble.tsx` : remplacer `<img>` par `next/image` pour l'avatar
- [x] `MessageBubble.tsx` : ajouter `aria-label` + `aria-expanded` + `aria-haspopup` sur le bouton sélecteur de langue
- [x] `MessageBubble.tsx` : externaliser le label "Original" via prop `originalLabel`
- [x] `MessageBubble.tsx` : ajouter bouton copie (hover, accessible, checked state 2s)

### ✅ iOS
- [x] `ContextActionMenu.swift` : utiliser `String(localized:)` dans les factory methods
- [x] `ConversationView+ContextOverlay.swift` : ajouter toast `FeedbackToastManager.showSuccess` après copie
- [x] `ConversationView+ContextOverlay.swift` : copier le contenu effectif (preferred translation ou original)
- [x] `BubbleFooter.swift` : localiser les `accessibilityLabel` hardcodés + timestamp accessibility
- [x] `Localizable.xcstrings` : ajouter 6 nouvelles clés bubble.footer.* + bubble.copy.success (en + fr)

## Itérations suivantes (backlog)
- Audit Dynamic Type : ~2969 instances de tailles fixes (cf. `apps/ios/Documentation/ACCESSIBILITY_AUDIT.md`)
- Indicateur scroll position dans la liste de conversations (web)
- Support RTL (web) pour les langues arabe/hébreux
- `useResolveUserLanguage()` hook web avec injection deviceLocale automatique
- Deep links iOS : audit `Router.swift` pour `meeshy://conversation/:id`
- Reduce Motion : 17 fichiers iOS avec animations `repeatForever` non guardées
- VoiceOver announcements dans la vue principale iOS
