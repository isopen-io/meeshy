# Itération 101i — Analyse UI/UX iOS : `TrackingLinksView`

**Date** : 2026-07-01
**Piste** : iOS (suffixe `i`).
**Fichier cible** : `apps/ios/Meeshy/Features/Main/Views/TrackingLinksView.swift` (227 lignes)
**Surface** : Écran « Liens de tracking » — vue d'ensemble stats (liens/clics/uniques/actifs) + liste des liens de tracking avec bouton copier + navigation vers le détail.

## Contexte routine

Suite des sweeps Dynamic Type + VoiceOver du cluster « liens » (91i `CommunityLinksView`, 99i `CommunityLinkDetailView`, tous deux mergés #1236/#1272). `TrackingLinksView` est le **jumeau structurel** de `CommunityLinksView` (même layout header/stats/liste/copier).
- Essaim d'agents iOS extrême au démarrage (labels 87i→100i saturés). `list_pull_requests` : **aucune PR ouverte ne touche `TrackingLinksView`** (surface FREE). Label **101i** choisi (> 100i) pour éviter toute collision.
- Choisi car : self-contained (`TrackingLinksViewModel` local), palette 100 % tokenisée (`MeeshyColors.trackingAccent`/`neutral500`), toutes chaînes déjà `String(localized:)`. La plupart des polices étaient déjà sémantiques (`.headline`/`.title3`/`.caption`/`.caption2`/`.subheadline`/`.footnote`) — seuls 7 sites `.system(size:)` restaient.

## Changements (1 fichier)

### Dynamic Type — 5/7 sites migrés
`.font(.system(size: N, weight:))` → `MeeshyFont.relative(N, weight:)` : chevron retour, bouton créer (`plus.circle.fill`), icône des cartes de stats, glyphe du bouton copier (`doc.on.doc`), `chevron.right` de disclosure.

### 2 sites gardés FIXES (justifiés, commentés)
1. `chart.bar.fill` **40pt** héros de l'état vide — décoratif ≥40pt (doctrine 74i/86i). **`.accessibilityHidden(true)`.**
2. `chart.bar.fill` **16pt** dans un cercle fixe **40×40** de la ligne — figé (doctrine 86i). **`.accessibilityHidden(true)`.**

### VoiceOver
- `.accessibilityAddTraits(.isHeader)` ×2 : titre d'écran + en-tête de section « MES LIENS ».
- `.accessibilityElement(children: .combine)` sur `trackingStatCard` (« 128 Clics ») + bloc d'état vide.
- `.accessibilityHidden(true)` sur glyphes décoratifs (icône de carte, 2 héros/glyphes fixes, `chevron.right`).

## Hors-scope (documenté)
- Ligne (`trackingLinkRow`) NON combinée : contient le bouton copier imbriqué qui doit rester atteignable — seuls les glyphes décoratifs sont masqués.
- Bouton copier `UIPasteboard.general.string` direct — convergence source unique = piste web, hors périmètre.

## Vérification
- **0 logique modifiée, 0 clé i18n neuve, 0 test neuf** (parité 55i/74i/86i/90i/91i/99i).
- Compte : 5 `relative` + 2 `.system(size:)` figés = 7 sites d'origine. ✅
- `MeeshyFont.relative` résout sans `import MeeshyUI` (précédent 91i/99i mergés, même import set).
- Gate = CI `iOS Tests` (Xcode 26.1.x + simu 18.2). Env Linux → CI seule autorité. NB : `Build (bun)` peut apparaître rouge sur la PR à cause d'une régression web pré-existante sur `main` (import `copyToClipboard` dupliqué) — **check non-requis** (merge iOS prouvé possible via #1272), hors périmètre iOS.

## Annotation continuité (à ne plus re-flagger)
- ⚠️ **Typographie/VoiceOver de `TrackingLinksView` = SOLDÉ 101i.** 2 `.system(size:)` restants figés à dessein + masqués.
- Sibling `TrackingLinkDetailView.swift` + `ShareLinksView.swift` (7 sites) restent candidats ouverts.
