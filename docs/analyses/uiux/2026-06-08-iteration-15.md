# UI/UX Audit — Iteration 15 (2026-06-08)

> Base: main after PR #416 (iteration 14). All findings verified against current code.
> Previous iterations 1–14 addressed: system colors (red/green/blue/pink), Dynamic Type in multiple views incl. MessageDetailSheet, i18n FR defaults, notification settings, ThreadView, AttachmentGallery delete dialog, TS6 build fix.

---

## iOS Issues

### HIGH — System Colors: `.orange` and `.yellow` (12 instances across 3 files)

**File A: `apps/ios/Meeshy/Features/Main/Components/ConversationEncryptionDetailSheet.swift`**
- L139: `.foregroundColor(.orange)` on `lock.open` icon — unencrypted conversation state
- Fix: → `MeeshyColors.warning`

**File B: `apps/ios/Meeshy/Features/Auth/Onboarding/OnboardingStepViews.swift`**
- L203: `.foregroundColor(.orange)` on `lightbulb.max.fill` icon in suggestions card
- L229: `.fill(Color.orange.opacity(0.08))` on suggestions card background
- L236: `.foregroundColor(.yellow)` on `lightbulb.fill` icon in tips card
- L824: `.foregroundColor(.orange)` on `map` icon in language section
- L833: `.foregroundColor(.orange)` on "Detectee" badge text
- L835: `.background(Capsule().fill(Color.orange.opacity(0.15)))` on "Detectee" badge
- L838: `.background(RoundedRectangle(...).fill(Color.orange.opacity(0.08)))` on row background
- L844: `let color: Color = target == .system ? ... : .orange` (regional language tab)
- L868: `let color: Color = editingTarget == .system ? ... : .orange` (language card)
- L988: `.foregroundColor(.orange)` on `sparkles` icon in optional step hint
- L992: `.background(RoundedRectangle(...).fill(Color.orange.opacity(0.1)))` on optional hint background
- Fix: all `.orange` / `.yellow` / `Color.orange` → `MeeshyColors.warning` / `MeeshyColors.warning.opacity(X)`

**File C: `apps/ios/Meeshy/Features/Main/Components/SecurityVerificationView.swift`**
- L18: `Color(hex: "4ECDC4")` on `lock.shield.fill` icon
- L45: `Color(hex: "4ECDC4")` on Done button foreground
- Fix: → `MeeshyColors.indigo400` (teal remapped to indigo400 per design system migration; legacy alias `MeeshyColors.cyan` → `MeeshyColors.indigo400`)

---

### HIGH — DetailTab.color: String hex values → Color (deferred from iter-14)

**File:** `apps/ios/Meeshy/Features/Main/Components/MessageDetailSheet.swift`
**File:** `apps/ios/Meeshy/Features/Main/Components/MessageOverlayMenu.swift`

`DetailTab.color` (lines 44–57) returns hex strings. The color property is consumed by `DetailGridItem.color` (line 129) and resolved at `Color(hex: item.color)` (line 318). Both `DetailTab` and `MessageAction` use `String` for color — must migrate both to `Color`.

**Mapping for `DetailTab.color`:**
| Case | Hex | → MeeshyColors token |
|------|-----|----------------------|
| `.language` | `"3498DB"` | `.info` |
| `.views` | `"2ECC71"` | `.success` |
| `.reactions` | `"F39C12"` | `.warning` |
| `.react` | `"E91E63"` | `.error` (closest semantic for pink-red action) |
| `.report` | `"E74C3C"` | `.error` |
| `.delete` | `"E74C3C"` | `.error` |
| `.forward` | `"9B59B6"` | `.indigo500` |
| `.sentiment` | `"1ABC9C"` | `.info` (closest to teal in palette) |
| `.transcription` | `"8E44AD"` | `.indigo600` |
| `.edits` | `"F8B500"` | `.warning` |

**Cascading type changes:**
1. `DetailTab.color: String → Color`
2. `MessageAction.color: String → Color` (used by `DetailGridItem.color`)
3. `DetailGridItem.color: String → Color`
4. `gridButton()` line 318: `Color(hex: item.color)` → `item.color`
5. `MessageOverlayMenu.swift` MessageAction creation sites: hex string literals → `Color` values

**Mapping for `MessageAction` in MessageOverlayMenu.swift:**
| Action | Hex | → Color |
|--------|-----|---------|
| reply | `"4ECDC4"` | `MeeshyColors.indigo400` |
| thread | `"F39C12"` | `MeeshyColors.warning` |
| copy | `"9B59B6"` | `MeeshyColors.indigo500` |
| pin | `"3498DB"` | `MeeshyColors.info` |
| star | `"FBBF24"` | `MeeshyColors.warning` |
| edit | `"F8B500"` | `MeeshyColors.warning` |
| deleteAttachment | `"F87171"` | `MeeshyColors.error` |

---

## Web Issues

### HIGH — `aria-label="Fermer"` hardcoded in 8 components

`common.close` exists in all 4 locale files (en: "Close", fr: "Fermer", es: "Cerrar", pt: "Fechar"). Need to add `useI18n('common')` to each component and replace the hardcoded string.

| File | Line |
|------|------|
| `apps/web/components/pptx/PPTXLightbox.tsx` | 102 |
| `apps/web/components/video/VideoLightbox.tsx` | 470 |
| `apps/web/components/attachments/ImageLightbox.tsx` | 195 |
| `apps/web/components/text/TextLightbox.tsx` | 285 |
| `apps/web/components/common/emoji-picker.tsx` | 358 |
| `apps/web/components/v2/StoryComposer.tsx` | 211 |
| `apps/web/components/v2/StoryViewer.tsx` | 746 |
| `apps/web/components/v2/ConversationDrawer.tsx` | 133 |

---

### MEDIUM — `title=` attributes with French text in MediaViewers.tsx

**File:** `apps/web/components/attachments/carousel/MediaViewers.tsx`
- L43: `title="Cliquez pour voir en plein écran"` → `t('gallery.fullscreen')` (key exists: "Open fullscreen")
- L150: `title="Ouvrir en plein écran"` → `t('gallery.fullscreen')`
- L206: `title="Cliquez pour voir en plein écran"` → `t('gallery.fullscreen')`
- File has no `useI18n` — must add hook.

---

## Summary

| ID | Severity | File | Issue |
|----|----------|------|-------|
| I1 | HIGH | ConversationEncryptionDetailSheet.swift | `.orange` on lock.open icon |
| I2 | HIGH | OnboardingStepViews.swift | 11× `.orange`/`.yellow`/`Color.orange` |
| I3 | HIGH | SecurityVerificationView.swift | `Color(hex: "4ECDC4")` ×2 |
| I4 | HIGH | MessageDetailSheet.swift + MessageOverlayMenu.swift | `color: String` hex → `Color` MeeshyColors tokens |
| W1 | HIGH | 8 web components | `aria-label="Fermer"` → `t('common.close')` |
| W2 | MEDIUM | MediaViewers.tsx | 3× French `title=` → `t('gallery.fullscreen')` |
