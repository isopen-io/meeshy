# Analyse UI/UX — Itération 65w (web)

## Contexte de continuité
- Base : `main` HEAD post-merge `#857` (iter-gq4cjc, `a8b3eae`).
- En vol (à NE PAS dupliquer) :
  - `#858` (64w) voice-profile tooltip i18n+theme
  - `#860` / `#861` (64w / 64wb) magic-link page `t()||fallback`
  - `#862` (63wb) `prefers-reduced-motion` Framer Motion
  - `#859` (63wc) image-crop letterbox dark-mode
  - `#863` (64wb) conversation-list category headers `t()||fallback`
- Surface choisie **strictement orthogonale** : la **page de détails d'un lien de tracking**
  `app/links/tracked/[token]/page.tsx` (destination de navigation primaire depuis `/links#tracked`).

## Classe d'anti-pattern ciblée
`t('key') || 'fallback'` (`use-i18n.ts` renvoie la clé brute truthy quand la clé est absente
→ le `||` court-circuite dessus → l'utilisateur voit la **clé brute** ; et quand la clé existe,
le fallback est du **dead-code**). Suite du différé 60w+.

## ⚠️ C'était un VRAI bug, pas seulement du dead-code
Sur les **15 occurrences** de l'anti-pattern dans ce fichier, **7 clés étaient totalement absentes**
des 4 locales (`tracking.errors.{authRequired, unauthorized, notFound, authRequiredTitle,
authRequiredDesc, unauthorizedTitle, unauthorizedDesc}`). Conséquence en production :

- **Toasts** d'erreur auth/403/404 → affichaient la **clé brute** littérale
  (`tracking.errors.authRequired`, etc.) à TOUS les utilisateurs.
- **Écran d'erreur plein cadre** (titre + description) pour les cas « authentification requise »
  et « accès refusé » → **clé brute** affichée en titre/description.

Les 8 autres occurrences pointaient des clés **déjà présentes** ×4 (dead-code + flash-of-raw-keys).

## Fichiers audités & corrigés

| Fichier | Namespace | Occ. corrigées | Nature avant | Après |
|---------|-----------|----------------|--------------|-------|
| `app/links/tracked/[token]/page.tsx` | links | 15 (`t()\|\|fb`) + 1 FR figé + 2 labels FR | clés absentes (clé brute) / dead-code / `Une erreur inattendue…` / `{n} clics`/`{n} uniques` | `t(key, 'EN')` (anti-flash, leçon 50w) ; FR figé → `tracking.details.unexpectedError` ; labels → `tracking.details.{clicksCount,uniqueCount}` (interpolation `{count}` existante) |

## Clés locales

**Ajoutées ×4 locales (fr/en/es/pt)** — 8 nouvelles clés :
- `links.tracking.errors.authRequired`
- `links.tracking.errors.unauthorized`
- `links.tracking.errors.notFound`
- `links.tracking.errors.authRequiredTitle`
- `links.tracking.errors.authRequiredDesc`
- `links.tracking.errors.unauthorizedTitle`
- `links.tracking.errors.unauthorizedDesc`
- `links.tracking.details.unexpectedError`

**Réutilisées (déjà ×4)** : `tracking.details.{linkNotFound, linkNotFoundDescription, backToLinks,
clicksCount, uniqueCount}`, `tracking.errors.statsFailed`.

## Décisions
- **Fallback EN exact en 2ᵉ argument** (leçon 50w) : les anciens fallbacks FR figés divergeaient et
  exposaient du français aux non-francophones ; alignés sur la valeur EN du locale.
- **`clicksCount`/`uniqueCount`** : les labels du tableau récapitulatif (`{item.clicks} clics`,
  `{item.uniqueClicks} uniques`) étaient du FR figé → réutilisent les clés d'interpolation `{count}`
  **déjà présentes ×4** ⇒ 0 clé neuve pour ce point.
- **`errorMessage.includes('non trouvé')`** (ligne 144) : chaîne du **message d'erreur backend**
  (logique de branchement), PAS un libellé UI → laissée intacte. Idem commentaires FR internes.

## Vérifications
- `grep` anti-pattern `t()||` sur le fichier = **0 restant**.
- `grep` FR user-facing (`clics`, `uniques`, `Une erreur`, `Retour aux`, `Lien non`) = **0 restant**
  (seuls subsistent : 1 match backend-error logique + 2 commentaires internes).
- 4 fichiers locale `links.json` valides (`jq empty`), append-only (parité 14 clés `tracking.errors` ×4).
- Tests jest `__tests__/app/links/tracked/token/page.test.tsx` : **30 passed / 8 skipped**, sans
  modification (le mock `t` retourne la clé ; un 2ᵉ arg string → `Object.entries` no-op inoffensif).

## ✅ SOLDÉ en 65w — NE PLUS re-flagger
- `app/links/tracked/[token]/page.tsx` (anti-pattern `t()||fb`, FR figé `unexpectedError`,
  labels `clics`/`uniques`).
- Bloc `links.tracking.errors.{authRequired, unauthorized, notFound, authRequiredTitle,
  authRequiredDesc, unauthorizedTitle, unauthorizedDesc}` + `links.tracking.details.unexpectedError`.
