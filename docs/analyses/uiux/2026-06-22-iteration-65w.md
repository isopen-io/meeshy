# Analyse UI/UX — Itération 65w (web)

## Contexte de continuité
- Base : `main` HEAD post-merge `#857` (iter-64w, cluster i18n `failed-message-banner`/`SystemStatusBanner`/`language-selector`/`emoji-picker`) + `#855` (iter-63w, épuration `settings/_archived/`) + `#850` (iter-62wp, perf vignettes admin).
- Surface choisie **orthogonale** à la très forte contention i18n `t()||fallback` en vol : **épuration de code mort** (non-i18n, zéro risque de collision avec les ~8 PR i18n en vol).
- Suit directement le « Suivi épuration 64w » consigné dans `branch-tracking.md` : `font-selector.tsx` devenu **fully orphaned** depuis la suppression de `_archived/` (63w).

## Classe ciblée
**Dead code / composants orphelins** (logique d'épuration — « toujours une logique d'épuration », CLAUDE.md). Un composant orphelin :
- gonfle le bundle barrel (`components/index.ts` réexporte tout `./settings`),
- pollue la surface d'audit (re-flaggé à chaque passe i18n/a11y/couleur),
- entretient des tests qui valident du code jamais rendu.

## Fichiers audités & supprimés

| Fichier | Nature | Preuve d'orphelinat |
|---------|--------|---------------------|
| `components/settings/font-selector.tsx` | UI sélecteur de police (settings) | Référencé **uniquement** par les 2 barrels (`components/index.ts`, `components/settings/index.ts`) + son propre test. **Aucun** `<FontSelector />` rendu dans une page/vue. |
| `components/settings/font-preview.tsx` | aperçu de police (export `FontPreview`) | **Aucun** barrel, **aucun** import externe, **aucun** test. Self-référence uniquement (démo interne ligne 131). Dead code total. |
| `__tests__/components/settings/font-selector.test.tsx` | test du composant supprimé | N'a plus de sujet. |

## Modifications barrel
- `components/settings/index.ts` : retrait de `export { FontSelector } from './font-selector';`
- `components/index.ts` : retrait de `FontSelector,` du ré-export agrégé `from './settings'`

## Hors-scope (conservé — décisions explicites)
- **`hooks/use-font-preference.ts`** + son test : building block testé unitairement. Conservé (réutilisable, faible coût). Après suppression de `FontSelector` il n'a plus de consommateur live → **différé** comme candidat d'épuration ultérieur si confirmé inutile.
- **`lib/fonts.ts`** : **TOUJOURS LIVE** — consommé par `app/layout.tsx` (application de la famille de police au document). NE PAS supprimer.

## Vérifications
- `grep -rn "FontSelector|font-selector|font-preview|FontPreview"` sur `components/ __tests__/ app/` (hors `node_modules`/`.next`) après suppression = **0 référence restante**.
- `git status` : 3 suppressions + 2 barrels modifiés, rien d'autre.
- Typecheck local non concluant (install workspace incomplet — `@meeshy/shared` non buildé + prisma non généré, cf. CLAUDE.md → ~1110 erreurs préexistantes sans rapport ; `application-settings.tsx` ne référence AUCUN fichier supprimé). **CI = porte de vérité.**

## ✅ SOLDÉ en 65w — NE PLUS re-flagger
- `components/settings/font-selector.tsx` (supprimé — orphelin)
- `components/settings/font-preview.tsx` (supprimé — dead code)
- `__tests__/components/settings/font-selector.test.tsx` (supprimé)
- ré-exports barrel `FontSelector` (retirés de `components/index.ts` + `components/settings/index.ts`)

## Différé 66w+ (épuration)
- `hooks/use-font-preference.ts` : confirmer l'orphelinat post-65w (plus aucun consommateur hors test/scripts d'analyse) → candidat suppression hook + test + entrée barrel `hooks/index.ts`. **NE PAS** toucher `lib/fonts.ts` (live via `app/layout.tsx`).
