# UI/UX Analysis — Iteration 90i (2026-07-01)

## Scope
**iOS exclusivement** (suffixe `i` — Web et Android couverts par d'autres agents).
Thème : **accessibilité — Dynamic Type** sur `NewConversationView` (écran « Nouvelle
conversation » : recherche d'utilisateurs, sélection multi-membres, titre de groupe). Migration
mécanique des polices figées `.font(.system(size:))` vers `MeeshyFont.relative(...)` (scaling
Dynamic Type), + masquage VoiceOver du glyphe décoratif d'état vide.

Vérification : la CI `ios-tests.yml` (compile Xcode 26.1.x + tests simulateur 18.2) sert de
build de validation (SwiftUI ne compile pas sous Linux). **Aucun test neuf** : sweep
typographique pur + 1 trait a11y déclaratif, aucune logique modifiée (parité 55i / 74i / 86i / 89i).
`MeeshyFont` est déjà en scope (`import MeeshyUI` ligne 4).

## Contexte / point de départ
`NewConversationView.swift` (412 lignes) est l'écran de création de conversation (direct ou
groupe) : header, champ titre de groupe (mode groupe), barre horizontale des membres
sélectionnés, champ de recherche, liste de résultats, état vide. Les couleurs sont **déjà** toutes
tokenisées (`MeeshyColors.*`, `theme.*`, accent déterministe `DynamicColorGenerator`) — aucune
dette palette. Mais **17 sites** de police fixaient une taille absolue `.font(.system(size:))` —
l'écran ignorait entièrement le réglage Dynamic Type de l'utilisateur (rupture directe de la règle
a11y CLAUDE.md « Use semantic fonts, NEVER use fixed font sizes for body text »).

## iOS Findings

### Dynamic Type — `.font(.system(size:))` → `MeeshyFont.relative(...)` (16/17 sites)
Migration mécanique préservant `weight`. Sites = texte-de-lecture ou glyphes SF Symbol appariés
inline à un `Text`/`TextField` scalable (chevron retour, titre, bouton Créer, icône+champ titre de
groupe, puce membre sélectionné + croix, loupe+champ recherche + croix d'effacement, texte état
vide, nom+@username de ligne résultat, badge « Bloqué » + main levée, accessoire de sélection
checkmark/cercle) :

| Ligne | Rôle | Avant | Après |
|---|---|---|---|
| 117 | glyphe chevron retour | `.system(size: 16, weight: .semibold)` | `MeeshyFont.relative(16, weight: .semibold)` |
| 125 | titre « Nouvelle conversation » | `.system(size: 17, weight: .bold)` | `MeeshyFont.relative(17, weight: .bold)` |
| 140 | bouton « Créer » | `.system(size: 14, weight: .bold)` | `MeeshyFont.relative(14, weight: .bold)` |
| 158 | icône `person.3.fill` (inline) | `.system(size: 14, weight: .medium)` | `MeeshyFont.relative(14, weight: .medium)` |
| 162 | champ titre de groupe | `.system(size: 15, weight: .medium)` | `MeeshyFont.relative(15, weight: .medium)` |
| 210 | nom puce membre | `.system(size: 12, weight: .semibold)` | `MeeshyFont.relative(12, weight: .semibold)` |
| 221 | croix retrait puce | `.system(size: 14)` | `MeeshyFont.relative(14)` |
| 243 | loupe recherche (inline) | `.system(size: 14, weight: .medium)` | `MeeshyFont.relative(14, weight: .medium)` |
| 247 | champ recherche | `.system(size: 15, weight: .medium)` | `MeeshyFont.relative(15, weight: .medium)` |
| 261 | croix effacement recherche | `.system(size: 14)` | `MeeshyFont.relative(14)` |
| 305 | texte état vide | `.system(size: 15, weight: .medium)` | `MeeshyFont.relative(15, weight: .medium)` |
| 339 | nom ligne résultat | `.system(size: 15, weight: .semibold)` | `MeeshyFont.relative(15, weight: .semibold)` |
| 343 | @username ligne résultat | `.system(size: 12, weight: .medium)` | `MeeshyFont.relative(12, weight: .medium)` |
| 351 | badge « Bloqué » | `.system(size: 11, weight: .semibold)` | `MeeshyFont.relative(11, weight: .semibold)` |
| 354 | main levée (inline badge) | `.system(size: 16)` | `MeeshyFont.relative(16)` |
| 364 | accessoire sélection | `.system(size: 20)` | `MeeshyFont.relative(20)` |

### VoiceOver — glyphe décoratif d'état vide masqué
Le glyphe hero `person.slash` (36pt, état « Aucun utilisateur trouvé ») porte désormais
`.accessibilityHidden(true)` : c'est un ornement dont le sens est déjà porté par le libellé
adjacent — VoiceOver n'a pas à le prononcer. Trait déclaratif pur.

## Périmètre délibérément exclu (ne pas re-flagger)

### `person.slash` état vide (ligne 303) — glyphe 36pt gardé figé
Glyphe **décoratif** d'état vide, de grande taille (36pt). Précédents 74i (« icône état-vide 24 »
figée) et 86i (héros décoratifs ≥ ~36–48pt figés + `accessibilityHidden`) : un ornement dont la
sémantique est portée par le texte adjacent reste figé et masqué de VoiceOver, plutôt que de
scaler jusqu'à déséquilibrer la mise en page de l'état vide. **Décision : conservé
`.font(.system(size: 36))` + `.accessibilityHidden(true)`.**

### Couleurs — déjà entièrement tokenisées (rien à faire)
`MeeshyColors.indigo400/600`, `MeeshyColors.error/success`, `theme.textPrimary/textMuted`, accent
déterministe `DynamicColorGenerator.colorForName` (couleur par utilisateur) et `theme.surfaceGradient/
border(tint:)` : aucune couleur hardcodée hors-charte. **Aucun swap palette dans ce lot.**

### i18n — laissé tel quel
Le fichier mêle des clés dot (`new_conversation.user.blocked`, `a11y.back`) et des clés = texte
français (`String(localized: "Nouvelle conversation", …)`). Toutes les chaînes **sont** localisées
via `String(localized:defaultValue:)` (résolues par le catalogue) ; une normalisation des clés
français→dot serait un refactor i18n distinct, à risque de collision avec le catalogue — **hors
scope** de ce sweep Dynamic Type.

## Anti-repetition check
Vérifié contre le § History du `branch-tracking.md` et les PRs iOS ouvertes (`list_pull_requests`,
2026-07-01 : **0 PR ouverte**) : aucune itération ni PR en vol ne touche `NewConversationView`.
`NewConversationView` était listé « différé prioritaire » par les pointeurs 84i/89i. Grandes
surfaces Dynamic Type déjà soldées toutes disjointes : `EffectsPickerView` (89i), `AboutView`
(86i), `ConversationDashboardView` (74i), `EditProfileView` (84i), `VoiceProfileWizardView` (87i).

## Status : ✅ analyse complète + corrections appliquées (16 swaps police / 1 glyphe figé documenté + masqué VoiceOver, 1 fichier).
Développement terminé → push + CI `ios-tests.yml` ; merge dans `main` après CI verte.
Voir plan `2026-07-01-plan-iteration-90i`.

### Annotation post-correction (ne pas reproduire)
**NE PLUS re-flagger** pour Dynamic Type les 16 sites de `NewConversationView` ci-dessus : ils
utilisent désormais `MeeshyFont.relative`. Le glyphe `person.slash` (36pt état vide) est
**volontairement** figé + masqué VoiceOver — le re-flagger reproduirait un déséquilibre de l'état
vide. Les couleurs sont déjà toutes tokenisées (rien à convertir).

### Différé prioritaire iOS 91i+
- **Dynamic Type autres surfaces** `MagicLinkView` (17), `DataExportView` (17), `AffiliateView`
  (17), `LocationPickerView` (17, + candidat Glass dédié), `StoryViewerView+Content` (grande
  surface, coordonner i18n).
- Palette : audit hexes proches-mais-non-exacts (checkmark `#4ADE80` → `success` avec vérif
  visuelle).
- Glass adoption reste (`MessageOverlayMenu` via `AdaptiveGlassContainer`).
# Analyse itération 90i — Dynamic Type `FeedCommentsSheet.swift` (iOS)

**Date** : 2026-07-01
**Piste** : iOS (suffixe `i` — Web/Android couverts par d'autres agents)
**Fichier** : `apps/ios/Meeshy/Features/Main/Views/FeedCommentsSheet.swift`
**Branche** : `claude/upbeat-euler-ghu3bp` (base `main` HEAD `c8063196`)
**Type** : Sweep typographique Dynamic Type (a11y) — 1 fichier, 0 logique, 0 test neuf

## Contexte

Surface prioritaire du différé 84i+ (« FeedCommentsSheet, 28 fixed / 0 relative — ⚠️
RE-VÉRIFIER : commit `429ad8b7` prétend l'avoir migré »). L'analyse 89i répétait encore la
mention erronée « FeedCommentsSheet (72i) » comme si la surface était soldée.

**Re-vérification effectuée avant tout travail** (`grep`) : le fichier portait bien **28**
sites `.font(.system(size:))` et **0** `MeeshyFont.relative`. L'historique était trompeur — la
migration n'avait **jamais** été appliquée sur cette surface. Le travail 90i comble ce trou réel.

`FeedCommentsSheet.swift` est une surface cœur UX : la feuille de commentaires du feed (threads,
réponses, composer, like, suppression), ouverte depuis un post ou un reel. Deux vues publiques :
`CommentsSheetView` (feuille + composer + aperçu du post) et `CommentRowView` (ligne de
commentaire réutilisable, `Equatable`).

## Diagnostic — 28 sites `.font(.system(size:))`

### Migrés → `MeeshyFont.relative(size, weight:)` — 23 sites (texte de lecture + glyphes inline appariés)

| Zone | Sites | Détail |
|---|---|---|
| Toolbar titre | 1 | « N commentaires » 16 semibold |
| Aperçu du post (`postPreview`) | 6 | auteur 14 / horodatage 12 / contenu 15 / stat cœur (glyphe 12 + count 12) / stat bulle (glyphe 12 + count 12) |
| Bannière de réponse (`commentReplyBanner`) | 2 | auteur 12 semibold / contenu 12 |
| `CommentRowView` en-tête | 4 | auteur (authorFont 13/14) / middot 12 / middot 12 (hidden) / horodatage 12 |
| `CommentRowView` corps | 1 | contenu (contentFont 14/15) |
| `CommentRowView` barre d'actions | 9 | cœur (glyphe 12/14 + count 12) / flèche répondre 13 + count réponses 12 + « Répondre » 12 / middot 12 / « Voir » 12 / ellipsis 12/14 |

**Note apparage** : les glyphes SF Symbols inline (`heart.fill`, `bubble.right.fill`,
`arrowshape.turn.up.left`, `ellipsis`) sont migrés avec le texte qu'ils accompagnent pour que
la ligne d'action grandisse d'un bloc. Les tailles conditionnelles (`isReply ? 12 : 14`,
`authorFont`, `contentFont`) sont préservées (`relative` accepte un `CGFloat`). Les modificateurs
d'état (`.scaleEffect(isLiked ? 1.1)`) restent intacts.

### Gardés figés — 5 sites (justifiés & commentés dans le code)

| Site | Raison |
|---|---|
| `xmark` toolbar 14 semibold (`.frame(32×32)` + `Circle`) | Chrome fermeture en cadre tap fixe — **doctrine 82i** |
| `xmark` bannière réponse 10 bold (`.frame(24×24)` + `Circle`) | Chrome annulation en cadre tap fixe — **doctrine 82i** |
| Drapeau origine 12/10 (`isOrigActive`) | Taille = **indicateur d'état** actif/inactif (emoji), apparié au soulignement fixe `10×1.5` |
| Drapeau cible 12/10 (`isTransActive`) | Idem — indicateur d'état, soulignement fixe |
| Icône `translate` 10 (`accessibilityHidden`) | Décoratif, géométrie fixe alignée sur la rangée de drapeaux d'état |

Les deux drapeaux ne sont **pas** de la typographie de lecture : la taille encode l'état
sélectionné (langue affichée) et est couplée à un soulignement de largeur fixe — les migrer
casserait l'alignement (soulignement figé vs emoji scalable) et réduirait l'écart 12/10.

## Vérification

- **Compte** : 23 `MeeshyFont.relative` + 5 `.font(.system(size:))` figés = 28 sites d'origine. ✅
- **Diff** : 1 fichier, +31/−23, sweep pur. Aucune logique, aucun test touché.
- **Build local** : impossible (environnement Linux, pas de toolchain Xcode). Gate = CI `iOS Tests`.
- **Parité** : identique aux sweeps 55i / 71i / 74i / 82i / 83i / 87i (`.system(size:)` → `MeeshyFont.relative`, weight/design préservés, 0 test neuf).
- **Import** : `MeeshyUI` déjà importé (ligne 7) — `MeeshyFont` = `public enum` (`MeeshyUI/Theme/DesignTokens.swift`). Aucun import ajouté.

## ✅ SOLDÉ (ne plus re-flagger)

- La typographie Dynamic Type de `FeedCommentsSheet.swift` (soldé 90i — l'historique 72i/429ad8b7 était **faux**, corrigé ici).
- Ses 5 sites `.system(size:)` figés à dessein (décisions ci-dessus).
# Analyse — Itération 90i (2026-07-01) — iOS Dynamic Type + a11y `DataExportView`

## Périmètre
**iOS exclusivement** (suffixe `i`). Piste indépendante des itérations web/android.
Surface : `apps/ios/Meeshy/Features/Main/Views/DataExportView.swift` — écran d'**export de
données personnelles (RGPD/GDPR)**, flux légal : carte d'info conformité, sélection de format
(JSON/CSV), sélection de contenu (messages/média/contacts), bouton d'export + partage système.

> Numéro : `88i` (DeleteAccountView) et `89i` (EffectsPickerView) déjà pris/mergés par des agents
> parallèles → cette itération prend le prochain numéro libre **`90i`**. Surface disjointe
> (`DataExportView` ≠ ces fichiers) → aucun conflit de code.

## Diagnostic
- **17 sites `.font(.system(size:))`** → l'écran ignorait entièrement Dynamic Type (rupture
  directe de la règle a11y CLAUDE.md « Use semantic fonts, NEVER use fixed font sizes for body
  text »). Comme le wizard de consentement (87i) et `DeleteAccountView` (88i), c'est un **flux
  juridique** : un utilisateur en « Larger Text » doit pouvoir lire les mentions RGPD à sa taille
  de police choisie.
- **Glyphes décoratifs non masqués** → VoiceOver lisait le nom de symbole SF :
  - `shield.checkered` (carte d'info conformité),
  - glyphes d'en-tête de section (`sectionHeader`),
  - glyphe format à l'intérieur des boutons JSON/CSV,
  - `exclamationmark.triangle.fill` (bannière d'erreur).
- **En-têtes de section non navigables au rotor** VoiceOver (pas de trait `.isHeader`, contrairement
  à `AboutView` soldé 86i).
- **Boutons de format (JSON/CSV) sans état sélectionné exposé** → VoiceOver n'annonçait pas
  qu'un format était actif (ce sont des boutons toggle-like : tap = ajoute/retire du set).
- **Carte d'info & bannière d'erreur** lues en deux fragments séparés (glyphe + texte) au lieu
  d'un seul élément.
- i18n : déjà couvert (tous les libellés en `String(localized:defaultValue:bundle:)`).
  Couleurs : token `info` (`MeeshyColors.infoHex`) + tokens sémantiques (`error`/`success`/
  `communityAccent`/`brandDeep`/`indigo500`) — **conformes, hors-scope palette**.

## Corrections appliquées
1. **Dynamic Type** : **16/17** `.font(.system(size:))` → `MeeshyFont.relative(size, weight:, design:)`
   (weight **et** design préservés 1:1, dont `.rounded` sur les titres de section). Swaps
   mécaniques, aucun changement de layout à taille par défaut.
2. **1 figé justifié & commenté** : glyphe `toggleRow` (14pt) rendu dans un **badge de dimension
   fixe 28×28** (`.frame(width: 28, height: 28)` + fond `RoundedRectangle`). Laisser la police
   scaler déborderait/tronquerait le glyphe hors du badge fixe → **doctrine 86i** (`fieldIcon`
   d'`AboutView`, `StatRing`/`ArcGauge` de 74i : glyphes contraints dans un conteneur fixe restent
   figés). Commenté in-situ.
3. **VoiceOver** :
   - `.accessibilityHidden(true)` sur les 4 glyphes décoratifs (shield info, glyphe en-tête de
     section, glyphe format dans les boutons, triangle d'erreur).
   - `.accessibilityElement(children: .combine)` sur la carte d'info, la bannière d'erreur et
     l'en-tête de section → VoiceOver annonce chacun d'un seul geste.
   - `.accessibilityAddTraits(.isHeader)` sur `sectionHeader` (parité `AboutView` 86i → navigation
     au rotor « En-têtes »).
   - Boutons format : `.accessibilityLabel(format.rawValue)` + `.accessibilityAddTraits(.isSelected)`
     quand le format est actif → l'utilisateur VoiceOver entend « JSON, sélectionné ».

## Invariants respectés
- **Zéro nouvelle clé de catalogue** → aucun risque de parité i18n.
- **Zéro changement de logique / comportement / layout** à taille par défaut.
- **Zéro test neuf** (sweep typographique + a11y pur, parité doctrine 55i/71i/82i/83i/84i/86i/87i/88i ;
  SwiftUI ne compile pas sous Linux → gate = CI `ios-tests.yml`).
- **1 seul fichier touché** → orthogonal, aucun conflit attendu.
- SDK non touché (`MeeshyFont.relative` déjà exporté par `MeeshyUI`, déjà importé ligne 4).

## Anti-répétition
Vérifié contre le § History du `branch-tracking.md`, les fichiers d'analyse 88i/89i et les PRs iOS
ouvertes (`list_pull_requests` = **0 PR ouverte** au 2026-07-01) : **aucune** itération ni PR n'a
touché `DataExportView`. Surfaces Dynamic Type déjà soldées disjointes : `DeleteAccountView` (88i),
`EffectsPickerView` (89i), `AboutView` (86i), `VoiceProfileWizardView` (87i), `AudioFullscreenView`
(82i), `TwoFactorSetupView` (71i), `ConversationInfoSheet` (55i).

## État final
- `grep .font(.system(size:` → 1 restant (badge fixe 28×28, commenté à dessein).
- `grep MeeshyFont.relative` → 16 sites.
- Gate = CI `ios-tests.yml` (compile Xcode 26.1 + tests simu 18.2).

## ✅ Corrigé & complet (ne pas reproduire)
`DataExportView` : Dynamic Type soldé (16 migrations) + a11y VoiceOver soldé (4 glyphes masqués,
carte/bannière/en-tête combinés, `.isHeader` sur en-têtes, état sélectionné des formats JSON/CSV).
**NE PAS re-flagger** la typographie ni le VoiceOver de cet écran. Le seul `.system(size:)` restant
(glyphe toggleRow 14pt, badge fixe 28×28) est figé **à dessein** et documenté (doctrine 86i).

### Différé prioritaire iOS 91i+
- Dynamic Type autres surfaces : `MagicLinkView` (17), `NewConversationView` (17),
  `AffiliateView` (17), `MemberManagementSection` (17), `LocationPickerView` (17),
  `StoryViewerView+Content` (31, coordonner avec i18n merged), `FeedCommentsSheet` (28).
- Palette : audit hexes proches-mais-non-exacts (checkmark `#4ADE80` → `success`, avec vérif visuelle).
- Glass adoption reste (`MessageOverlayMenu` via `AdaptiveGlassContainer`, lot dédié).
# Itération 90i — Analyse UI/UX iOS : `MagicLinkView`

**Date** : 2026-07-01
**Piste** : iOS (suffixe `i`) — indépendante des pistes Web/Android.
**Fichier cible** : `apps/ios/Meeshy/Features/Main/Views/MagicLinkView.swift` (327 lignes)
**Surface** : Écran d'authentification passwordless (« Connexion par lien magique ») — 2 étapes : saisie e-mail → attente/renvoi.

## Contexte routine

Suite du sweep Dynamic Type + VoiceOver « grandes surfaces restantes » différé en 88i/89i.
- 88i = `DeleteAccountView` (mergé, PR #1215 / #1217).
- 89i = `EffectsPickerView` (PR #1218, session parallèle) — **NE PAS re-toucher**.
- 90i (cette itération) = `MagicLinkView`, choisi car : self-contained (aucune dépendance ViewModel/socket partagée), forte valeur a11y (surface d'onboarding vue par tout nouvel utilisateur), zéro collision avec les PR ouvertes (#1217 DeleteAccount, #1218 EffectsPicker, #1219/#1216/#1220 web/translator).

## État avant

- **17** sites `.font(.system(size:))` → figent la taille, **ignorent totalement Dynamic Type**. Les tailles étaient déjà tokenisées (`MeeshyFont.headlineSize/titleSize/subheadSize/bodySize`) mais enveloppées dans `.system(size:)` non-scalable.
- Le bouton de fermeture `xmark` (toolbar leading) **n'avait aucun `.accessibilityLabel`** → VoiceOver l'annonçait « xmark » (nom de glyphe brut).
- Les 2 glyphes héros décoratifs (`wand.and.stars` 56pt, `envelope.open.fill` 48pt) n'étaient pas masqués à VoiceOver → doublon vocal du titre adjacent.
- Toutes les chaînes étaient déjà i18n (`String(localized:)`, clés `auth.magiclink.*` + `common.*`). Aucune couleur brute hors charte (indigo brand + `MeeshyColors.error/indigo400/indigo600` sémantiques ; `Color(hex: "8B5CF6")` = indigo500 accent du champ e-mail, conservé).

## Changements (1 fichier, sweep pur présentation + traits déclaratifs)

### Dynamic Type — 14/17 sites migrés
`.font(.system(size: <token>, weight:))` → `MeeshyFont.relative(<token>, weight:)` (weight préservé). Couvre : titre principal (toolbar), titre + sous-titre étape e-mail, message d'erreur, libellé bouton d'envoi, titre + sous-titre + adresse e-mail étape attente, message d'expiration, instructions, compte à rebours (`.monospacedDigit()` chaîné sur le `Font` retourné, préservé), glyphe + libellé du bouton « Renvoyer », bouton « Annuler ».

### 3 sites gardés FIXES (justifiés, commentés)
1. `xmark` chrome (17pt, cadre de tap toolbar) — doctrine 82i/87i (chrome de fermeture figé). **`.accessibilityLabel(common.close)` ajouté** (comblement du manque).
2. `wand.and.stars` 56pt — héros décoratif ≥40pt (doctrine 84i/87i). **`.accessibilityHidden(true)` ajouté.**
3. `envelope.open.fill` 48pt — héros décoratif ≥40pt. **`.accessibilityHidden(true)` ajouté.**

### VoiceOver — 3 traits déclaratifs
- 1 `.accessibilityLabel` (xmark).
- 2 `.accessibilityHidden(true)` (héros décoratifs) → VoiceOver ne lit plus les glyphes redondants avec le titre.

## Hors-scope (documenté, NON changé)
- Restructuration de groupement VoiceOver (combiner sous-titre + adresse e-mail) — évitée car nécessiterait un conteneur `VStack` intercalé → changement d'espacement visuel. Écartée pour préserver « zéro régression visuelle » du sweep.
- Regex de validation e-mail inline (`isValidEmail`) — logique, hors périmètre a11y.

## Vérification
- Sweep présentation + 3 traits a11y déclaratifs, **0 logique modifiée, 0 clé i18n neuve, 0 test neuf** (parité 55i/71i/74i/83i/86i/87i/88i).
- Tailles de base inchangées à la cadence Dynamic Type par défaut (`.large`) → **zéro régression visuelle** au réglage standard.
- Gate = CI `ios-tests.yml` (compile Xcode 26.1.x + simulateur 18.2). Environnement de dev Linux → build iOS non exécutable localement, CI seule autorité.

## Annotation continuité (à ne plus re-flagger)
- ⚠️ **Typographie/VoiceOver de `MagicLinkView` = SOLDÉ 90i.** Les 3 `.system(size:)` restants (xmark 17 + 2 héros 56/48) sont figés à dessein.
