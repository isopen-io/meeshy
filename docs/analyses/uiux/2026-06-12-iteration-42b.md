# UI/UX Review — Iteration 42b (2026-06-12, passe parallèle)

> Base: main @ 167ef31c (post-merge PR #577 / itération 41). Cette itération solde les différés
> tracés en 41 : migration des hex iOS hors charte sur les surfaces Settings/Notifications/
> DataExport/Composer/Onboarding, libellés `ContactsTab` Android, et couvre les pages v2 web
> communities/links/settings jamais auditées (100 % françaises codées en dur).

## Part 1 — Web (pages v2 protégées, jamais auditées)

### i18n — chaînes hardcodées
- **HIGH** `app/v2/(protected)/links/page.tsx` — page entière en français codé en dur :
  toasts ("Veuillez entrer un nom pour le lien" L39, "Lien cree avec succes" L56, "URL copiee…" L62,
  "Erreur lors de la copie" L64, "Lien desactive/active" L76), titre "Mes liens" L86, "Creer" L92,
  stats "Clics totaux"/"Liens actifs" L102/106, "MES LIENS" L112, badges "Actif"/"Inactif" L121-123,
  "{n} clics" L159, modale "Creer un nouveau lien"/"Nom du lien"/"URL generee"/"Annuler"/"Creer"
  L188-246, placeholder L199, tooltip "Regenerer le slug" L215
- **HIGH** `app/v2/(protected)/communities/page.tsx` — idem : 5 toasts L32-67, titre
  "Communautes" L78, "Creer" L84, "MES COMMUNAUTES"/"DECOUVRIR" L92/171, badges "Membre"/"Prive"
  L129-130, "{n} membres"/"{n} conversations" L138-141, "Quitter"/"Rejoindre" L153/212, états vides
  L162/221/226, dialog création complète L237-276
- **HIGH** `app/v2/(protected)/communities/[id]/page.tsx` — "Retour" L56/98, "Communaute
  introuvable" L80, "Retour aux communautes" L82, badges, "Creee le …" L146, "Quitter/Rejoindre la
  communaute" L157, "PREFERENCES"/"MEMBRES"/"CONVERSATIONS", map de labels préférences inline
  française L168-177 (Epingler/Muet/Archiver…), "Aucun membre", "+{n} autres membres",
  "Sans titre" L232, fallback "Unknown" L248
- **MEDIUM** `app/v2/(protected)/settings/page.tsx:248-249` — toast thème avec map française inline
  (`{ light: 'clair', dark: 'sombre', system: 'systeme' }`) ; L255/257 toasts notifications
  hardcodés alors que la page utilise déjà `useI18n('settings')` + namespace `v2settings`

### Dates sans locale
- **MEDIUM** `communities/[id]/page.tsx:146` — `toLocaleDateString()` sans locale (incohérent avec
  la règle iter-41 : locale dynamique du hook `useI18n`)
- `links/page.tsx:48` — `toLocaleDateString('fr-FR', …)` figé

### A11y
- **MEDIUM** `links/page.tsx:146` — bouton copier (icône presse-papier) sans aria-label
- **MEDIUM** `links/page.tsx:216` — bouton régénérer le slug sans aria-label (le Tooltip ne suffit
  pas pour VoiceOver/NVDA)
- **MEDIUM** `settings/page.tsx:417-453` — 4 aria-labels de switchs notifications hardcodés français
- **LOW** `links/page.tsx:136` — aria-label du toggle actif/inactif hardcodé français

### Dark mode
- ✅ Aucun finding : les pages v2 utilisent les variables `var(--gp-*)` définies light/dark.

## Part 2 — iOS (différé itération 41 : hex hors charte, hors surface liens)

Inventaire précis (grep `"[0-9A-F]{6}"`) sur les 6 surfaces différées :

### SettingsView.swift — 38 occurrences, 15 hex hors charte
Paramètres `color:` de `settingsSection`/`settingsRow` (tints d'icônes). Mapping charte
(conforme aux décisions iter-41 et au tableau Legacy → New de `apps/ios/CLAUDE.md`) :
| Hex actuel | Token charte |
|---|---|
| `9B59B6`, `A855F7` (violets) | `indigo600Hex` |
| `E91E63` (rose) | `brandPrimaryHex` (pink→indigo500) |
| `3498DB`, `45B7D1` (bleus) | `infoHex` |
| `EF4444`, `FF6B6B` (rouges) | `errorHex` |
| `F8B500`, `E67E22`, `F39C12`, `FBBF24` (oranges) | `warningHex` |
| `4ECDC4` (teal) | `indigo300Hex` |
| `2ECC71`, `27AE60` (verts) | `successHex` |
| `6B7280` (gris) | `neutral500Hex` |
| `6366F1` littéral | `brandPrimaryHex` |

### NotificationSettingsView.swift — 40 occurrences, mêmes familles
`FF6B6B`→error, `60A5FA`→info (littéral → token), `6366F1`→brandPrimary, `9B59B6`→indigo600,
`F8B500`→warning, `4ADE80`→success.

### DataExportView.swift — 10 occurrences
- **HIGH** `accentColor = "3498DB"` (L22) hors charte → `infoHex` (bleu → info, garde l'identité visuelle)
- `FF6B6B` L234/237/243 (annulation destructive) → `MeeshyColors.error`
- `2ECC71` L274 (export complet) → `MeeshyColors.success`
- `F8B500` L185/194/197 + toggleRow tints → `warningHex`/`indigo600Hex`/`indigo300Hex`/`errorHex`

### ConversationView+Composer.swift — 12 occurrences
- L100 : accents dynamiques `FF6B6B`(éphémère)→errorHex, `A855F7`(blur)→indigo600Hex, `6366F1`→brandPrimaryHex
- Bannière édition `F8B500` L400-438 → `warning`/`warningHex`
- États envoyé `2ECC71`/`27AE60` L563-789 → `success`/`successDeep` (nouveau token, voir ci-dessous)

### OnboardingView.swift — stops de gradients sémantiques
- L211 `1C1917` (logo light) → `indigo950` (texte light = indigo950 selon charte)
- L252 `FF8A80` → `errorSoft` (nouveau) ; L473 `E55555` → `errorStrong` (nouveau)
- L262/483 `8B5CF6` → `indigo500` ; L267/488 `22C55E` → `successDeep` ; L478 `06B6B3` → `indigo300`
- **Décision documentée** : les washes de fond par page (L40-85, 18 hex) sont un theming
  d'ambiance intentionnel par page d'onboarding (dark/light) — conservés, hors périmètre charte.

### Extension SDK requise (MeeshyColors)
L'échelle sémantique actuelle (Tailwind 400 : success=34D399, error=F87171, warning=FBBF24,
info=60A5FA, + errorDark=991B1B) manque de variantes tonales pour les paires de gradients et de
constantes hex pour les paramètres `String` :
- Couleurs : `errorSoft` (FCA5A5, red-300), `errorStrong` (EF4444, red-500), `successDeep` (10B981, emerald-500)
- Hex : `errorHex`, `infoHex`, `indigo400Hex`, `indigo600Hex`

## Part 3 — Android

- **HIGH** (différé 41) `feature/contacts ContactsScreen.kt:62` — TabRow affiche `tab.name`
  (noms d'enum bruts `Contacts/Requests/Discover/Blocked`, jamais traduits) → mapping
  enum → `stringResource` + 4 clés en/fr
- **MEDIUM** `sdk-ui MeeshyAvatar.kt:39` — fallback contentDescription `"Avatar"` hardcodé alors
  que le module a déjà un strings.xml (bubble_*) → `avatar_fallback` en/fr
- **LOW justifié** `sdk-ui BrandLogo.kt:36` — `"Meeshy"` = nom de marque, invariant, conservé
- **LOW justifié** `SettingsScreen.kt:104,248` — chevrons `contentDescription = null` : icônes
  décoratives, conforme Material (le row entier porte la sémantique)
- **LOW justifié** `ChatScreen.kt:331` / `MessageBubble.kt:238` — tailles sp d'émojis : glyphes,
  décision iter-40/41 maintenue
- ✅ Dark mode : darkColorScheme/lightColorScheme + tokens light/dark complets, rien à corriger

## Part 4 — Cohérence cross-frontend

| Sujet | Web | iOS | Android |
|---|---|---|---|
| Surface liens localisée | ✅ (cette itération, page v2) | ✅ (iter-41) | n/a |
| Badges Actif/Inactif localisés | ✅ (cette itération) | ✅ (iter-41) | n/a |
| Communautés localisées | ✅ (cette itération, pages v2) | ✅ (déjà localisé) | n/a (pas de surface) |
| Accents Settings dans la charte | n/a (CSS vars) | ✅ (cette itération) | ✅ (M3 tokens) |
| Onglets contacts localisés | ✅ (contacts page déjà i18n) | ✅ | ✅ (cette itération) |
| Avatar fallback a11y localisé | n/a | ✅ | ✅ (cette itération) |

## Différés (itération 43+)
- iOS : hex restants hors des 6 surfaces de cette itération (balayage complet `Color(hex: "` à
  re-prioriser) ; polices fixes des vues liens (list/detail)
- Android : parité stories (surface absente) ; tokens MeeshySpacing pour les paddings 16.dp de
  SettingsScreen (cosmétique)
- Web + Android : réactions par pièce jointe (parité iOS) — nécessite wiring gateway, hors pass UI/UX
- Web : `toLocaleDateString('fr-FR')`/sans locale restants hors v2 (~40 occurrences admin/groups/
  contacts) — passe dédiée ; qualité es/pt machine-translated à relire par namespace

## Status : corrections implémentées — voir plan 2026-06-12-plan-iteration-42b

**MAJ 2026-06-12 : PR #579 mergée — itération soldée.**
