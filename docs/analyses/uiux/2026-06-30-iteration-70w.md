# Analyse UI/UX — Itération 70w (web)

**Date** : 2026-06-30
**Périmètre** : application web (`apps/web`) — EXCLUSIVEMENT
**Base** : `main` HEAD `769f55a` (post-merge #1082 iter-68w + #1078 iter-67w)
**Branche** : `claude/practical-fermat-w9pjo3`

## Contexte de continuité & non-répétition

État de `main` au démarrage : dernières itérations web mergées **67w** (#1078, a11y clavier liste
conv + tuile audio) et **68w** (#1082, a11y clavier contrôles plein écran appel vidéo). En vol au
moment de l'analyse (vérifié via `list_pull_requests`) :
- **#1084 (iter-69w)** — a11y clavier `create-link` modal config controls → **évité** (surface modale liens).
- **#1077 (iter-67w)** — i18n `auth.verifyPhone` (`verify-phone/page.tsx`) → **évité** (clés `verifyPhone.*`).
- **#1081** — perf `shared` (hors-web).

### Doublons d'analyses
Aucun doublon introduit. Le cluster `t()||fallback` reste suivi (50w/60wb/61w/62w/63w/64w/65w/66w).
Surface choisie **strictement orthogonale** aux PR en vol : `components/auth/PhoneResetFlow.tsx`
(namespace `auth`, clés `phoneReset.*` + `otp.*`) — **disjointe** des clés `verifyPhone.*` de #1077.
`PhoneResetFlow.tsx` était explicitement désigné « gros porteur » du cluster `t()||fb` dans le différé
66w+ et **n'apparaît dans aucune analyse/plan antérieur**.

## Cible : `components/auth/PhoneResetFlow.tsx` (flux reset mot de passe par téléphone)

Flux multi-étapes (saisie téléphone → vérification d'identité → saisie code SMS) accessible depuis
l'écran « mot de passe oublié ». **56 occurrences** de l'anti-pattern `t('clé') || 'fallback FR'`,
namespace `useI18n('auth')`.

### Bug 1 (réel, visible) — clé brute affichée aux utilisateurs EN
`phoneReset.identityHint` **était absente de `en/auth.json`** (présente en `fr/es/pt`). `use-i18n.ts`
ne fait **aucun fallback per-clé inter-locale** : pour une clé manquante, `t()` retourne la **clé brute**
(truthy), donc `t('phoneReset.identityHint') || 'FR'` court-circuitait sur la clé brute →
l'utilisateur **EN voyait littéralement `phoneReset.identityHint`** sous le formulaire de vérification
d'identité (écran « Verify Your Identity »). Jamais signalé.

### Bug 2 (systémique) — flash-of-raw-keys + langue figée FR
Les 56 fallbacks `|| 'FR'` sont :
1. du **code mort** quand la clé existe (`t()` renvoie déjà la valeur localisée),
2. un **flash de clé brute en français** pendant le chargement async du namespace (l'objet
   `translations` est vide → `t()` renvoie la clé brute truthy → `||` sert le **français figé**
   à TOUS les utilisateurs EN/ES/PT). Rupture Prisme sur une surface auth sensible.

## Correction

1. **+1 clé `phoneReset.identityHint`** ajoutée à `en/auth.json` (« by filling in the missing
   characters (replace the * and ...) ») — insertion chirurgicale (1 ligne), parité 4 locales
   rétablie (symdiff `en`↔`fr/es/pt` = 0 sur `phoneReset.*` + `otp.*`).
2. **56** `t('clé') || 'FR'` → `t('clé', 'EN exacte')` (leçon 50w anti-flash) : valeur de secours =
   valeur EN **mot pour mot** de `en/auth.json` (anti-flash + anti-incohérence). Aucune nouvelle clé
   hors `identityHint`.
3. Commentaire interne périmé `// ... with French fallbacks` → `// ... native English fallbacks for anti-flash`.

Les 2 `otp.{groupLabel,digitLabel}` utilisaient déjà la forme paramétrée `t(k, {…})` (non concernées),
de même que `phoneReset.selectCountry` déjà converti antérieurement.

## Vérifications
- `grep` anti-pattern `t()||'…'` résiduel sur le fichier = **0**.
- `grep` chaîne FR user-facing résiduelle (hors commentaires) = **0**.
- Parité JSON `auth.json` : `fr`/`es`/`pt` symdiff = **0** vs `en` sur `phoneReset.*`/`otp.*`.
- Diff `en/auth.json` = **+1 / -0** (insertion chirurgicale, pas de reformat).
- Test `__tests__/components/auth/PhoneResetFlow.test.tsx` : **39 passed / 39** (jest local) — le
  2ᵉ argument string est ignoré par le mock `t` (`typeof params === 'object'` guard), rendu inchangé.

## Périmètre
1 composant + 1 locale (`en/auth.json`, +1 ligne). Aucun nouvel import/namespace.

---

## ✅ Statut : CORRIGÉ (iter-70w)

**NE PLUS re-flagger** `components/auth/PhoneResetFlow.tsx` pour l'anti-pattern `t()||'fb'`, ni la clé
`auth.phoneReset.identityHint` (désormais présente ×4 locales). Surface entièrement internationalisée
et anti-flash.

### Différé 71w+ (cluster `t()||fallback`, vérifier collisions/clés au cas par cas)
`hooks/conversations/useMessageActions.ts` (≈10), `ConversationDetailsStep.tsx` (≈3),
`conversation-participants-drawer.tsx` (≈2), `use-recovery-submission.ts`/`use-recovery-flow.ts`,
`ConversationLayout.tsx`, `LanguageSelectionMessageView.tsx`, `settings/page.tsx`,
`contacts/page.tsx`, `LastMessagePreview.tsx`. **Éviter** `verify-phone/page.tsx` (#1077 en vol).
