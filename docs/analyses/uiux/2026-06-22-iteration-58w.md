# Analyse — Iteration 58w (web)

## Revue préalable (anti-duplication)
- `branch-tracking.md` à jour : cluster feed 53w avancé — `ReelPlayer` (57w #774), `ReelsFeedScreen` (57wb #780) **déjà soldés sur main**.
- **Collision évitée** : la cible initiale 58w (`ReelsFeedScreen`) ayant été livrée par 57wb pendant ce run, l'implémentation dupliquée a été **abandonnée** (reset sur main) et l'itération **repivotée** sur le volet réellement restant : `PostsFeedScreen.tsx`. Vérifié que `PostsFeedScreen` n'est PAS encore i18n sur main.
- Pas de duplication de clés : nouveau namespace `feed` (les toasts reel `linkCopied`/`linkCopyError` vivent dans `reel`, périmètre disjoint).

## Constat
`components/feed/PostsFeedScreen.tsx` (écran `/feed/posts` + alias `/feeds`) — gros écran (story tray + status bar + composers + liste de posts + 5 modales). **Aucun hook i18n.** ~40 chaînes figées avec **incohérence FR/EN** : toasts FR (`Story publiée !`, `Lien copié !`, `Publié !`…) mélangés à de l'UI EN dure (`Updating...`, `Unable to load feed.`, `No posts yet…`, `Retry`, `Feed`, `new posts`) + `aria-label`/`sr-only`/sections FR + `formatRelativeTime` FR. Rupture Prisme + a11y, sur une surface centrale.

## Correction appliquée
- Namespace dédié **`feed.json`** ×4 locales (47 clés à parité).
- `useI18n('feed')` câblé sur `PostsFeedScreen` **et** `FeedTabs` (composant exporté séparé → propre hook).
- `formatRelativeTime(date, t)` reçoit `t` → `time.{now,minutes,hours,days}` paramétrées.
- Fallbacks EN 2e arg (anti-flash, leçon 50w) ; clés paramétrées sans fallback string (parité ×4).
- `t` ajouté aux deps des `useCallback` concernés.

## Exclusions documentées (NE PLUS re-flagger)
- `mockStatuses` : données démo placeholder « to be replaced » — pas du chrome UI.
- Onglet `Reels` (marque) littéral, cohérent avec `ReelsFeedScreen` (57wb).

## Vérifications
- JSON valide ×4 ; parité 47 clés ×4 (script flatten : 0 manquante/extra).
- Grep résiduel sur le composant : seules les 3 lignes mock.
- tsc local impossible (node_modules absent en conteneur frais) ; `TranslateFn` typée (`Record<string, string|number> | string`), aucun `any`.

## Statut : ✅ COMPLET — NE PLUS RE-FLAGGER
`PostsFeedScreen.tsx`/`FeedTabs` + `feed.*` entièrement internationalisés. **Cluster feed 53w intégralement soldé** (player 57w + reels screen 57wb + posts screen 58w).

## Incident de run (traçabilité)
CI ne créait aucun run pour la branche tant que le PR était en conflit (`mergeable_state: dirty`, ref de merge non calculable) — résolu par reset/pivot sur main à jour. Le PR #781 est réutilisé (repurposé du ReelsFeedScreen dupliqué vers PostsFeedScreen).
