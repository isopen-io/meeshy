# Analyse UI/UX — Itération 2 (2026-06-08)

## Périmètre

Audit des deux frontends (iOS SwiftUI + Web Next.js) sur la base du code post-itération-1.
Issues itération 1 confirmées upstream (PR #345).

## Issues identifiées

### Web — HIGH

#### W1 · i18n — OngoingCallBanner.tsx : strings anglaises hardcodées
- Fichier : `apps/web/components/video-calls/OngoingCallBanner.tsx`
- Lines 59, 63, 75, 84 : `"Call in progress"`, `"participant/participants"`, `"Join"`, `"Dismiss"` hardcodés en anglais
- Manque namespace `calls` dans les locales

#### W2 · i18n — VideoCallInterface.tsx : "Loading call..." hardcodé
- Fichier : `apps/web/components/video-calls/VideoCallInterface.tsx`
- Line 503 : `"Loading call..."` (anglais, non localisé)

#### W3 · i18n + a11y — CallErrorBoundary.tsx : class component avec strings hardcodées
- Fichier : `apps/web/components/video-calls/CallErrorBoundary.tsx`
- Lines 57–89 : "Call Error", "Try Again", "Return Home", troubleshooting tips tous hardcodés
- Class component → impossible d'utiliser `useI18n` directement ; fallback UI doit être extrait dans un composant fonctionnel `CallErrorDisplay`

### iOS — HIGH

#### I1 · i18n — ConversationView+Composer.swift : labels attachements hardcodés
- Fichier : `apps/ios/Meeshy/Features/Main/Views/ConversationView+Composer.swift`
- Lines 731–735 : `"Photo"`, `"Video"`, `"Audio"`, `"Fichier"`, `"Position"` retournés directement
- `"Fichier"` est français hardcodé ; les autres sont anglais hardcodés
- Fix : `String(localized: "attachment.label.X", defaultValue: "...", bundle: .main)`

### Web — MEDIUM

#### W4 · Dark mode — OngoingCallBanner.tsx : bouton "Join" bg-white
- Line 73 : `className="bg-white text-green-700 hover:bg-green-50"` — fond blanc cassé en dark mode sur fond vert foncé (contraste insuffisant)
- Fix : `bg-white/90 dark:bg-white/10 text-green-700 dark:text-white hover:bg-white/80 dark:hover:bg-white/20`

## Issues hors périmètre itération 2 (reportées à itération 3)

- Markdown/Mermaid error messages hardcodés (W-M1)
- Admin agent components (W-A1)
- iOS StatusBarView empty state retry
- iOS PrivacySettingsView accentColor hardcodé
