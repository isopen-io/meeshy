# UI/UX Audit — Itération 3 (2026-06-08)

> Base : PR #346 mergé (iter-2 : i18n video-calls + iOS attachment labels)

## Contexte
Audit post-merge des deux frontends. Focus sur les zones non couvertes par les itérations 1-2.

---

## WEB — Problèmes identifiés

### CRITIQUE — Strings hardcodées hors système i18n

| # | Fichier | Lignes | Problème |
|---|---------|--------|---------|
| W1 | `PermissionRequest.tsx` | 43–150 | ~15 strings EN hardcodées (titres, erreurs, instructions navigateur) |
| W2 | `PushPermissionBanner.tsx` | 38–73 | 4 strings EN hardcodées (titre, desc, boutons, toasts) |
| W3 | `notification-settings.tsx` | 74, 102, 129 | 3 toasts FR hardcodés (`Préférences enregistrées`, `Erreur lors de la sauvegarde`, `L'heure de début…`) |
| W4 | `notification-settings.tsx` | 140 | `window.confirm()` avec texte FR non-traduisible — UX bloquant |
| W5 | `notification-settings.tsx` | 146, 174, 176 | 3 toasts FR hardcodés (push permission) |

### HIGH — Locale par défaut incorrecte

| # | Fichier | Ligne | Problème |
|---|---------|-------|---------|
| W6 | `message-composer/index.tsx` | 71 | `formatReplyDate` default `'fr-FR'` → doit être `'en'` |

---

## iOS — Problèmes identifiés

### CRITIQUE — Labels FR hardcodés dans le menu long-press

| # | Fichier | Lignes | Problème |
|---|---------|--------|---------|
| I1 | `ContextActionMenu.swift` | 56–76 | 7 labels FR (Répondre, Transférer, Réagir, Traduire, Copier, Supprimer, Éditer) — affects tout menu long-press |
| I2 | `Router.swift` | 58–104 | 22 titres de navigation FR hardcodés (Parametres, Profil, Contacts, etc.) |

### HIGH — Labels d'accessibilité FR hardcodés

| # | Fichier | Lignes | Problème |
|---|---------|--------|---------|
| I3 | `BubbleFooter.swift` | 54, 160, 192, 222 | 4 `accessibilityLabel` FR (Renvoyer, Traduction disponible, statut lecture) |
| I4 | `BubbleFooter.swift` | 273–276 | Labels contexte avatar FR (Voir la story, Voir le profil) |
| I5 | `RootViewComponents.swift` | 934, 944, 975 | Feed menu FR (Copier le texte, Partager, favoris) |

### HIGH — Couleur hardcodée ignorant le thème

| # | Fichier | Ligne | Problème |
|---|---------|-------|---------|
| I6 | `AboutView.swift` | 252 | `Color(hex: "4ADE80")` — vert fixe, cassé en dark mode → doit être `MeeshyColors.success` |

---

## Non-couverts (scope futur)

- `DataStorageView.swift`, `ThreadView.swift`, `DataExportView.swift` — hex colors multiples
- `MediaDownloadSettingsView.swift` — hardcoding couleurs settings
- `TextBubbleCell.swift` — `UIColor(hex:)` en UIKit sans dark mode
- `OnboardingStepViews.swift` — avatar overlay text `.accessibilityHidden(true)` manquant
- Web : `AudioEffectsPanel.tsx` — dark-only intentionnel (video call overlay), laissé tel quel
- Web : `ContactStep.tsx` — `focus:outline-none focus:ring-0` (a11y keyboard)
- Web : manque i18n de/it locale directories (scope distinct)
