# Analyse UI/UX — Itération 3 (2026-06-08)

## Périmètre

Audit post-itération-2 réalisé en deux passes parallèles couvrant iOS (SwiftUI) et Web (Next.js 15)
sur : i18n, accessibilité, dark mode, couleurs hardcodées, Dynamic Type, cohérence cross-platform.

---

## Passe A — Viewers web + iOS MeeshyColors restants

_(Corrigé dans PR #350)_

### Web — Viewer Components (PDF/PPTX/MarkdownLightbox)

**`apps/web/components/pdf/PDFViewerWrapper.tsx`**
- `'Impossible de charger le PDF'`, `'Ouvrir dans un nouvel onglet'`, `'Supprimer ce PDF'`, `'Ouvrir en plein écran'`, `'Télécharger'` — tous en français

**`apps/web/components/pdf/PDFLightboxSimple.tsx`**
- 7 strings françaises : aria-labels, textes de bouton, message d'erreur, hint clavier

**`apps/web/components/pptx/PPTXViewer.tsx`**
- 6 strings françaises : erreur de chargement, label fichier public, titre suppression, etc.

**`apps/web/components/markdown/MarkdownLightbox.tsx`**
- `'Markdown Document'`, `'Vue formatée'`/`'Vue brute'`, `'Télécharger le fichier Markdown'`, `'Fermer'`, `'Impossible de charger le fichier'`, `'Appuyez sur Échap pour fermer'`

**Cause racine :** namespace `viewers` inexistant ; les trois composants ont été construits sans `useI18n`.

### iOS — Couleurs brand restantes (Passe A)

| Fichier | Lignes | Hex | Token |
|---------|--------|-----|-------|
| `LanguagePickerSheet.swift` | 52, 75, 83 | `6366F1` | `MeeshyColors.indigo500` |
| `MiniAudioPlayerBar.swift` | 124 | `6366F1` + `4338CA` | `MeeshyColors.indigo500/indigo700` |
| `MiniAudioPlayerBar.swift` | 142 | `6366F1` | `MeeshyColors.indigo500` |
| `ConversationView.swift` | 475, 477 | `4ECDC4` | `MeeshyColors.indigo400` |

---

## Passe B — Video-calls i18n + Accessibilité iOS systémique + Dark mode

_(Corrigé dans PR #354)_

### Issues confirmées depuis itération 2

#### W-M1 · MermaidDiagramImpl.tsx : messages d'erreur français hardcodés — HIGH
- Lignes 89, 104, 128, 134, 147–150, 189–190
- Fix : namespace `mermaid` + `useI18n('mermaid')`

#### W-A1 · Admin AgentOverviewTab.tsx : strings françaises hardcodées — HIGH (déferré)
- Labels KPI, confirmations de reset, messages d'erreur — 100% en français
- Effort large → déferré à l'itération 4

#### iOS-A1 · PrivacySettingsView.swift : accentColor legacy teal — MEDIUM
- `private let accentColor = "08D9D6"` → `"6366F1"`

### Nouveaux problèmes Web

| ID | Fichier | Problème | Sévérité |
|----|---------|---------|---------|
| W-V1 | `CallStatusIndicator.tsx` | Labels d'état anglais hardcodés | HIGH |
| W-V2 | `PermissionRequest.tsx` | Toute l'UI permissions en anglais | HIGH |
| W-V3 | `CallControls.tsx` | aria-labels + titles en anglais | MEDIUM |
| W-V4 | `VideoStream.tsx` | "Disconnected", "Participant" en anglais | MEDIUM |
| W-V5 | `DraggableParticipantOverlay` + `VideoCallInterface` | Strings mineures | LOW/MEDIUM |
| W-MV1 | `MarkdownViewer.tsx` | 4 title attrs en français | MEDIUM |
| W-D1 | `AdminLayout.tsx` | Header sans dark mode | MEDIUM |
| W-NF1 | `not-found-page.tsx` | Props par défaut en français + pas de dark mode | MEDIUM |

### Nouveaux problèmes iOS

#### iOS-A2 · Accessibilité systémique — HIGH
Pattern : `.accessibilityLabel("Texte français hardcodé")` sans `String(localized:)` dans ~15 fichiers :
- `BubbleFooter`, `BubbleExpandableText`, `BubbleReactionsOverlay`, `BubbleStandardLayout`
- `MessageContextOverlay`, `StoryViewerView+Canvas`, `SharePickerView`
- `UniversalComposerBar+Recording`, `MessageOverlayMenu`, `EmojiPickerSheet`
- `ConversationView+Header`, `ContactsHubView`, `MentionSuggestionPanel`
- `StoryRepostEmbedCell`, `AchievementBadgeView`

#### iOS-C1 · Couleurs legacy (Passe B) — MEDIUM
25+ vues settings avec `accentColor = "08D9D6"` / `"4ECDC4"` (teal legacy) :
- `PrivacySettingsView`, `NotificationSettingsView`, `SettingsView`, `ChangePasswordView`, `ShareLinksView`
- `UserStatsView`, `LicensesView`, `PrivacyPolicyView`
- `StatusBarView`: `.tint("4ECDC4")` + `.breathingGlow("08D9D6")`
- `BubbleFooter`: icône translate `Color(hex:"4ECDC4")`

---

## ✅ COMPLÉTÉE — PR #350 (Passe A) + PR #354 (Passe B) mergées dans main
