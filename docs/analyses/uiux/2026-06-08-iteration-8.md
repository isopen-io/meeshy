# UI/UX Analysis — Iteration 8 (2026-06-08)

## Context

Builds on iterations 1–7. Most deferred items from iteration 7 were already resolved by
intermediate main-branch merges (PRs #384, iter-6 passe B). This iteration targets the
remaining confirmed gaps.

Pre-analysis verification:
- iOS Dynamic Type `ThreadView` / `ReplyThreadOverlay` ✅ already migrated
- iOS a11y `OnboardingStepViews` / `MagicLinkView` ✅ already done
- Web a11y icon-only buttons (8×) ✅ already done

---

## Web — i18n `language-select.tsx` — HIGH (silent failure)

**File:** `apps/web/components/ui/language-select.tsx`

### Issue 1 — Double-prefix key path (shows raw key in UI)
`useI18n('components')` extracts the `components` sub-object from `components.json`.
Key lookup should omit the namespace prefix.

| Bug | Line | Broken | Fixed |
|-----|------|--------|-------|
| Double prefix | 105 | `t('components.languageSelect.notFound')` | `t('languageSelect.notFound')` |

### Issue 2 — Hardcoded French placeholders
| Location | Hardcoded | Key |
|----------|-----------|-----|
| Default prop | `"Sélectionner une langue"` | `languageSelect.selectPlaceholder` |
| Search input | `"Rechercher une langue..."` | `languageSelect.searchPlaceholder` |

Both keys added to `components.json` × 4 languages.

---

## iOS — Dynamic Type `PostDetailView.swift` — MEDIUM (3 remaining)

3 instances of `.font(.system(size:))` remained:

| Line | Original | Migrated |
|------|----------|----------|
| ~690 | `.font(.system(size: isActive ? 12 : 10))` | `.font(isActive ? .caption : .caption2)` |
| ~770 | `.font(.system(size: 11))` | `.font(.caption2)` |
| ~1004 | `.font(.system(size: isActive ? 11 : 9))` | `.font(.caption2)` |

(All on flag/emoji glyph rows in language-flag strips.)

---

## ✅ COMPLÉTÉE — corrections mergées dans main (cf. branch-tracking.md, History iter-8)
Annoté le 2026-06-12 (iter-46w) : numéro de PR non retrouvé (placeholder « #XXX » d'origine corrigé). NE PAS re-auditer ces fichiers pour les mêmes findings.
