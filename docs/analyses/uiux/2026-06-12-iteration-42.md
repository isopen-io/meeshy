# UI/UX Analysis — Iteration 42 (2026-06-12)

## Scope
Web search page i18n + locale + a11y, chats v2 empty state; iOS Dynamic Type on Bookmarks/PostTranslationSheet/LinksHub + icon-button a11y; Android i18n on Settings/Contacts/MessageBubble/TypingIndicator + delivery-status a11y. First iteration covering the Android app (`apps/android`).

## Web Findings

### Critical: search page has zero i18n
- `app/search/page.tsx:7` — Suspense fallback hardcoded French `"Chargement..."`
- `app/search/SearchPageContent.tsx` — ~35 hardcoded French strings, no `useI18n` import at all:
  - Toasts: lines 128, 171, 243, 247, 251, 267, 270, 274, 311, 315, 319, 336, 339, 343
  - UI: hero title/subtitle (368, 371), search placeholder (389), submit button (403, 408), tab labels (423, 430, 437), results count w/ manual pluralization (447), loading (464), empty states (478-481, 624-627, 697-700), online badge (526), dropdown actions (541, 549, 557, 564), action buttons (587, 596), conversation type badges (652-659), unread count (666), untitled fallback (646), last activity (672), private/public badges (729, 734), member count (748), join button (758)
- No `search.json` namespace exists in `locales/{en,fr,es,pt}/`

### Locale hardcoding
- `app/search/SearchPageContent.tsx:672` — `toLocaleDateString('fr-FR', ...)` regardless of interface language

### Accessibility
- `SearchPageContent.tsx:528-533` — icon-only MoreVertical dropdown trigger without `aria-label`
- `SearchPageContent.tsx:386-393` — search input without `aria-label` (placeholder-only labeling)

### Minor
- `app/v2/(protected)/chats/page.tsx:238` — `EmptyConversation` hardcodes `Selectionnez une conversation` (also missing the é accent); key `conversationLayout.selectConversation` already exists in conversations.json for all 4 locales

### Deferred (noted, not fixed this iteration)
- `app/admin/debug.tsx` — ~15 hardcoded French strings (admin-only debug page, low traffic)
- `components/admin/agent/AgentArchetypesTab.tsx` — ~10 hardcoded strings + French label maps
- `components/translation/translation-monitor.tsx:233` — `Cache Hit` hardcoded English

## iOS Findings

### Critical: Dynamic Type frozen fonts (pattern continuation of iteration 32)
- `Features/Main/Views/BookmarksView.swift:93,96` — empty-state title `.font(.system(size: 17, weight: .semibold))` and subtitle `size: 14` frozen
- `Features/Main/Views/PostTranslationSheet.swift` — 16 hardcoded `.font(.system(size: N))` (sizes 10-20) across original section, available translations, request section; core Prisme Linguistique surface broken at accessibility text sizes
- `Features/Main/Views/LinksHubView.swift:84,88,91,173,180,183,198,205` — 8 hardcoded sizes (12-28) on the monetization hub

### Accessibility
- `PostTranslationSheet.swift:57-67` — xmark close button is icon-only without `.accessibilityLabel`
- `LinksHubView.swift:193-201` — plus.circle.fill create button icon-only without `.accessibilityLabel`
- `BookmarksView.swift:89-91` — decorative bookmark hero icon not `.accessibilityHidden(true)`

### Deferred (noted for future iterations)
- `TwoFactorSetupView.swift` — 11 hardcoded hero-icon sizes (50-80pt); hero icons intentionally fixed per iteration-32 precedent, text fonts to review
- `SettingsView.swift` (8), `NewConversationView.swift` (7), `DataExportView/DataStorageView` (8), `ChangePasswordView` (2), `ConversationView.swift:495,551` (2), `ProfileView.swift:65` — remaining fixed text fonts
- `IncomingCallView.swift:136,180,199` — hardcoded `.white` over call overlay (intentional over video/blur, verify contrast)
- `PostDetailView.swift` — missing `.textSelection(.enabled)` on post content; `ThemedConversationRow.swift:45-51` hex colors vs accentColor rule (needs design pass)
- `VoiceProfileWizardView`, `PrivacySettingsView`, `TrackingLinksView` — `Color(hex:)` bypassing palette

## Android Findings (app ~45% feature-complete)

### Major: zero i18n on stub feature screens
- `feature/settings/.../SettingsScreen.kt` — 25+ hardcoded strings ("Settings", "Back", "Profile", "Language", "Display language", "Regional language", "Notifications", "Push notifications", "Privacy & Security", "Two-factor auth", "Active sessions", "Blocked users", "Data", "Export my data", "Clear media cache", "Storage used", "About", "Version 1.0.0", "Terms of Service", "Privacy Policy", "Danger zone", "Log out", "Delete account"); no res/ directory in module
- `feature/contacts/.../ContactsScreen.kt:38,41,57,66` — "Contacts", "Back", raw enum `.name` as tab labels, "Coming soon"; no res/ directory in module

### Major: sdk-ui MessageBubble hardcoded user-facing strings
- `sdk-ui/.../MessageBubble.kt:93` — "Message deleted" text
- `sdk-ui/.../MessageBubble.kt:125` — Translate icon `contentDescription = "Translated"`
- `sdk-ui/.../MessageBubble.kt:132` — "edited" label
- `sdk-ui/.../MessageBubble.kt:255-279` — delivery status contentDescriptions "Pending"/"Sent"/"Delivered"/"Read"/"Failed" hardcoded (a11y + i18n)
- `sdk-ui/.../BubbleContentBuilder.kt:38` — reply preview bakes "Message deleted" string into the value model (untranslatable; belongs in the rendering layer)

### Major: TypingIndicator hardcoded English
- `feature/chat/.../ChatScreen.kt:355-361` — "is typing..." / "are typing..." / "people are typing..." string-concatenated, untranslatable

### Confirmed clean
- Conversation accent color rule respected (accentHex from domain model)
- No hardcoded hex colors in feature composables (all via MeeshyTheme tokens)
- Touch targets ≥48dp via Material3 defaults
- auth/chat/conversations/feed/notifications/profile feature modules have en+fr strings.xml

### Deferred
- `SettingsScreen.kt` raw dp spacing vs MeeshySpacing tokens (design-system purity; screen is a stub pending real Settings build-out)
- `ChatScreen.kt:329` emoji `fontSize = 22.sp` (quick-reaction button, intentional fixed emoji size)
- es/pt strings.xml missing across all Android modules (only en+fr today — tracked as platform-wide gap)

## Anti-repetition check
Verified against previous iterations (1-40): admin i18n (32), settings/me pages (24), bubble textSelection + call notice fonts (32), contacts empty states (19) — none of the above findings overlap. Iteration 40 (PR #575, merged in parallel) covered the Android bubble and ChatScreen a11y/i18n — overlapping bubble work was reconciled at merge time (replyToDeleted naming, identical string keys). This iteration additionally covers `apps/android` settings/contacts, `app/search/`, `BookmarksView`, `PostTranslationSheet`, `LinksHubView`.

## Merge reconciliation (2026-06-12)
Two parallel routine iterations landed on main during this iteration's development:
- Iter-40 (PR #575): same sdk-ui bubble i18n converged — resolved by adopting main's `replyToDeleted` naming, `MeeshyTheme.tokens.info` tint, and dropping duplicate tests
- Iter-41 (PR #577): Settings/Contacts Android i18n + iOS links-surface color tokens — resolved by adopting main's Settings wiring/keys verbatim (this iteration's distinct contribution there reduces to the Contacts tab labels), and combining main's color tokens with this iteration's semantic fonts in LinksHubView
