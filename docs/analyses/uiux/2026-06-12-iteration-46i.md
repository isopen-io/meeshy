# UI/UX Analysis — Iteration 46i (2026-06-12)

## Scope
**iOS exclusivement** (suffixe `i` — Web et Android sont couverts par d'autres agents).
Solde la « passe dédiée » annoncée par le carry-over 45i : **éradication finale des
reliquats de l'ancienne palette pré-charte (`08D9D6`/`FF2E63`/`4ECDC4`)** sur toutes les
surfaces restantes, plus les 11 polices figées de `FriendRequestListView` (différé 45i),
plus l'épuration des deux fichiers morts dont la suppression exigeait une édition pbxproj
(`SampleData.swift`, différé explicite ; `MessageComposer.swift`, découvert cette passe).

## iOS Findings

### Critical : Épuration — 3 îlots de code mort (zéro usage vérifié)
L'audit symbole par symbole (grep app + tests + extensions, hors commentaires) prouve :
- `Components/MessageComposer.swift` (218 lignes) — `struct MessageComposer` n'est
  référencé NULLE PART : remplacé par `UniversalComposerBar` (le fichier porte 4 hexes
  ancienne palette : gradient focus `08D9D6`/`FF2E63`, bouton envoi `FF2E63`×2)
- `Models/SampleData.swift` (~320 lignes) — code mort intégral confirmé en 45i (seule
  référence : un commentaire dans `Models.swift`), 6 × `4ECDC4` + palette arc-en-ciel
- `Views/RootViewComponents.swift:697-1113` — îlot mort complet : `ThemedFeedComposer`
  (remplacé par le placeholder tap-to-open de `ThemedFeedOverlay` + `PostComposer`),
  `ThemedFeedCard`, `FeedActionButton`, et les 10 wrappers « Legacy Support »
  (`FeedOverlay`, `ColorfulFeedOverlay`, `ColorfulFeedComposer`, `ColorfulFeedCard`,
  `ColorfulFeedAction`, `ColorfulQuickActionButton`, `QuickActionButton`, `FeedComposer`,
  `LegacyFeedCard`, `FeedAction`) — ces trois niveaux ne se référencent qu'entre eux ;
  seuls `ThemedFloatingButton`, `ThemedActionButton`, `ThemedFeedOverlay` sont vivants
  (`RootView:253,1279,1283`). La suppression élimine d'un coup 8 occurrences trio +
  les couleurs ladder du bloc (45B7D1, F8B500, 9B59B6) sans arbitrage charte nécessaire.

Suppression des 2 fichiers = 8 entrées pbxproj à retirer (4 par fichier : PBXBuildFile,
PBXFileReference, group child, Sources phase). La compilation est validée par le CI
`ios-tests.yml` (pas de build local possible dans le conteneur).

### Critical : Ancienne palette sur surfaces vivantes (passe finale)
Mappings : convention iter-42b/44b/45i — cyan→`indigo400`, record/mic→`errorStrong`,
fallback d'accent→`brandPrimaryHex`, spinners→`brandPrimary`, coral adjacent→`error*`.
- `RootViewComponents` (post-épuration) — `:421` orbe ambiante `4ECDC4` → `indigo400`
  (règle charte : orbes = famille indigo) ; `:455-460` avatar self feed
  `FF6B6B`/`4ECDC4` → paire signature `brandPrimaryHex`/`brandDeepHex` ; `:560` spinner
  → `.tint(MeeshyColors.brandPrimary)`
- `FeedView` — `:596,599,1137` `theme.surfaceGradient/border(tint: "4ECDC4")` →
  `indigo400Hex` ; `:989` avatar self → paire brand ; `:1057` `UploadProgressBar` accent
  → `brandPrimaryHex` ; `:1070` icône photo toolbar `4ECDC4` → `indigo400` ; `:1100`
  icône micro `FF2E63` → `errorStrong` (précédent 44b record) ; `:1161`
  `LocationPickerView(accentColor:)` → `brandPrimaryHex`
- `FeedView+Attachments` — mêmes patterns : `:32,985,994` accents préparation →
  `brandPrimaryHex` ; `:309,870` spinners → `brandPrimary` ; `:555` avatar self → paire
  brand ; `:654` progress bar → `brandPrimaryHex` ; `:666` photo → `indigo400` ; `:691`
  micro → `errorStrong` ; `:758` LocationPicker → `brandPrimaryHex`
- `WidgetPreviewView` — `:157-164` carte résumé : gradient non-lus `[FF2E63, A855F7]` →
  `[errorStrong, error]` + ombre `errorStrong` (sémantique non-lus = rouge, cf.
  `unreadBadgeBackground`) ; gradient « tout lu » `[08D9D6, 4ECDC4]` →
  `[indigo500, indigo700]` + ombre `brandPrimary` (état calme = signature brand) ;
  `:291` action « Nouveau » `[08D9D6, 4ECDC4]` → `[indigo400, indigo500]`
  (= `brandGradientLight`) ; `:302` « Partager » `[08D9D6, 2ECC71]` →
  `[success, successDeep]` (tokens gradient succès exacts, identité verte conservée) ;
  `:321` « Réglages » `[FF6B6B, FF2E63]` → `[errorSoft, errorStrong]` (stops gradient
  erreur, identité rouge conservée) ; `:410` carte « Partage » `08D9D6` →
  `shareAccentHex` (token dédié aux surfaces share-link)
- `AboutView` — section Liens `4ECDC4` ×4 + tint → `indigo400Hex` (distinct des autres
  sections : 45B7D1 ladder/9B59B6/F8B500, différées avec l'arbitrage charte)
- `AttachmentPreparationService` — défauts `accentColor = "4ECDC4"` (image ×2) →
  `brandPrimaryHex`, `= "FF6B6B"` (vidéo) → `errorHex` ; `:201` ladder
  `(isVideo ? "FF6B6B" : "4ECDC4")` → `(errorHex : brandPrimaryHex)` ; ajout
  `import MeeshyUI`. Tests existants intacts (ils passent l'accent explicitement et
  vérifient le pass-through)
- `ConversationAnimatedBackground` — défauts `accentHex = "FF2E63"`,
  `secondaryHex = "08D9D6"` → `brandPrimaryHex`/`brandDeepHex` (tous les appelants réels
  passent l'accent de conversation — pur fallback) ; défaut adjacent
  `groupEndHex ?? "9933CC"` → `indigo600Hex` (mapping purple→indigo600)
- `ConversationInfoSheet:885` — bouton « Partager » `4ECDC4` → `brandPrimaryHex` ; bouton
  frère « Quitter » `FF6B6B` → `errorHex` (sémantique destructive, même HStack)
- `MemberManagementSection:223` — badge rôle modérateur `4ECDC4` → `indigo400`
  (admin reste `3B82F6` pinnedBlue — différenciation conservée)
- `BlockedUsersView:161` — spinner → `.tint(MeeshyColors.brandPrimary)`
- `UserStatsView:78` — stat « Conversations » `4ECDC4` → `brandPrimaryHex` (distinct des
  voisines FF6B6B/9B59B6/3498DB/F8B500, ladder différé)
- `MediaDownloadSettingsView:74` — rangée « Images » `4ECDC4` → `brandPrimaryHex`
  (distinct de 9B59B6/F39C12/E74C3C, ladder différé)
- `MeeshyWidgets.swift:237,730` — données sample widget `4ECDC4` → littéral `"818CF8"`
  (indigo400 ; la target widget n'importe pas MeeshyUI — littéral cohérent avec les
  voisins `"6366F1"`/`"34D399"` déjà littéraux)

### Critical : Dynamic Type figé — FriendRequestListView (différé 45i)
10 polices texte/icône migrées en sémantique (mapping conventionnel iter-32/42 :
16→`.callout`, 17→`.headline`, 18→`.headline`, 14-15→`.subheadline`, 13→`.footnote`,
12→`.caption`, 11→`.caption2`, poids conservés via `.weight()`, design `.rounded`
conservé via `Font.system(_:design:)`) ; héros 48pt `person.2.slash` conservé figé
(précédent iter-32).

### Prisme Linguistique : formateur de date verrouillé fr_FR (bonus découvert)
`FriendRequestListView.relativeTime:183-186` — le fallback > 7 jours formate la date avec
`Locale(identifier: "fr_FR")` en dur : un utilisateur EN/ES voit « 12 juin » au lieu de
« Jun 12 ». → `date.formatted(.dateTime.day().month(.abbreviated))` (locale courante,
zéro clé nécessaire). Au passage `sender?.name ?? "Inconnu"` (`:101`) → clé existante
`common.unknown` (déjà utilisée par `RequestsTab:103,198` — aucune clé à ajouter).

### Conforme (vérifié, PAS des violations)
- `StoryViewerView+Content:180` — wash filtre « cool » `08D9D6@0.1` softLight : couleur
  ARTISTIQUE du filtre stories (froid = cyan, comme « vintage »=ambre, « warm »=corail).
  Précédent : washes AudioPostComposer (décision design 44b). LAISSÉ — retirer des
  reliquats du carry-over
- Icônes ladder toolbar feed restantes (F8B500 emoji, 9B59B6 fichier) + `AboutView`
  `accentColor = "45B7D1"` + stats/media ladder voisines — catégorielles, arbitrage
  charte toujours différé (seuls les membres du trio ancien y ont été remplacés)
- Tests (`ThumbHashTests`, `StoryViewModelTests`, `WidgetDataManagerTests`,
  `StatusViewModelTests`, `AttachmentPreparationServiceTests`) — hexes = valeurs de
  fixture arbitraires pass-through, non rendues : laissés

## Anti-repetition check
Vérifié contre 44b (composer Universal/AudioPost — fichiers disjoints), 45 (FeedCommentsSheet/
ChangePasswordView), 45i (ConversationListHelpers/ConversationHelperViews/ThreadView/
StoryTray/StoryViewer/FriendRequestList HEX UNIQUEMENT — les polices y étaient explicitement
différées ; cette passe les traite). Aucun fichier de cette itération n'a reçu deux fois le
même traitement. Le grep trio post-45i listait exactement les fichiers traités ici.

## Différés (pour itérations suivantes)
- Ladder « arc-en-ciel » catégoriel (FF9F43/45B7D1/2ECC71/F8B500/9B59B6/E74C3C/F39C12/
  3498DB) — toolbar feed, AboutView, UserStatsView, MediaDownloadSettingsView — arbitrage
  charte global (une décision, une passe)
- Grandes surfaces polices : ConversationInfoSheet (52), ConversationDashboardView (43),
  TwoFactorSetupView (42, héros intentionnels), CallView (34), InviteFriendsSheet (33),
  ProfileView/GlobalSearchView (32), SettingsView (27), NewConversationView (16),
  DataExportView (16), DataStorageView (11)
- Arbitrage familles de clés `time.*` (FeedPostCard) vs `time.short.*` (ShortRelativeTime) ;
  `FriendRequestListView.relativeTime` garde ses clés `friends.requests.time.*` (wording
  long « Il y a Xmin » distinct du compact)
- `VoiceProfileWizardView`/`TrackingLinksView` `Color(hex:)` hors palette ;
  `IncomingCallView` `.white` contraste ; `AvatarContextMenuItem` → LocalizedStringKey ;
  `ThemedConversationRow` theme-aware (leaf-view) ; washes AudioPostComposer (design)
- `BubbleStandardLayout:340` — « Inconnu » brut dans une chaîne a11y (à localiser avec la
  prochaine passe bulle)

## Status : ⏳ en cours — voir plan 2026-06-12-plan-iteration-46i
