# UI/UX Analysis — Iteration 46i (2026-06-12)

## Scope
**iOS exclusivement** (suffixe `i` — Web et Android sont couverts par d'autres agents).
Solde la « passe dédiée » annoncée au différé de 45i : **éradication finale des reliquats
de l'ancienne palette (`08D9D6`/`FF2E63`/`4ECDC4`) sur toutes les surfaces restantes**,
plus deux différés de longue date : suppression des fichiers morts `SampleData.swift` /
`MessageComposer.swift` (pbxproj inclus, CI `ios-tests.yml` comme filet de build) et la
passe Dynamic Type de `FriendRequestListView`. Épuration majeure découverte pendant
l'audit : la moitié de `RootViewComponents.swift` est du code mort.

## iOS Findings

### Critical : Épuration — RootViewComponents, 14 structs mortes (~370 lignes)
Audit d'usage symbole par symbole (grep app + tests + extensions, hors commentaires) :
- `ThemedFloatingButton` — zéro référence
- `ThemedFeedComposer` (698–912) — référencée uniquement par les wrappers legacy morts ;
  contient 6 reliquats palette (`4ECDC4` ×5, `F8B500`/`FF9500`/`45B7D1` ladder) et un
  `AvatarContextMenuItem(label: "Mon profil")` français en dur
- `ThemedFeedCard` + `FeedActionButton` — référencées uniquement par les wrappers legacy
  (contiennent `4ECDC4`/`FF6B6B`/`9B59B6`)
- Section « Legacy Support » complète (1045–1113) : `FeedOverlay`, `ColorfulFeedOverlay`,
  `ColorfulFeedComposer`, `ColorfulFeedCard`, `ColorfulFeedAction`,
  `ColorfulQuickActionButton`, `QuickActionButton`, `FeedComposer`, `LegacyFeedCard`,
  `FeedAction` — zéro usage
Reste vivant : `ThemedActionButton` (2 refs), `ThemedFeedOverlay` (RootView:253).
Comme en 45i : on supprime le code mort au lieu de le re-peindre. Le commentaire d'index
de `RootView.swift:8-9` est mis à jour.

### Critical : Épuration — 2 fichiers morts entiers (pbxproj)
- `Components/MessageComposer.swift` (218 lignes, zéro référence) — supersédé par
  `UniversalComposerBar` ; contient 4 reliquats (`08D9D6` ×2, `FF2E63` ×2)
- `Models/SampleData.swift` (390 lignes, zéro référence hors un commentaire dans
  `Models.swift` — différé depuis 45i) ; contient 6 × `4ECDC4`
Chaque suppression retire 4 entrées `project.pbxproj` (PBXBuildFile, PBXFileReference,
children du groupe, Sources). Pas de build local possible (conteneur Linux) — la
compilation est validée par `ios-tests.yml` sur la PR.

### Critical : Ancienne palette sur surfaces vivantes (24 occurrences)
Mappings choisis sur précédents existants (44b/45i) et tokens `MeeshyColors` :

| Site | Avant | Après | Justification |
|---|---|---|---|
| RootViewComponents:421 orbe ambiante | `4ECDC4` | `indigo400` | règle charte « ambient orbs = famille indigo » |
| RootViewComponents:459, FeedView:989, FeedView+Attachments:555 avatar utilisateur | `accentColor: "FF6B6B", secondaryColor: "4ECDC4"` | args supprimés | `MeeshyAvatar` retombe sur `DynamicColorGenerator.colorForName(name)` (règle charte sender names) |
| RootViewComponents:560, FeedView+Attachments:309,870 spinners | `.tint(4ECDC4)` | `.tint(MeeshyColors.brandPrimary)` | précédent 45i FriendRequestListView |
| FeedView:596,599,1137 surfaces composer feed | `tint: "4ECDC4"` | `tint: MeeshyColors.brandPrimaryHex` | précédent ChangePasswordView/ShareLinksView |
| FeedView:1057, FeedView+Attachments:654 UploadProgressBar | `"4ECDC4"` | `MeeshyColors.brandPrimaryHex` | accent brand |
| FeedView:1070, FeedView+Attachments:666 icône photo toolbar | `4ECDC4` | `MeeshyColors.info` | catégoriel bleu média, cohérent MediaDownloadSettings ; distinct de caméra (`error`), emoji (`F8B500` ladder), doc (`9B59B6` ladder), position (`success`) |
| FeedView:1100, FeedView+Attachments:691 icône micro toolbar | `FF2E63` | `MeeshyColors.brandPrimary` | la voix est LA feature signature Meeshy ; `error` est déjà pris par caméra dans la même barre |
| FeedView:1161, FeedView+Attachments:758 LocationPickerView | `"4ECDC4"` | `MeeshyColors.brandPrimaryHex` | accent brand |
| AttachmentPreparationService:132,146,201 défauts placeholder | `"4ECDC4"` / `"FF6B6B"` vidéo | `brandPrimaryHex` / `brandDeepHex` | paire image/vidéo dans la famille brand (placeholder remplacé par ThumbHash ensuite) ; call-sites FeedView+Attachments:32,985,994 passent désormais les défauts |
| WidgetPreviewView:157-164 héros unread/all-read | `[FF2E63,A855F7]` / `[08D9D6,4ECDC4]` | `[error,errorStrong]` / `[success,successDeep]` | sémantique attention/repos, tokens tonals exacts |
| WidgetPreviewView:291 « Nouveau » | `[08D9D6,4ECDC4]` | `[indigo500,indigo700]` | action primaire = signature brand |
| WidgetPreviewView:302 « Partager » | `[08D9D6,2ECC71]` | `[success,successDeep]` | conserve la dominante verte d'origine |
| WidgetPreviewView:321 « Réglages » | `[FF6B6B,FF2E63]` | `[error,errorStrong]` | conserve la dominante rouge d'origine |
| WidgetPreviewView:410 carte « Partage » | `"08D9D6"` | `MeeshyColors.shareAccentHex` | token feature dédié (précédent ShareLinksView) |
| AboutView:164-171 section Liens ×5 | `"4ECDC4"` | `MeeshyColors.infoHex` | liens = bleu conventionnel, distinct des sections sœurs |
| UserStatsView:78 stat Conversations | `"4ECDC4"` | `MeeshyColors.brandPrimaryHex` | entité cœur = brand, distinct des 5 stats sœurs |
| MediaDownloadSettingsView:74 rangée Images | `"4ECDC4"` | `MeeshyColors.infoHex` | aligné sur le mapping photo→info de la toolbar |
| MemberManagementSection:223 badge Modérateur | `4ECDC4` | `MeeshyColors.success` | vert confiance ; distinct de creator (`F8B500`) et admin (`3B82F6`) |
| BlockedUsersView:161 swipe Débloquer | `4ECDC4` | `MeeshyColors.success` | action constructive |
| ConversationInfoSheet:885 action Partager | `"4ECDC4"` | `MeeshyColors.shareAccentHex` | surface share-link |
| ConversationAnimatedBackground:42-43 défauts init | `FF2E63`/`08D9D6` | `brandPrimaryHex`/`brandDeepHex` | l'appelant vivant passe l'accent conversation ; défauts = previews |
| MeeshyWidgets:237,730 données d'exemple widget | `"4ECDC4"` | `"818CF8"` | littéral indigo400, idiome des voisins (`"6366F1"`, `"34D399"`) |

### Critical : Dynamic Type figé — FriendRequestListView (différé 44b/45i)
11 occurrences `.font(.system(size:))` ; 10 migrées via le mapping iter-32/42
(16→`.callout`, 17 rounded→`.system(.body, design: .rounded, weight: .semibold)`,
18→`.headline`, 14-15→`.subheadline`, 13→`.footnote`, 12→`.caption`, 11→`.caption2`),
l'icône d'état vide 48pt est un héros intentionnel conservé (précédent ChangePasswordView).

### i18n & accessibilité (bonus, surfaces touchées)
- `WidgetPreviewView:291-321` — 4 labels français bruts (« Nouveau », « Partager »,
  « Post », « Réglages ») → `String(localized: "widget.preview.action.*")`, 5 locales
- `FeedView+Attachments:559` — fallback `"Moi"` en dur alors que le site jumeau
  FeedView:994 utilise déjà `feed.composer.me` → réutilise la clé (+ alignement de la
  police du même élément sur le jumeau : `.subheadline.weight(.semibold)`)
- `FeedView+Attachments` toolbar — les 6 boutons n'avaient AUCUN `.accessibilityLabel`
  alors que la toolbar jumelle de FeedView les a → ajout des 6 labels (mêmes clés)
- `FriendRequestListView` — boutons accepter/refuser (icônes seules) sans label
  VoiceOver → `friends.requests.accept` / `friends.requests.decline`, 5 locales

### Conforme (vérifié, PAS des violations)
- `StoryViewerView+Content:180` — `Color(hex: "08D9D6").opacity(0.1)` est la constante de
  color-grading du **filtre photo « cool »** des stories (comme `F8B500` vintage, `FF6B6B`
  warm) : c'est une teinte d'étalonnage, pas un accent UI. Exception documentée — à ne
  plus re-flagger (même statut que les washes AudioPostComposer 44b)
- Toolbar feed : emoji `F8B500`, doc `9B59B6` et caméra `MeeshyColors.error` — ladder
  catégoriel différé intentionnel (44b), hors trio ancien
- `ConversationAnimatedBackground` `groupEndHex ?? "9933CC"` — hors trio, écrasé par la
  palette conversation chez l'appelant vivant (laissé, noté)
- `MeeshyTests` (StatusViewModel/StoryViewModel/WidgetDataManager/AttachmentPreparation/
  ThumbHash) — hexes = fixtures arbitraires de tests, non rendues

## Anti-repetition check
Vérifié contre : 44b (composer UniversalComposerBar/AudioPostComposer — non retouchés),
45i (ConversationListHelpers/ConversationHelperViews/ThreadView/StoryTrayView/
StoryViewerView/FriendRequestListView **hex uniquement**/StoryViewModel — cette passe
traite les polices de FriendRequestListView, jamais faites, et des fichiers jamais
traités). Aucun fichier de cette passe n'a reçu de passe palette antérieure. Greps de
clôture : zéro `08D9D6|FF2E63|4ECDC4` restant dans `apps/ios` hors `DynamicColorGenerator`
(algorithme légitime), filtre « cool » documenté et fixtures de tests.

## Différés (pour itérations suivantes)
- `PostDetailView` — `.textSelection(.enabled)` + 21 hex
- Grandes surfaces polices : ConversationInfoSheet (52), ConversationDashboardView (43),
  TwoFactorSetupView (42, héros intentionnels), CallView (34), InviteFriendsSheet (33),
  ProfileView/GlobalSearchView (32), SettingsView (27), NewConversationView (16),
  DataExportView (16), DataStorageView (11)
- Ladder pièces jointes/catégoriel « arc-en-ciel » (`FF9F43`/`45B7D1`/`2ECC71`/`F8B500`/
  `9B59B6`/`E74C3C`/`FF6B6B`/`E91E63`/`3498DB`/`A855F7`) — à arbitrer charte (UserStatsView,
  AboutView sections, MediaDownloadSettingsView, toolbars feed, WidgetPreviewView « Post »)
- `VoiceProfileWizardView`/`TrackingLinksView` — `Color(hex:)` hors palette
- `IncomingCallView` `.white` contraste ; `AvatarContextMenuItem` → LocalizedStringKey ;
  `ThemedConversationRow` theme-aware (leaf-view)
- `FeedItem`/`ConversationTag` dans `Models.swift` — possiblement orphelins après cette
  passe (audit d'usage à faire avant suppression)
- Arbitrage `time.*` (FeedPostCard) vs `time.short.*` (ShortRelativeTime)

## Status : corrections implémentées — voir plan 2026-06-12-plan-iteration-46i
