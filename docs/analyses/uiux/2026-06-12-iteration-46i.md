# UI/UX Analysis — Iteration 46i (2026-06-12)

## Scope
**iOS exclusivement** (suffixe `i` — Web et Android sont couverts par d'autres agents).
Solde les deux différés majeurs listés au carry-over post-45i : la **passe « reliquats
ancienne palette »** (`08D9D6`/`FF2E63`/`4ECDC4` hors composer, sur les fichiers
RootViewComponents, FeedView, FeedView+Attachments, WidgetPreviewView, AboutView,
MessageComposer, AttachmentPreparationService, ConversationAnimatedBackground + divers ×1)
et la **passe Dynamic Type de FriendRequestListView** (11 polices). S'y ajoutent la
suppression du fichier mort `SampleData.swift` (différé 45i, pbxproj désormais validé par
le CI `ios-tests.yml` qui buildl'app sur toute PR `apps/ios/**`) et trois bonus i18n
découverts pendant l'audit.

## iOS Findings

### Critical : Épuration — code mort intégral (zéro usage vérifié symbole par symbole)
- `MessageComposer.swift` (Components) — **fichier entier mort**. Zéro instanciation :
  l'app utilise `UniversalComposerBar` (le seul co-usage, `TextAnalyzer`, est vivant via
  UniversalComposerBar:225). Supprime d'un coup 4 hex ancienne palette
  (`08D9D6`×2, `FF2E63`×2 + `FF6B6B` gradient). Exige 4 entrées pbxproj.
- `SampleData.swift` (Models) — différé 45i, toujours zéro référence (seul un commentaire
  dans `Models.swift:12`). 6×`4ECDC4`. Exige 4 entrées pbxproj.
- `RootViewComponents.swift` — `ThemedFeedCard` (jamais instancié hors wrappers morts,
  contient du FR codé en dur « Copier le texte »/« Partager » et un menu copie/partage
  jamais rendu), `FeedActionButton` (référencé uniquement par ThemedFeedCard), et les
  **10 wrappers « Legacy Support »** (`FeedOverlay`, `ColorfulFeedOverlay`,
  `ColorfulFeedComposer`, `ColorfulFeedCard`, `ColorfulFeedAction`,
  `ColorfulQuickActionButton`, `QuickActionButton`, `FeedComposer`, `LegacyFeedCard`,
  `FeedAction`) — seules références : deux commentaires (RootView.swift:9,
  Router.swift:122) à mettre à jour. Élimine `4ECDC4`×2 + `F8B500`/`FF6B6B`/`9B59B6` morts.

### Critical : Ancienne palette en UI vivante
Rappel charte : `4ECDC4`/`FF2E63`/`08D9D6` ne sont légitimes QUE dans l'algorithme
`DynamicColorGenerator` et les ladders catégorielles arbitrées (`AttachmentKind.hexTintColor`).

**RootViewComponents (ThemedFeedOverlay/ThemedFeedComposer — feed iPhone overlay, vivant via RootView:262)**
- :421 orbe ambiante `4ECDC4` → `MeeshyColors.indigo400` (règle charte : orbes = famille indigo ;
  l'orbe jumelle :428 est déjà `MeeshyColors.error`)
- :456-459, :727-744 `MeeshyAvatar(accentColor: "FF6B6B", secondaryColor: "4ECDC4")` sur
  l'avatar du composer → **suppression des arguments** : `MeeshyAvatar` retombe sur le
  fallback charte `DynamicColorGenerator.colorForName(name)` (couleur déterministe de
  l'utilisateur courant, cohérente avec toutes les autres surfaces avatar)
- :560 spinner `.tint(Color(hex: "4ECDC4"))` → `MeeshyColors.brandPrimary` (précédent 45i)
- :775 bordure focus input `4ECDC4@0.5` → `MeeshyColors.brandPrimary.opacity(0.5)`
- :803-810 bouton (+) du composer : état fermé `[4ECDC4, 45B7D1]` → stops du brand gradient
  `[indigo500, indigo700]` ; état ouvert (mic) `[F8B500, FF9500]` → `[warning, warningDeep]`
  — **nouveau token SDK `MeeshyColors.warningDeep` (amber-500 `F59E0B`)**, symétrique des
  tonal variants existants `successDeep`/`errorStrong` ; shadows miroirs
- :852/:855 `surfaceGradient(tint:)`/`border(tint:)` `"4ECDC4"` → `MeeshyColors.brandPrimaryHex`

**FeedView (feed iPad, vivant via iPadRootView:221)**
- :604/:607/:1176 tints surface/bordure `"4ECDC4"` → `brandPrimaryHex`
- :1028 avatar composer → suppression args (fallback charte, idem ci-dessus)
- :1096 `UploadProgressBar(accentColor: "4ECDC4")` → `brandPrimaryHex`
- :1109-1139 toolbar du composer — **déjà à moitié migrée** (camera→`error`,
  location→`success` posés par une passe antérieure) ; on finit selon la table de
  migration officielle (apps/ios/CLAUDE.md) : photo `4ECDC4`→`indigo400`,
  emoji `F8B500`→`warning`, fichier `9B59B6`→`indigo600`, micro `FF2E63`→`brandPrimary`
- :1200 `LocationPickerView(accentColor: "4ECDC4")` → `brandPrimaryHex` (hors contexte
  conversation, pas d'accent dynamique disponible)

**FeedView+Attachments (extension vivante de FeedView)**
- :29-44, :985-1001 `prepareImage/prepareVideo/preparePhotosPickerItem(accentColor: "4ECDC4"/"FF6B6B"/"")`
  → suppression des arguments : les défauts du service deviennent la source de vérité
- :309/:870 spinners → `MeeshyColors.brandPrimary`
- :555 avatar composer → suppression args ; **bonus i18n** : fallback `"Moi"` brut (:558)
  → clé existante `feed.composer.me` (déjà utilisée par la rangée jumelle de FeedView:1032)
- :654 `UploadProgressBar` → `brandPrimaryHex` ; :666/:691 toolbar (photo/emoji/fichier/micro)
  → même mapping que FeedView ; :758 `LocationPickerView` → `brandPrimaryHex`
- **Bonus a11y** : la toolbar du sheet composer n'avait AUCUN `accessibilityLabel`
  (contrairement à sa jumelle de FeedView) → 6 labels ajoutés, mêmes clés que FeedView

**AttachmentPreparationService**
- :132/:146/:160/:200-201 défauts raw `"4ECDC4"`/`"FF6B6B"` → `AttachmentKind.image.hexTintColor`
  / `AttachmentKind.video.hexTintColor` : valeurs identiques (zéro changement visuel),
  mais la ladder catégorielle est désormais référencée depuis sa source de vérité SDK
  unique — l'arbitrage charte de la ladder (différé) ne touchera plus qu'un seul fichier

**WidgetPreviewView (dashboard widgets, vivant via ConversationListView)**
- :155-164 carte résumé non-lus : l'original `[FF2E63, A855F7]` (non-lus) était le brand
  pink→purple pré-charte, `[08D9D6, 4ECDC4]` (tout lu) le calme cyan→teal — mapping fidèle
  à l'intention : `[indigo500, indigo700]` (brand signature) / `[success, successDeep]`
  (« Tout est lu » + checkmark = succès) ; shadows miroirs
- :291 quick action « Nouveau » `[08D9D6, 4ECDC4]` → signature brand `[indigo500, indigo700]`
- :302 « Partager » `[08D9D6, 2ECC71]` → famille share `[shareAccent(indigo400), indigo600]`
- :321 « Réglages » `[FF6B6B, FF2E63]` → neutre `[neutral500, neutral600]` (un engrenage
  rouge signale à tort un état d'erreur)
- :410 `linkTypeCard` « Partage » `"08D9D6"` → `MeeshyColors.shareAccentHex` (règle feature
  accents : surfaces liens = échelle indigo)
- **Bonus i18n** : les 4 labels des quick actions (« Nouveau », « Partager », « Post »,
  « Réglages ») étaient du FR brut dans un fichier par ailleurs entièrement localisé →
  clés `widget.preview.action.*`, 5 locales

**ConversationAnimatedBackground**
- :42-43 défauts `accentHex: "FF2E63"`, `secondaryHex: "08D9D6"` — jamais utilisés en
  production (tous les call sites passent la palette de conversation) → `brandPrimaryHex`/
  `indigo400Hex` par parité charte

**Divers ×1**
- `ConversationInfoSheet:885` action « Partager » `"4ECDC4"` → `shareAccentHex` ; au passage
  :894 « Quitter » `"FF6B6B"` → `MeeshyColors.errorHex` (action destructive = sémantique error)
- `MemberManagementSection:223` badge rôle modérateur `4ECDC4` → `MeeshyColors.success`
  (distinct du badge admin `3B82F6`, sémantique « vérifié/confiance »)
- `BlockedUsersView:161` swipe « Débloquer » `.tint(4ECDC4)` → `MeeshyColors.success`
  (action positive de restauration)
- `UserStatsView:78` carte « Conversations » `"4ECDC4"` → `brandPrimaryHex` (la conversation
  est l'objet cœur de la marque ; le reste de la ladder stats — FF6B6B/9B59B6/3498DB/F8B500/
  E91E63 — reste différé arbitrage)
- `MediaDownloadSettingsView:74` icône Images `"4ECDC4"` → `AttachmentKind.image.hexTintColor`
  (+ :78 Audio `"9B59B6"` → `AttachmentKind.audio.hexTintColor`) — valeurs identiques,
  source de vérité SDK
- `MeeshyWidgets.swift:237/:730` données d'exemple de la galerie widgets `"4ECDC4"` →
  `"818CF8"` (indigo400) ; au passage :728 Sarah `"F39C12"` → `"FBBF24"` (token warning)
  — les autres exemples utilisent déjà `6366F1`/`34D399`

### Critical : Dynamic Type figé — FriendRequestListView (différé 44b/45i)
11 occurrences `.font(.system(size: N))`. Mapping conventionnel iter-32/42/45i :
16→`.callout`, 17→`.headline` (taille système exacte), 18→`.headline`, 14-15→`.subheadline`,
13→`.footnote`, 12→`.caption`, 11→`.caption2`. L'icône héros 48pt de l'empty state est
conservée (précédent TwoFactorSetupView : héros intentionnels).

### i18n : locale codée en dur + FR brut (FriendRequestListView)
- `relativeTime(from:)` (:177-187) duplique le formateur relatif avec
  `Locale(identifier: "fr_FR")` codée en dur et 4 clés `friends.requests.time.*`
  **absentes du catalogue** (donc français pour toutes les locales). Remplacé par
  `ShortRelativeTime.label(for:)` — le helper unifié en iter-45, localisé 5 locales —
  et suppression du helper dupliqué (épuration, même mouvement que FeedCommentsSheet en 45)
- :101 `sender?.name ?? "Inconnu"` → clé `common.unknown` (précédent RequestsTab:103).
  **La clé était absente du catalogue** (les call sites existants — RequestsTab, ThreadView —
  retombaient sur leur defaultValue, FR ou EN selon le fichier !) → ajout au catalogue,
  5 locales, ce qui corrige aussi ces deux écrans

### Conforme (vérifié, PAS des violations)
- `StoryViewerView+Content:180` — `08D9D6@0.1` est le wash du **filtre photo « cool »**
  (effet de refroidissement), pas un accent UI ; même statut que le wash vintage `F8B500`
  juste au-dessus et que les palettes de filtres SDK (`StoryFilterPicker.vivid`).
  Intentionnel, retiré du carry-over
- `PostDetailView` — re-vérifié : `.textSelection(.enabled)` présent (:744), zéro
  `Color(hex:)` hors charte (tous les hex sont dynamiques : accent post, langue, média).
  Retiré du carry-over
- Toolbars attachments : les couleurs catégorielles de la ladder pièces jointes restent
  différées (arbitrage charte) — mais les **icônes de toolbar** ne sont pas la ladder
  (précédent : camera/location déjà migrées vers error/success dans ces mêmes toolbars)

## Anti-repetition check
Vérifié contre : iter-44b (composer UniversalComposerBar/AudioPostComposer — non touchés),
iter-45i (ConversationListHelpers/ConversationHelperViews/ThreadView/StoryTrayView/
StoryViewerView/FriendRequestListView-hex/StoryViewModel — cette itération ne retouche
aucun de ces hex ; FriendRequestListView n'est repris QUE pour ses polices, explicitement
différées en 45i), iter-45 (FeedCommentsSheet timeAgo/ChangePasswordView — non touchés).
RootViewComponents, FeedView, FeedView+Attachments, WidgetPreviewView, AboutView*,
MessageComposer, AttachmentPreparationService, ConversationAnimatedBackground,
MeeshyWidgets n'ont jamais eu de passe palette.
(*AboutView : seuls les `4ECDC4` de la section Liens sont traités → `shareAccentHex` ;
la ladder de sections 9B59B6/F8B500 reste différée avec les autres ladders.)

## Différés (pour itérations suivantes)
- Ladders catégorielles à arbitrer charte (un seul lot) : pièces jointes
  (`AttachmentKind.hexTintColor` — désormais le SEUL point à changer), sections AboutView
  (9B59B6/F8B500), cartes UserStatsView (5 hex), `attachmentOptions` ThemedFeedComposer,
  policyPickers MediaDownloadSettingsView (F39C12/E74C3C), quick action « Post »
  WidgetPreviewView (A855F7)
- Grandes surfaces polices : ConversationInfoSheet (52), ConversationDashboardView (43),
  TwoFactorSetupView (42, héros intentionnels), CallView (34), InviteFriendsSheet (33),
  ProfileView/GlobalSearchView (32), SettingsView (27), NewConversationView (16),
  DataExportView (16), DataStorageView (11)
- `VoiceProfileWizardView`/`TrackingLinksView` `Color(hex:)` hors palette ;
  IncomingCallView `.white` contraste ; AvatarContextMenuItem → LocalizedStringKey
  (API SDK à évaluer) ; ThemedConversationRow theme-aware (leaf-view) ;
  washes AudioPostComposer (décision design, conforme)
- Reliquats SDK MeeshyUI hors app (Auth/Community/Story/Primitives — `4ECDC4`/`FF2E63`/
  `08D9D6` dans AuthTextField, LanguageSelector, MeeshyForgotPasswordView, CommunityCreateView/
  ListView/DetailView, FloatingButtons, LanguagePickerSheet, EmojiReactionPicker, ChatBubble,
  UserIdentityBar, StatsCard, AchievementBadge, EmptyStateView défaut, viewers Media `08D9D6`
  défauts) — passe SDK dédiée à planifier (surfaces partagées, fort volume)

## Status : corrections implémentées — voir plan 2026-06-12-plan-iteration-46i
