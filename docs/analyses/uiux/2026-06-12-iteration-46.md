# Analyse UI/UX — Itération 46 (2026-06-12)

Base : `main` post-merge #597/#604 (commit 945a8d74). Reprend le carry-over de
`branch-tracking.md` (itérations 45/45i/45w), vérifié point par point contre l'état réel du
code par trois audits parallèles (web, iOS, Android).

## Partie 1 — Application Web

### Vérifié déjà corrigé (items fermés, retirés du carry-over)
- `app/admin/debug.tsx` — utilise `useI18n('admin')`, 0 string FR hardcodée.
- `AgentArchetypesTab.tsx` — toutes les strings via `t('agent.archetypes.*')`.
- `BackSoundDetails.tsx` — `useI18n('audioEffects')`, toutes les strings via `t('backSound.*')`.
- `ConversationPicker.tsx` — `.toLocaleDateString(locale)` correct.

### À corriger cette itération
1. **`components/admin/agent/UserPicker.tsx`** — 5 strings FR hardcodées (`:34` « Aucun
   utilisateur sélectionné », `:42` « Retirer » (title), `:72` « Recherche... », `:96`
   « Ajouté », `:22` placeholder par défaut « Rechercher un utilisateur... ») + `:51`
   `aria-label="Add user"` en anglais dur. Le composant est consommé par AgentConfigDialog
   (admin) ; clés à créer dans `admin.json` ×4 locales.
2. **`components/admin/agent/AgentOverviewTab.tsx:240`** — « Inactifs » hardcodé dans les
   données du Pie chart alors que le composant a `useI18n('admin')` ; et `#94a3b8`
   (slate-400) comme couleur de segment — quasi invisible sur fond dark.
3. **`components/admin/ranking/RankingStatsImpl.tsx`** — composant sans `useI18n` : 4 strings
   FR (`:44` « Visualisation - Top N », `:97` « Évolution et distribution des performances »,
   `:152-153` deux phrases d'explication) + palette amber/yellow hex inline (axes `#d97706`,
   tooltip `#fffbeb`/`#92400e`, barres `#fbbf24`/`#fcd34d`, grille `#fef3c7`) illisible en
   dark mode. Correction : `useI18n('admin')` + palette dark-aware via le mécanisme de thème
   existant (`next-themes`).
4. **`components/conversations/ConversationSettingsModal.tsx:1049`** — « Modifier » hardcodé
   dans l'overlay hover de l'avatar ; le composant a déjà `useI18n('conversations')`.
5. **`components/conversations/MessageSearch.tsx:92`** — `toLocaleDateString(undefined, …)` :
   la locale navigateur supplante la langue d'interface (violation Prisme sur les dates des
   résultats de recherche).
6. **`components/admin/agent/InfoIcon.tsx:11`** — icône d'aide sans `aria-label`.

### Différés (mis à jour)
- **`AgentConfigDialog.tsx`** — ~40-45 strings FR (labels, options de select, 20+ tooltips
  InfoIcon de 50-300 chars). Chantier dédié, planifié pour une itération à part (47+) : le
  volume de clés ×4 locales justifie une passe isolée et review-able.
- `MermaidDiagramImpl.tsx` — `themeVariables` Mermaid fixes (init global, pas d'API
  mode-aware native) — amélioration architecturale, non bloquant.
- Consolidation `app/notifications/preferences/page.tsx` vs composant.
- Réactions par pièce jointe (wiring gateway, cross-platform web+Android).
- Audit qualité es/pt ; console.error FR (logs dev, non bloquant).
- Deep links universels `https://meeshy.me/...` — parité avec Android `meeshy://` (voir
  Partie 3) : arbitrage produit nécessaire (assetlinks.json + apple-app-site-association).

## Partie 2 — Application iOS

### Vérifié déjà corrigé (items fermés)
- `PostDetailView` — `.textSelection(.enabled)` :744 + zéro hex hors palette, re-confirmé →
  retiré du carry-over.
- `AddParticipantSheet` — boutons icon-only ont leurs `.accessibilityLabel` (:92, :126).

### À corriger cette itération
1. **`FriendRequestListView.swift`** — 8 polices texte figées → sémantiques selon le ladder
   iter-44b (17→`.headline`, 18→`.title3`, 15/14→`.subheadline`, 13→`.callout`,
   12→`.footnote`/`.caption`, 11→`.caption2`) ; les 3 tailles d'icônes (`:82` héros 48,
   `:145`/`:155` glyphes 12) restent figées conformément au précédent iter-32/44b.
   Zéro `Color(hex:)` hors palette dans ce fichier.
2. **`CameraView.swift`** — 3 boutons icon-only sans `.accessibilityLabel` (`:57` xmark
   fermer, `:67` bolt flash avec état, `:111` camera.rotate bascule caméra) → labels
   localisés `String(localized:)` (pattern projet).
3. **Éradication palette legacy — 10 petits fichiers (~25 occurrences)**, selon la table de
   mapping iter-42b (`9B59B6`→`indigo600Hex`, `08D9D6`/`4ECDC4`→`indigo300Hex`,
   `FF2E63`/`FF6B6B`→`errorHex` quand sémantique erreur, sinon brand ; `2ECC71`→`successHex`) :
   - `AboutView.swift` (7 : 9B59B6 ×2, 4ECDC4 ×5 — tints de sections)
   - `MessageComposer.swift` (4 : gradients focus/send — mapping contextuel brand, PAS error :
     un bouton d'envoi ne doit pas porter la couleur d'erreur)
   - `AttachmentPreparationService.swift` (3 : 4ECDC4 ×2 accent images, FF6B6B erreur vidéo)
   - `ConversationAnimatedBackground.swift` (2 : defaults secondaryHex/tertiaryHex)
   - `MemberManagementSection.swift` (2), `UserStatsView.swift` (2), `MediaDownloadSettingsView.swift` (2)
   - `ConversationInfoSheet.swift` (1), `BlockedUsersView.swift` (1), `StoryViewerView+Content.swift` (1)

### Différés (mis à jour)
- **Gros fichiers palette legacy** (planifié 47) : RootViewComponents (13), FeedView (9),
  FeedView+Attachments (11), WidgetPreviewView (8) — gradients décoratifs multiples,
  nécessitent un arbitrage visuel par surface.
- `SampleData.swift` ×2 (app + SDK) — morts confirmés (0 référence), suppression nécessite
  édition pbxproj + build local de validation (pas d'environnement macOS dans la routine).
- `BubbleStandardLayout.swift:835` — quoted reply sans `.textSelection` : à évaluer (conflit
  potentiel avec le tap-to-scroll vers le message original).
- Grandes surfaces polices : ConversationInfoSheet (52), ConversationDashboardView (43),
  TwoFactorSetupView (42, héros intentionnels), CallView (34), InviteFriendsSheet (33),
  ProfileView/GlobalSearchView (32), SettingsView (27), NewConversationView (16),
  DataExportView (16), DataStorageView (11).
- Washes AudioPostComposer (décision design), ladder pièces jointes arc-en-ciel (charte),
  VoiceProfileWizardView/TrackingLinksView Color(hex:), IncomingCallView .white contraste,
  AvatarContextMenuItem → LocalizedStringKey, ThemedConversationRow theme-aware.

## Partie 3 — Application Android

### Vérifié sain (audit complet de cette itération)
- Strings : parité 100 % sur les 10 modules ×4 locales (en/fr/es/pt), zéro doublon — la
  correction iter-45 des doublons `chat_date_*` es/pt est confirmée propre.
- 0 littéral UI hors `stringResource()` ; couleurs 100 % via tokens ; zéro TODO/FIXME UI ;
  exceptions documentées inchangées (SettingsScreen 14.dp, emoji 22.sp — non re-flaggées).
- Deep links : `meeshy://` (générique + `chat/` + `profile/`) déclarés dans le manifest.

### À corriger cette itération — a11y `Role.Button` manquant sur 4 surfaces cliquables
TalkBack n'annonce pas ces éléments comme boutons :
1. `feature/settings/.../SettingsScreen.kt:85` — Row profil (avatar + username) cliquable
   sans rôle, alors que `SettingsRow` du même fichier le fait correctement (:224).
2. `sdk-ui/.../MeeshyPrimaryButton.kt:48` — le bouton primaire du design system (utilisé
   partout, dont LoginScreen) sans `Role.Button`.
3. `sdk-ui/.../bubble/MessageBubble.kt:419` — ReactionChip cliquable (toggle réaction).
4. `sdk-ui/.../bubble/MessageBubble.kt:227,251` — images cliquables des bulles (ouverture
   viewer) annoncées « image » au lieu de « image, bouton ».

### Différés (inchangés)
- Parité stories/calls/communities/search/links/media (Phase 2+,
  `apps/android/tasks/feature-parity.md`) ; réactions par pièce jointe (avec web).
- App Links `https://meeshy.me` (assetlinks.json) — arbitrage produit cross-platform.

## Cohérence cross-platform (post-modifications)
- **iOS couleurs** : les 10 fichiers migrés rejoignent les tokens `MeeshyColors` déjà utilisés
  par les surfaces traitées en 42b/44/45i — aucune nouvelle divergence ; le composer garde
  une intention brand (indigo) cohérente avec le reste de l'app.
- **Web admin i18n** : les nouvelles clés `admin.json` suivent l'arborescence existante
  (`agent.*`, `ranking.*`) ; aucune incidence iOS/Android (surfaces admin web-only).
- **Android a11y** : aucune modification de wording ni de visuel — neutre pour la parité.
- **Prisme** : MessageSearch (web) rejoint la règle locale-interface partout ; aucun wording
  nouveau introduit sur aucune plateforme.

## Anti-répétition
Vérifié contre 40-45w : UserPicker/InfoIcon/AgentOverviewTab/RankingStatsImpl jamais traités
(les passes admin-i18n 33-39 ont couvert d'autres tabs ; 45w a traité les `'fr-FR'` hardcodés,
pas ces fichiers) ; ConversationSettingsModal:1049 et MessageSearch:92 absents de toutes les
analyses précédentes ; FriendRequestListView et les 10 fichiers palette listés explicitement
en différé depuis 44b/45i — soldés ici ; CameraView a11y jamais audité ; les 4 sites Android
`Role.Button` sont de nouvelles trouvailles (l'audit iter-44 avait validé les
contentDescription, pas les rôles).

## Status
En cours — branche `claude/blissful-ritchie-8rma6f` (footer mis à jour au merge).
