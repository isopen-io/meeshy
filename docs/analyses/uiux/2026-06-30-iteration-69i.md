# UI/UX Analysis — Iteration 69i (2026-06-30)

## Scope
**iOS exclusivement** (suffixe `i` — Web et Android sont couverts par d'autres agents).
Thème : **accessibilité VoiceOver — boutons retour « icône-seule »**. Plusieurs en-têtes
custom rendent un bouton retour `chevron.left` **sans texte visible ni
`accessibilityLabel`**, si bien que VoiceOver annonce littéralement « chevron.left ».
Itération **bornée, additive et cohérente** (« logique épurée ») : un seul concern a11y,
une ligne par fichier, **zéro changement visuel**.

La CI `ios-tests.yml` (compile Xcode 26.1.x + tests simulateur 18.2) sert de vérification
de build (pas de build local SwiftUI/UIKit sur Linux).

## Contexte / point de départ
- L'app utilise un pattern hybride NavigationStack + en-têtes custom (cf. `apps/ios/CLAUDE.md`).
  Beaucoup d'écrans masquent la barre de nav native et re-dessinent un bouton retour
  `Button { dismiss() } label: { Image(systemName: "chevron.left") … }`.
- Le composant canonique `ThemedBackButton` (`ConversationHelperViews.swift`) est **déjà**
  accessible : `.accessibilityLabel(a11y.back / a11y.back.with_unread)`.
- La clé **`a11y.back`** existe dans `Localizable.xcstrings` et est **entièrement traduite**
  (fr `Retour`, en `Back`, es `Atrás`, de `Zurück`, pt-BR `Voltar`) — c'est la SSOT pour
  ce label.
- **Important** : un `Button` icône-seule sans `accessibilityLabel` hérite du nom du symbole
  SF (« chevron.left ») comme label VoiceOver. Quand le bouton contient un **texte visible**
  « Retour » à côté du chevron, VoiceOver lit ce texte → pas de gap. Le gap n'existe que
  pour les boutons **icône-seule**.

## iOS Findings

### a11y — 7 boutons retour icône-seule sans label
Audit repo-wide des `Button` contenant `Image(systemName: "chevron.left")`. Tri :
- **Déjà conformes** (NE PAS re-flagger) :
  - texte visible « Retour » adjacent (VoiceOver lit le texte) : `SecurityView`,
    `PrivacySettingsView`, `NotificationSettingsView`, `ChangePasswordView`,
    `BlockedUsersView`, `EditProfileView`, `ActiveSessionsView`, `OnboardingFlowView`.
  - `.accessibilityLabel` déjà présent : `CommunityLinksView`, `TrackingLinksView`,
    `ParticipantsView`, `MediaDownloadSettingsView`, `DataStorageView`, `DataExportView`,
    `DeleteAccountView`, `PrivacyPolicyView`, `TermsOfServiceView`, `LicensesView`,
    `AboutView`, `SupportView`, `GlobalSearchView` (cette dernière en vol PR #1080).
  - `ThemedBackButton` (`ConversationHelperViews`) : déjà `a11y.back`.
- **Gaps réels corrigés (icône-seule, ni texte ni label)** :
  1. `FriendRequestListView` (en-tête « Demandes d'amis »)
  2. `UserStatsView` (en-tête « Statistiques »)
  3. `ShareLinksView` (en-tête « Liens de partage »)
  4. `AffiliateView` (en-tête « Parrainage »)
  5. `NewConversationView` (en-tête création de conversation)
  6. `ThreadView` (en-tête fil de discussion)
  7. `LoginView` (retour « désélectionner le compte » sélectionné)

**Correctif** (chaque site) : ajout de
`.accessibilityLabel(String(localized: "a11y.back", bundle: .main))` sur le `Button`
(forme **identique** à celle du `ThemedBackButton` canonique). Purement additif : aucune
modification de `Image`, de police, de couleur, de frame ou de geste. VoiceOver annonce
désormais « Retour / Back / … » au lieu de « chevron.left ».

### Choix de la clé : `a11y.back` (pas `common.back`)
Les frères déjà labellisés utilisaient `common.back` (texte du bouton **visible** ailleurs),
mais `common.back` **n'est pas une entrée du catalogue** (résolue uniquement via
`defaultValue: "Retour"` → non traduite hors fr/en). `a11y.back` est la clé **dédiée a11y,
traduite dans 5 locales**, déjà adoptée par `ThemedBackButton` → meilleur choix Prisme /
VoiceOver multilingue, et SSOT unique.

## Conforme (vérifié — ne pas re-flagger)
- Les en-têtes à texte visible « Retour » : label implicite correct (texte = label VoiceOver).
- Boutons retour déjà munis de `.accessibilityLabel` (liste ci-dessus).
- `ThemedBackButton` : référence gold-standard, inchangé.
- **PÉRIMÉ** : la note 51i « `FriendRequestListView` 11 polices figées » — ce fichier utilise
  déjà des polices **sémantiques Dynamic Type** (`.subheadline`/`.caption`/`.footnote`/
  `.headline`/`.body`) ; seule l'icône décorative empty-state garde une taille fixe (légitime).
  Ne plus citer ce fichier comme « polices figées ».

## Anti-repetition check
Vérifié contre : iter-51i (`ContextActionMenu` Liquid Glass + dead code — n'a pas touché ces
en-têtes), iter-49i/48i (palettes SDK/app), iter-45i/46i (Story). Aucune itération antérieure
n'avait labellisé ces boutons retour. **Orthogonalité avec les PR en vol** confirmée par
diff de fichiers : #1076 (`MentionSuggestionPanel`/`MiniAudioPlayerBar`), #1080
(`MentionSuggestionPanel`/`GlobalSearchView`/`CompatibilityLayerTests`), #1083
(`MentionSuggestionPanel`/`LocationPickerView`) — **aucun** des 7 fichiers de 69i n'y figure.

## Différés (pour itérations suivantes — iOS)
- **Glass adoption (reste)** : `MessageOverlayMenu.panelBackground` (bottom-sheet, gros
  fichier, lot dédié `AdaptiveGlassContainer` pour éviter glass-on-glass) ; `ReplyThreadOverlay`
  (vérifier si backdrop vs surface flottante) ; `CallEffectsOverlay` toolbar pill.
  (`MentionSuggestionPanel`/`MiniAudioPlayerBar`/`GlobalSearchView`/`LocationPickerView`
  **en cours de merge** via #1076/#1080/#1083 — ne pas re-prendre.)
- **a11y** : autres surfaces icône-seule non-back (boutons toolbar custom sans label) ;
  regroupement VoiceOver des lignes (`accessibilityElement(children: .combine)`) sur les
  rangées listant avatar+nom+meta (ex. `FriendRequestListView` row).
- **Palette tokens** : `ContactCardView` hex `#2ECC71`/`#3498DB` — **ATTENTION** : ce sont des
  couleurs de **contenu/affordance** (convention contacts phone=vert/email=bleu), partie de la
  palette de génération de couleurs (langues/thèmes/tags/speakerPalette, assertées en tests),
  **pas** des états `success`/`info`. Ne PAS migrer mécaniquement vers `MeeshyColors.success/.info`
  (sémantiquement faux) — réévaluer en charte palette dédiée.
- **Ladder catégoriel arc-en-ciel** : `NotificationListView.color`, `TagInputView`,
  `MediaTypes.speakerPalette`.

## Status : ⏳ développement terminé — push branche, merge dans main après CI verte —
voir plan `2026-06-30-plan-iteration-69i`
Thème : **poursuite directe de la série Liquid Glass iOS 26 (51i → 68i)** sur la dernière
grosse surface de chrome flottant non convertie — l'**overlay de fil de discussion**
(`ReplyThreadOverlay`, la carte modale de thread de réponses) — couplée à une **épuration
a11y** (bouton de fermeture sans label VoiceOver, drag-indicator + skeleton décoratifs hors
arbre VoiceOver). Itération bornée et cohérente (« logique épurée »).

Vérification : la CI `ios-tests.yml` (compile Xcode 26.1.x + tests simulateur) sert de build
de validation (pas de build local SwiftUI sur Linux). Le smoke test `CompatibilityLayerTests`
couvre **déjà** la forme `RoundedRectangle` + la variante teintée d'`adaptiveGlass`
(lignes 70/85, ajoutées par 68i) — la surface API de 69i est donc déjà exercée, aucune
extension de test requise.

## Contexte / point de départ
- `adaptiveGlass(in:tint:interactive:)` (`packages/MeeshySDK/.../Compatibility/AdaptiveGlass.swift`)
  est l'atome qui gate le vrai `glassEffect` iOS 26 et dégrade en `.ultraThinMaterial` teinté
  (+ liseré) avant. Adopté jusqu'ici sur les pilules/menus flottants (`FloatingCallPillView`,
  `CallView`, `ContextActionMenu` [51i], `MentionSuggestionPanel` + `GlobalSearchView` [68i],
  overlays liste conv, `ReelsPlayerView`, `IncomingCallView`).
- `ReplyThreadOverlay` était listé **explicitement en différé glass #1 par 68i**
  (« `.ultraThinMaterial` plein écran, vérifier clip »). C'est une **carte de contrôle
  flottante au-dessus du contenu** (thread de réponses présenté en overlay) — cas d'usage
  canonique du Liquid Glass selon la doctrine Apple (verre = chrome flottant, pas contenu).

## iOS Findings

### Adoption — carte `ReplyThreadOverlay` → Liquid Glass natif
La carte de thread utilisait `.background(RoundedRectangle(18).fill(theme.surfaceGradient(tint:))
.overlay(stroke))`. Remplacement du remplissage par
`.adaptiveGlass(in: RoundedRectangle(cornerRadius: 18, style: .continuous), tint:
Color(hex: accentColor).opacity(0.18))` **+** conservation du `.overlay(stroke)` de marque,
du `.clipShape(18)` et de l'ombre d'élévation d'accent.
- **iOS 26+** : carte en **Liquid Glass natif translucide teinté accent** — le thread flotte
  réellement en verre au-dessus de la conversation.
- **iOS < 26** : `.ultraThinMaterial` + teinte accent + hairline (via le repli de l'atome) —
  équivalent perceptuel à l'existant (`surfaceGradient` était déjà semi-translucide), plus le
  liseré qui ancre la surface.
- L'identité visuelle (accent de conversation) est **préservée** : teinte passée à l'atome +
  stroke `theme.border(tint: accentColor)` conservé au-dessus du verre. Le `.clipShape` partage
  exactement la forme du verre (RoundedRectangle 18) ⇒ pas d'artefact de clip glass-sur-bord.

### Périmètre délibérément exclu (doctrine Apple : verre = chrome, pas contenu)
- Le **scrim de fond** (`Color.black.opacity(0.3).background(.ultraThinMaterial)` plein écran,
  tap-to-dismiss) reste en material : c'est un **voile d'assombrissement de contenu**, pas du
  chrome de contrôle. Conforme, ne pas re-flagger.
- Les **fonds de lignes du thread** (`Color(hex: accentColor).opacity(0.1)` sur la bulle
  parente, chips de réponse) restent en teinte plate : contenu scrollable interne à la carte,
  pas du chrome ⇒ pas de glass-sur-glass.

### Épuration a11y — overlay de thread
1. **Bouton de fermeture sans label** : le `Button { dismiss() }` (icône `xmark.circle.fill`)
   n'exposait **aucun** `accessibilityLabel` ⇒ VoiceOver l'annonçait par le nom brut du symbole
   SF (« xmark circle fill »). Ajout de `.accessibilityLabel("common.close" → « Fermer »)`
   (réutilisation de la clé i18n existante, pas de nouvelle clé). C'est le **seul** affordance
   de fermeture exposé à VoiceOver (le scrim tap-to-dismiss et le drag-to-dismiss sont
   invisibles au rotor) — combler ce label est donc critique.
2. **Drag-indicator décoratif** (capsule de glissement) → `.accessibilityHidden(true)` : forme
   vide sans sémantique, le rotor ne doit pas s'y arrêter.
3. **Skeleton de chargement décoratif** (3 lignes shimmer) → `.accessibilityHidden(true)` :
   exact parallèle de l'épuration `mentionSkeletonRows` de 68i — avatars/barres vides sans
   contenu sémantique pendant le streaming.

## Conforme (vérifié — ne pas re-flagger)
- Le drag-to-dismiss (`DragGesture` > 100pt) et le tap-scrim de fermeture sont déjà câblés.
- Les lignes de réponse exposent déjà nom + horodatage relatif + contenu (texte natif,
  Prisme Linguistique via `translationResolver`). Conforme.
- Le `RoundedRectangle(18)` du verre == forme du `.clipShape` ⇒ pas de clip glass défectueux
  (réponse au « vérifier clip » noté par 68i).

## Anti-repetition check
Vérifié contre 68i (a converti `MentionSuggestionPanel` + `GlobalSearchView` ; a explicitement
**laissé `ReplyThreadOverlay` en différé**), 51i (`ContextActionMenu`), 49i/48i (palette).
Aucune itération antérieure n'avait adopté le Liquid Glass sur `ReplyThreadOverlay` ni rendu
son bouton de fermeture accessible / son skeleton non-accessible. Le pattern « glass + skeleton
`accessibilityHidden` » prolonge 68i sur une nouvelle surface, sans recouper son périmètre.

## Différés (pour itérations suivantes — iOS)
- **Glass adoption (reste)** : panneau `MessageOverlayMenu` (chrome glow/ombre finement réglé —
  gros fichier, lot dédié avec `AdaptiveGlassContainer` pour éviter glass-on-glass), toolbars de
  composer app-side.
- **Palette tokens** : `ContactCardView` hex durs `#2ECC71` (téléphone) / `#3498DB` (email)
  → `MeeshyColors.success` / `.info` ; verts d'online/présence (`MeeshyAvatar`, `UserIdentityBar`,
  `LiveLocationBadge` : `#2ECC71`) → `MeeshyColors.success` (lot dédié — vérifier baselines
  snapshot avant swap SDK).
Thème : **continuation directe du ladder d'adoption iOS 26 Liquid Glass** sur les
**toolbars/headers de contrôle flottant interactif au-dessus du contenu** — deux surfaces
encore en `.ultraThinMaterial` brut :
1. **`CallEffectsOverlay.secondaryToolbar`** — barre d'outils capsule (Effets/Filtres)
   flottant **au-dessus du flux vidéo d'appel** (candidat « textbook glass » explicitement
   différé par 52i).
2. **`ConversationView` — en-tête de recherche in-conversation** — barre de recherche
   `RoundedRectangle(16)` flottant **au-dessus de la conversation** (même famille UX que
   `GlobalSearchView`, convertie en 68i).

Itération bornée et cohérente (« logique épurée ») : 2 surfaces, même atome, même famille
UX (chrome de contrôle interactif flottant) — pas de passe mécanique large.

Vérification : la CI `ios-tests.yml` (compile Xcode 26.1.x + tests simulateur 18.2) sert de
build de validation (pas de build SwiftUI local sur Linux). Le smoke test
`CompatibilityLayerTests` est étendu pour couvrir la forme `Capsule()` (jusqu'ici non
couverte, alors qu'elle est utilisée en production par `FloatingCallPillView` /
`MiniAudioPlayerBar`).

## Contexte / point de départ
- `adaptiveGlass(in:tint:interactive:)` (`packages/MeeshySDK/.../Compatibility/AdaptiveGlass.swift`)
  est l'atome qui gate le vrai `glassEffect` iOS 26 et dégrade en `.ultraThinMaterial`
  (+ teinte + liseré) avant. Ladder d'adoption : 51i (`ContextActionMenu`) → 52i
  (`MentionSuggestionPanel` + `LocationPickerView` dropdowns) → 68i (`GlobalSearchView`
  champ de recherche + `MentionSuggestionPanel`) → **69i (toolbars/headers d'appel et de
  recherche in-conversation)**.
- Doctrine Apple appliquée depuis 68i : **le verre est réservé au chrome de contrôle
  interactif flottant au-dessus du contenu**, jamais aux fonds de listes/cartes de contenu
  scrollable (coût de sampling + lisibilité). Les deux cibles 69i sont exactement ce cas.

## iOS Findings

### Adoption — `CallEffectsOverlay.secondaryToolbar` → Liquid Glass natif (Capsule)
La toolbar secondaire (boutons « Effets » / « Filtres ») flotte au-dessus du flux vidéo et
composait `.background(.ultraThinMaterial).clipShape(Capsule())`.
- **Correctif** : `.background(.ultraThinMaterial).clipShape(Capsule())` →
  `.adaptiveGlass(in: Capsule())` — empreinte capsule identique, le `clipShape` séparé
  devient superflu (le fond est lui-même galbé en capsule, comme `FloatingCallPillView:90`
  et `MiniAudioPlayerBar:185` déjà établis).
- **iOS 26+** : Liquid Glass natif translucide.
- **iOS < 26** : `.ultraThinMaterial` + hairline (via le repli de l'atome) — strictement
  équivalent à l'existant, plus le liseré qui ancre la surface.
- **Pas de teinte** : aucune `accentColor` de conversation en scope pour un overlay d'appel
  (cohérent avec le verre neutre de `FloatingCallPillView`). Le contrôle conserve ses
  couleurs internes (indigo500 sur l'état actif).

### Adoption — en-tête de recherche in-conversation → Liquid Glass natif teinté accent
La barre de recherche in-conversation (`searchBar`, `ConversationView+MessageRow.swift`)
flotte en haut de la conversation et composait
`.background(RoundedRectangle(16).fill(.ultraThinMaterial).shadow(...))`.
- **Correctif** : remplacement par
  `.adaptiveGlass(in: RoundedRectangle(cornerRadius: 16), tint: Color(hex: accentColor).opacity(0.12))`
  **+** `.shadow(color:.black.opacity(0.1), radius: 4, y: 2)` en aval du verre (pattern
  établi `ContextActionMenu`/`LocationPickerView` : l'ombre passe après le glass).
- **iOS 26+** : Liquid Glass natif teinté à l'accent de la conversation.
- **iOS < 26** : `.ultraThinMaterial` + teinte accent + hairline (repli atome).
- **Conformité règle marque** (CLAUDE.md « ALL conversation-context components MUST use
  `accentColor` ») : l'en-tête adopte enfin la teinte de conversation (auparavant un
  material neutre, alors que `accentColor` était déjà en scope, utilisé par le spinner et
  le bouton « Fermer »). L'`accentColor` n'était jusqu'ici utilisé que pour les éléments
  internes ; la surface flottante elle-même est désormais alignée.

### Périmètre délibérément exclu (doctrine Apple : verre = chrome interactif, pas contenu)
- **`searchResultsBanner`** (`ConversationView+MessageRow.swift:140`, `Capsule().fill(.ultraThinMaterial)`)
  est une **bannière non-interactive** d'annonce du nombre de résultats — pas un contrôle.
  Conforme à la doctrine 68i (verre = chrome interactif). **Ne pas re-flagger.**
- **Champ de saisie interne** de la barre de recherche
  (`RoundedRectangle(10).fill(isDark ? white@0.08 : black@0.05)`, ligne 89-92) : c'est le
  fond du `TextField` **à l'intérieur** du conteneur en verre. Le mettre en verre créerait
  un verre-sur-verre (sampling). Reste en fill plat opaque : conforme.
- **`AudioEffectsPanel` / `VideoFiltersPanel`** (cartes de contrôle stateful **dans** le
  `ScrollView` de `CallEffectsOverlay`) : marquées MARGINAL par 52i (glass-card seulement si
  redesign). Hors scope.
- **Backdrop** `Color.black.opacity(0.25)` de `CallEffectsOverlay` : c'est un dimming
  modal, pas une surface flottante. Conforme.

## Conforme (vérifié — ne pas re-flagger)
- Chaque `toolbarButton` de `CallEffectsOverlay` a déjà `accessibilityValue` (on/off) +
  `accessibilityHint` (ouvre/ferme), cible 48pt, `.pressable()`. Le label vient du `Text`
  interne (« Effets »/« Filtres »). Conforme.
- Le bouton « effacer la recherche » de la barre in-conversation a déjà `accessibilityLabel`
  localisé. Le bouton « Fermer » aussi. Conforme.

## Anti-repetition check
Vérifié contre : 68i (`GlobalSearchView` champ de recherche **global** + `MentionSuggestionPanel`
+ skeleton a11y — n'a PAS touché `CallEffectsOverlay` ni la recherche **in-conversation**),
52i (`MentionSuggestionPanel` + `LocationPickerView` dropdowns — `CallEffectsOverlay`
explicitement listé en différé « GOOD restant », jamais converti), 51i (`ContextActionMenu`),
49i/48i (palettes). Aucune itération antérieure n'a adopté Liquid Glass sur ces deux
surfaces. Le smoke test ne couvrait pas `Capsule()` (seulement `Circle`/`RoundedRectangle`/
`Rectangle`) malgré son usage production — comblé ici.

## Différés (pour itérations suivantes — iOS)
- **Glass adoption (reste)** : `MessageOverlayMenu` (chrome glow/ombre finement réglé — gros
  fichier, lot dédié avec `AdaptiveGlassContainer` pour éviter glass-on-glass) ;
  `ReplyThreadOverlay` (backdrop modal plein écran — vérifier si interactif/flottant ou
  simple dimming avant tout swap).
- **Palette tokens** : `ContactCardView` hex durs `#2ECC71` (téléphone) / `#3498DB` (email)
  → `MeeshyColors.success` / `.info` (épuration palette, distinct du glass — candidat propre
  et borné pour une itération palette dédiée).
- **Ladder catégoriel arc-en-ciel** (décision charte unique) : `NotificationListView.color`,
  `TagInputView`, `MediaTypes.speakerPalette`, ladder app-side.
- Grandes surfaces polices figées (`FriendRequestListView` 11, `ConversationInfoSheet` 52,
  `ConversationDashboardView` 43, `CallView` 34…) ; `ThemedConversationRow` theme-aware.
Thème : **épuration palette — couleurs sémantiques codées en dur → tokens `MeeshyColors`**
(lot 1). Itération bornée et « épurée » : on solde le différé explicite de 68i
(« `ContactCardView` hex durs `#2ECC71`/`#3498DB` → `MeeshyColors.success`/`.info` »), élargi
au seul autre usage **foreground sémantique** strictement équivalent (icône partage
`AffiliateView`). Zéro changement de comportement, zéro changement de layout.

Vérification : la CI `ios-tests.yml` (compile Xcode 26.1.x + tests simulateur 18.2) sert de
build de validation — pas de build SwiftUI local sur Linux.

## Contexte / point de départ
- La charte iOS (`apps/ios/CLAUDE.md` § Semantic State Colors + § Color Migration) impose :
  « New code MUST use the Indigo scale or semantic names ». Les **couleurs d'état
  sémantiques** ont des tokens dédiés dans le SDK (`MeeshyColors`, `MeeshyUI/Theme`) :
  `success = #34D399`, `info = #60A5FA`, `error = #F87171`, `warning = #FBBF24`.
- Plusieurs composants composaient encore des **hex bruts** quasi-identiques aux tokens
  (`#2ECC71` ≈ vert « réussi/téléphone », `#3498DB` ≈ bleu « info/email ») au lieu de
  référencer le token. Conséquences : (1) dérive de palette (deux verts différents à
  l'écran), (2) ces hex sont **figés** et n'évoluent pas si la charte ajuste le token,
  (3) ils ne passent pas par le pipeline de tokens (cohérence dark/light).
- `MeeshyColors` est **résolu app-wide** via `@_exported import MeeshyUI`
  (`apps/ios/Meeshy/MeeshyUIExports.swift`) — déjà consommé par `ContactsListTab`,
  `OnboardingStepViews`, `AffiliateView` (qui utilise déjà `MeeshyColors.error` sur le
  bouton frère « supprimer »). Aucun nouvel import requis.

## iOS Findings

### Tokenisation — `ContactCardView` (carte contact dans une bulle)
- Icône **téléphone** : `.foregroundColor(Color(hex: "2ECC71"))` → `MeeshyColors.success`.
- Icône **email** : `.foregroundColor(Color(hex: "3498DB"))` → `MeeshyColors.info`.
Les deux sont des **foregrounds sémantiques d'affordance** (vert = canal voix, bleu =
canal écrit) — exactement la sémantique `success`/`info`. Le rendu reste vert/bleu, mais
désormais aligné sur la palette de marque et tuné dark/light par le token.

### Tokenisation — `AffiliateView` (ligne de token d'affiliation)
- Icône **partager** : `.foregroundColor(Color(hex: "2ECC71"))` → `MeeshyColors.success`.
Le bouton **frère immédiat** (« supprimer ») utilise déjà `MeeshyColors.error`. La paire
action verte (partager) / action rouge (supprimer) devient cohérente : les deux passent
par les tokens, fini le vert orphelin codé en dur à côté d'un rouge tokenisé.

### Périmètre délibérément exclu (ne pas re-flagger)
- **`FeedView+Attachments.swift:1011`** — vignette placeholder de pièce jointe *location* :
  `LinearGradient([#2ECC71, #27AE60])`. C'est un **dégradé décoratif** (paire de verts ;
  `#27AE60` n'a **pas** de token correspondant), pas un foreground d'état. Tokeniser
  seulement la 1re couleur casserait l'appairage du dégradé. Laissé tel quel — conforme à
  la doctrine « tokeniser le sémantique, pas le décoratif ». À traiter, si besoin, par
  l'introduction d'un token de paire dédié, pas par swap nu.
- Les hex de **surfaces/dégradés de marque** (`#13111C`, `#09090B`, `#F8F7FF`, scales
  indigo…) et les **palettes catégorielles** (thumbnails par type) restent hors-scope :
  ce ne sont pas des couleurs d'état.

## Conforme (vérifié — ne pas re-flagger)
- `ContactCardView` : `Color(hex: accentColor)` (avatar/liséré/label) est **dynamique**
  (couleur d'accent de conversation injectée) — ce n'est PAS un hex sémantique figé, il
  reste correct. Le liséré dégradé de marque (51i/53i) et le `adaptiveGlass` (53i) sont
  conservés intacts.
- `AffiliateView` : le bouton « supprimer » utilise déjà `MeeshyColors.error`. Conforme.

## Anti-repetition check
Vérifié contre 53i (Liquid Glass `ContactCardView` — a touché le **fond** verre + liséré,
**pas** les couleurs d'icônes téléphone/email), 49i/48i (palette SDK/app — palettes
catégorielles & MeeshyUI, n'ont **pas** tokenisé ces foregrounds sémantiques de contact),
68i (qui a explicitement **listé ce point en différé**). Aucune itération antérieure
n'avait remplacé `#2ECC71`/`#3498DB` par `success`/`info`.

## Différés (pour itérations suivantes — iOS)
- **Palette tokens (reste)** : paire dégradé location `#2ECC71`/`#27AE60` (nécessite un
  token de paire) ; autres foregrounds sémantiques candidats à auditer un par un
  (`#FF6B6B`/`#4ADE80`/`#34B7F1`/`#3B82F6` — vérifier qu'ils sont bien des états et non du
  décoratif/marque-tierce avant tout swap).
- **Ladder catégoriel arc-en-ciel** (décision charte unique) : `NotificationListView.color`,
  `TagInputView`, `MediaTypes.speakerPalette`.
- **Glass adoption (reste)** : `ReplyThreadOverlay`, `MessageOverlayMenu` (lot dédié avec
  `AdaptiveGlassContainer` pour éviter glass-on-glass).
- **Dynamic Type** : grandes surfaces à `font(.system(size:))` figé
  (`ConversationInfoSheet`, `ConversationDashboardView`, `CallView`…) — défait le Dynamic
  Type (a11y), lot dédié à venir.
Thème : **épuration palette — remplacement de deux couleurs flat hors-charte par les
tokens sémantiques `MeeshyColors`** dans `ContactCardView` (carte de contact partagée
affichée dans une bulle de message). Itération bornée et cohérente (« logique épurée »),
solde un différé explicite de 68i (section « Palette tokens »).

Vérification : la CI `ios-tests.yml` (compile Xcode 26.1.x Swift 6.2 + tests simulateur
iOS 18.2) sert de build de validation (pas de build local SwiftUI sur Linux).

## Contexte / point de départ
- La charte Meeshy (cf. `apps/ios/CLAUDE.md` § Design System) définit des **tokens
  sémantiques** d'état : `MeeshyColors.success` (`#34D399`, vert de marque),
  `MeeshyColors.info` (`#60A5FA`, bleu de marque). Source de vérité :
  `packages/MeeshySDK/Sources/MeeshyUI/Theme/MeeshyColors.swift`.
- `ContactCardView` (`apps/ios/Meeshy/Features/Main/Components/ContactCardView.swift`)
  affiche les numéros de téléphone et adresses e-mail d'un contact partagé. Les icônes
  `phone.fill` / `envelope.fill` étaient teintées par **deux hex codés en dur, hors
  palette** : `#2ECC71` (vert « Flat UI / Emerald » web) pour le téléphone et `#3498DB`
  (bleu « Flat UI / Peter River » web) pour l'e-mail. Ces valeurs ne correspondent à
  aucun token de la charte indigo + sémantique de Meeshy.
- 68i avait déjà converti la **carte** elle-même au Liquid Glass natif iOS 26 (`adaptiveGlass`)
  et listait en différé « Palette tokens : `ContactCardView` hex durs `#2ECC71`
  (téléphone) / `#3498DB` (email) → `MeeshyColors.success` / `.info` ».

## iOS Findings

### Épuration palette — icône téléphone → `MeeshyColors.success`
`.foregroundColor(Color(hex: "2ECC71"))` → `.foregroundColor(MeeshyColors.success)`.
Sémantique correcte : le vert de succès/confirmation de la charte (le téléphone est une
affordance positive « appeler »). Aligne la carte sur le vert de marque `#34D399` au lieu
d'un vert web arbitraire (`#2ECC71`).

### Épuration palette — icône e-mail → `MeeshyColors.info`
`.foregroundColor(Color(hex: "3498DB"))` → `.foregroundColor(MeeshyColors.info)`.
Sémantique correcte : le bleu d'information de la charte. Aligne la carte sur le bleu de
marque `#60A5FA` au lieu d'un bleu web arbitraire (`#3498DB`).

### Détail module — ajout de `import MeeshyUI`
`MeeshyColors` est un **type nommé** du module `MeeshyUI` (et non une extension d'un type
du framework SwiftUI). À la différence de `Color(hex:)` — extension publique sur `Color`
qui « fuit » via la closure d'import du target — un type nommé exige un `import MeeshyUI`
explicite dans le fichier. Ajout d'`import MeeshyUI` après `import MeeshySDK` : c'est le
**pattern déjà établi** par de nombreux fichiers frères de `Components/` (`ConnectionBanner`,
`UniversalComposerBar`, `ConversationInfoSheet`, `EditPostSheet`…). `MeeshyColors` est un
`nonisolated struct` à statiques constantes — accessible sans friction depuis le contexte
MainActor de la `View`.

## Périmètre délibérément exclu (ne pas re-flagger)
- Les `Color(hex: accentColor)` restants (header dégradé, liséré de marque) utilisent
  l'**accent déterministe de la conversation** (identité couleur calculée, cf. § Conversation
  Accent Color). Ce ne sont **pas** des hex hors-charte : ils doivent rester paramétrés.
- Les usages `color: "3498DB"` / `"2ECC71"` ailleurs (`ReportUserView`, `PrivacySettingsView`,
  `RootView`, `AffiliateView`, `SupportView`) sont passés comme **identité de teinte** à des
  helpers de section thémés (`theme.surfaceGradient(tint:)`), **pas** comme couleur sémantique
  d'icône. Ils relèvent d'un autre pattern (accent décoratif par section) — **hors-scope** de
  cette épuration sémantique ; les convertir changerait l'identité visuelle de ces écrans.

## Conforme (vérifié — ne pas re-flagger)
- `ContactCardView` a déjà `accessibilityElement(children: .combine)` + `accessibilityLabel`
  + `accessibilityHint` localisés, `buttonStyle(.plain)`, et la carte en `adaptiveGlass`
  (68i) avec liséré de marque préservé. Conforme.

## Anti-repetition check
Vérifié contre 68i (a converti la carte au Liquid Glass et **listé ces deux hex en différé**,
sans les corriger), 52i (`adaptiveGlass` sur la carte — stroke préservé), 51i/49i/48i/45i.
Aucune itération antérieure n'avait remplacé ces deux hex flat par les tokens sémantiques.
Le différé « Palette tokens `ContactCardView` » de 68i est désormais **soldé**.

## Différés (pour itérations suivantes — iOS)
- **Glass adoption (reste)** : `ReplyThreadOverlay` (`.ultraThinMaterial` plein écran,
  vérifier clip), `MessageOverlayMenu` (chrome glow/ombre finement réglé — gros fichier,
  lot dédié avec `AdaptiveGlassContainer` pour éviter glass-on-glass).
- **Ladder catégoriel arc-en-ciel** (décision charte unique) : `NotificationListView.color`,
  `TagInputView`, `MediaTypes.speakerPalette`, ladder app-side.
- **Grandes surfaces polices figées** (`FriendRequestListView` 11, `ConversationInfoSheet` 52,
  `ConversationDashboardView` 43, `CallView` 34…) ; `ThemedConversationRow` theme-aware.
Thème : **épuration palette — alignement des indicateurs *sémantiques* de statut/confirmation
sur les tokens de marque `MeeshyColors`**. Tous les verts/oranges génériques hors-charte
(`#2ECC71`, `#F39C12`) utilisés pour exprimer un **état sémantique** (online, absent, confirmé,
prêt, actif/live) sont remplacés par les tokens `MeeshyColors.success` / `.warning` / `.info`.
Itération bornée et cohérente (« logique épurée »), dans la continuité directe du différé 68i
(« `ContactCardView` hex durs → tokens »), élargi à TOUTES les surfaces de même nature.

Vérification : la CI `ios-tests.yml` (compile Xcode 26.1.x + tests simulateur) sert de build de
validation (pas de build local SwiftUI sur Linux). Changement **pur token** (aucune logique
modifiée) ; les vues touchées sont exercées par la compilation CI + les smoke tests présence
existants (`IdentityBarElementTests`, `AvatarContextTests`).

## Contexte / point de départ
- La charte Meeshy (apps/ios/CLAUDE.md § *Semantic State Colors*) définit :
  `success = #34D399` (« Sent, **online**, **confirmed** »), `warning = #FBBF24`
  (« Attention, **pending** »), `info = #60A5FA` (« Information, help »).
- Règle SDK (`packages/MeeshySDK/CLAUDE.md`) : « Les couleurs sémantiques (error, success,
  warning) restent **statiques** via `MeeshyColors` » — jamais hardcodées.
- Plusieurs composants utilisaient encore des verts/oranges **génériques iOS** (`#2ECC71`
  vert un peu plus saturé que l'emeraude de marque, `#3498DB` bleu générique, `#F39C12`
  orange générique) pour des états qui sont **exactement** ceux couverts par la charte
  sémantique. C'est une dérive de palette : deux verts « succès » coexistaient à l'écran.

## iOS Findings — substitutions (literal hex → token)

| Composant | Élément | Avant | Après | Sémantique |
|---|---|---|---|---|
| `ContactCardView` (app) | icône téléphone | `#2ECC71` | `MeeshyColors.success` | numéro joignable |
| `ContactCardView` (app) | icône email | `#3498DB` | `MeeshyColors.info` | information de contact |
| `MeeshyAvatar` (SDK) | dot présence online | `#2ECC71` | `MeeshyColors.success` | en ligne |
| `MeeshyAvatar` (SDK) | dot présence away | `#F39C12` | `MeeshyColors.warning` | absent |
| `UserIdentityBar` (SDK) | dot + label présence | `#2ECC71` / `#F39C12` | `success` / `warning` | en ligne / absent |
| `UserProfileSheet` (SDK) | badge E2EE (icône+texte+fond+bord) | `#2ECC71` ×4 | `MeeshyColors.success` | chiffrement confirmé |
| `VoiceRecordingView` (SDK) | checkmark échantillon | `#2ECC71` | `MeeshyColors.success` | échantillon enregistré |
| `VoiceProfileWizardView` (SDK) | checkmark final « Profil créé » | `#2ECC71` | `MeeshyColors.success` | succès création |
| `VoiceProfileManageView` (SDK) | waveform « profil prêt » | `#2ECC71` (ternaire) | `MeeshyColors.success` | profil prêt |
| `LiveLocationBadge` (SDK) | dot pulsant « live » | `#2ECC71` | `MeeshyColors.success` | localisation active |

`ContactCardView` (app) a reçu `import MeeshyUI` (le token `MeeshyColors` vit dans le target
`MeeshyUI` ; les autres fichiers sont déjà dans ce module → accès direct).

## Périmètre délibérément exclu (ladders catégoriels — décision « charte unique » différée)
Les `#2ECC71` / `#3498DB` restants dans `MeeshyUI` sont **tous** des **palettes catégorielles
arc-en-ciel** (sélecteurs multi-couleurs), pas des états sémantiques — laissés intacts,
conformément au différé 68i (« ladder catégoriel = décision charte unique, ne pas toucher au
coup par coup ») :
- `DrawingEditToolOptions.swift` (palette pinceau Story)
- `StoryComposerView.swift` / `StoryTextEditorView.swift` (palettes texte/fond Story)
- `TagInputView.swift` (ladder couleurs de tags)
- `MediaTypes.speakerPalette` (couleurs de locuteurs audio)

De même, `ColorGeneration.swift` (mapping langue/thème → couleur d'accent déterministe) et
`CoreModels`/`SampleData` (couleurs par défaut de tags/sections/attachments) sont des
**données catégorielles**, hors-scope sémantique : ne pas re-flagger.

## Conforme (vérifié — ne pas re-flagger)
- `MeeshyColors.success` (#34D399) est explicitement « Sent, online, confirmed » dans la
  charte → mapping online/confirmé/prêt/live = success est canonique.
- `MeeshyColors.warning` (#FBBF24) = « Attention, pending » → mapping away/absent = warning.
- Aucun test n'assertait ces hex (vérifié : `dotColor` est `private`, les smoke tests présence
  ne testent que la construction `.body` et les comptes structurels). Aucune baseline cassée.

## Anti-repetition check
Vérifié contre 68i (Liquid Glass champ recherche + mention ; a **différé** explicitement
`ContactCardView` hex → tokens — c'est ici qu'on solde ce différé, élargi), 52i/53i (Liquid
Glass surfaces flottantes), 51i (`ContextActionMenu`), 49i/48i (palettes SDK/app — couvraient
les **gradients de marque** indigo, **pas** les verts/oranges sémantiques). Aucune itération
antérieure n'avait aligné les indicateurs de présence/confirmation sur les tokens sémantiques.

## Différés (pour itérations suivantes — iOS)
- **Glass adoption (reste)** : `ReplyThreadOverlay` (`.ultraThinMaterial` plein écran, vérifier
  clip), panneau `MessageOverlayMenu` (chrome glow/ombre — lot dédié avec `AdaptiveGlassContainer`).
- **Ladder catégoriel arc-en-ciel** (décision charte unique, encore différée) :
  `NotificationListView.color`, `TagInputView`, `MediaTypes.speakerPalette`, palettes Story.
- **Grandes surfaces polices figées** (`FriendRequestListView`, `ConversationInfoSheet`,
  `ConversationDashboardView`, `CallView`…) ; `ThemedConversationRow` theme-aware.
Thème : **adoption native iOS 26 Liquid Glass sur le chrome de contrôle/info flottant
au-dessus du flux vidéo d'appel**. Famille UX unique et cohérente (« chrome-over-content »
au sens strict d'Apple), continuité directe du ladder Glass amorcé en 51i (`ContextActionMenu`)
puis 52i (panneaux de suggestion) et 68i (champ de recherche + autocomplétion mention).
Itération bornée (« logique épurée ») : 3 surfaces, même atome, même pattern — pas de passe
mécanique large.

Vérification : la CI `ios-tests.yml` (compile Xcode 26.1.x + tests simulateur 18.2) sert de
build de validation (pas de build SwiftUI local sur Linux). Le smoke test
`CompatibilityLayerTests` est étendu pour couvrir la forme `Capsule` (nouvelle parmi les
sites d'adoption 69i).

## Contexte / point de départ
- `adaptiveGlass(in:tint:interactive:)`
  (`packages/MeeshySDK/Sources/MeeshyUI/Compatibility/AdaptiveGlass.swift`) est l'atome qui
  gate le vrai `glassEffect` iOS 26 et dégrade en `.ultraThinMaterial` (+ hairline) avant.
  Déjà adopté sur les pilules/menus flottants (`FloatingCallPillView` `Capsule`, `CallView`
  boutons de contrôle `Circle`, `ContextActionMenu` [51i], overlays liste conv,
  `ReelsPlayerView`, `IncomingCallView`, `GlobalSearchView`/`MentionSuggestionPanel` [68i]).
- L'écran d'appel vidéo (`CallView.videoCallLayout`) affiche un **flux vidéo plein cadre
  edge-to-edge**, par-dessus lequel **trois surfaces de chrome flottant** composaient encore
  leur fond via un brut `.background(.ultraThinMaterial)` :
  - **badge de durée** (`CallView` L681, `Capsule`, coin haut-gauche),
  - **panneau de transcription live** (`CallView` L951, `RoundedRectangle(12)`, bas d'écran),
  - **toolbar d'effets** (`CallEffectsOverlay` L79, `Capsule`, au-dessus des contrôles).
  52i avait explicitement listé `CallEffectsOverlay:79` comme candidat **GOOD** (« toolbar
  pill Capsule au-dessus du flux vidéo, textbook glass »).

## iOS Findings

### Adoption — badge de durée d'appel → Liquid Glass natif (`CallView` L681)
`.padding().background(.ultraThinMaterial).clipShape(Capsule())` →
`.padding().adaptiveGlass(in: Capsule()).clipShape(Capsule())`. Le `.clipShape(Capsule())`
est **conservé** (clip du contenu, conforme au pattern établi `FloatingCallPillView` L90-91).
- **iOS 26+** : vrai Liquid Glass natif au-dessus de la vidéo.
- **iOS < 26** : `.ultraThinMaterial` + hairline (via le repli de l'atome) — strictement
  équivalent à l'existant, plus le liseré qui ancre la surface.
Swap **pur gain** : aucune décoration perdue, chrome neutre (texte blanc, pas de teinte).

### Adoption — panneau transcription live → Liquid Glass natif (`CallView` L951)
`.padding().background(.ultraThinMaterial).clipShape(RoundedRectangle(12))` →
`.adaptiveGlass(in: RoundedRectangle(cornerRadius: 12)).clipShape(RoundedRectangle(12))`.
Même famille (surface d'info flottant au-dessus de la vidéo). Le clip arrondi est préservé.

### Adoption — toolbar d'effets d'appel → Liquid Glass natif (`CallEffectsOverlay` L79)
`.padding().background(.ultraThinMaterial).clipShape(Capsule())` →
`.adaptiveGlass(in: Capsule()).clipShape(Capsule())`. Cas **textbook** Apple : barre de
contrôle flottante content-agnostic au-dessus du flux. Soldé le candidat GOOD de 52i.

### Smoke test étendu — forme `Capsule`
`CompatibilityLayerTests.test_adaptiveGlass_appliesToAnyView_*` couvrait `Circle`,
`RoundedRectangle`, `Rectangle` (sites 68i). Ajout d'un cas `Capsule()` pour garder la
surface API des sites d'adoption 69i (badge de durée + toolbar d'effets) à l'abri d'une
dérive de signature.

## Périmètre délibérément exclu (doctrine Apple : verre = chrome, pas contenu)
- Les boutons de contrôle d'appel (`CallView` L1429 `Circle`) sont **déjà** en `adaptiveGlass`
  (lot antérieur) — non re-touchés.
- `AudioEffectsPanel` / `VideoFiltersPanel` (cartes de contrôle stateful d'effets, scrollées
  dans `CallEffectsOverlay`) : **fonds de contenu**, pas du chrome flottant — restent en
  material. Conforme, ne pas re-flagger (déjà acté SKIP en 52i).
- Les `.ultraThinMaterial` hors domaine appel (`MessageInfoSheet`, `LocationPickerView`
  barre/bottom card, `MessageOverlayMenu`, `InviteFriendsSheet`, `StatusBubbleOverlay`,
  `ContactCardView`) : hors scope de ce lot borné (familles UX distinctes ; certains exigent
  `AdaptiveGlassContainer` anti glass-on-glass — lots dédiés).

## Conforme (vérifié — ne pas re-flagger)
- Le badge de durée a déjà `accessibilityLabel`/`accessibilityValue`/`updatesFrequently` ;
  le panneau transcript combine les segments avec labels speaker ; la toolbar d'effets a
  `accessibilityValue` (on/off) + `accessibilityHint` (ouvre/ferme). a11y intacte, inchangée.
- `clipShape` conservé partout → empreinte visuelle identique (coins, dimensions).

## Anti-repetition check
Vérifié contre 68i (`GlobalSearchView` + `MentionSuggestionPanel` — n'a PAS touché le domaine
appel), 52i (panneaux suggestion `MentionSuggestionPanel`/`LocationPickerView` dropdown ;
listait `CallEffectsOverlay:79` en différé GOOD — désormais soldé), 51i (`ContextActionMenu`),
49i (palette SDK), 48i (palette apps/ios). Aucune itération antérieure n'avait converti le
badge de durée, le panneau transcript, ni la toolbar d'effets d'appel. Le `Capsule` du smoke
test n'était couvert par aucun cas (seulement `Circle`/`RoundedRectangle`/`Rectangle`).

## Différés (pour itérations suivantes — iOS)
- **Glass adoption (reste)** : `MessageOverlayMenu` (chrome glow/ombre finement réglé — gros
  fichier, lot dédié avec `AdaptiveGlassContainer` pour éviter glass-on-glass), `MessageInfoSheet`
  L641, `InviteFriendsSheet`, `StatusBubbleOverlay`, `LocationPickerView` barre/bottom card.
- **Palette tokens** (épuration distincte du glass) : `ContactCardView` hex durs `#2ECC71`
  (téléphone) / `#3498DB` (email) → `MeeshyColors.success` / `.info` ; autres hex sémantiques
  durs (`AffiliateView`, `MemberManagementSection`, `AddParticipantSheet`, `AboutView`,
  `MessageInfoSheet` read/delivered) — passe palette bornée, vérifier shift visuel acceptable.
- **Ladder catégoriel arc-en-ciel** (décision charte unique) : `NotificationListView.color`,
  `TagInputView`, `MediaTypes.speakerPalette`, ladder app-side.
- Grandes surfaces polices figées (`FriendRequestListView`, `ConversationInfoSheet`,
  `ConversationDashboardView`…) ; `ThemedConversationRow` theme-aware.

## Status : ⏳ développement terminé — push + CI ; merge dans main après CI verte —
voir plan 2026-06-30-plan-iteration-69i
Thème : **épuration palette + accessibilité Dynamic Type sur la carte de contact partagé**
(`ContactCardView`). Itération bornée (1 fichier app, « logique épurée »), qui solde le
différé **palette** annoncé par 52i/53i/68i (« `ContactCardView` hex durs `#2ECC71` /
`#3498DB` → tokens sémantiques »).

CI `ios-tests.yml` (compile Xcode 26.1.x + tests simulateur 18.2) = seule vérif de build
(pas de SwiftUI sur Linux). Aucune couverture de test/snapshot ne référence ce composant
(vérifié : 0 occurrence dans `MeeshyTests/` et `MeeshySDK/Tests/`), donc swap sûr.

## Contexte / point de départ
- 53i a modernisé le **fond** de `ContactCardView` (Liquid Glass natif iOS 26 + stroke
  dégradé accent en overlay). Restaient deux dettes **distinctes du glass**, explicitement
  différées :
  1. **Couleurs dures non sémantiques** : l'icône téléphone utilisait `Color(hex: "2ECC71")`
     (vert émeraude brut) et l'icône email `Color(hex: "3498DB")` (bleu brut). Le design
     system Meeshy (`apps/ios/CLAUDE.md` § Semantic State Colors) impose des tokens
     sémantiques : `MeeshyColors.success` (#34D399) et `MeeshyColors.info` (#60A5FA). Les
     hex bruts ne s'adaptent pas à la charte et n'ont aucune variante dark/light cohérente.
  2. **Polices figées** : tous les textes utilisaient `.font(.system(size: 10/13/14))`, ce
     qui **ignore Dynamic Type** — violation directe de la règle iOS (« NEVER use fixed font
     sizes for body text », `apps/ios/CLAUDE.md` § Accessibility).

## iOS Findings

### Épuration palette — affordances sémantiques `success` / `info`
- Téléphone : `Color(hex: "2ECC71")` → `MeeshyColors.success`. Le vert est l'affordance
  canonique « appeler » ; `success` est le token vert de la charte (déjà utilisé app-side :
  `ContactsListTab`, `OnboardingStepViews`…).
- Email : `Color(hex: "3498DB")` → `MeeshyColors.info`. Le bleu est l'affordance canonique
  « message/information » ; `info` est le token bleu de la charte.
- **Exclus délibérément** : le `accentColor` reçu en paramètre (cercle avatar + label
  « CONTACT PARTAGÉ » + stroke) reste **dynamique** — c'est la couleur de marque de la
  conversation hôte (Prisme/accentColor déterministe), **pas** une affordance sémantique.
  Ne pas le tokeniser.

### Accessibilité — Dynamic Type
Remplacement des tailles fixes par des **styles de police sémantiques** qui se mettent à
l'échelle avec les réglages « Larger Text » de l'utilisateur, en conservant les graisses
de marque :
- label section `.system(size: 10, .semibold)` → `.caption2.weight(.semibold)`
- nom complet `.system(size: 14, .bold)` → `.subheadline.weight(.bold)` + `minimumScaleFactor(0.85)`
- chevron `.system(size: 11, .semibold)` → `.caption.weight(.semibold)`
- icônes phone/email `.system(size: 11, .medium)` → `.caption.weight(.medium)`
- valeurs phone/email `.system(size: 13, .medium)` → `.footnote.weight(.medium)` +
  `minimumScaleFactor(0.8)`
Le `minimumScaleFactor` garantit une dégradation **élégante** (réduction progressive avant
troncature `lineLimit(1)`) dans la largeur fixe 240pt de la carte en bulle, aux grandes
tailles Dynamic Type. La carte reste lisible sans casser le layout.
- **Conservé fixe** : le glyphe `person.crop.circle.fill` (18pt) dans le cercle avatar 36pt
  fixe — c'est un atome décoratif dimensionné dans un conteneur fixe ; le scaler
  déborderait du cercle. Justifié.

## Conforme (vérifié — ne pas re-flagger)
- A11y structurelle déjà solide : `accessibilityElement(children: .combine)` + label + hint
  localisés (cf. `apps/ios/Documentation/ACCESSIBILITY_AUDIT.md:407` « ✅ Solides »). Non
  touchée — les icônes internes restent fusionnées dans l'élément combiné.
- Le Liquid Glass + stroke dégradé du fond (53i) reste intact.
- Geste : la carte est un `Button(.plain)` → `onTap` (ouvre le contact). Inchangé.

## Anti-repetition check
Vérifié contre 53i (a fait le **glass** de cette carte, PAS les couleurs d'icône ni les
polices — les a au contraire **listés en différé palette**), 52i (a écarté la carte
entièrement), 68i (a re-cité explicitement « `ContactCardView` hex durs `#2ECC71` /
`#3498DB` → `MeeshyColors.success` / `.info` » comme différé palette restant). Aucune
itération antérieure n'a tokenisé ces couleurs ni converti ces polices en Dynamic Type.
C'est la **suite logique annoncée**.

## Différés (pour itérations suivantes — iOS)
- **Glass adoption (reste)** : `MessageOverlayMenu` (reaction picker, gros fichier — glass-in-glass
  + clip-shape), `LocationPickerView` (sheet 5+ matériaux), `ReplyThreadOverlay`,
  `MessageInfoSheet`, `InviteFriendsSheet`, `UniversalComposerBar` (toolbar).
- **Couleurs présence sémantiques (SDK)** : `MeeshyAvatar.online` / `UserIdentityBar` vert
  `2ECC71` → `MeeshyColors.success`, away `F39C12` → `.warning` (vérifier baselines snapshot
  avant — `MeeshyAvatar` a des snapshots 18.2 ; lot SDK dédié, distinct de l'app).
- **Ladder catégoriel arc-en-ciel** : `NotificationListView.color`, `TagInputView`,
  `MediaTypes.speakerPalette`, ladder app-side.
- **Dynamic Type (reste)** : grandes surfaces à polices figées (`FriendRequestListView` 11,
  `ConversationInfoSheet` 52, `ConversationDashboardView` 43, `CallView` 34…) — lots prudents
  par écran. NE PAS retoucher les couleurs **catégorielles/accent** (tags, story palette,
  speaker palette, language/theme colors de `ColorGeneration`) — ce ne sont PAS des
  sémantiques success/info.

## Status : ⏳ développement terminé — push + PR ; merge dans main après CI verte —
voir plan 2026-06-30-plan-iteration-69i
</content>
</invoke>
Thème : **épuration de code mort** — suppression du cluster `ReplyThreadOverlay`
(ancienne implémentation de lecture de fil de réponses, **superseded** par `ThreadView`)
et de son service exclusif `ReplyThreadLoader` + son test. Itération bornée et « épurée »
dans l'esprit de 51i (suppression de `OverlayMenu`, code mort) — pas une passe mécanique
large, un seul concern : retirer un cluster mort qui pollue les audits futurs.

Vérification : la CI `ios-tests.yml` (régénère le projet via `xcodegen generate` puis
compile Xcode 26.1.x + tests simulateur 18.2) sert de build de validation — la couche
SwiftUI/UIKit ne build pas sous Linux. Le projet utilise le **globbing récursif XcodeGen**
(`project.yml`), donc supprimer les `.swift` suffit : CI régénère le `project.pbxproj` et
exclut automatiquement les fichiers supprimés (interdiction d'éditer le pbxproj à la main).

## Contexte / point de départ
Le repo contient **deux** implémentations de lecture de fil de réponses :

| Composant | Statut | Présentation | Loader | Endpoint |
|---|---|---|---|---|
| **`ThreadView`** (vivant) | rendu via `.sheet` dans `ConversationView.swift:581` (`onShowThread` → `overlayState.showReplyThread`) | feuille plein écran | `ThreadRepliesLoader` | `GET /conversations/:id/messages?replyToId=…&limit=50` |
| **`ReplyThreadOverlay`** (mort) | **jamais instancié** (vérifié repo-wide) | carte overlay flottante | `ReplyThreadLoader` | `GET /conversations/:id/threads/:parentId` |

`ReplyThreadOverlay` est l'**ancienne** implémentation (carte overlay) remplacée par
`ThreadView` (feuille plein écran). Le commentaire de `ThreadView.swift:247` la citait encore
en comparaison de comportement (« no `loadError` like ReplyThreadOverlay »), preuve de la
filiation. Son service `ReplyThreadLoader` a pour **unique** consommateur cette vue morte
(docstring : « Used by `ReplyThreadOverlay` »). `ThreadView` utilise un loader **distinct**
(`ThreadRepliesLoader`, endpoint différent) → aucun chevauchement.

## iOS Findings

### Code mort — `ReplyThreadOverlay.swift` (378 lignes, vue jamais rendue)
Vérification d'absence d'instanciation : `grep -rn "ReplyThreadOverlay(" apps/ios` → **0**
résultat hors définition. Les seules occurrences du symbole étaient des **commentaires**
(`ThreadView.swift`, `ReplyThreadLoader.swift`/`ThreadRepliesLoader.swift` docstrings) et le
`project.pbxproj` généré. Aucun `#Preview`, aucune route, aucune sheet ne la monte. Supprimée.

### Code mort — `ReplyThreadLoader.swift` (52 lignes) + son test (140 lignes)
`ReplyThreadLoader` était consommé **uniquement** par `ReplyThreadOverlay:98`
(`grep -rn "ReplyThreadLoader" apps/ios` hors fichier propre → seulement la vue morte + 2
commentaires). Une fois la vue retirée, le service est orphelin. Le garder (avec son test
`ReplyThreadLoaderTests`, 4 cas) aurait laissé un service **testé mais sans consommateur de
production** — exactement le piège de « fausse couverture » que la doctrine d'épuration
proscrit (« No temporary fixes / single source of truth »). Supprimés ensemble. `ThreadView`
n'est pas affecté : il passe par `ThreadRepliesLoader` (toujours présent + testé).

### Nettoyage des commentaires pendants
- `ThreadView.swift:247` : la comparaison « no `loadError` like ReplyThreadOverlay »
  référençait un symbole supprimé → reformulée (« no dedicated error surface (no `loadError`
  state) ») sans changer le sens.
- `ThreadRepliesLoader.swift` (docstring) : retrait des mentions « distinct from the overlay
  covered by `ReplyThreadLoader` » et « Sibling of `ReplyThreadLoader` » → docstring autonome.

## Conforme / délibérément exclu (ne pas re-flagger)
- **`ThreadData` (modèle public SDK, `packages/MeeshySDK/.../Models/ThreadModels.swift`)** :
  devient sans consommateur app après ce retrait, mais c'est un **modèle d'API public du
  SDK** (réponse du endpoint gateway `/threads/:parentId`, qui existe toujours côté backend).
  Supprimer une API **publique** du SDK est hors-scope d'une itération UI/UX iOS et plus
  risqué (consommateurs externes potentiels). **Laissé en place** — voir Différés.
- **`project.pbxproj`** : artefact généré (migration XcodeGen). **Non édité à la main**
  (doctrine `apps/ios/CLAUDE.md`). CI régénère via `xcodegen generate` → références aux
  fichiers supprimés retirées automatiquement. Le laisser périmé dans le commit est conforme.
- **`tasks/todo*.md`** : logs historiques non compilés (F1 déjà marqué « ✅ Livré »).
  Laissés tels quels (édition = bruit sans valeur de build).
- **Scrim de `ReplyThreadOverlay`** : N.B. — la surface `.ultraThinMaterial` (ligne 27) était
  un **backdrop de dimming** derrière une carte modale opaque (`surfaceGradient`), **pas** du
  chrome flottant → elle n'était de toute façon **pas** un candidat Liquid Glass (la doctrine
  51i/52i/53i réserve le verre au chrome flottant, jamais aux scrims/contenus). Point sans
  objet désormais (fichier supprimé) — noté pour clore le différé « ReplyThreadOverlay
  (.ultraThinMaterial plein écran, vérifier clip) » listé par 68i : **résolution = c'était du
  code mort, supprimé, pas converti**.

## Anti-repetition check
Vérifié contre : 68i (qui listait `ReplyThreadOverlay` en différé Glass « vérifier clip » —
ce différé est ici **clos par suppression**, pas par conversion), 53i (`EmojiReactionPicker`),
52i (`MentionSuggestionPanel`/`MiniAudioPlayerBar`), 51i (`ContextActionMenu` + **suppression
du code mort `OverlayMenu`** — même pattern d'épuration, fichiers différents). Aucune
itération antérieure n'avait identifié `ReplyThreadOverlay`/`ReplyThreadLoader` comme morts ;
au contraire 68i s'apprêtait à y investir une conversion Glass — ce qui aurait été du travail
gaspillé sur une vue jamais rendue. Orthogonal aux branches iOS en vol (68i `GlobalSearchView`,
53i `EmojiReactionPicker`, 52i `MentionSuggestionPanel`/`LocationPickerView`) : aucun fichier
partagé.

## Différés (pour itérations suivantes — iOS)
- **`ThreadData` (SDK)** : si confirmé inutilisé par tout consommateur SDK externe, candidat à
  suppression dans une itération **SDK dédiée** (`packages/MeeshySDK/`), avec le endpoint
  gateway `/threads/:parentId` à arbitrer côté backend (hors scope iOS). À ne PAS supprimer
  dans une itération app.
- **Palette tokens** : `ContactCardView` hex durs `#2ECC71` (téléphone) / `#3498DB` (email)
  → `MeeshyColors.success` / `.info` (épuration palette, déjà passé en Glass). Autres hex
  flat-UI épars : `MemberManagementSection` (`F8B500`/`3B82F6`), `MessageInfoSheet`
  (`34B7F1` read-receipt — arbitrer vs token `readReceipt`).
- **Glass adoption (reste)** : `MessageOverlayMenu` (gros fichier, `AdaptiveGlassContainer`
  pour éviter glass-on-glass) ; arbitrage `ContactCardView`/`LocationPickerView` selon l'état
  des branches en vol.
- **Ladder catégoriel arc-en-ciel** (décision charte unique) : `NotificationListView.color`,
  `TagInputView`, `MediaTypes.speakerPalette`.
- Grandes surfaces polices figées (`FriendRequestListView` 11, `ConversationInfoSheet` 52,
  `ConversationDashboardView` 43, `CallView` 34…) ; `ThemedConversationRow` theme-aware.

## Status : ⏳ développement terminé — push + CI ; merge dans `main` après CI verte —
voir plan 2026-06-30-plan-iteration-69i
