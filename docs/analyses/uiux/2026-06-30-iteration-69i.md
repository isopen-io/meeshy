# UI/UX Analysis — Iteration 69i (2026-06-30)

## Scope
**iOS exclusivement** (suffixe `i` — Web et Android sont couverts par d'autres agents).
Thème : **accessibilité VoiceOver — boutons retour « icône-seule »**. Plusieurs en-têtes
custom rendent un bouton retour `chevron.left` **sans texte visible ni
`accessibilityLabel`**, si bien que VoiceOver annonce littéralement « chevron.left ».
Itération **bornée, additive et cohérente** (« logique épurée ») : un seul concern a11y,
une ligne par fichier, **zéro changement visuel**.

La CI `ios-tests.yml` (compile Xcode 26.1.x + tests simulateur 18.2) sert de vérification
de build (pas de build local SwiftUI/UIKit sur Linux).

## Contexte / point de départ
- L'app utilise un pattern hybride NavigationStack + en-têtes custom (cf. `apps/ios/CLAUDE.md`).
  Beaucoup d'écrans masquent la barre de nav native et re-dessinent un bouton retour
  `Button { dismiss() } label: { Image(systemName: "chevron.left") … }`.
- Le composant canonique `ThemedBackButton` (`ConversationHelperViews.swift`) est **déjà**
  accessible : `.accessibilityLabel(a11y.back / a11y.back.with_unread)`.
- La clé **`a11y.back`** existe dans `Localizable.xcstrings` et est **entièrement traduite**
  (fr `Retour`, en `Back`, es `Atrás`, de `Zurück`, pt-BR `Voltar`) — c'est la SSOT pour
  ce label.
- **Important** : un `Button` icône-seule sans `accessibilityLabel` hérite du nom du symbole
  SF (« chevron.left ») comme label VoiceOver. Quand le bouton contient un **texte visible**
  « Retour » à côté du chevron, VoiceOver lit ce texte → pas de gap. Le gap n'existe que
  pour les boutons **icône-seule**.

## iOS Findings

### a11y — 7 boutons retour icône-seule sans label
Audit repo-wide des `Button` contenant `Image(systemName: "chevron.left")`. Tri :
- **Déjà conformes** (NE PAS re-flagger) :
  - texte visible « Retour » adjacent (VoiceOver lit le texte) : `SecurityView`,
    `PrivacySettingsView`, `NotificationSettingsView`, `ChangePasswordView`,
    `BlockedUsersView`, `EditProfileView`, `ActiveSessionsView`, `OnboardingFlowView`.
  - `.accessibilityLabel` déjà présent : `CommunityLinksView`, `TrackingLinksView`,
    `ParticipantsView`, `MediaDownloadSettingsView`, `DataStorageView`, `DataExportView`,
    `DeleteAccountView`, `PrivacyPolicyView`, `TermsOfServiceView`, `LicensesView`,
    `AboutView`, `SupportView`, `GlobalSearchView` (cette dernière en vol PR #1080).
  - `ThemedBackButton` (`ConversationHelperViews`) : déjà `a11y.back`.
- **Gaps réels corrigés (icône-seule, ni texte ni label)** :
  1. `FriendRequestListView` (en-tête « Demandes d'amis »)
  2. `UserStatsView` (en-tête « Statistiques »)
  3. `ShareLinksView` (en-tête « Liens de partage »)
  4. `AffiliateView` (en-tête « Parrainage »)
  5. `NewConversationView` (en-tête création de conversation)
  6. `ThreadView` (en-tête fil de discussion)
  7. `LoginView` (retour « désélectionner le compte » sélectionné)

**Correctif** (chaque site) : ajout de
`.accessibilityLabel(String(localized: "a11y.back", bundle: .main))` sur le `Button`
(forme **identique** à celle du `ThemedBackButton` canonique). Purement additif : aucune
modification de `Image`, de police, de couleur, de frame ou de geste. VoiceOver annonce
désormais « Retour / Back / … » au lieu de « chevron.left ».

### Choix de la clé : `a11y.back` (pas `common.back`)
Les frères déjà labellisés utilisaient `common.back` (texte du bouton **visible** ailleurs),
mais `common.back` **n'est pas une entrée du catalogue** (résolue uniquement via
`defaultValue: "Retour"` → non traduite hors fr/en). `a11y.back` est la clé **dédiée a11y,
traduite dans 5 locales**, déjà adoptée par `ThemedBackButton` → meilleur choix Prisme /
VoiceOver multilingue, et SSOT unique.

## Conforme (vérifié — ne pas re-flagger)
- Les en-têtes à texte visible « Retour » : label implicite correct (texte = label VoiceOver).
- Boutons retour déjà munis de `.accessibilityLabel` (liste ci-dessus).
- `ThemedBackButton` : référence gold-standard, inchangé.
- **PÉRIMÉ** : la note 51i « `FriendRequestListView` 11 polices figées » — ce fichier utilise
  déjà des polices **sémantiques Dynamic Type** (`.subheadline`/`.caption`/`.footnote`/
  `.headline`/`.body`) ; seule l'icône décorative empty-state garde une taille fixe (légitime).
  Ne plus citer ce fichier comme « polices figées ».

## Anti-repetition check
Vérifié contre : iter-51i (`ContextActionMenu` Liquid Glass + dead code — n'a pas touché ces
en-têtes), iter-49i/48i (palettes SDK/app), iter-45i/46i (Story). Aucune itération antérieure
n'avait labellisé ces boutons retour. **Orthogonalité avec les PR en vol** confirmée par
diff de fichiers : #1076 (`MentionSuggestionPanel`/`MiniAudioPlayerBar`), #1080
(`MentionSuggestionPanel`/`GlobalSearchView`/`CompatibilityLayerTests`), #1083
(`MentionSuggestionPanel`/`LocationPickerView`) — **aucun** des 7 fichiers de 69i n'y figure.

## Différés (pour itérations suivantes — iOS)
- **Glass adoption (reste)** : `MessageOverlayMenu.panelBackground` (bottom-sheet, gros
  fichier, lot dédié `AdaptiveGlassContainer` pour éviter glass-on-glass) ; `ReplyThreadOverlay`
  (vérifier si backdrop vs surface flottante) ; `CallEffectsOverlay` toolbar pill.
  (`MentionSuggestionPanel`/`MiniAudioPlayerBar`/`GlobalSearchView`/`LocationPickerView`
  **en cours de merge** via #1076/#1080/#1083 — ne pas re-prendre.)
- **a11y** : autres surfaces icône-seule non-back (boutons toolbar custom sans label) ;
  regroupement VoiceOver des lignes (`accessibilityElement(children: .combine)`) sur les
  rangées listant avatar+nom+meta (ex. `FriendRequestListView` row).
- **Palette tokens** : `ContactCardView` hex `#2ECC71`/`#3498DB` — **ATTENTION** : ce sont des
  couleurs de **contenu/affordance** (convention contacts phone=vert/email=bleu), partie de la
  palette de génération de couleurs (langues/thèmes/tags/speakerPalette, assertées en tests),
  **pas** des états `success`/`info`. Ne PAS migrer mécaniquement vers `MeeshyColors.success/.info`
  (sémantiquement faux) — réévaluer en charte palette dédiée.
- **Ladder catégoriel arc-en-ciel** : `NotificationListView.color`, `TagInputView`,
  `MediaTypes.speakerPalette`.

## Status : ⏳ développement terminé — push branche, merge dans main après CI verte —
voir plan `2026-06-30-plan-iteration-69i`
Thème : **poursuite directe de la série Liquid Glass iOS 26 (51i → 68i)** sur la dernière
grosse surface de chrome flottant non convertie — l'**overlay de fil de discussion**
(`ReplyThreadOverlay`, la carte modale de thread de réponses) — couplée à une **épuration
a11y** (bouton de fermeture sans label VoiceOver, drag-indicator + skeleton décoratifs hors
arbre VoiceOver). Itération bornée et cohérente (« logique épurée »).

Vérification : la CI `ios-tests.yml` (compile Xcode 26.1.x + tests simulateur) sert de build
de validation (pas de build local SwiftUI sur Linux). Le smoke test `CompatibilityLayerTests`
couvre **déjà** la forme `RoundedRectangle` + la variante teintée d'`adaptiveGlass`
(lignes 70/85, ajoutées par 68i) — la surface API de 69i est donc déjà exercée, aucune
extension de test requise.

## Contexte / point de départ
- `adaptiveGlass(in:tint:interactive:)` (`packages/MeeshySDK/.../Compatibility/AdaptiveGlass.swift`)
  est l'atome qui gate le vrai `glassEffect` iOS 26 et dégrade en `.ultraThinMaterial` teinté
  (+ liseré) avant. Adopté jusqu'ici sur les pilules/menus flottants (`FloatingCallPillView`,
  `CallView`, `ContextActionMenu` [51i], `MentionSuggestionPanel` + `GlobalSearchView` [68i],
  overlays liste conv, `ReelsPlayerView`, `IncomingCallView`).
- `ReplyThreadOverlay` était listé **explicitement en différé glass #1 par 68i**
  (« `.ultraThinMaterial` plein écran, vérifier clip »). C'est une **carte de contrôle
  flottante au-dessus du contenu** (thread de réponses présenté en overlay) — cas d'usage
  canonique du Liquid Glass selon la doctrine Apple (verre = chrome flottant, pas contenu).

## iOS Findings

### Adoption — carte `ReplyThreadOverlay` → Liquid Glass natif
La carte de thread utilisait `.background(RoundedRectangle(18).fill(theme.surfaceGradient(tint:))
.overlay(stroke))`. Remplacement du remplissage par
`.adaptiveGlass(in: RoundedRectangle(cornerRadius: 18, style: .continuous), tint:
Color(hex: accentColor).opacity(0.18))` **+** conservation du `.overlay(stroke)` de marque,
du `.clipShape(18)` et de l'ombre d'élévation d'accent.
- **iOS 26+** : carte en **Liquid Glass natif translucide teinté accent** — le thread flotte
  réellement en verre au-dessus de la conversation.
- **iOS < 26** : `.ultraThinMaterial` + teinte accent + hairline (via le repli de l'atome) —
  équivalent perceptuel à l'existant (`surfaceGradient` était déjà semi-translucide), plus le
  liseré qui ancre la surface.
- L'identité visuelle (accent de conversation) est **préservée** : teinte passée à l'atome +
  stroke `theme.border(tint: accentColor)` conservé au-dessus du verre. Le `.clipShape` partage
  exactement la forme du verre (RoundedRectangle 18) ⇒ pas d'artefact de clip glass-sur-bord.

### Périmètre délibérément exclu (doctrine Apple : verre = chrome, pas contenu)
- Le **scrim de fond** (`Color.black.opacity(0.3).background(.ultraThinMaterial)` plein écran,
  tap-to-dismiss) reste en material : c'est un **voile d'assombrissement de contenu**, pas du
  chrome de contrôle. Conforme, ne pas re-flagger.
- Les **fonds de lignes du thread** (`Color(hex: accentColor).opacity(0.1)` sur la bulle
  parente, chips de réponse) restent en teinte plate : contenu scrollable interne à la carte,
  pas du chrome ⇒ pas de glass-sur-glass.

### Épuration a11y — overlay de thread
1. **Bouton de fermeture sans label** : le `Button { dismiss() }` (icône `xmark.circle.fill`)
   n'exposait **aucun** `accessibilityLabel` ⇒ VoiceOver l'annonçait par le nom brut du symbole
   SF (« xmark circle fill »). Ajout de `.accessibilityLabel("common.close" → « Fermer »)`
   (réutilisation de la clé i18n existante, pas de nouvelle clé). C'est le **seul** affordance
   de fermeture exposé à VoiceOver (le scrim tap-to-dismiss et le drag-to-dismiss sont
   invisibles au rotor) — combler ce label est donc critique.
2. **Drag-indicator décoratif** (capsule de glissement) → `.accessibilityHidden(true)` : forme
   vide sans sémantique, le rotor ne doit pas s'y arrêter.
3. **Skeleton de chargement décoratif** (3 lignes shimmer) → `.accessibilityHidden(true)` :
   exact parallèle de l'épuration `mentionSkeletonRows` de 68i — avatars/barres vides sans
   contenu sémantique pendant le streaming.

## Conforme (vérifié — ne pas re-flagger)
- Le drag-to-dismiss (`DragGesture` > 100pt) et le tap-scrim de fermeture sont déjà câblés.
- Les lignes de réponse exposent déjà nom + horodatage relatif + contenu (texte natif,
  Prisme Linguistique via `translationResolver`). Conforme.
- Le `RoundedRectangle(18)` du verre == forme du `.clipShape` ⇒ pas de clip glass défectueux
  (réponse au « vérifier clip » noté par 68i).

## Anti-repetition check
Vérifié contre 68i (a converti `MentionSuggestionPanel` + `GlobalSearchView` ; a explicitement
**laissé `ReplyThreadOverlay` en différé**), 51i (`ContextActionMenu`), 49i/48i (palette).
Aucune itération antérieure n'avait adopté le Liquid Glass sur `ReplyThreadOverlay` ni rendu
son bouton de fermeture accessible / son skeleton non-accessible. Le pattern « glass + skeleton
`accessibilityHidden` » prolonge 68i sur une nouvelle surface, sans recouper son périmètre.

## Différés (pour itérations suivantes — iOS)
- **Glass adoption (reste)** : panneau `MessageOverlayMenu` (chrome glow/ombre finement réglé —
  gros fichier, lot dédié avec `AdaptiveGlassContainer` pour éviter glass-on-glass), toolbars de
  composer app-side.
- **Palette tokens** : `ContactCardView` hex durs `#2ECC71` (téléphone) / `#3498DB` (email)
  → `MeeshyColors.success` / `.info` ; verts d'online/présence (`MeeshyAvatar`, `UserIdentityBar`,
  `LiveLocationBadge` : `#2ECC71`) → `MeeshyColors.success` (lot dédié — vérifier baselines
  snapshot avant swap SDK).
- **Ladder catégoriel arc-en-ciel** (décision charte unique) : `NotificationListView.color`,
  `TagInputView`, `MediaTypes.speakerPalette`, ladder app-side.
- Grandes surfaces polices figées (`FriendRequestListView` 11, `ConversationInfoSheet` 52,
  `ConversationDashboardView` 43, `CallView` 34…) ; `ThemedConversationRow` theme-aware.

## Status : ⏳ développement terminé — push + CI ; merge dans main après CI verte —
voir plan 2026-06-30-plan-iteration-69i
