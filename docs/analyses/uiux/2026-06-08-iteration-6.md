# UI/UX Analysis — Iteration 6 (2026-06-08)

## Context

Builds on iterations 1–5. Previous work covered: MeeshyColors full migration (all major views),
Dynamic Type (FloatingCallPillView, StatusBarView, Contacts folder), i18n (Header, ErrorBoundary,
clipboard, video-calls, viewers, MentionAutocomplete, PushPermissionBanner, PermissionRequest,
AudioControls, admin agent panel × 6 components), iOS a11y systemic (15 files, FeedPostCard media
grid), admin dark mode, iOS brand indigo migration, web security (rel="noopener noreferrer").

This iteration targets the remaining deferred items from iteration 5: web i18n in 7 files,
web a11y icon-only buttons, iOS a11y in 4 files, iOS Dynamic Type in 3 large views.

Pre-analysis corrections (items already fixed, removed from scope):
- `RankingFilters.tsx:109` — already has `dark:bg-gray-950` ✅ already correct
- `BubbleFooter.swift:153` — already has `.accessibilityLabel` ✅ already correct
- `MiniAudioPlayerBar.swift:161` — already has `.accessibilityLabel` ✅ already correct

---

## Web — i18n Hardcoded Strings — HIGH

### 1. AnonymousForm.tsx (joinPage namespace)

| Line | Hardcoded | Key | Status |
|------|-----------|-----|--------|
| 116 | `"Ce pseudo est déjà utilisé"` | `joinPage.usernameAlreadyTaken` | Key exists ✅ — just wire `t()` |
| 154 | `"Ce pseudo est déjà utilisé"` | `joinPage.usernameAlreadyTaken` | Key exists ✅ — just wire `t()` |
| 157 | `"Ce pseudo est disponible"` | `joinPage.usernameAvailable` | Key missing — add |

Component already has `const { t } = useI18n('joinPage')`.

### 2. groups-layout-responsive.tsx (groups namespace)

| Line | Hardcoded | Key | Status |
|------|-----------|-----|--------|
| 402 | `"Aucune communauté"` | `groups.noGroups` | Key exists ✅ |
| 403 | `"Créez-en une pour commencer"` | `groups.noGroupsDescription` | Key exists ✅ |
| 576 | `"Créée le {date}"` | `groups.details.createdOn` | Key exists ✅ |
| 590 | `"Sélectionnez une communauté"` | `groups.list.selectCommunity` | Key exists ✅ |
| 592 | `"Choisissez une communauté..."` | `groups.list.selectCommunityDescription` | Key exists ✅ |

Component already has `const { t: tGroups } = useI18n('groups')`. All keys exist — just wire.

### 3. AgentLiveTab.tsx (admin namespace)

| Line | Hardcoded | Key |
|------|-----------|-----|
| 451 | `"Sélectionnez une conversation pour superviser son état en temps réel"` | `admin.agentLive.selectConversation` |

No `useI18n` hook — add import + add key to admin.json.

### 4. RankingFilters.tsx (admin namespace)

| Line | Hardcoded | Key |
|------|-----------|-----|
| 69 | SelectTrigger — entity select placeholder | `admin.ranking.selectEntity` |
| 106 | `"Sélectionnez le critère"` | `admin.ranking.selectCriterion` |
| 151 | `"Sélectionnez la période"` | `admin.ranking.selectPeriod` |

No `useI18n` hook — add import + add keys to admin.json ranking section.

### 5. UserLanguageSection.tsx (admin namespace)

| Line | Hardcoded | Key |
|------|-----------|-----|
| 96 | `"Préférences de Langue"` | `admin.userDetail.languagePreferences` |

No `useI18n` hook — add import + add key to admin.json.

### 6. translation-monitor.tsx (admin namespace)

| Line | Hardcoded | Key |
|------|-----------|-----|
| 156 | `"Chargement des métriques..."` | `admin.translationMonitor.loadingMetrics` |
| 270 | `"Répartition par langue"` | `admin.translationMonitor.languageBreakdown` |

No `useI18n` hook — add import + add keys to admin.json.

### 7. language-select.tsx (components namespace)

| Line | Hardcoded | Key |
|------|-----------|-----|
| 103 | `"Aucune langue trouvée"` | `components.languageSelect.notFound` |

No `useI18n` hook — add import + add key to components.json.

---

## Web — Accessibility (icon-only buttons) — HIGH

| File | Line | Icon | aria-label to add |
|------|------|------|------------------|
| `admin/agent/AgentConversationsTab.tsx` | 328 | `<Settings />` | `"Edit agent configuration"` |
| `admin/agent/AgentConversationsTab.tsx` | 335 | `<Trash2 />` | `"Delete configuration"` |
| `admin/agent/TriggerSchedulingModal.tsx` | 298 | `<X />` | `"Cancel scheduled trigger"` |
| `admin/agent/UserPicker.tsx` | 51 | `<Plus />` | `"Add user"` |
| `admin/agent/ConversationPicker.tsx` | 120 | `<X />` | `"Clear selection"` |
| `video-calls/AudioEffectsCarousel.tsx` | 104 | `<X />` | `"Close audio effects"` |
| `conversations/details-sidebar/CustomizationManager.tsx` | 143 | `<X />` | `"Cancel editing name"` |
| `conversations/details-sidebar/CustomizationManager.tsx` | 211 | `<X />` | `"Cancel editing reaction"` |

---

## iOS — Accessibility (missing accessibilityLabel) — HIGH

| File | Line | Element | Label to add |
|------|------|---------|-------------|
| `OnboardingStepViews.swift` | 43 | eye/eye.slash toggle | `"Toggle password visibility"` |
| `OnboardingStepViews.swift` | 326 | arrow.right.circle skip | `"Skip step"` |
| `OnboardingStepViews.swift` | 771 | xmark.circle.fill clear | `"Clear search"` |
| `MagicLinkView.swift` | 236 | arrow.clockwise retry | `"Resend magic link"` |

---

## iOS — Dynamic Type (PostDetailView, ThreadView, ReplyThreadOverlay) — MEDIUM

| File | Fixed `.font(.system(size:` instances |
|------|--------------------------------------|
| `PostDetailView.swift` | 48 instances |
| `ThreadView.swift` | 13 instances |
| `ReplyThreadOverlay.swift` | 15 instances |

Semantic mapping to apply:
- 10–11pt → `.caption2`
- 12pt → `.caption`
- 13pt → `.footnote`
- 14–15pt → `.subheadline`
- 16pt → `.callout`
- 17pt → `.body`
- 18–20pt → `.headline`
- 24pt+ (decorative icons/titles) → `.largeTitle` or leave fixed with `.minimumScaleFactor(0.7)`
- Bold/Medium weights preserved via `.font(.semantic.weight(.x))`

---

## Deferred → Itération 7

- iOS Toast messages hardcodés: `FeedViewModel`, `PostDetailViewModel`, `StoryViewModel`, `ConversationView` (~40+ strings)
- iOS Color(hex:) audit: `UniversalComposerBar` (47×), `ConversationInfoSheet` (24×), `ConversationDashboardView` (24×), `ConversationPreferencesTab` (22×)
- Full accessibility review of newly added components (MessageSearch, PinnedMessageBanner)
- Web: New components added in iter-5 (MessageSearch.tsx, PinnedMessageBanner.tsx) — i18n/a11y audit
