# UI/UX Analysis — Iteration 44 (2026-06-12)

## Scope
Full three-platform audit from main HEAD (post-merge #576/#579/#588). Web: locale-frozen dates on the v2 message thread, notification-preferences page with zero i18n, residual `'fr-FR'` in conversation modals/translation components. iOS: Dynamic Type on the two remaining link surfaces (CreateShareLinkView, TrackingLinkDetailView), off-palette hex sweep on ProfileView/PrivacySettingsView, avatar-edit a11y. Android: es/pt string parity restoration (14 keys), localizable sender-prefix format, SettingsScreen spacing tokens.

## Web Findings

### Fixed this iteration
- `components/v2/MessageTimestamp.tsx` — 9 hardcoded `'fr-FR'` calls + hardcoded French strings ("Aujourd'hui", "Hier", "Aujourd'hui a …" with missing accent) on the **core v2 chat thread separator**; manual `${day} ${month}` assembly produced wrong word order for en. Rewired on `useI18n('conversations')` + new `messageTimestamp.{today,yesterday,todayAt,yesterdayAt,dayAt}` subtree (4 locales), dates via `toLocaleDateString(locale, …)`. English aria-label `"Message timestamp: …"` replaced by the localized formatted value.
- `app/notifications/preferences/page.tsx` — entire page hardcoded French (~30 strings) with **zero useI18n import**, while the `notifications.notifPrefs` subtree already existed with channels/types/dnd labels in all 4 locales. Page rewired on existing keys; only 14 missing keys added (pageTitle/pageSubtitle/4 section headers/buttons/errors). Spinner got `role="status"` + localized aria-label.
- `components/conversations/quick-link-config-modal.tsx:128`, `link-summary-modal.tsx:84` — `toLocaleDateString('fr-FR')` → hook locale.
- `components/translation/translation-stats.tsx` — title/description/labels hardcoded French + `'fr-FR'` date + "Jamais"; new `settings.translationStats` subtree (4 locales).
- `components/translation/translation-monitor.tsx:233` — "Cache Hit" hardcoded English → `admin.translationMonitor.cacheHit` (new key, 4 locales); `:307` `toLocaleTimeString()` without locale → hook locale.
- `components/conversations/ConversationLayout.tsx:81` — hoisted static loader with hardcoded "Vérification de l'authentification…" → small component using existing `common.authGuard.checking`; spinner `role="status"`.
- `components/conversations/create-conversation-modal.tsx:302` — "Récapitulatif de la conversation" → existing `modals.createConversationModal.preview.title`.
- `components/v2/FriendRequestCard.tsx:42` — `toLocaleDateString()` without locale; component now accepts optional `locale` prop, wired from `useI18n('contacts')` in `app/v2/(protected)/contacts/page.tsx` (3 call sites).

### Deferred (admin-only, low traffic)
- `app/admin/debug.tsx` (~13 FR strings, no i18n), `components/admin/agent/AgentArchetypesTab.tsx` (~10 + EMOJI_LABELS map), `AgentConfigDialog.tsx` (~15), `AgentConversationsTab.tsx` (3), `UserPicker.tsx` (5)
- Hardcoded `'fr-FR'` in admin components: `ranking/RankingStatsImpl.tsx:16`, `ranking/utils.tsx:7`, `agent/TriggerSchedulingModal.tsx:38`, `agent/AgentLiveTab.tsx:336`, `agent/AgentScheduleTimeline.tsx:26`, `agent/AgentMessagesModal.tsx:23,25`, `user-detail/UserSecuritySection.tsx:31`, `user-detail/UserActivitySection.tsx:93`
- `components/affiliate/share-affiliate-modal.tsx:205` — local locale map with `'fr-FR'` fallback (works but should use hook locale)
- No-locale dates in low-traffic surfaces: `ConnectionQualityBadge.tsx:95`, `ConversationEncryptionSection.tsx:135`, `LinkTypeStep.tsx:163`
- `app/notifications/preferences/page.tsx` toggles vs the richer `NotificationPreferences` component (`notifications/preferences` page duplicates a subset — candidate for consolidation)
- Chart hex colors without dark variants: `RankingStatsImpl.tsx`, `MermaidDiagramImpl.tsx`, `AgentOverviewTab.tsx` (needs a chart-theming pass, not a string swap)
- `components/video-calls/audio-effects/effect-details/BackSoundDetails.tsx` (4 FR strings)

## iOS Findings

### Fixed this iteration
- `CreateShareLinkView.swift` — 24 frozen `.font(.system(size:))` → semantic (`.subheadline`/`.caption`/`.caption2`/`.footnote`/`.callout`, stepper count `.system(.title, design: .rounded)`); icon badges in fixed 34pt frames intentionally kept. **Bonus i18n**: `ExpirationOption.label` (Jamais/24 heures/7 jours/30 jours/3 mois) and picker section headers (Groupes/Communautés/Canaux & Public) were raw French — now `String(localized:)` following the file's own convention.
- `TrackingLinkDetailView.swift` — 18 frozen text fonts → semantic (title `.headline`, monospaced URL `.system(.caption, design: .monospaced)`, stat values `.title2`, rows `.footnote`/`.caption`/`.caption2`); hero/badge icons (26/24/18/14pt) kept fixed per iteration-32 precedent.
- `PrivacySettingsView.swift:13` — `accentColor = "6366F1"` literal → `MeeshyColors.brandPrimaryHex`.
- `ProfileView.swift` — off-palette hex sweep: `4ECDC4` ×5 → `indigo300`/`indigo300Hex`, `E91E63` ×2 → `brandPrimaryHex`/`indigo500`, `FF6B6B` ×2 → `errorHex`, `9B59B6` ×2 → `indigo600Hex` (mappings identical to iter-42b SettingsView precedent). Avatar-edit PhotosPicker (pencil.circle.fill) got `.accessibilityLabel`.

### Verified already fixed (no action)
- `ShareLinkDetailView`, `CreateTrackingLinkView`, `ConversationView:495,551`, `PostDetailView` textSelection, `IncomingCallView` .white overlays (semantically correct), `TrackingLinksView` tokens.

### Deferred
- Large font-migration surfaces (counts of `.font(.system(size:`): TwoFactorSetupView (42, mostly icons), CallView (34), GlobalSearchView (32), remaining ProfileView text fonts (~30), SettingsView (31), VoiceProfileManageView (29), FeedView+Attachments (29), StoryViewerView+Content (28), WidgetPreviewView (27, widget-fixed by nature), FeedCommentsSheet (27)
- `AudioPostComposerView.swift:81-88` — 6 hex gradient stops (`13111C`, `0F0D19`, `1E1B4B`, `EEF2FF`, `E0E7FF`, `C7D2FE`); indigo-family values exist as tokens but the two dark washes need design decision
- `ThemedConversationRow.swift:45-51` — 8 text-color hexes (needs theme-aware pass, leaf-view rule: no singleton observation)
- `.textSelection(.enabled)` audit on bubble components + FeedCommentsSheet
- `ProfileView.swift:526` chevron disclosure (decorative, low priority)

## Android Findings

### Fixed this iteration
- **es/pt parity restored** (14 keys × 2 locales): `feature/auth` `login_error_required`; `feature/chat` `chat_date_today/yesterday/day_before_yesterday`, `chat_scroll_to_bottom`; `feature/conversations` 3 banner + 6 preview keys; `sdk-ui` `avatar_fallback`. Root cause: iterations 40-42 added en+fr keys without touching es/pt added in iter-42.
- `LastMessagePreview.kt:44` — `"$sender : $body"` hardcoded the **French** typographic colon (space before) for every language. New `senderFormat` label backed by `conversations_preview_sender_format` (`%1$s: %2$s` en/es/pt, `%1$s : %2$s` fr), wired in `ConversationListScreen`, covered by a new unit test (`sender prefix follows the locale format`).
- `SettingsScreen.kt:88` — profile row `16.dp/12.dp` raw padding → `MeeshySpacing.lg/md`.

### Verified clean
- Hardcoded strings: all feature modules + sdk-ui pass (stringResource everywhere)
- Interactive icons: all null contentDescription cases are decorative-with-text (acceptable)
- Dark theme: complete Light/Dark token schemes via `isSystemInDarkTheme()`
- Colors: tokens only in composables (hex confined to theme files)

### Accepted/Deferred
- `ChatScreen.kt:442` emoji `fontSize = 22.sp` — **accepted** (fixed emoji glyph size in quick-reaction button, per iter-43; stop re-flagging)
- `SettingsScreen.kt:225-229` `vertical = 14.dp`, `:255` chevron `size(14.dp)` — no matching MeeshySpacing token (md=12, lg=16); left as-is rather than changing visual rhythm
- Stories UI — still absent (~45% feature-complete app; tracked in `apps/android/tasks/feature-parity.md`)
- sdk-ui `MessageBubble` internal layout dp (300/252/2dp) — intentional component dimensions

## Cross-platform coherence check
- Date "today/yesterday" wording now localized on **all three** platforms (Android `chat_date_*` since iter-40, iOS native formatters, web `messageTimestamp.*` this iteration) with identical fr/es/pt vocabulary (Aujourd'hui/Hoy/Hoje, Hier/Ayer/Ontem).
- Sender-prefix format: Android now matches iOS/web behavior (locale-aware separator; web uses key interpolation, iOS uses system list formatting).
- Off-palette hex mappings reuse the exact iter-42b table (E91E63→brandPrimary, 4ECDC4→indigo300, FF6B6B→error, 9B59B6→indigo600) — no new mapping invented.

## Anti-repetition check
Verified against iterations 32-43: search page (43), v2 links/communities/settings (42b), Settings/Notifications/DataExport/Conversation hex (42), BookmarksView/PostTranslationSheet/LinksHubView fonts (43), Android settings/contacts i18n (41/42b), sdk-ui bubble (40/43) — none overlap. This iteration uniquely covers: web MessageTimestamp, notifications/preferences page, translation-stats/monitor, conversation modals' dates, FriendRequestCard; iOS CreateShareLinkView/TrackingLinkDetailView/ProfileView/PrivacySettingsView; Android es/pt parity + sender format.
