# UI/UX Analysis — Iteration 44 (2026-06-12)

## Scope
Web date/locale infrastructure + admin i18n leftovers; iOS links-surface Dynamic Type completion (carry-over from iteration 43); Android notification i18n leftover + conversations deep link wiring.

## Web Findings

### Critical: `utils/date-format.ts` hardcodes `fr-FR` (single source of truth broken)
The central date utility — consumed by BubbleMessage, MessageNameDate, ConversationsWidget, CommunitiesWidget, use-message-interactions (message copy) — hardcodes `'fr-FR'` in 12 `toLocale*` calls and concatenates the French word `à` in `formatFullDate`. Every non-French user sees French weekday/month names and French date layout.
- `formatRelativeDate` lines 67, 76-77, 87-89
- `formatConversationDate` lines 127, 135, 144-145, 155-157
- `formatFullDate` lines 171-180 (manual `à` concatenation)

### Critical: `components/v2/MessageTimestamp.tsx` hardcoded French + locale
High-traffic v2 chat separator component:
- 6 × `toLocale*('fr-FR', …)` (lines 41, 60, 66, 71, 87, 93, 98)
- Hardcoded strings `"Aujourd'hui"`, `'Hier'`, and the typo `a` instead of `à` (lines 54, 57, 81, 84, 88)
- English `aria-label={'Message timestamp: …'}` hardcoded (line 164)

### Confirmed deferred items from iteration 43 (still present)
- `app/admin/debug.tsx` — 11 hardcoded French strings (lines 86, 95, 97, 106, 110, 113, 116, 127, 139, 151, 163)
- `components/admin/agent/AgentArchetypesTab.tsx` — 10 hardcoded French strings + `EMOJI_LABELS` French map (lines 28-30, 74, 76, 78, 102, 111, 120, 129, 137, 155, 160, 170)
- `components/translation/translation-monitor.tsx:233` — `Cache Hit` hardcoded (rest of the component already uses `t('translationMonitor.*')` from admin.json)

### Minor
- `components/v2/AudioPostComposer.tsx:154` — speech recognition language uses `navigator.language || 'fr-FR'` instead of the user's resolved preferred language (Prisme: systemLanguage > regional > custom > deviceLocale). A French user with an English browser gets English speech recognition.

### Deferred (noted, not fixed this iteration)
- `components/settings/user-settings.tsx` — 17 hardcoded French toast/error strings (largest remaining offender; needs its own pass with settings.json keys)
- `components/conversation-participants-drawer.tsx` (5), `conversation-links-section.tsx` (3) — hardcoded French fallback strings in toasts
- 14 other files with scattered `'fr-FR'` literals (admin/agent/*, admin/user-detail/*, attachments/*, groups/*, links/*) — should migrate to the now-locale-aware date-format helpers
- `components/v2/FriendRequestCard.tsx:33` — local `formatRelativeDate` duplicate; should converge on `utils/date-format.ts`
- SwipeableRow aria-label propagation from `ConversationItem` swipe actions — verify in a dedicated a11y pass

## iOS Findings

### Carry-over confirmed: links surface fixed fonts (deferred since iteration 42)
- `Features/Main/Views/ShareLinkDetailView.swift` — 13 × `.font(.system(size:))`; text fonts (20 bold title, 13/12 metadata, 12 badge, 10 action labels, 22 stat values) frozen; icon-only action buttons without `.accessibilityLabel`
- `Features/Main/Views/TrackingLinkDetailView.swift` — 25 occurrences, mostly user-facing text (headers, descriptions, stat labels)
- `Features/Main/Views/CreateShareLinkView.swift` — 24 occurrences (form labels, buttons)
- `Features/Main/Views/CreateTrackingLinkView.swift` — 6 occurrences
These complete the links/monetization surface started in iterations 42-43 (LinksHubView, links list views already converted).

### Resolved upstream (no action needed)
- `PostDetailView.swift` — `.textSelection(.enabled)` present at line 744 (iteration-43 deferred item resolved)
- `ThemedConversationRow`, `VoiceProfileWizardView`, `PrivacySettingsView` `Color(hex:)` — verified context-driven (`conversation.accentColor`), compliant with the accent-color rule, NOT violations
- `IncomingCallView` `.white` — intentional over tinted glass; both call buttons have `.accessibilityLabel`

### Escalations for future iterations (counts re-measured, larger than iteration-43 estimates)
- `ConversationInfoSheet.swift` (52 fixed fonts), `ConversationDashboardView.swift` (43), `TwoFactorSetupView.swift` (42 — hero icons stay fixed), `CallView.swift` (34), `InviteFriendsSheet.swift` (33), `ProfileView.swift` (32), `GlobalSearchView.swift` (32), `SettingsView.swift` (31, was estimated ~8), `VoiceProfileManageView.swift` (29), `FeedView+Attachments.swift` (29), `DataExportView.swift` (17), `NewConversationView.swift` (15), `ChangePasswordView.swift` (14)

## Android Findings (post-iterations 40-43, app is largely clean)

### Confirmed clean
- All 10 UI modules have complete en/fr/es/pt strings.xml (iteration-42 es/pt gap closed)
- SettingsScreen profile row navigates correctly (`onOpenProfile(state.userId)` wired in MeeshyApp.kt) — iteration-43 deferred item resolved
- Zero hardcoded `Color(0xFF…)` outside theme module
- Icon-only buttons: decorative icons correctly `contentDescription = null` under rows with `role = Role.Button`

### Remaining issues
- `feature/notifications/.../NotificationsScreen.kt:133` — `"System"` fallback sender name hardcoded (user-facing, untranslated)
- Deep link gap: AndroidManifest declares `meeshy://conversations` but the NavHost only wires `chat` and `profile` deep links — `meeshy://conversations` opens the app without routing

### Accepted as-is
- `ProfileScreen.kt:164` `"@"` username prefix — universal convention, not localized on iOS/web either
- `FeedScreen.kt:148` like-count interpolation — numeric, acceptable

## Anti-repetition check
Verified against iterations 1-43: search page i18n (43), Android Settings/Contacts/MessageBubble/TypingIndicator (43), LinksHubView + links list fonts (42-43), admin i18n passes (32-39), es/pt Android locales (42). None of this iteration's fixes overlap: date-format.ts and MessageTimestamp.tsx were never touched; debug.tsx/AgentArchetypesTab/translation-monitor were explicitly deferred in 43; the 4 iOS link detail/create views are the explicit carry-over list in branch-tracking.md; Android NotificationsScreen and deep link wiring are new findings.
