# UI/UX Analysis — Iteration 44 (2026-06-12)

## Scope
First audit of the Android attachment/chat-polish code merged in PR #587 (never reviewed by the routine); web video-calls surface i18n + dark mode bubble fixes; iOS localization of relative date label + Dynamic Type on identity-critical text (caller name, 2FA) + PrivacySettingsView palette compliance. Base: main `7358047` (post #587/#588).

## Web Findings

### Critical
- `components/video-calls/ConnectionQualityBadge.tsx:95` — `toLocaleTimeString()` without locale argument; ignores interface language
- `components/video-calls/LocalVideoTile.tsx:67` — `participantName="You"` hardcoded English on the user's own tile

### Major — video-calls surface i18n gaps
- `components/video-calls/VideoStream.tsx:485` — `participantName || 'Unknown'` hardcoded English fallback
- `components/video-calls/audio-effects/CarouselNavigation.tsx:39,47` — `aria-label="Scroll left"/"Scroll right"` hardcoded English (a11y + i18n)
- `components/video-calls/audio-effects/EffectCard.tsx:78` — "ON"/"OFF" hardcoded display strings
- `components/video-calls/audio-effects/effect-details/BackSoundDetails.tsx:51,95-96,103` — hardcoded French ("Musique de fond", "Nombre de fois", "Durée (minutes)", "Répétitions"/"Minutes")

### Major — user-facing fallbacks & locale hardcoding
- `components/groups/ConversationsList.tsx:92,99` — "Aucune description" + "Dernière activité:" hardcoded French, with `toLocaleDateString('fr-FR')`
- `components/conversations/conversation-image-upload-dialog.tsx:135` — hardcoded French error toast
- `components/translation/translation-monitor.tsx:307` — `toLocaleTimeString()` without locale
- `components/translation/translation-stats.tsx:67` — `toLocaleDateString('fr-FR')`

### Major — dark mode regressions in message bubbles
- `components/common/bubble-message/DeleteConfirmationView.tsx:347` — `bg-white` without `dark:` variant
- `components/common/bubble-message/ReactionSelectionMessageView.tsx:273,302,407` — `bg-white` without `dark:` variant

### Deferred (admin-only, low traffic — carried over)
- `app/admin/debug.tsx` ~15 French strings (confirmed still broken)
- `components/admin/agent/AgentArchetypesTab.tsx` — TONE_COLORS/EMOJI_LABELS maps + ~12 French strings (confirmed still broken)
- `components/admin/agent/{AgentLlmTab,AgentGlobalConfigTab}.tsx` — 15+ InfoIcon tooltip contents in hardcoded French (systematic)
- `components/admin/agent/{TriggerSchedulingModal:288,AgentConfigDialog:484,AgentConversationsTab:291,319,UserPicker:42,AgentOverviewTab:240,ConversationPicker:100,185}` — scattered French labels
- `components/admin/ranking/{utils.tsx:7,RankingStatsImpl.tsx:16}`, `app/admin/{monitoring:445,anonymous-users:80}` — `toLocaleString('fr-FR')`
- Client-side conversation ID validation: verified — no broken path found, validation handled server-side

## iOS Findings

### Critical
- `Features/Main/Views/ConversationView+MessageRow.swift:243` — `"Hier"` hardcoded French in date separator logic (untranslatable)
- `Features/Settings/PrivacySettingsView.swift:13` — hardcoded hex `"6366F1"` + 7 hex section-color parameters (lines ~80-127) bypassing MeeshyColors

### Major — Dynamic Type on text (pattern continuation iterations 32/42/43)
- `ProfileView.swift:65` — `.font(.system(size: 13, weight: .medium))` on error message text
- `TwoFactorSetupView.swift:~86-150` — fixed text font sizes (hero icons ≥40pt stay fixed per iteration-32 precedent)
- `CallView.swift:35` + `IncomingCallView.swift:35` — caller name `.font(.system(size: 30, weight: .bold))` frozen

### Verified fixed (deferred items closed)
- SettingsView/NewConversationView fixed fonts → migrated ✅
- PostDetailView `.textSelection(.enabled)` present (line 744) ✅
- ThemedConversationRow uses conversation `accentColor` correctly ✅
- IncomingCallView/CallView `.white` over call overlay → intentional FaceTime-style dark surface, accepted ✅

### Deferred (needs design pass — identity colors)
- `ComposerModels.swift:37-48`, `UniversalComposerBar.swift:51-52`, `AudioPostComposerView.swift:81-91`, `VoiceProfileWizardView.swift:85-138` — `Color(hex:)` literals are per-mode identity colors/gradients of the composer family; mapping to tokens requires a deliberate palette decision, not a mechanical swap
- `AudioFullscreenView.swift:300+` — 10+ `.foregroundColor(.white)` over fullscreen player artwork (likely intentional, verify against blurred-artwork backgrounds)
- `FeedPostCard.swift:186` — `"\(post.commentCount) commentaires"` interpolation to verify for plural localization

## Android Findings (PR #587 surface, first audit)

### Critical — sdk-ui hardcoded strings (break all locales)
- `sdk-ui/.../bubble/MessageBubble.kt:331-334` — `formatFileSize()` hardcodes French units "Mo"/"Ko"/"o" for every locale
- `sdk-ui/.../viewer/MeeshyImageViewer.kt:82` — close button `contentDescription = "Fermer"` hardcoded French
- `sdk-ui/.../bubble/MessageBubble.kt:217,255` + `MeeshyImageViewer.kt:159` — `contentDescription = "Image"` hardcoded English
- `sdk-ui/.../bubble/BubbleContentBuilder.kt:57` — fallback attachment name "Fichier" baked into the value model (untranslatable; same anti-pattern as iter-43's "Message deleted" — belongs in the rendering layer)

### Critical — es/pt locale gaps for #587 strings
- `feature/conversations/res/values-{es,pt}/strings.xml` — 9 missing keys: `conversations_preview_{photo,video,voice,file,location,you}`, `conversations_banner_{syncing,reconnecting,offline}`
- `feature/chat/res/values-{es,pt}/strings.xml` — 3 missing keys: `chat_date_{today,yesterday,day_before_yesterday}`

### Major — accessibility
- `feature/chat/.../ChatScreen.kt:533,564` — reply/edit cancel icons sized 18dp without 48dp minimum touch target

### Verified clean / intentional
- SettingsScreen profile row navigation wired (`onOpenProfile(state.userId)`) ✅
- TypingIndicator pluralization via separate string resources ✅
- `Color.Black.copy(alpha=0.45f)` "+N" scrim over images and image-viewer black immersive background — intentional over media, both modes ✅ (kept)
- Decorative reply/edit icons `contentDescription = null` — correct, non-actionable ✅

### Deferred
- `MessageBubble.kt:351,415,417` raw 2.dp spacing vs MeeshySpacing tokens (cosmetic)
- `MessageBubble.kt:141` emoji `lineHeight = (emojiFontSize + 8).sp` magic offset (works, document later)

## Anti-repetition check
Verified against iterations 32/33/40-43: admin user-detail + agent LlmTab/GlobalConfigTab label i18n was done in iter-33 (PR #588) — the remaining InfoIcon tooltips listed above are the *untouched* residue, kept deferred. The Android sdk-ui bubble i18n of iter-40/43 covered "Message deleted"/"edited"/delivery status — this iteration covers only the NEW #587 strings (file size, viewer, image descriptions, previews/banners es-pt). iOS font work in 32/42/43 covered Bookmarks/PostTranslationSheet/LinksHub/Settings — this iteration covers ProfileView/TwoFactorSetup/Call surfaces only. No overlap.
