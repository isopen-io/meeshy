# UI/UX Analysis — Iteration 44 (2026-06-12)

## Scope
**iOS exclusivement** (directive routine : laisser les autres frontends). Solde les différés
Dynamic Type des itérations 42/43 sur les vues liens détail/création, et étend la passe à la
surface composer (UniversalComposerBar + AudioPostComposerView) : polices figées + hex hors
charte + a11y boutons icône seule.

## iOS Findings

### Critical : Dynamic Type figé — vues liens détail/création (différé iter-42/43)
Les couleurs de ces vues ont été tokenisées en iter-41 ; les polices restent figées
(`.font(.system(size: N))`), cassées aux tailles d'accessibilité :
- `Features/Main/Views/ShareLinkDetailView.swift` — 13 occurrences (10–28pt) : héros lien,
  nom, URL monospace, boutons d'action, stat cards, rangées détail, badge statut
- `Features/Main/Views/TrackingLinkDetailView.swift` — 25 occurrences : héros, tags UTM,
  boutons d'action, big stat cards, breakdown pays/appareils/navigateurs
- `Features/Main/Views/CreateShareLinkView.swift` — 24 occurrences : champs formulaire,
  badges icône, sections expiration/limites, bouton créer
- `Features/Main/Views/CreateTrackingLinkView.swift` — 6 occurrences : section UTM, labels
  champs, bouton créer

### Critical : Dynamic Type figé — surface composer
- `Features/Main/Components/UniversalComposerBar.swift` — 12 occurrences (icônes toolbar,
  placeholder, chips, badges)
- `Features/Main/Components/UniversalComposerBar+Recording.swift` — 6 occurrences
- `Features/Main/Components/UniversalComposerBar+Attachments.swift` — 11 occurrences
  (vignettes pièces jointes, noms fichiers, badges)
- `Features/Main/Views/AudioPostComposerView.swift` — 27 occurrences (héros micro, timer,
  labels langues, boutons publier)

### Hex hors charte — composer
- `UniversalComposerBar+Recording.swift:25,81` — `08D9D6` (cyan) / `FF2E63` (rose) : reliquats
  de l'ancienne palette pré-charte. Bordure focus → famille indigo de la charte ; rouge
  d'enregistrement → `MeeshyColors.errorStrong` (sémantique record/stop)
- `UniversalComposerBar+Recording.swift:121,125` — `FF2E63` fill+icône du bouton stop → `errorStrong`
- `AudioPostComposerView.swift:81,87-88` — washes de fond : `EEF2FF`/`E0E7FF`/`C7D2FE`/`1E1B4B`
  sont des correspondances EXACTES de `indigo50/100/200/950` → tokens. `13111C`/`0F0D19`
  (canvas sombres quasi noirs teintés indigo) : pas de token équivalent, conservés en
  constantes locales nommées (wash intentionnel, précédent iter-42 OnboardingView)

### Conforme (vérifié, PAS des violations)
- `Color(hex: accentColor)` dans tout le composer = couleur d'accent de conversation
  (règle charte : OBLIGATOIRE en contexte conversation) — inchangé
- Couleurs des vues liens : déjà 100 % `MeeshyColors.*` (iter-41)
- `ComposerModels.swift:225` — gradient sur accentColor de conversation, conforme

### Accessibility
- À vérifier pendant l'implémentation : boutons icône seule sans `.accessibilityLabel` sur
  les surfaces touchées (toolbar composer, stop recording, fermeture audio composer)

## Mapping polices (convention iter-32/42, échelle Large par défaut)
| Taille figée | Police sémantique |
|---|---|
| 28 | `.title` |
| 22–24 | `.title2` |
| 20 | `.title3` |
| 17–18 | `.headline` / `.body` |
| 16 | `.callout` |
| 14–15 | `.subheadline` |
| 13 | `.footnote` |
| 12 | `.caption` |
| 10–11 | `.caption2` |
| Héros ≥40pt | figé (précédent iter-32) |

## Anti-repetition check
Vérifié contre les itérations 32 (Dynamic Type vagues 1), 41 (couleurs vues liens),
42 (LinksHubView/Bookmarks/PostTranslationSheet fonts), 42b (hex Settings/Onboarding/
ConversationView+Composer — ce dernier ne couvrait PAS UniversalComposerBar), 43 (déflake
tests). Aucun chevauchement : les 8 fichiers de cette itération n'ont jamais eu de passe
polices ; les hex Recording/AudioPost n'ont jamais été traités.

## Constats d'implémentation (2026-06-12)
- Défauts de paramètres `accentColor = "08D9D6"` / `secondaryColor = "4ECDC4"` de
  `UniversalComposerBar` (fallback quand aucun accent de conversation n'est passé) →
  `MeeshyColors.indigo400Hex`/`indigo600Hex` ; le mécanisme d'accent dynamique est intact
- Hors périmètre composer : `theme.surface(tint: "C7D2FE"/"A5B4FC")` (hexes exacts charte
  passés en String à un helper theme) laissés tels quels
- Timer AudioPost 36pt → `.largeTitle` monospaced ; héros 56/48pt conservés figés

## Différés (pour itérations suivantes)
- **Ancienne palette `08D9D6`/`FF2E63`/`4ECDC4` encore présente dans ~10 fichiers hors
  composer** : SampleData, StoryViewModel, ConversationView, StoryViewerView, StoryTrayView,
  ThreadView, ConversationHelperViews, ProfileView, ConversationListHelpers,
  FriendRequestListView — passe tokens dédiée
- Couleurs "arc-en-ciel" du ladder d'attachements (`FF9F43`, `45B7D1`, `2ECC71`, `F8B500`,
  `9B59B6`, `E74C3C`) — catégorielles intentionnelles, à arbitrer avec la charte
- `SettingsView` (8), `NewConversationView` (7), `DataExportView`/`DataStorageView` (8),
  `ChangePasswordView` (2), `ConversationView:495,551`, `ProfileView:65` — polices figées restantes
- `TwoFactorSetupView` — héros 50-80pt intentionnels, polices texte à revoir
- `PostDetailView` — `.textSelection(.enabled)` manquant sur le contenu de post
- `ThemedConversationRow:45-51` — hex vs règle accentColor (passe design dédiée)
- `VoiceProfileWizardView`, `PrivacySettingsView`, `TrackingLinksView` — `Color(hex:)` hors palette
- `IncomingCallView:136,180,199` — `.white` sur overlay appel (vérifier contraste, sans doute intentionnel)

## Status : corrections implémentées — voir plan 2026-06-12-plan-iteration-44
