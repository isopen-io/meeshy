# UI/UX Audit — Iteration 18 (2026-06-08)

> Base: main after PR #430 (iteration 17). All findings verified against current code.
> Previous iterations 1–17 addressed: color tokens, aria-labels, Dynamic Type, accessibility labels, media controls i18n, TextLightbox/PPTXLightbox/SimpleAudioPlayer/PDFViewer/AudioRecorderCard/UserSecuritySection (partial), iOS magic link keys.

---

## Web Issues

### HIGH — French toasts in ScanControlPanel (5 instances)

**File:** `apps/web/components/admin/agent/ScanControlPanel.tsx`
All 5 toasts replaceable with existing or new `admin.agent.toasts.*` keys. File already has `useI18n('admin')`.

| Line | Before | After |
|------|--------|-------|
| 119 | `'Config globale mise a jour'` | `t('agent.toasts.globalConfigUpdated')` ✅ key exists |
| 122 | `'Erreur'` | `t('agent.toasts.globalConfigUpdateError')` ✅ key exists |
| 127 | `'Config conversation mise a jour'` | `t('agent.toasts.conversationConfigUpdated')` NEW |
| 130 | `'Erreur'` | `t('agent.toasts.updateError')` ✅ key exists |
| 134 | `'Erreur reseau'` | `t('agent.toasts.networkError')` ✅ key exists |

---

### HIGH — French toast in AgentArchetypesTab (1 instance)

**File:** `apps/web/components/admin/agent/AgentArchetypesTab.tsx`

| Line | Before | After |
|------|--------|-------|
| 44 | `'Erreur lors du chargement des archétypes'` | `t('agent.toasts.archetypesLoadError')` NEW |

---

### HIGH — French toasts in AgentConfigDialog (2 instances)

**File:** `apps/web/components/admin/agent/AgentConfigDialog.tsx`

| Line | Before | After |
|------|--------|-------|
| 148 | `'ID de conversation invalide (24 caractères hexadécimaux)'` | `t('agent.toasts.invalidConversationId')` ✅ key exists |
| 212 | `'ID copie'` | `t('common.copied')` NEW |

---

### HIGH — French toasts in AgentConversationsTab (8 instances)

**File:** `apps/web/components/admin/agent/AgentConversationsTab.tsx`

| Line | Before | After |
|------|--------|-------|
| 76 | `'Erreur lors du chargement des configurations'` | `t('agent.toasts.configLoadError')` NEW |
| 94 | `` `Agent ${!config.enabled ? 'activé' : 'désactivé'}` `` | `t(config.enabled ? 'agent.toasts.agentDisabled' : 'agent.toasts.agentEnabled')` NEW |
| 96 | `'Erreur lors de la mise à jour'` | `t('agent.toasts.updateError')` ✅ key exists |
| 105 | `'Configuration supprimée'` | `t('agent.toasts.configDeleted')` NEW |
| 107 | `'Erreur lors de la suppression'` | `t('agent.toasts.deleteError')` NEW |
| 115 | `'Interruption demandée'` | `t('agent.toasts.triggerInterrupted')` ✅ key exists |
| 118 | `'Scan déclenché'` | `t('agent.toasts.scanTriggered')` ✅ key exists |
| 122 | `'Erreur lors du déclenchement'` | `t('agent.toasts.scanTriggerError')` ✅ key exists |

---

### HIGH — French labels in TriggerSchedulingModal (7 instances)

**File:** `apps/web/components/admin/agent/TriggerSchedulingModal.tsx`
File already has `useI18n('admin')` → `t`.

| Line | Before | After |
|------|--------|-------|
| 349 | `{isScanning ? 'STOP!' : 'Declencher maintenant'}` | `{isScanning ? t('agent.scheduling.stop') : t('agent.scheduling.triggerNow')}` NEW |
| 357 | `'Heure fixe'` | `t('agent.scheduling.fixedTime')` NEW |
| 382 | `'Dans X temps'` | `t('agent.scheduling.delay')` NEW |
| 417 | `'Frequence'` | `t('agent.scheduling.frequency')` NEW |
| 455 | `'Ce timer est perdu si vous fermez la page ou le navigateur.'` | `t('agent.scheduling.timerWarning')` NEW |
| 564 | `'glisser pour deplacer'` | `t('agent.scheduling.dragToMove')` NEW |
| 675 | `'Schedule non disponible'` | `t('agent.scheduling.notAvailable')` NEW |

---

### HIGH — Remaining French in UserSecuritySection (4 instances)

**File:** `apps/web/components/admin/user-detail/UserSecuritySection.tsx`
File already has `useI18n('admin')`.

| Line | Before | After |
|------|--------|-------|
| 28 | `'Jamais'` | `t('common.never')` NEW in common.json |
| 45 | `'Compte déverrouillé avec succès'` | `t('security.accountUnlocked')` NEW |
| 56 | `` `2FA ${has2FA ? 'désactivé' : 'activé'} avec succès` `` | `t(has2FA ? 'security.twoFactorDisabled' : 'security.twoFactorEnabled')` NEW |
| 142 | `<span>Désactivé</span>` | `<span>{t('security.disabled')}</span>` NEW |

---

### MEDIUM — French strings in TextViewer (3 instances)

**File:** `apps/web/components/text/TextViewer.tsx`
File already has `useI18n('viewers')` → `t`. All 3 keys already exist (added iter-17).

| Line | Before | After |
|------|--------|-------|
| 69 | `'Impossible de charger le fichier'` | `t('text.loadError')` ✅ key exists |
| 82 | `'Copié dans le presse-papiers'` | `t('text.copied')` ✅ key exists |
| 86 | `'Impossible de copier'` | `t('text.copyError')` ✅ key exists |

---

### MEDIUM — English hardcoded toast in CallManager (1 instance)

**File:** `apps/web/components/video-call/CallManager.tsx`

| Line | Before | After |
|------|--------|-------|
| 367 | `'Failed to join call'` | `t('calls.toasts.joinFailed')` NEW in calls.json |

---

## New locale keys summary

### admin.json (en/fr/es/pt) — under `agent.toasts`:
- `conversationConfigUpdated`, `archetypesLoadError`, `configLoadError`, `agentEnabled`, `agentDisabled`, `configDeleted`, `deleteError`

### admin.json (en/fr/es/pt) — new `agent.scheduling` section:
- `fixedTime`, `delay`, `frequency`, `timerWarning`, `dragToMove`, `notAvailable`, `triggerNow`, `stop`

### admin.json (en/fr/es/pt) — add to `security` section:
- `accountUnlocked`, `twoFactorEnabled`, `twoFactorDisabled`, `disabled`

### common.json (en/fr/es/pt):
- `copied`, `never`

### calls.json (en/fr/es/pt) — new `toasts` section:
- `joinFailed`

---

## Summary

| ID | Severity | File | Issue |
|----|----------|------|-------|
| W1 | HIGH | admin.json ×4 | 15 new keys (agent.toasts + scheduling + security) |
| W2 | HIGH | common.json ×4 | 2 new keys (copied, never) |
| W3 | HIGH | calls.json ×4 | 1 new key (toasts.joinFailed) |
| W4 | HIGH | ScanControlPanel.tsx | 5× French toasts → t() |
| W5 | HIGH | AgentArchetypesTab.tsx | 1× French toast → t() |
| W6 | HIGH | AgentConfigDialog.tsx | 2× French toasts → t() |
| W7 | HIGH | AgentConversationsTab.tsx | 8× French toasts → t() |
| W8 | HIGH | TriggerSchedulingModal.tsx | 7× French labels → t() |
| W9 | HIGH | UserSecuritySection.tsx | 4× remaining French → t() |
| W10 | MEDIUM | TextViewer.tsx | 3× French strings → t() (keys exist) |
| W11 | MEDIUM | CallManager.tsx | 1× English hardcoded toast → t() |

---

## ✅ Status : itération soldée — corrections mergées dans main (cf. branch-tracking.md, History iter-18)
Annoté le 2026-06-12 (iter-45w). NE PAS re-auditer ces fichiers pour les mêmes findings ; tout reliquat est tracé dans les carry-overs de branch-tracking.md.
