# Analyse UI/UX — Itération 69w (Web)

> **Scope** : `apps/web` **exclusivement**. Les vues iOS ne servent que de référence de parité (couleurs/features naturelles Meeshy), jamais d'objet de revue.
> **Thème** : accessibilité clavier (WCAG 2.1.1 *Keyboard* / 4.1.2 *Name, Role, Value* / 2.4.7 *Focus Visible*) du **modal de création de lien de conversation** — en-têtes de section repliables + substitut de case à cocher, tous `<div>`/`CardHeader` souris-only. Catégorie « **différé prioritaire 69w+** » du pointeur autoritaire 68w (a11y clavier des `<div onClick>` non-`<button>` HORS video-calls/conversation-list).

## Contexte
La routine a déjà soldé exhaustivement : anti-pattern i18n `t('key')||'fallback'`, aria-labels de contenu, focus-traps modales, `prefers-reduced-motion` global (MotionProvider #862), consolidation tokens dark-mode, épuration code mort, lazy-load images, **a11y clavier liste de conversations + tuile audio (67w / #1078)** et **contrôles plein écran d'appel vidéo (68w / #1082)** — tous deux mergés sur `main`.

Le différé prioritaire 68w+ visait l'audit transverse des `<div onClick>`/`role="button"` sans `onKeyDown`/`focus-visible`. Un audit ciblé de `apps/web` (hors clusters déjà soldés) a remonté 13 candidats ; le cluster le plus **cohérent, user-facing et auto-contenu** est le **modal de création de lien** (`create-link-modal/`), où l'intégralité des contrôles de configuration est inaccessible au clavier.
> **Thème** : accessibilité clavier (WCAG 2.1.1 / 4.1.2 / 2.4.7) des **cartes de communauté** (`GroupCard`) — `<div onClick>` / `<span onClick>` souris-only. Suite directe du « différé prioritaire 68w+ » : balayage de l'anti-pattern `<div onClick>` non-`<button>` **hors** video-calls/ et conversations/ (déjà soldés en 67w/68w).

## Contexte
La routine a déjà soldé : anti-pattern i18n `t('key')||'fallback'`, aria-labels de contenu, focus-traps modales, `prefers-reduced-motion` global (#862), consolidation tokens dark-mode, épuration code mort, lazy-load images, **a11y clavier liste de conversations + tuile audio (67w/#1078)** et **contrôles plein écran appel vidéo (68w/#1082)**.

Le pointeur 68w signalait comme gisement restant : « **a11y clavier des `<div onClick>` non-`<button>`** — reste à balayer hors video-calls/conversations ». Audit transverse ciblé (`<div>/<span>/<li>` porteurs de `onClick` sans `role`/`tabIndex`/`onKeyDown`) → cluster **cartes de communauté** sur la page `/groups` (surface utilisateur à fort trafic, layout actif `groups-layout.tsx`).
> **Thème** : résidus **i18n + a11y** des **overlays d'appel vidéo** (continuité directe du thème video-calls 68w/#1082). Catégorie distincte des vagues soldées (`t()||fallback` chrome/auth, focus-trap modales, reduced-motion global #862, tokens dark-mode, aria-label contenu).
> **Base de départ** : `main` HEAD (`769f55a`, post-merge 67w/#1078 + 68w/#1082 ; resync de la branche assignée effectué). Branche : `claude/practical-fermat-c2m1kd`.

## Contexte & dé-duplication
La routine a déjà couvert exhaustivement (ledger `branch-tracking.md`) : anti-pattern i18n `t('key')||'fallback'` (chrome, auth, links, conversations…), aria-labels de contenu, focus-traps modales, `prefers-reduced-motion` global (#862), consolidation tokens dark-mode, épuration code mort, a11y clavier liste conversations (67w/#1078) et contrôles plein écran appel vidéo (68w/#1082).

**Vérification anti-répétition** : les 2 constats ci-dessous figuraient déjà comme **candidats audit « Différé »** dans le plan 68w (« autres `<div onClick>`/`button` icon-only sans nom accessible » ; résidus i18n) mais **n'avaient jamais été corrigés** — ils ne recoupent aucune ligne `✅` du ledger. Surface **strictement orthogonale** aux PR en vol.
> **Thème** : accessibilité clavier (WCAG 2.1.1 *Keyboard* / 2.4.7 *Focus Visible* / 4.1.2 *Name, Role, Value*) des **previews de pièces jointes en zone de réponse** — contrôles `role="button"` *focusables mais inertes au clavier*.

## Contexte
La routine a déjà soldé exhaustivement : anti-pattern i18n `t('key')||'fallback'`, aria-labels de contenu, focus-traps modales, `prefers-reduced-motion` global (MotionProvider #862), consolidation tokens dark-mode, épuration code mort, lazy-load images, **a11y clavier de la liste de conversations + tuile audio (67w / #1078)** et **des contrôles plein écran d'appel vidéo (68w / #1082)**.

Le gisement nommé « **a11y clavier des `<div onClick>` / `role="button"` sans `onKeyDown` HORS video-calls/conversations** » (pointeur autoritaire 68w+) reste ouvert. Audit ciblé `role="button"` vs `onKeyDown` au niveau fichier → **un cas net et auto-contenu** : `components/attachments/AttachmentPreviewReply.tsx` (3 occurrences `role="button"`, **0** `onKeyDown`).
> **Thème** : accessibilité clavier (WCAG 2.1.1 *Keyboard* / 2.4.7 *Focus Visible*) des **previews d'attachments en zone de réponse** — `<div role="button">` Tab-focusables mais **non activables au clavier** (pas de `onKeyDown`) et **sans anneau de focus visible**.

## Contexte
La catégorie « **a11y clavier des `<div onClick>` non-`<button>`** » a été ouverte par 67w (liste de conversations + tuile audio, #1078) puis 68w (contrôles plein écran appel vidéo, #1082, mergé). Le pointeur 68w signalait : « Reste à balayer hors video-calls/conversations pour itérations futures (audit transverse `role="button"` sans `onKeyDown`/`focus-visible`) ».

Audit transverse ciblé (`<div ... onClick>` dans `components/`, hors surfaces en vol #1084 create-link-modal / #1077 auth) → cluster cohérent confirmé dans **`components/attachments/AttachmentPreviewReply.tsx`**.

> **Anti-doublon vérifié** : `ImageAttachment.tsx` / `FileAttachment.tsx` (même répertoire) ont **déjà** `role`/`tabIndex`/`onKeyDown`/`aria-label`/`focus-visible` (soldés antérieurement). `MediaImageCard.tsx` utilise déjà des `<button>` natifs. `MediaVideoCard.tsx:245` est un **backdrop** click-outside (pas de besoin clavier). Le cluster `AttachmentPreviewReply` est le seul gisement net restant sur cette surface.

## Constats vérifiés (file:line) et corrections

| # | Fichier | Problème | Correction |
|---|---------|----------|-----------|
| 1 | `components/conversations/create-link-modal/steps/config-sections/LanguagesSection.tsx:32-35` | En-tête repliable « Langues autorisées » = `CardHeader` avec `onClick` toggle **souris-only** : pas de `role`, `tabIndex`, `onKeyDown`, ni état `aria-expanded`. Inactivable/invisible au clavier et au lecteur d'écran. | `role="button"` + `tabIndex={0}` + `aria-expanded={isLanguagesOpen}` + `onKeyDown` Enter/Space (`preventDefault`) + anneau `focus-visible:ring-inset`. Le nom accessible vient du `CardTitle` contenu (clé i18n existante). |
| 2 | `components/conversations/create-link-modal/steps/config-sections/PermissionsSection.tsx:66-69` | En-tête repliable « Permissions » = même `CardHeader` toggle **souris-only**, mêmes manques. | Idem #1 avec `aria-expanded={isPermissionsOpen}`. |
| 3 | `components/conversations/create-link-modal/components/SelectableSquare.tsx:15-26` | **Substitut de case à cocher réutilisé pour TOUS les toggles du modal** (langues, messages anonymes, images, fichiers, historique, compte requis, pseudo, e-mail, anniversaire). `<div onClick>` **souris-only** : pas de `role`, `tabIndex`, `onKeyDown`, ni `aria-checked`/`aria-disabled`. C'est le contrôle réel que l'utilisateur bascule — gap **plus impactant** que les en-têtes (un en-tête sans sa case accessible serait incohérent). | `role="checkbox"` + `aria-checked={checked}` + `aria-label={label}` + `aria-disabled={disabled}` + `tabIndex={disabled ? -1 : 0}` (retrait du tab order si désactivé) + `onKeyDown` Enter/Space (no-op si `disabled`, `preventDefault`) + anneau `focus-visible:ring-offset-1`. Clic souris inchangé. |

> **Pourquoi ce cluster est cohérent** : les 3 contrôles vivent dans le même flux (`create-link-modal/`), partagent l'action « basculer un état de configuration » et le même défaut clavier. Corriger les en-têtes sans les cases (ou l'inverse) laisserait le modal partiellement inutilisable au clavier. **Aucune nouvelle clé i18n** (les libellés/titres existants servent de nom accessible).

## Tests
- **NOUVEAU** `__tests__/components/conversations/create-link-config-sections-a11y.test.tsx` — 10 cas : `LanguagesSection` & `PermissionsSection` exposées comme `button` focusable (`tabIndex=0`), `aria-expanded` reflète l'état (false/true), activation **Enter**, activation **Space**, no-op touche neutre (`Tab`), parité **clic** souris préservée.
- **NOUVEAU** `__tests__/components/conversations/selectable-square-a11y.test.tsx` — 6 cas : `role="checkbox"` avec nom accessible + `tabIndex=0`, `aria-checked` reflète l'état (true/false), toggle **Enter**, toggle **Space**, **désactivé** → `tabIndex=-1` + `aria-disabled` + `onKeyDown` no-op, parité **clic** souris préservée.
- **Résultat** : `jest` ciblé → **2 suites / 16 tests passed**. Non-régression `create-link-modal.test.tsx` + `ActiveUsersSection.test.tsx` → **2 suites / 25 tests passed**. `tsc --noEmit` : 0 erreur sur les fichiers modifiés (attributs JSX standards).

## Hors-scope confirmé / différé
- Reste de l'audit 69w (à traiter 70w+, **bornés/orthogonaux**) : `admin/agent/AgentConfigDialog.tsx` + `AgentGlobalConfigTab.tsx` (toggles `Badge`), `audio/AudioEffectsTimeline.tsx` (seek clavier), `details-sidebar/*` (DetailsHeader/CustomizationManager/DescriptionSection — édition au clic), `invite-user-modal.tsx` (résultats de recherche). ⚠️ `details-sidebar` & `invite-user-modal` sont dans `components/conversations/` mais **hors** du cluster « liste de conversations » 67w.
- Backdrops/dismiss (`UserConversationsSection.tsx`, `MarkdownLightbox.tsx`, `MediaVideoCard.tsx`) : `onClick={onClose}` doublé d'un bouton de fermeture visible + Escape → **basse priorité**, pas un gap clavier bloquant.
- ESLint local KO (mismatch version d'env, artefact, non bloquant — le gate `Quality (bun)` CI épingle le linter). Typecheck `@meeshy/shared/dist` non build localement = pré-existant, identique sur `main`.
- `Test shared` rouge sur `main` = régression migration zod v4 (hors-scope web, propriétaire shared ; cf. branch-tracking).
| 1 | `components/groups/GroupCard.tsx:30-38` | **Sélection de la carte** = `<div onClick={() => onSelect(group)}>` **souris-only** : aucun `role`, `tabIndex`, `onKeyDown`, ni nom accessible. La carte (contrôle principal de navigation entre communautés) est invisible/inactivable au clavier et au lecteur d'écran (WCAG 2.1.1). | `role="button"` + `tabIndex={0}` + `aria-pressed={isSelected}` (état sélectionné exposé programmatiquement) + `aria-label={t('card.openLabel',{name})}` + `onKeyDown` Enter/Space (`preventDefault`) + anneau `focus-visible:ring-2` (WCAG 2.4.7). |
| 2 | `components/groups/GroupCard.tsx:67-72` | **Copie de l'identifiant** = `<span onClick>` **souris-only** : même manque. Inactivable au clavier ; de plus, sans `stopPropagation` clavier, une activation aurait aussi déclenché la sélection de la carte parente. | `role="button"` + `tabIndex={0}` + `aria-label={t('card.copyIdentifier',{identifier})}` + `onKeyDown` Enter/Space (`preventDefault` **+ `stopPropagation`** → la copie ne sélectionne jamais la carte) + anneau `focus-visible`. |

> **Cohérence du cluster** : les 2 contrôles vivent dans le **même composant** `GroupCard` et partagent le même défaut (clic-only). Correction atomique, orthogonale aux PR en vol. `groups-layout-responsive.tsx` n'utilise **pas** `GroupCard` (il inline ses propres cartes avec une signature de copie différente) → hors-scope, non touché.

### i18n
2 nouvelles clés `groups.card.*` ajoutées aux **4 locales** (en/fr/es/pt), avec interpolation `{name}` / `{identifier}` (syntaxe `{param}` du `t()` maison) :
- `card.openLabel` — « Open {name} » / « Ouvrir {name} » / « Abrir {name} » / « Abrir {name} »
- `card.copyIdentifier` — « Copy identifier {identifier} » / « Copier l'identifiant {identifier} » / « Copiar identificador {identifier} » / « Copiar identificador {identifier} »

### Type-safety (ripple minimal)
La signature `onCopyIdentifier` est élargie de `React.MouseEvent` à `React.MouseEvent | React.KeyboardEvent` sur la chaîne de consommation réelle (`GroupCard` → `GroupsList` → `groups-layout.tsx` `copyIdentifier`). Le handler sous-jacent n'utilise que `e?.stopPropagation()`, présent sur les deux types ⇒ aucun cast, aucune assertion (`as`). 3 fichiers, 3 lignes.

## Tests
- **NOUVEAU** `__tests__/components/groups/GroupCard.test.tsx` — 7 cas : nom accessible carte (`button`, `tabIndex=0`), `aria-pressed` reflète la sélection, sélection **Enter** + **Space**, no-op touche neutre, **parité clic** souris préservée, identifiant exposé comme `button` focusable, **copie Enter sans sélectionner la carte** (vérifie le `stopPropagation`).
- **Résultat** : `jest __tests__/components/groups` → **1 suite / 7 tests passed**.
- Type-check : **0 erreur** sur les fichiers modifiés (les ~1184 erreurs `@meeshy/shared/dist` locales sont pré-existantes, identiques sur `main` ; CI build `shared` en amont — job `Quality (bun)` vert sur `main`).

## Hors-scope confirmé
- ESLint local KO (circular config `ESLint 10.5.0` vs `eslintrc 3.x`) — **artefact d'environnement** documenté (67w/68w), non imputable au diff ; le gate `Quality (bun)` CI utilise le linter épinglé.
- `groups-layout-responsive.tsx` (cartes inline, layout non actif sur `/groups`) — non rendu par la page, non touché.
- Imbrication `role="button"` (span copie dans div carte) : conforme au pattern carte-cliquable-avec-action des itérations précédentes ; activation clavier de la copie isolée par `stopPropagation`.
| 1 | `components/video-calls/CallInfoOverlay.tsx:29` | **Bug i18n + rupture Prisme** : la chaîne `{participantCount} participant(s)` était **codée en dur en anglais**, affichée en **TOUTES langues**, et non pluralisée (`participant(s)` disgracieux). Seul overlay d'appel non localisé. | Câblage `useI18n('calls')` ; rendu `t(count === 1 ? 'info.participant' : 'info.participants', { count })`. Pluralisation singulier/pluriel correcte + interpolation `{count}`. |
| 2 | `components/video-calls/AudioEffectsPanel.tsx:98,192` | **Bug a11y (WCAG 4.1.2 Name, Role, Value)** : 2 boutons **icône-seule** `<Info>` (popover d'aide) sans `aria-label` ⇒ lecteurs d'écran annoncent un bouton **sans nom**. (Le `<button>` ligne 192 manquait aussi `type="button"`.) | `aria-label={t('moreInfo')}` sur les 2 `PopoverTrigger` ; ajout `type="button"` manquant. |

## Clés i18n (parité ×4 locales en/fr/es/pt)
- **`calls.info.participant` / `calls.info.participants`** (neuves ×4) — `{count} participant(e)(s)` selon locale. Distinctes de `calls.banner.participant(s)` (bannière d'appel, sémantique séparée).
- **`audioEffects.moreInfo`** (neuve ×4) — « More information » / « Plus d'informations » / « Más información » / « Mais informações ».
- Diff locale : **8 fichiers, +24 lignes** (insertion pure, formatage préservé, JSON valides round-trip).

## Tests (TDD RED→GREEN)
- **MIS À JOUR** `__tests__/components/video-calls/CallInfoOverlay.test.tsx` — mock `useI18n` interpolant `{count}` ; 3 cas : durée, **pluriel** (`3 participants`), **singulier** (`1 participant`).
- **NOUVEAU** `__tests__/components/video-calls/AudioEffectsPanel.test.tsx` — rend le panneau complet (Radix Popover/Select/Slider OK en jsdom), assertion : **tout** bouton info icône-seule expose un nom accessible (`getAllByRole('button', {name:'moreInfo'})` > 0).
- **Résultat** : répertoire `video-calls` = **7 suites / 26 tests passed** (aucune régression sur les suites 67w/68w).

## Faux positifs écartés (ne PAS re-flagger)
- `text-white` / `bg-black/60` des overlays vidéo (`CallInfoOverlay`, `LocalVideoTile`, `CallQualityOverlay`, `DraggableParticipantOverlay`) : **intentionnels** — texte/élévation **au-dessus d'un flux vidéo**, indépendants du thème clair/sombre (un overlay vidéo n'hérite pas du fond applicatif). Ce ne sont **pas** des manques de parité dark-mode.
- `t('title','Audio Effects')` / `t('subtitle',…)` (AudioEffectsPanel) : la clé **existe** ×4 locales ⇒ secours natif anti-flash légitime (pattern `t(k,'En')`), pas l'anti-pattern `t(k)||'fb'`.

## Hors-scope confirmé
- Typecheck local : `@meeshy/shared/dist` non build localement ⇒ erreurs TS pré-existantes identiques sur `main` pristine (CI build `shared` en amont). Mes diffs n'introduisent aucune erreur. Le transformeur jest compile les 2 composants modifiés sans erreur.
- ESLint en invocation directe locale KO (mismatch flat/legacy config de l'env — artefact, non bloquant) ; le gate CI `Quality (bun)` épingle le linter.
| 1 | `components/attachments/AttachmentPreviewReply.tsx:156` (miniature image) | `<div role="button" tabIndex={0} aria-label>` **focusable** (dans l'ordre de tabulation) mais **sans `onKeyDown`** → Enter/Espace ne déclenchent **rien**. Contrôle *mort au clavier* alors qu'il s'annonce comme bouton (pire qu'un `<div>` non focusable : promet une interaction inopérante). | Ajout `onKeyDown={handleKeyActivate(handleImageClick(index))}` (Enter/Espace → ouverture lightbox, `preventDefault`). |
| 2 | `components/attachments/AttachmentPreviewReply.tsx:228` (icône PDF) | Idem : `role="button"`/`tabIndex`/`aria-label` présents, **`onKeyDown` absent** → impossible d'ouvrir le PDF au clavier. | `onKeyDown={handleKeyActivate(handlePdfClick(attachment))}`. |
| 3 | `components/attachments/AttachmentPreviewReply.tsx:250` (icône texte/code) | Idem PDF. | `onKeyDown={handleKeyActivate(handleTextClick(attachment))}`. |
| 4 | mêmes 3 contrôles | **Aucun anneau de focus visible** (`cursor-pointer hover:opacity-80` seulement) → focus clavier invisible (WCAG 2.4.7). | Ajout `outline-none focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-1` (token `--primary`, parité 67w/68w). |

> **Pourquoi ce cluster est cohérent** : les 3 contrôles vivent dans **un seul composant**, partagent la même mécanique (« ouvrir le contenu en lightbox »), le même défaut d'accessibilité clavier, et la même correction. Refactor minimal et DRY : un helper unique `handleKeyActivate(activate)` (Enter/Espace → `preventDefault` → `activate`), réutilisé sur les 3 contrôles ; les signatures `handle*Click` passent de `React.MouseEvent` à `React.SyntheticEvent` (souris + clavier). **Aucune nouvelle clé i18n** (les `aria-label` existaient déjà — c'est précisément ce qui rendait le défaut trompeur).

## Tests
- **MIS À JOUR** `__tests__/components/attachments/AttachmentPreviewReply.test.tsx` — le bloc `Accessibility` n'asserait que `tabindex=0` (présence dans le tab order) **sans jamais vérifier l'activation clavier** : exactement la faille. +6 cas comportementaux : ouverture lightbox **image** (Enter **et** Espace), **PDF** (Enter), **texte** (Espace), et **no-op** sur touche neutre (`a`).
- **RED→GREEN** confirmé : avant correction, 4 nouveaux cas échouent (lightbox jamais montée) ; après, **34/34** verts.
- **Régression** : dossier `__tests__/components/attachments` entier → **7 suites / 236 passed (3 skipped)**.
- **Typecheck** : seules erreurs résiduelles = casts `images/videos as unknown` (lignes 304/338) **pré-existants et identiques sur `main`** (props `ImageLightbox`/`VideoLightbox`), non touchés par ce diff.

## Hors-scope confirmé
- **Vidéo plein écran** (ligne 200) : déjà un `<button>` natif → pas de gap clavier, aucune correction.
- **Audio / « autres fichiers »** (`role="listitem"`) : non interactifs (l'audio embarque son propre player ; « autres » = affichage seul), pas de `onClick` d'activation.
- Autres candidats a11y clavier repérés par l'audit transverse (différés, itérations futures) : `v2/MediaAudioCard` + `v2/MediaVideoCard` (overlays play/transcription = `<div onClick>` **sans** `role`/`tabIndex`), `ui/foldable-section` + `v2/CategoryHeader` (accordéons), `affiliate/share-affiliate-modal`. **Non inclus** ici pour garder le lot borné/orthogonal aux PR en vol.
| 1 | `components/attachments/AttachmentPreviewReply.tsx:152-169` (preview image) | `<div role="button" tabIndex={0} aria-label>` ouvrant l'`ImageLightbox` au **clic seul** : `onClick` présent mais **aucun `onKeyDown`** → annoncé comme bouton au lecteur d'écran et focusable au Tab, **mais Enter/Espace ne font rien** (WCAG 2.1.1 échec). Aucun anneau `focus-visible`. | Ajout `onKeyDown={activateOnEnterOrSpace(handleImageClick(index))}` + classes `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-blue-500 focus-visible:ring-offset-1` (style aligné sur `ImageAttachment.tsx`/`FileAttachment.tsx` du même répertoire). |
| 2 | `components/attachments/AttachmentPreviewReply.tsx:217-235` (preview PDF) | Idem : `<div role="button">` ouvrant `PDFLightboxSimple` au clic seul, pas d'activation clavier ni focus visible. | Ajout `onKeyDown={activateOnEnterOrSpace(handlePdfClick(attachment))}` + mêmes classes `focus-visible`. |
| 3 | `components/attachments/AttachmentPreviewReply.tsx:239-257` (preview texte/code) | Idem : `<div role="button">` ouvrant `TextLightbox` au clic seul. | Ajout `onKeyDown={activateOnEnterOrSpace(handleTextClick(attachment))}` + mêmes classes `focus-visible`. |

### Implémentation — épuration / DRY
- Helper unique **`activateOnEnterOrSpace`** (mémoïsé `useCallback`) : `(action) => (e) => { if (Enter|Espace) { preventDefault(); action(e); } }`. Un seul point de vérité pour les 3 contrôles, pas de répétition inline.
- Les 3 handlers existants (`handleImageClick`/`handlePdfClick`/`handleTextClick`) voient leur signature d'événement **élargie** `React.MouseEvent → React.MouseEvent | React.KeyboardEvent` (les deux exposent `stopPropagation`), ce qui **évite tout cast `as unknown`** (contrairement au pattern hérité de `ImageAttachment.tsx`). Type-safe, aligné sur les guidelines TS du repo (« No type assertions without justification »).
- Clés i18n : **0 clé neuve** — les `aria-label` (`actions.openImageNamed` / `actions.openPdfNamed` / `actions.openTextFileNamed`) existaient déjà ×4 locales (en/fr/es/pt).

## Tests
- **MIS À JOUR** `__tests__/components/attachments/AttachmentPreviewReply.test.tsx` — bloc *Accessibility* étendu de +6 cas : ouverture lightbox image sur **Enter** et sur **Espace**, **no-op** sur touche neutre (Tab), ouverture lightbox **PDF** sur Enter, ouverture lightbox **texte** sur Espace.
- **Résultat** : suite `AttachmentPreviewReply` → **34/34 passed** (28 → 34). Répertoire `__tests__/components/attachments` complet → **236 passed / 3 skipped / 7 suites**.
- **Typecheck** : `tsc --noEmit` n'ajoute **aucune erreur** (les 2 erreurs `images/videos as unknown` aux lignes lightbox sont **pré-existantes sur `main`**, hors-scope de cette itération).

## Hors-scope confirmé (ne pas re-flagger)
- `ImageAttachment.tsx` / `FileAttachment.tsx` : déjà clavier-accessibles (soldés).
- `MediaImageCard.tsx` : `<button>` natifs partout — aucun gap.
- `MediaVideoCard.tsx:245` : backdrop click-outside, pas de besoin clavier.
- Casts `images as unknown` / `videos as unknown` (lignes lightbox `ImageLightbox`/`VideoLightbox`) : dette TS **pré-existante**, orthogonale à l'a11y clavier — itération typage dédiée ultérieure.

---

## ✅ ANALYSE CORRIGÉE & COMPLÈTE (69w — 2026-06-30)
Les 3 constats sont **corrigés et testés** (en attente merge `main`). **NE PLUS re-flagger** :
- `LanguagesSection.tsx` / `PermissionsSection.tsx` en-têtes repliables clavier (soldés) ;
- `SelectableSquare.tsx` substitut de case à cocher clavier/`aria-checked` (soldé — vaut pour **toutes** ses instances dans le modal).
Catégorie « **a11y clavier des `<div onClick>` non-`<button>`** » : cluster `create-link-modal/` **épuisé**. Reste à balayer hors video-calls/conversation-list/create-link-modal (cf. § Hors-scope/différé ci-dessus) pour 70w+.
Les 2 constats sont **corrigés et testés** (en attente merge `main`). **NE PLUS re-flagger** :
- `GroupCard.tsx` sélection de carte clavier (soldé) ;
- `GroupCard.tsx` copie d'identifiant clavier + `stopPropagation` (soldé).
Catégorie « **a11y clavier des `<div onClick>` non-`<button>`** » : gisement poursuivi sur la feature `groups/`. Reste à balayer pour itérations futures (candidats backlog audit 69w, non détaillés) : `components/links/` (cartes de liens extensibles), `components/contacts/` (lignes de contact cliquables), `components/admin/` (lignes de tableaux). Choisir un cluster borné et orthogonal aux PR en vol.
Les 2 constats sont **corrigés et mergés**. **NE PLUS re-flagger** :
- `CallInfoOverlay.tsx` `participant(s)` codé en dur (soldé — `calls.info.participant(s)` ×4) ;
- `AudioEffectsPanel.tsx` boutons info icône-seule sans `aria-label` (soldé — `audioEffects.moreInfo` ×4).
Catégorie « **i18n + a11y des overlays d'appel vidéo** » désormais **soldée** (avec 68w). Gisement restant valable (itérations futures) : a11y clavier transverse HORS `video-calls`/`conversations`, classe résiduelle `t()||fallback` (`PhoneResetFlow`, `StoryViewer`, `app/settings`…), `preferences.reducedMotion` applicatif (toggle quasi no-op).
Les 3 constats (+ focus-visible) sont **corrigés et testés**. **NE PLUS re-flagger** :
- `AttachmentPreviewReply.tsx` miniature image / icône PDF / icône texte — activation clavier Enter/Espace + anneau focus-visible (soldé).

Reste à balayer pour la catégorie « **a11y clavier des `<div onClick>`/`role="button"` sans `onKeyDown`** » (gisement HORS video-calls/conversations/attachments-reply, désormais soldés) : `v2/MediaAudioCard`, `v2/MediaVideoCard`, `ui/foldable-section`, `v2/CategoryHeader`, `affiliate/share-affiliate-modal` → itérations 70w+.
Les 3 constats sont **corrigés et testés**. **NE PLUS re-flagger** :
- `AttachmentPreviewReply.tsx` previews image/PDF/texte — activation clavier Enter/Espace + anneau `focus-visible` (soldé).

Catégorie « **a11y clavier des `<div onClick>` non-`<button>`** » sur `components/attachments/` : **gisement épuisé** (toutes les previews interactives du répertoire sont désormais clavier-accessibles). Reste à balayer pour itérations futures (audit transverse `role="button"` sans `onKeyDown`/`focus-visible`) : `components/groups/`, `components/dashboard/`, `components/feed/`, `components/admin/` — vérifier collisions PR avant de choisir.
