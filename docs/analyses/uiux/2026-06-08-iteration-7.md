# UI/UX Analysis — Iteration 7 (2026-06-08)

## Context

Builds on iterations 1–6. Previous work covered: MeeshyColors migration, Dynamic Type (all major
iOS views), iOS a11y (FeedPostCard, OnboardingStepViews, MagicLinkView), web i18n (15+ components),
web a11y (icon-only buttons), web dark mode (not-found, user-selector, access-denied).

---

## Web — Broken i18n Key Paths — HIGH (silent failures)

The `useI18n(namespace)` hook extracts the namespace sub-object from the JSON file. This means
after loading `admin.json` with `useI18n('admin')`, the translations object IS the `admin.json`
root — no extra `admin.` prefix is needed.

### Admin components: extra `admin.` prefix (key-not-found, shows raw key string in UI)

| File | Broken call | Correct call |
|------|------------|-------------|
| `RankingFilters.tsx:71` | `t('admin.ranking.selectEntity')` | `t('ranking.selectEntity')` |
| `RankingFilters.tsx:108` | `t('admin.ranking.selectCriterion')` | `t('ranking.selectCriterion')` |
| `RankingFilters.tsx:153` | `t('admin.ranking.selectPeriod')` | `t('ranking.selectPeriod')` |
| `AgentLiveTab.tsx:453` | `t('admin.agentLive.selectConversation')` | `t('agentLive.selectConversation')` |
| `UserLanguageSection.tsx:98` | `t('admin.userDetail.languagePreferences')` | `t('userDetail.languagePreferences')` |
| `translation-monitor.tsx:158` | `t('admin.translationMonitor.loadingMetrics')` | `t('translationMonitor.loadingMetrics')` |

### Groups component: extra `groups.` prefix (same issue)

| File | Broken call | Correct call |
|------|------------|-------------|
| `groups-layout-responsive.tsx:402` | `tGroups('groups.noGroups')` | `tGroups('noGroups')` |
| `groups-layout-responsive.tsx:403` | `tGroups('groups.noGroupsDescription')` | `tGroups('noGroupsDescription')` |
| `groups-layout-responsive.tsx:576` | `tGroups('groups.details.createdOn')` | `tGroups('details.createdOn')` |
| `groups-layout-responsive.tsx:590` | `tGroups('groups.list.selectCommunity')` | `tGroups('list.selectCommunity')` |
| `groups-layout-responsive.tsx:592` | `tGroups('groups.list.selectCommunityDescription')` | `tGroups('list.selectCommunityDescription')` |

---

## Web — Hardcoded Strings — HIGH

| File | Line | Hardcoded | Key |
|------|------|-----------|-----|
| `translation-monitor.tsx` | 272 | `"Répartition par langue"` | `translationMonitor.languageBreakdown` |
| `groups-layout-responsive.tsx` | 397 | `"Chargement..."` | `list.loadingInProgress` |
| `UserLanguageSection.tsx` | 77 | `'Préférences de langue mises à jour'` | `userDetail.languagePreferencesSaved` |
| `UserLanguageSection.tsx` | 82 | `'Erreur lors de la mise à jour'` | `userDetail.languagePreferencesError` |

---

## Web — New Components Missing i18n — HIGH

### MessageSearch.tsx
- No `useI18n` hook
- Hardcoded: placeholder, close aria-label, error text, empty state
- Keys to add: `conversations.messageSearch.{placeholder,close,error,noResults}`

### PinnedMessageBanner.tsx
- No `useI18n` hook
- Hardcoded: `aria-label="Fermer le message épinglé"`
- Key to add: `conversations.pinnedBanner.close`

---

## iOS — Hardcoded Sentiment Colors — HIGH

`ConversationDashboardView.swift` uses literal hex colors for sentiment analysis displays.
These are exact matches for existing `MeeshyColors` semantic tokens:

| Hex | Semantic | Occurrences |
|-----|----------|-------------|
| `"34D399"` | `MeeshyColors.success` | 8 |
| `"FBBF24"` | `MeeshyColors.warning` | 4 |
| `"F87171"` | `MeeshyColors.error` | 4 |

`ConversationInfoSheet.swift`:

| Hex | Semantic | Usage |
|-----|----------|-------|
| `"EF4444"` | `MeeshyColors.error` | Block button (4×) |
| `"4ECDC4"` | `MeeshyColors.indigo300` | E2EE lock icon |

---

## iOS — Hardcoded Toast Strings — HIGH

### FeedViewModel.swift (11 non-localized strings)
bookmark.error, comment.error, like.error, repost.error, share.error,
delete.success, delete.error, report.success, report.error, pin.success, pin.error

### PostDetailViewModel.swift (4 non-localized strings)
comments.loadError, like.error (in `toast:` param), comment.error (in `toast:` param),
reply.error

All wrapped with `String(localized: "post.xxx", defaultValue: "...", bundle: .main)`.

---

## Deferred → Itération 8

- iOS `ConversationPreferencesTab.swift` — hardcoded section header colors (purple/blue/red/gray)
- iOS `UniversalComposerBar.swift` — `Color(hex: accentColor)` pattern review (accent is correct,
  but UI polish opportunities in effects panel)
- Web dark mode audit: `app/admin/users/[id]/page.tsx` and `app/admin/users/new/page.tsx`
  — several `bg-white`, `text-gray-900` without dark: pairs
- Web a11y: groups-layout-responsive — member count concatenation (`"{n} membres"`) should
  use plural i18n form
- iOS StoryViewModel — 4 hardcoded French toast strings pending localization
