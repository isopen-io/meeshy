# UI/UX Analysis — Iteration 7 (2026-06-08)

## Context

Builds on iterations 1–6. Previous work covered: MeeshyColors full migration (all major views),
Dynamic Type (FloatingCallPillView, StatusBarView, Contacts, FeedView, PostDetailView, ThreadView,
ReplyThreadOverlay), i18n (Header, ErrorBoundary, clipboard, video-calls, viewers,
MentionAutocomplete, PushPermissionBanner, PermissionRequest, AudioControls, admin panels ×7,
groups-layout-responsive, AnonymousForm, AgentLiveTab, RankingFilters, UserLanguageSection,
translation-monitor, language-select, MessageActionsBar, notification-settings),
iOS a11y systemic (15 files), admin dark mode, iOS brand indigo migration,
web security (rel="noopener noreferrer"), web a11y (8 icon-only buttons),
iOS a11y (OnboardingStepViews, MagicLinkView).

This iteration targets items deferred from iteration 6:
- iOS: ConversationInfoSheet hex color remnants
- Web: i18n + a11y audit of MessageSearch.tsx and PinnedMessageBanner.tsx (new in iter-5)
- Web: i18n cleanup in ConversationItemActions.tsx (5 hardcoded French menu strings)
- Web: accessibility on icon-only buttons in CustomizationManager.tsx + CategorySelector.tsx

Pre-analysis verification:
- `ConversationDashboardView.swift` — already clean ✅
- `ConversationPreferencesTab.swift` — already clean ✅

---

## iOS — Color(hex:) Remnants — HIGH

### ConversationInfoSheet.swift

| Line | Hardcoded | Token to use |
|------|-----------|--------------|
| 975 | `Color(hex: "4ECDC4")` (encryption lock icon) | `MeeshyColors.indigo300` |
| 1212 | `Color(hex: "EF4444")` (ProgressView tint) | `MeeshyColors.error` |
| 1221 | `Color(hex: "EF4444")` (HStack foreground) | `MeeshyColors.error` |
| 1226 | `Color(hex: "EF4444")` (fill background) | `MeeshyColors.error` |
| 1229 | `Color(hex: "EF4444")` (stroke overlay) | `MeeshyColors.error` |

These are in the E2EE security section (lock icon teal color) and the block-user button
(all red instances). The lock icon should use the brand indigo token rather than a custom teal,
consistent with indigo migration performed in iterations 3–4.

---

## Web — i18n Hardcoded Strings — HIGH

### 1. MessageSearch.tsx (conversations namespace)

Component added in iter-5. No `useI18n` hook present.

| Line | Hardcoded | Key |
|------|-----------|-----|
| 120 | `"Rechercher dans la conversation…"` | `conversations.messageSearch.placeholder` |
| 150 | `"Aucun résultat"` | `conversations.messageSearch.noResults` |
| 152 | `"Une erreur est survenue lors de la recherche."` | `conversations.messageSearch.searchError` |

### 2. PinnedMessageBanner.tsx (conversations namespace)

Component added in iter-5. No `useI18n` hook present.

| Line | Hardcoded | Key |
|------|-----------|-----|
| 110 | `aria-label="Fermer le message épinglé"` | `conversations.pinnedMessage.dismiss` |

### 3. ConversationItemActions.tsx (conversations namespace)

Component already receives a `t` prop but hardcodes French labels in the dropdown content.

| Line | Hardcoded | Key (already exists) |
|------|-----------|-----|
| 64 | `aria-label="Actions de la conversation"` | `conversations.conversationHeader.menuActions` ✅ |
| 71 | `"Paramètres"` | `conversations.conversationHeader.settings` ✅ |
| 76 | `"Désépingler" / "Épingler"` | `conversations.conversationHeader.unpin / pin` ✅ |
| 85 | `"Activer notifications" / "Désactiver notifications"` | `conversations.conversationHeader.unmute / mute` ✅ |
| 90 | `"Désarchiver" / "Archiver"` | `conversations.conversationHeader.unarchive / archive` ✅ |
| 105 | `"Réactions"` | `conversations.conversationHeader.reactions` — add |

Only `reactions` is missing from locale files; all others already exist.

---

## Web — Accessibility (icon-only buttons) — HIGH

### CustomizationManager.tsx

| Line | Button | Missing |
|------|--------|---------|
| 136 | `<Check />` (save custom name) | `aria-label` |
| 205 | `<Check />` (save reaction) | `aria-label` |

Key `conversations.conversationDetails.save` already exists → reuse it.

### CategorySelector.tsx

| Lines | Button | Missing |
|-------|--------|---------|
| 280–289 | `<Check />` (confirm category rename) | `aria-label` |
| 290–300 | `<XIcon />` (cancel category rename) | `aria-label` |
| 310–319 | `<Pencil />` (edit category) | `aria-label` (has `title` but not `aria-label`) |
| 323–332 | `<Trash2 />` (delete category) | `aria-label` (has `title` but not `aria-label`) |

Keys `conversationDetails.save`, `conversationDetails.cancel`, `conversationDetails.editCategory`,
`conversationDetails.deleteCategory` all exist → reuse them.

---

## New Locale Keys Required

### conversations namespace (all 4 locales)

Under `conversations.conversationHeader`:
- `reactions`

Under `conversations.messageSearch` (new section):
- `placeholder`
- `noResults`
- `searchError`

Under `conversations.pinnedMessage` (new section):
- `dismiss`

---

## Deferred → Iteration 8

- iOS Toast messages hardcodés: `FeedViewModel`, `PostDetailViewModel`, `StoryViewModel`, `ConversationView` (~40+ strings)
- iOS Color(hex:) audit: `UniversalComposerBar` (47×), `ConversationInfoSheet` non-destructive colors if any remain
- Full accessibility review of newly added components (post-iter-7)
- Web: Full audit of admin panel newly added components
