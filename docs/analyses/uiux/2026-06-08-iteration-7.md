# UI/UX Analysis — Iteration 7 (2026-06-08)

## Context

Builds on iterations 1–6. Previous work covered: MeeshyColors full migration, Dynamic Type
(FloatingCallPillView, StatusBarView, Contacts, ThreadView, ReplyThreadOverlay), i18n (Header,
ErrorBoundary, clipboard, video-calls, viewers, MentionAutocomplete, AudioControls, admin panels,
AnonymousForm, groups-layout, AgentLiveTab, RankingFilters, UserLanguageSection, translation-monitor,
language-select), iOS a11y (OnboardingStepViews, BubbleFooter, MagicLinkView, MiniAudioPlayerBar),
web a11y icon-only buttons (AgentConversationsTab, TriggerSchedulingModal, UserPicker,
AudioEffectsCarousel, CustomizationManager), admin dark mode, iOS brand indigo migration.

Cette itération traite : i18n des composants d'attachement (aria-labels brisés pour les non-francophones)
et des placeholders de sélection de langue dans les paramètres. Les instances restantes de
`.font(.system(size:))` dans PostDetailView sont des indicateurs emoji/drapeaux décoratifs —
intentionnellement maintenus en taille fixe.

---

## Web — i18n Attachments aria-labels ✅ TRAITÉ

### Sévérité HAUTE — accessibilité brisée pour utilisateurs non-francophones

Les aria-labels codés en dur en français étaient lus par les lecteurs d'écran quelle que soit
la langue de l'interface. Corrigé en ajoutant `useI18n('attachments')` + section `"actions"`
dans les 4 locales (fr/en/es/pt).

| File | Strings migrées | Clés ajoutées |
|------|----------------|---------------|
| `attachments/VideoAttachment.tsx` | `title`, `aria-label` (delete) | `actions.deleteVideo`, `actions.deleteVideoNamed` |
| `attachments/ImageAttachment.tsx` | `aria-label` (open), `title`, `aria-label` (delete) | `actions.openImageNamed`, `actions.deleteImage`, `actions.deleteImageNamed` |
| `attachments/FileAttachment.tsx` | `aria-label` (open), `title`, `aria-label` (delete) | `actions.openFileNamed`, `actions.deleteFile`, `actions.deleteFileNamed` |
| `attachments/DocumentAttachment.tsx` | `title`, `aria-label` (delete) | `actions.deleteTextFile`, `actions.deleteFileNamed` |

Clés ajoutées dans `locales/{fr,en,es,pt}/attachments.json` sous `"actions"`.

---

## Web — i18n Settings Language Placeholders ✅ TRAITÉ

### Sévérité MOYENNE

`components/settings/user-settings-content.tsx` utilisait des placeholders français hardcodés
pour les 3 sélecteurs de langue (système, régional, personnalisé).

Ajout de `useI18n('settings')` et clés `translation.mainLanguages.*Placeholder` dans les 4 locales.

---

## iOS — Dynamic Type PostDetailView (emoji flags) — NON MIGRÉ (intentionnel)

Les 3 instances restantes de `.font(.system(size:))` dans `PostDetailView.swift` sont des
rendus de caractères emoji (drapeaux de langue) utilisés comme indicateurs décoratifs d'état actif/inactif :
- `Text(display?.flag ?? "?").font(.system(size: isActive ? 12 : 10))` — drapeau avec état actif
- `Text(display.flag).font(.system(size: 11))` — drapeau dans panneau secondaire
- `Text(display?.flag ?? "?").font(.system(size: isActive ? 11 : 9))` — drapeau de repost

Les tailles 9–12pt sont sub-caption2 et conditionnelles selon l'état. Le scaling Dynamic Type
sur ces indicateurs emoji créerait des problèmes de layout. Maintenu en `.system(size:)` par
convention des itérations précédentes (même traitement que les icônes état vide 32–48pt).

---

## Findings pour itérations suivantes

### Web — Strings FR résiduelles (Moyenne Priorité)

| File | Strings |
|------|---------|
| `attachments/AttachmentGallery.tsx:300` | `title="Supprimer"` (déjà useI18n — juste câbler `t('contextMenu.delete')`) |
| `pdf/PDFViewer.tsx:167,260` | `title="Supprimer ce PDF"`, `title="Télécharger"` |
| `video/VideoControls.tsx:153` | `title="Télécharger"` |
| `text/TextViewer.tsx:313` | `title="Télécharger"` |
| `settings/user-settings-content.tsx` | Nombreux labels et descriptions hardcodés (Langue système, Langue régionale, Paramètres de traduction, etc.) |
| `admin/agent/AgentConfigDialog.tsx:195,422,655` | Placeholders hardcodés |
| `admin/agent/AgentGlobalConfigTab.tsx:201,213` | `placeholder="Aucun"` × 2 |
| `v2/ConversationDrawer.tsx:256,273,285` | Labels/placeholders hardcodés |
| `conversations/MessageSearch.tsx:120` | `placeholder="Rechercher dans la conversation…"` |
| `common/messages-display.tsx:57` | `emptyStateMessage = "Aucun message pour le moment"` |

### iOS — Color(hex:) Résiduels (Moyenne Priorité)

| File | Occurrences | Nature |
|------|------------|--------|
| `UniversalComposerBar.swift` | ~18 | Recording states, ephemeral gradients |
| `ConversationInfoSheet.swift` | ~12 | Member role colors, error states |
| `ConversationDashboardView.swift` | ~24 | Sentiment analytics score colors |
| `ConversationPreferencesTab.swift` | ~22 | Feature flag icon colors |

Note : certaines occurrences sont des valeurs dynamiques runtime (`accentColor`, `authorColor`)
non-migrables — audit requis avant migration.

### iOS — Dynamic Type restant (Haute Priorité)

- `PostDetailView.swift` restants (emoji flags) — intentionnellement maintenus
- `FeedPostCard+Media.swift` — accessibility labels sur media grid (non Dynamic Type)
- `MessageDetailSheet.swift` — scope large, non évalué
Builds on iterations 1–6. Previous work covered: MeeshyColors migration, Dynamic Type (all major
iOS views), iOS a11y (FeedPostCard, OnboardingStepViews, MagicLinkView), web i18n (15+ components),
web a11y (icon-only buttons), web dark mode (not-found, user-selector, access-denied).

This iteration was split across two parallel workstreams merged separately:
- **PR #384**: Web broken i18n key paths, new components i18n, iOS sentiment colors + ConversationInfoSheet
- **PR #389**: iOS toast string localization (remaining ViewModels) + ConversationPreferencesTab colors

---

## Web — Broken i18n Key Paths — HIGH (silent failures)

_(Fixed in PR #384)_

The `useI18n(namespace)` hook extracts the namespace sub-object from the JSON file. This means
after loading `admin.json` with `useI18n('admin')`, the translations object IS the `admin.json`
root — no extra `admin.` prefix is needed.

### Admin components: extra `admin.` prefix (key-not-found, shows raw key string in UI)

| File | Broken call | Correct call |
|------|------------|-------------|
| `RankingFilters.tsx:71` | `t('admin.ranking.selectEntity')` | `t('ranking.selectEntity')` |
| `RankingFilters.tsx:108` | `t('admin.ranking.selectCriterion')` | `t('ranking.selectCriterion')` |
| `RankingFilters.tsx:153` | `t('admin.ranking.selectPeriod')` | `t('ranking.selectPeriod')` |
| `AgentLiveTab.tsx:453` | `t('admin.agentLive.selectConversation')` | `t('agentLive.selectConversation')` |
| `UserLanguageSection.tsx:98` | `t('admin.userDetail.languagePreferences')` | `t('userDetail.languagePreferences')` |
| `translation-monitor.tsx:158` | `t('admin.translationMonitor.loadingMetrics')` | `t('translationMonitor.loadingMetrics')` |

### Groups component: extra `groups.` prefix (same issue)

| File | Broken call | Correct call |
|------|------------|-------------|
| `groups-layout-responsive.tsx:402` | `tGroups('groups.noGroups')` | `tGroups('noGroups')` |
| `groups-layout-responsive.tsx:403` | `tGroups('groups.noGroupsDescription')` | `tGroups('noGroupsDescription')` |
| `groups-layout-responsive.tsx:576` | `tGroups('groups.details.createdOn')` | `tGroups('details.createdOn')` |
| `groups-layout-responsive.tsx:590` | `tGroups('groups.list.selectCommunity')` | `tGroups('list.selectCommunity')` |
| `groups-layout-responsive.tsx:592` | `tGroups('groups.list.selectCommunityDescription')` | `tGroups('list.selectCommunityDescription')` |

---

## Web — Hardcoded Strings — HIGH

_(Fixed in PR #384)_

| File | Line | Hardcoded | Key |
|------|------|-----------|-----|
| `translation-monitor.tsx` | 272 | `"Répartition par langue"` | `translationMonitor.languageBreakdown` |
| `groups-layout-responsive.tsx` | 397 | `"Chargement..."` | `list.loadingInProgress` |
| `UserLanguageSection.tsx` | 77 | `'Préférences de langue mises à jour'` | `userDetail.languagePreferencesSaved` |
| `UserLanguageSection.tsx` | 82 | `'Erreur lors de la mise à jour'` | `userDetail.languagePreferencesError` |

---

## Web — New Components Missing i18n — HIGH

_(Fixed in PR #384)_

### MessageSearch.tsx
- No `useI18n` hook
- Hardcoded: placeholder, close aria-label, error text, empty state
- Keys added: `conversations.messageSearch.{placeholder,close,error,noResults}`

### PinnedMessageBanner.tsx
- No `useI18n` hook
- Hardcoded: `aria-label="Fermer le message épinglé"`
- Key added: `conversations.pinnedBanner.close`

---

## iOS — Hardcoded Sentiment Colors — HIGH

_(Fixed in PR #384)_

`ConversationDashboardView.swift` uses literal hex colors for sentiment analysis displays:

| Hex | Semantic | Occurrences |
|-----|----------|-------------|
| `"34D399"` | `MeeshyColors.success` | 8 |
| `"FBBF24"` | `MeeshyColors.warning` | 4 |
| `"F87171"` | `MeeshyColors.error` | 4 |

`ConversationInfoSheet.swift`:

| Hex | Semantic | Usage |
|-----|----------|-------|
| `"EF4444"` | `MeeshyColors.error` | Block button (4×) |
| `"4ECDC4"` | `MeeshyColors.indigo400` | E2EE lock icon |

---

## iOS — Hardcoded Toast Strings — HIGH

_(Fixed in PR #389)_

All `FeedbackToastManager.shared.show*()` calls with raw French/mixed strings in ViewModels.

### FeedViewModel.swift (15 strings)
| Function | Old string | Key |
|----------|-----------|-----|
| `likePost` observeOutcome | `"Erreur lors du like"` | `feed.like.error` |
| `bookmarkPost` success | `"Ajoute aux favoris"` | `feed.bookmark.success` |
| `bookmarkPost` error | `"Erreur lors de l'enregistrement"` | `feed.bookmark.error` |
| `sendComment` (×2) | `"Erreur lors de l'envoi du commentaire"` | `feed.comment.sendError` |
| `likeComment` | `"Erreur lors du like"` | `feed.like.error` |
| `repostPost` | `"Erreur lors du repost"` | `feed.repost.error` |
| `sharePost` | `"Erreur lors du partage"` | `feed.share.error` |
| `deletePost` success/error | `"Post supprime"` / `"Erreur lors de la suppression"` | `feed.post.deleted` / `feed.post.deleteError` |
| `reportPost` success/error | `"Signalement envoye"` / `"Erreur lors du signalement"` | `feed.post.reported` / `feed.post.reportError` |
| `updatePost` success/error | wrong `String(localized:)` pattern | `feed.post.edited` / `feed.post.editError` |
| `pinPost` success/error | `"Post epingle"` / `"Erreur lors de l'epinglage"` | `feed.post.pinned` / `feed.post.pinError` |

### PostDetailViewModel.swift (6 strings)
| Function | Old string | Key |
|----------|-----------|-----|
| `fetchCommentsFromNetwork` | `"Erreur lors du chargement des commentaires"` | `feed.comment.loadError` |
| `likePost` (×2) | `"Erreur lors du like"` | `feed.like.error` |
| `sendComment` (×2) | `"Erreur lors de l'envoi du commentaire"` | `feed.comment.sendError` |
| `sendReply` | `"Erreur lors de l'envoi de la réponse"` | `feed.comment.replyError` |

### StoryViewModel.swift (4 strings — 2 unique)
| Function | Old string | Key |
|----------|-----------|-----|
| `publishStory` success (×2) | `"Story publiee"` | `story.published` |
| `publishStory` error (×2) | `"Echec de la publication de la story"` | `story.publishError` |

### BookmarksViewModel.swift (2 strings)
| Function | Old string | Key |
|----------|-----------|-----|
| `load` error | `"Erreur lors du chargement des favoris"` | `feed.bookmark.loadError` |
| `removeBookmark` error | `"Erreur lors de la suppression du favori"` | `feed.bookmark.removeError` |

### StatusViewModel.swift (3 strings)
| Function | Old string | Key |
|----------|-----------|-----|
| `publishStatus` error | `"Erreur lors de la publication du statut"` | `status.publishError` |
| `clearStatus` error | `"Erreur lors de la suppression du statut"` | `status.deleteError` |
| `reactToStatus` error | `"Erreur lors de la reaction"` | `status.reactError` |

### ConversationView.swift (2 strings)
| Location | Old string | Key |
|----------|-----------|-----|
| `accessRevoked` handler | `"Vous n'avez plus acces a cette conversation"` | `conversation.accessRevoked` |
| Message deep-link notFound | `"Message introuvable"` | `conversation.messageNotFound` |

---

## iOS — Color(hex:) Semantic Migration — MEDIUM

_(Fixed in PR #389)_

### ConversationPreferencesTab.swift (11 occurrences)
| Hex | Token | Context |
|-----|-------|---------|
| `F87171` | `MeeshyColors.error` | Error text + delete button |
| `3B82F6` | `MeeshyColors.info` | Organization section (7×) |
| `FF6B6B` | `MeeshyColors.error` | Notification toggles (2×) |
| `F59E0B` | `MeeshyColors.warning` | Archive button |
| `F97316` | `MeeshyColors.warning` | Leave group button |

---

## ✅ COMPLÉTÉE — PR #384 (web + iOS colors) + PR #389 (iOS toasts + remaining colors) mergées dans main
