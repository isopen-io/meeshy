# UI/UX Analysis — Iteration 7 (2026-06-08)

## Context

Builds on iterations 1–6. Previous work covered: MeeshyColors full migration, Dynamic Type (most views), iOS a11y systemic (FeedPostCard, ThreadView, ThemedConversationRow, OnboardingStepViews, MagicLink), web i18n (30+ components), web a11y (8+ icon buttons), dark mode (admin layout).

---

## iOS — MeeshyColors — HIGH

### MessageEffectModifiers.swift

| Line | Hardcoded | Replace with |
|------|-----------|-------------|
| 163 | `Color(hex: "#6366F1")` in `spawnFireworks` color array | `MeeshyColors.indigo500` |
| 186 | `Color(hex: "#6366F1").opacity(0.4)` in `ExplodeOverlay` | `MeeshyColors.indigo500.opacity(0.4)` |
| 231 | `Color(hex: "#6366F1").opacity(...)` in `GlowEffect` | `MeeshyColors.indigo500.opacity(...)` |

---

## Web — i18n Hardcoded Strings — CRITICAL/HIGH

### language-select.tsx (components namespace)
- Line 34: default prop `placeholder = "Sélectionner une langue"` → `t('languageSelect.placeholder')`
- Line 86: `placeholder="Rechercher une langue..."` → `{t('languageSelect.search')}`
- Component already has `useI18n('components')` ✅

### translation-monitor.tsx (admin namespace)
- Line 264: `File d'attente` → `{t('translationMonitor.queue')}`
- Line 272: `Répartition par langue` → `{t('translationMonitor.languageBreakdown')}` (key exists)
- Line 285: `{data.errorRate}% erreurs` → `{data.errorRate}% {t('translationMonitor.errorsUnit')}`
- Line 307: `Dernière mise à jour:` → `{t('translationMonitor.lastUpdated')}`
- Component already has `useI18n('admin')` ✅

### AgentGlobalConfigTab.tsx (admin namespace)
- Line 151: `placeholder="Prompt système global..."` → `{t('agentGlobal.systemPromptPlaceholder')}`
- Component already has `useI18n('admin')` ✅

### AgentConfigDialog.tsx (admin namespace — no hook yet)
- Line 195: `placeholder="Rechercher un groupe..."` → add `useI18n('admin')` + `{t('agentConfig.searchGroup')}`
- Line 368: `placeholder="Chercher pour restreindre..."` → `{t('agentConfig.searchTriggers')}`
- Line 422: `placeholder="Ajouter un utilisateur..."` → `{t('agentConfig.addControlledUser')}`
- Line 448: `placeholder="Exclure un utilisateur..."` → `{t('agentConfig.excludeUser')}`
- Line 583: `placeholder="Instructions personnalisées..."` → `{t('agentConfig.agentInstructions')}`

### AgentTopicEditModal.tsx (admin namespace — no hook yet)
- Line 171: `placeholder="Lance un NOUVEAU sujet..."` → add `useI18n('admin')` + `{t('agentTopic.newTopicPrompt')}`
- Line 182: `placeholder="actualité {{label}} cette semaine"` → `{t('agentTopic.searchQuery')}`

### RankingFilters.tsx (admin namespace)
- Line 114: `placeholder="Filtrer les critères..."` → `{t('ranking.filterCriteria')}`
- Line 170: label `Nombre de résultats` → `{t('ranking.resultsCount')}`
- Line 174: `placeholder="Nombre de résultats"` → `{t('ranking.resultsCount')}`
- Component already has `useI18n('admin')` ✅

### ConversationDrawer.tsx (conversations namespace)
- Line 273: `placeholder="Rechercher ou créer..."` → `{t('conversationDetails.searchOrAddTag')}` (key exists)
- Line 285: `placeholder="Rechercher ou créer..."` → `{t('conversationDetails.searchOrAddTag')}`
- Component already has `useI18n('conversations')` ✅

### MessageSearch.tsx (conversations namespace — no hook yet)
- Line 120: `placeholder="Rechercher dans la conversation…"` → add `useI18n('conversations')` + `{t('messageSearch.placeholder')}`
- Line 133: `aria-label="Fermer la recherche"` → `{t('messageSearch.closeSearch')}` (also a11y win)

### DeliveryQueuePanel.tsx (admin namespace)
- Line 94: Button text `Réessayer` → `{t('deliveryQueue.retry')}`
- Line 110: `en attente` → `{t('deliveryQueue.pending')}`
- Component already has `useI18n('admin')` ✅

---

## Web — a11y Missing aria-labels — HIGH

### ScanLogTable.tsx
- Line 158: ChevronLeft Button → add `aria-label={t('scanLog.previousPage')}`
- Line 161: ChevronRight Button → add `aria-label={t('scanLog.nextPage')}`

### DeliveryQueuePanel.tsx
- Line 119: Icon-only RefreshCw Button → add `aria-label={t('deliveryQueue.refresh')}`

### ConversationSidebar.tsx
- Plus SVG button (new conversation) → add `aria-label={t('conversationSidebar.newConversation')}`
- Settings SVG button → add `aria-label={t('conversationSidebar.settings')}`

---

## Web — Dark Mode Gaps — MEDIUM

### translation-monitor.tsx
- Line 272: `text-gray-900` → `text-gray-900 dark:text-gray-50`
- Line 277: `bg-gray-50` → `bg-gray-50 dark:bg-gray-800`
- Line 284: `text-gray-600` → `text-gray-600 dark:text-gray-400`
- Line 290: `bg-gray-200` → `bg-gray-200 dark:bg-gray-700`
- Line 306: `text-gray-500` → `text-gray-500 dark:text-gray-400`

---

## Deferred to Iteration 8

- `share-affiliate-modal.tsx` custom link placeholder (minor)
- `TagInput.tsx` default prop (generic component; callers should pass t())
- `MessageComposer.tsx` French aria-labels (complex component)
- `Switch.tsx` dark mode on knob
- `access-denied.tsx` dark mode
- `ContactsList.tsx` MoreVertical aria-label
- `AgentConfigDialog.tsx` section heading strings (`Sujets neufs & actualité`, etc.)
