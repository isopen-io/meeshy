# UI/UX Analysis — Iteration 47w (2026-06-12)

## Scope
**Web exclusivement** (suffixe `w` — agents distincts pour iOS/Android ; les vues iOS servent
uniquement de référence de parité). Base : `main` post-merge #605 (46w) / #608 (iOS).
Solde le carry-over n°1 du branch-tracking : le lot i18n `agent.config` admin.

## Audit documentaire (étapes 1–3 de la routine)

### Doublons entre analyses
- Aucun doublon de findings entre 45w/46w/45/45i : périmètres disjoints re-vérifiés
  (46w = surface v2 strings + tokens erreur ; 45w = locales de formatage 'fr-FR' ;
  cette passe = labels/tooltips admin agent, jamais audités en détail auparavant).
- Vérification footer automatisée : 6 analyses flaggées « sans Status » par le grep
  (1, 3, 5, 7, 8, 33) sont des **faux positifs** — toutes portent un marqueur
  « ✅ COMPLÉTÉE » ou « Status: Analysis complete ». Les 37 analyses restent annotées.

### Couverture analyses → plans
Chaque analyse 1–47w possède son plan homonyme dans `docs/plans/uiux/` (vérifié par script).
Aucune orpheline.

### Annotations de complétion
`branch-tracking.md` mis à jour : iter-46w marquée mergée (PR #605) dans History —
elle était encore « ⏳ » alors que le merge est dans main (commit d10a80c).

## Web Findings

### High : lot admin agent — 144 strings FR hardcodées (carry-over 46w, inventaire réel)
L'estimation du carry-over (« ~58 labels/tooltips + 6 + 15 ») sous-comptait. Inventaire exact :
1. **AgentConfigDialog** (122) : tout le formulaire après la section « Comportement » était
   en dur — sections Triggers, Seuils & Contrôle, Planificateur, Instructions, Recherche Web,
   Sujets neufs, Génération, Quality Gate, Scheduling & Rythme, Timeline, Rôles ; labels,
   tooltips InfoIcon, hints, placeholders, options du select type d'agent, unités
   (« jour(s) », « Heures/Minutes »), compteurs interpolés (topics éligibles,
   « Auto-détectés (n) », température %, score %, boost x), boutons footer
   (Annuler/Créer/Enregistrer), libellés métadonnées (Type/Participants/Messages).
2. **AgentLlmTab** (6) : tooltips InfoIcon provider/model/température/maxTokens/budget/coût.
3. **AgentGlobalConfigTab** (16) : 15 tooltips InfoIcon + placeholder du system prompt.

Un admin en interface en/es/pt voyait l'écran de configuration agent intégralement en
français. Pattern appliqué : clés sœurs `label` + `labelHelp` dans les arbres **existants**
`agentConfig.*` / `llm.*` / `globalConfig.*` (décision : PAS de nouvel arbre `agent.config.*`
proposé par le carry-over — les trois arbres existaient déjà avec ce découpage, note de
l'iter-33 confirmée).

### Conformes (vérifiés, PAS des violations)
- `placeholder={config?.hasApiKey ? '********' : 'sk-...'}` (AgentLlmTab) — formats techniques.
- Codes de rôles `USER/ADMIN/MODO/...` et noms de providers/modèles — identifiants produit.
- Badges types de conversation (AgentGlobalConfigTab) — déjà via `agent.overview.conversationType.*`.
- `@deprecated` (strong, AgentConfigDialog) — marqueur technique conservé tel quel ;
  seule l'explication qui suit est i18n-isée.

## Parité iOS (référence uniquement)
Pas d'équivalent iOS de la console d'admin agent — pas de contrainte de parité sur cette passe.
Le pattern label+Help suit la convention xcstrings clé/commentaire utilisée côté iOS.

## Anti-repetition check
Vérifié contre 46w (UserPicker DÉJÀ corrigé — non retouché ici ; debug.tsx,
AgentArchetypesTab, BackSoundDetails purgés du carry-over en 46w — non re-audités),
45w (locales de formatage — AgentConfigDialog utilise déjà `locale` de useI18n pour
`toLocaleDateString`, conservé), 44/45 (fichiers non touchés).
Exclusions intentionnelles respectées (maps speech/partage, tests, scripts).

## Différés (48w+) — tracés dans branch-tracking.md
- Chart hex sans variante dark : RankingStatsImpl (10+), MermaidDiagramImpl (6, thème
  mermaid fixe `default`), AgentOverviewTab (2 pie).
- Consolidation `notifications/preferences` page vs composant.
- Réactions par pièce jointe (wiring gateway, commun web+Android).
- Audit qualité es/pt des traductions existantes.
- Optimisations 45w/46w toujours ouvertes : deep links `/v2/chats?id=`, swipe-back mobile
  web, audit dark pages admin.
- Dette environnement repérée (hors UI, à signaler) : `tsc --noEmit` sur apps/web remonte
  ~1100 erreurs préexistantes hors périmètre (tests + pages admin, TS 6.0.3
  `downlevelIteration` déprécié dans tsconfig) — aucune dans les fichiers de cette passe.

## ✅ Status : itération soldée — corrections implémentées, voir plan 2026-06-12-plan-iteration-47w
NE PAS retraiter les findings ci-dessus.
