# UI/UX Analysis — Iteration 33 (2026-06-09)

## Scope
Restore iter-32 locale keys lost in merge conflict, wire admin user-detail sub-components + agent tabs i18n, dark mode input fixes, iOS Dynamic Type + accessibility labels.

## Web Findings

### Critical: Lost locale keys (iter-32 merge conflict)
- `apps/web/locales/{en,fr,es,pt}/admin.json` — merge commit `64971fe6` ("Merge branch 'main' into feat/uiux-iter32") resolved a conflict by keeping main's admin.json, which discarded 254+ lines added in commit `43ffda02`. The following 6 sections are missing from all 4 locale files, but the components already reference them:
  - `dashboard.*` (48 keys) — used by `app/admin/page.tsx`
  - `layout.*` (20 keys) — used by `components/admin/AdminLayout.tsx`
  - `usersList.*` (36 keys) — used by `app/admin/users/page.tsx`
  - `usersDetail.*` (44 keys) — used by `app/admin/users/[id]/page.tsx`
  - `rankingPage.*` (7 keys) — used by `app/admin/ranking/page.tsx`, `RankingPodium.tsx`, `RankingFilters.tsx`
  - `settingsModal.*` (5 keys) — used by `components/settings/user-settings-modal.tsx`

### No i18n — user-detail sub-components
- `components/admin/user-detail/UserContactInfoSection.tsx` — ~14 hardcoded French strings: section title, field labels (Email, Pays, Téléphone, Fuseau horaire), edit/cancel/save buttons, success/error toasts
- `components/admin/user-detail/UserPersonalInfoSection.tsx` — ~16 hardcoded strings: section title, field labels (Prénom, Nom, Nom d'affichage, Username, Biographie), placeholder, helper text, char count
- `components/admin/user-detail/UserGeolocationSection.tsx` — ~8 hardcoded strings: section title, last connection section, Localisation/Adresse IP/Appareil labels, registration context section, Pays label
- `components/admin/user-detail/UserSecuritySection.tsx` — ~20 hardcoded strings: section title, account state section, locked/unlocked badges, failed attempts/reason labels, unlock button, 2FA section, enabled/disabled badges, password management section, reset button
- `components/admin/user-detail/UserActivitySection.tsx` — ~20 hardcoded strings: section title, loading text, collapsible section titles (share links, tracked links, affiliation tokens, contacts), column labels (clics, inscrits, conversion, etc.)

### No i18n — agent tabs
- `components/admin/agent/AgentLlmTab.tsx` — ~30 hardcoded strings: card title ("Configuration LLM"), form labels (Provider, Modèle, Clé API, Température, Max Tokens), info tooltips, save button; partial: uses `t('llm.budgetManagement')`, `t('llm.generationSettings')`
- `components/admin/agent/AgentGlobalConfigTab.tsx` — ~35 hardcoded strings: card title ("Configuration Globale"), subtitle, system prompt label+info+placeholder, provider/model labels+info, fallback section, budget/concurrency section labels+info, scan parameters section; partial: uses `t('globalConfig.*')`

### Dark mode issues
- `app/admin/users/new/page.tsx:333,349,372` — `<select className="... bg-white">` and `<input className="... bg-white">` without `dark:bg-gray-800 dark:border-gray-700 dark:text-gray-100`

## iOS Findings

### Dynamic Type
- `Features/Main/Components/SecurityVerificationView.swift:18,69,102` — `.font(.system(size: 64))`, `.font(.system(size: 16))`, `.font(.system(size: 14))` hardcoded; frozen for large accessibility font users

### Accessibility — gesture labels
- `Features/Main/Views/Bubble/BubbleStandardLayout+Media.swift:565` — `.onLongPressGesture` on media bubble without `.accessibilityLabel`
- `Features/Main/Views/Bubble/BubbleReactionsOverlay.swift:134,233` — `.onLongPressGesture` on reaction bubbles without accessibility labels

## Status: Analysis complete — iteration implemented below.

### Notes
- iter-32 auditLogs section already exists in admin.json from iter-31 (keys overlap, current version is correct — no duplicate addition needed)
- UserContactInfoSection/UserPersonalInfoSection/etc. live in `components/admin/user-detail/` (not `components/admin/users/`)
- AgentLlmTab already uses `t('llm.*)` and `t('globalConfig.*)` for some keys — extend those sections rather than creating new namespaces
