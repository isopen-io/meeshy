# UI/UX Analysis — Iteration 48i (2026-06-12)

## Scope
**iOS exclusivement** (suffixe `i` — Web et Android sont couverts par d'autres agents).
Solde la « passe dédiée » annoncée au différé de l'itération 45i : **éradication complète
des reliquats de l'ancienne palette pré-charte (`08D9D6`/`FF2E63`/`4ECDC4`) côté app**
(`apps/ios/Meeshy` + `apps/ios/MeeshyWidgets`), plus les deux suppressions de code mort
différées (SampleData.swift, et MessageComposer.swift découvert mort pendant l'audit).
La CI `ios-tests.yml` (compile Xcode 26.1.1 + tests sur simulateur iOS 18.x) sert de
vérification de build pour les éditions pbxproj, ce qui débloque les suppressions
différées « faute de build local ».

## iOS Findings

### Critical : Épuration — 2 fichiers morts + 13 structs feed legacy (zéro usage vérifié)
- `Models/SampleData.swift` (390 lignes, 6 × `4ECDC4`) — différé 45i ; zéro référence hors
  un commentaire d'index dans `Models.swift` (mis à jour). Suppression fichier + 4 entrées
  pbxproj.
- `Components/MessageComposer.swift` (219 lignes, 4 occurrences trio) — composer legacy
  intégralement supplanté par `UniversalComposerBar` ; les seules « références » sont des
  commentaires de parité dans UniversalComposerBar. Suppression fichier + 4 entrées pbxproj.
- `RootViewComponents.swift` lignes 697–1113 — audit de liveness (subagent, symbole par
  symbole, word-boundary) : `ThemedFeedComposer`, `ThemedFeedCard`, `FeedActionButton` et
  les 10 wrappers « Legacy Support » (`FeedOverlay`, `ColorfulFeedOverlay`,
  `ColorfulFeedComposer`, `ColorfulFeedCard`, `ColorfulFeedAction`,
  `ColorfulQuickActionButton`, `QuickActionButton`, `FeedComposer`, `LegacyFeedCard`,
  `FeedAction`) ne sont instanciés nulle part — morts transitifs entre eux. Le composer
  feed vivant est `FeedComposerSheet` (FeedView+Attachments) + `composerOverlay` (FeedView,
  iPad). Suppression de 418 lignes (dont 8 occurrences trio + gradient `[4ECDC4, 45B7D1]`
  + `F8B500` shadow). Commentaire d'index de `RootView.swift` mis à jour.

### Critical : Ancienne palette sur surfaces vivantes (mapping charte)
Remplacements par tokens `MeeshyColors` (jamais de re-hardcode) :
- `RootViewComponents` (ThemedFeedOverlay, LIVE) — orb ambiant `4ECDC4` → `indigo400`
  (règle « ambient orbs = famille Indigo » ; l'orb jumeau est déjà `error`) ; avatar
  composer `FF6B6B`/`4ECDC4` → `brandPrimaryHex`/`brandDeepHex` (gradient signature pour
  l'avatar « moi ») ; spinner load-more → `brandPrimary`
- `FeedView` — carte « ajouter du contenu » `surfaceGradient(tint:)`/`border(tint:)`
  `4ECDC4` → `brandPrimaryHex` ; avatar composer → paire brand ; `UploadProgressBar`,
  bordure carte composer, `LocationPickerView` → `brandPrimaryHex` ; toolbar : photo →
  `brandPrimary`, mic `FF2E63` → `errorStrong` (convention « enregistrement = rouge »,
  ton distinct du camera déjà `error`)
- `FeedView+Attachments` (FeedComposerSheet, composer feed principal) — mêmes mappings
  (avatar, 2 spinners, UploadProgressBar, toolbar photo/mic, LocationPickerView,
  3 fallbacks `prepareXXX(accentColor:)` → `brandPrimaryHex`)
- `AttachmentPreparationService` — défauts `accentColor: "4ECDC4"` (×2) et fallback
  `isVideo ? "FF6B6B" : "4ECDC4"` → `brandPrimaryHex` uniforme (couleur de placeholder
  thumbnail) ; +`import MeeshyUI`
- `ConversationAnimatedBackground` — défauts `accentHex: "FF2E63"` / `secondaryHex:
  "08D9D6"` → `brandPrimaryHex` / `indigo400Hex` (tous les appelants vivants passent la
  palette de la conversation ; seuls les fallbacks changent)
- `WidgetPreviewView` — carte header : gradient non-lu `[FF2E63, A855F7]` → `[error,
  indigo600]`, lu `[08D9D6, 4ECDC4]` → `[indigo400, indigo500]`, ombre assortie ; quick
  actions : Nouveau → `[indigo500, indigo700]` (gradient signature), Partager →
  `[shareAccent, indigo500]`, Réglages `[FF6B6B, FF2E63]` → `[indigo600, indigo800]`
  (réglages ≠ destructif, le rouge était un contresens sémantique) ; carte lien Partage
  `08D9D6` → `shareAccentHex` (accent dédié partage de la charte)
- `ConversationInfoSheet` — bouton Partager `4ECDC4` → `shareAccentHex`
- `MemberManagementSection` — badge rôle Modérateur `4ECDC4` → `success` (distinct de
  admin `3B82F6` et creator or `F8B500`)
- `BlockedUsersView` — swipe « Débloquer » `.tint(4ECDC4)` → `success` (action positive,
  précédent 45i FriendRequestListView)
- `UserStatsView` — carte Conversations `4ECDC4` → `brandPrimaryHex`
- `AboutView` — section Liens (header + 3 rows + fond, ×5) `4ECDC4` → `infoHex`
  (sémantique « lien/info », distincte des sections violette `9B59B6` et or `F8B500`)
- `MediaDownloadSettingsView` — row Images `4ECDC4` → `brandPrimaryHex`
- `MeeshyWidgets.swift` (extension widget, miroir `WidgetColors`) — sample data galerie :
  `4ECDC4` ×2 → `indigo400Hex` (nouveau miroir) / `brandDeepHex`

### Accessibility (surfaces touchées)
- `FeedComposerSheet` (toolbar) — 6 boutons icône-seuls SANS `.accessibilityLabel`
  alors que la toolbar jumelle de `FeedView.composerOverlay` les a → labels ajoutés
  (mêmes clés localisées que FeedView pour cohérence VoiceOver)

### i18n (bonus, surface touchée)
- `WidgetPreviewView` quick actions — labels « Nouveau »/« Partager »/« Post »/« Réglages »
  en français brut → `String(localized: "widget.preview.action.*")` + 4 clés ×5 locales
  (de/en/es/fr/pt-BR) ajoutées à `Localizable.xcstrings` (append en fin, format Xcode
  préservé — diff +140 lignes propres)

### Conforme (vérifié, PAS des violations — ne pas re-flagger)
- `StoryViewerView+Content:180` — `08D9D6@0.1 softLight` = teinte du **filtre artistique
  « cool »** (contenu, pas chrome UI) ; symétrique du rendu SDK `SlideMiniPreview` qui
  utilise `colorMultiply(0.80, 0.92, 1.0)` pour le même filtre. Laissé.
- `MeeshyTests` — hex trio dans les fixtures de tests (valeurs arbitraires de roundtrip)
- `FeedView`/`FeedView+Attachments` toolbar emoji `F8B500` / doc `9B59B6` et
  `prepareVideo` défaut `FF6B6B` — hors trio, relèvent du ladder catégoriel différé

## Anti-repetition check
Vérifié contre : iter-44b (composer UniversalComposerBar/AudioPostComposer — non touchés),
iter-45i (ThreadView/StoryTrayView/StoryViewerView/FriendRequestListView/StoryViewModel/
ConversationListHelpers/ConversationHelperViews — non touchés ; cette passe couvre
exactement la liste « reliquats » de son différé), iter-42/42b (Settings/Onboarding/
ConversationView — non touchés). Aucun fichier de cette passe n'avait reçu de passe
palette.

## Différés (pour itérations suivantes)
- **SDK MeeshyUI — ancienne palette (découvert 48i, hors périmètre app de cette passe)** :
  `EmojiReactionPicker` (accents `08D9D6` chrome UI ×3), `NotificationListView:64`
  (catégorie translations `08D9D6`), `MeeshyAvatar:417` (gradient fallback avec `08D9D6`),
  `CommunitySettingsView:19` (palette de choix), `AuthTextField`/`LanguageSelector`/
  `MeeshyForgotPasswordView` et autres fichiers MeeshyUI listés par grep — distinguer
  chrome UI (à migrer) des palettes de contenu utilisateur (Story
  `DrawingEditToolOptions`/`StoryComposerView`/`StoryTextEditorView` = swatches de dessin/
  fond choisis par l'utilisateur, à documenter comme intentionnels)
- `FriendRequestListView` — 11 polices figées (différé 45i, toujours ouvert)
- `PostDetailView` — `.textSelection(.enabled)` + 21 hex (re-vérifier puis solder)
- Arbitrage `time.*` vs `time.short.*` (FeedPostCard vs ShortRelativeTime)
- Grandes surfaces polices : ConversationInfoSheet (52), ConversationDashboardView (43),
  TwoFactorSetupView (42, héros intentionnels), CallView (34), InviteFriendsSheet (33),
  ProfileView/GlobalSearchView (32), SettingsView (27), NewConversationView (16),
  DataExportView (16), DataStorageView (11)
- Ladder catégoriel « arc-en-ciel » (FF9F43/45B7D1/2ECC71/F8B500/9B59B6/E74C3C/FF6B6B) —
  UniversalComposerBar+Attachments, toolbars FeedView/FeedComposerSheet (emoji/doc),
  UserStatsView/MediaDownloadSettingsView/AboutView (autres rows), `prepareVideo` défaut
  FF6B6B — à arbitrer avec la charte en une décision unique
- `VoiceProfileWizardView`/`TrackingLinksView` — `Color(hex:)` hors palette
- `AvatarContextMenuItem` → LocalizedStringKey (API SDK à évaluer)
- `ThemedConversationRow` theme-aware (leaf-view)
- Washes AudioPostComposer (décision design, acceptés)

## Status : ⏳ développement terminé — PR ouverte, merge dans main après CI verte — voir plan 2026-06-12-plan-iteration-48i
