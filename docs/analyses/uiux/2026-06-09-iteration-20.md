# UI/UX Analysis — Iteration 20 (2026-06-09)

## Scope
Settings components with hardcoded strings (no i18n or partial i18n).

## Findings

### Critical: No i18n at all
- `components/settings/video-settings.tsx` — 38 hardcoded English strings across 5 sections
- `components/settings/voice/VoiceQualityConfig.tsx` — 60+ hardcoded French strings

### Partial: strings not wrapped in t()
- `components/settings/audio-settings.tsx` lines 325-327 — MP3/OGG/WAV format labels
- `components/settings/message-settings.tsx` lines 347-350 — draft expiration day labels

## Changes

### Locale files (en/fr/es/pt settings.json)
Added new sections:
- `settings.video.*` — video call settings section (38 keys across 5 sub-sections)
- `settings.voiceQuality.*` — voice analysis metrics (60+ keys)
- `settings.audio.translation.formatMp3/Ogg/Wav` — audio format labels
- `settings.message.draftDays7/30/60/90` — draft expiration day options

### Components
- `video-settings.tsx` — added `useI18n('settings')`, all 38 strings now via `t()`
- `voice/VoiceQualityConfig.tsx` — added `useI18n('settings')`, refactored `getQualityLevel` to return i18n key, all 60+ strings via `t()`; `QualityMetric` accepts `recommendedRangeText` prop
- `audio-settings.tsx` — fixed 3 format SelectItem strings
- `message-settings.tsx` — fixed 4 draft expiration day SelectItem strings

---

## ✅ Status : itération soldée — corrections mergées dans main (cf. branch-tracking.md, History iter-20)
Annoté le 2026-06-12 (iter-45w). NE PAS re-auditer ces fichiers pour les mêmes findings ; tout reliquat est tracé dans les carry-overs de branch-tracking.md.
