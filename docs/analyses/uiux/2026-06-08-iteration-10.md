# UI/UX Audit — Iteration 10 (2026-06-08)

> Base: main after PR #381 (iteration 9). Issues verified as still present in current code.
> Previous iterations 1–9 did not address these specific files/lines.

---

## iOS Issues

### HIGH

**I1 — ReportUserView: 14 French defaultValues**
- File: `apps/ios/Meeshy/Features/Main/Views/ReportUserView.swift`
- Lines: 38, 53, 86, 122, 134, 156, 179, 191, 192, 208, 212, 255, 256, 257
- All `String(localized:defaultValue:bundle:)` calls use French defaults. Verified present in main after iterations 1–9.
- Affected keys: `report.user.title`, `common.close`, `report.user.reason`, `common.selected`, `report.user.details`, `report.user.details.a11y`, `report.user.submit` (×2), `report.user.submit.hint`, `report.user.success`, `report.user.error`, `report.user.reason.harassment`, `report.user.reason.inappropriate`, `report.user.reason.impersonation`

**I2 — OnboardingStepViews: 18+ hardcoded `.green`/`.red` system colors**
- File: `apps/ios/Meeshy/Features/Auth/Onboarding/OnboardingStepViews.swift`
- Lines: 57, 61, 86, 250, 299, 323, 391, 448, 463, 594, 597, 601, 644, 666, 669, 1007, 1167, 1170, 1221, 1224 (approx)
- SwiftUI system `.green`/`.red` bypass the design system. Should use `MeeshyColors.success` (#34D399) and `MeeshyColors.error` (#F87171).

**I3 — FloatingCallPillView: one French defaultValue**
- File: `apps/ios/Meeshy/Features/Main/Views/FloatingCallPillView.swift`
- Line: 106
- `defaultValue: "Inconnu"` → should be `"Unknown"`

---

## Web Issues

### MEDIUM

**W1 — ConversationList: skeleton loader missing ARIA live-region**
- File: `apps/web/components/conversations/ConversationList.tsx`
- Line: 133
- Loading skeleton has no `role="status"` or `aria-busy` — screen readers receive no loading signal.
- Fix: add `role="status" aria-busy="true" aria-label={t('loadingConversations')}` to wrapper; `aria-hidden="true"` on each skeleton row.

---

## Summary

| ID | Severity | File | Issue |
|----|----------|------|-------|
| I1 | HIGH | ReportUserView.swift | 14 French defaultValues |
| I2 | HIGH | OnboardingStepViews.swift | 18+ `.green`/`.red` system colors |
| I3 | HIGH | FloatingCallPillView.swift | 1 French defaultValue ("Inconnu") |
| W1 | MEDIUM | ConversationList.tsx | Skeleton missing role="status" |

---

## ✅ Status : itération soldée — corrections mergées dans main (cf. branch-tracking.md, History iter-10)
Annoté le 2026-06-12 (iter-45w). NE PAS re-auditer ces fichiers pour les mêmes findings ; tout reliquat est tracé dans les carry-overs de branch-tracking.md.
