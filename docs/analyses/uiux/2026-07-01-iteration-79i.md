# Itération 79i — Dynamic Type `FeedView+Attachments.swift`

**Date** : 2026-07-01
**Piste** : iOS (suffixe `i`)
**Branche** : `claude/upbeat-euler-szzmyg` (base = `main` HEAD `2261ca0f`)
**Gate** : CI `iOS Tests` (`ios-tests.yml`)
**Type** : sweep typographique a11y pur — 0 logique, 0 test neuf (parité 55i / 74i / 78i)

## Contexte

Le différé prioritaire iOS depuis 71i cite `FeedView+Attachments` (30 `.font(.system(size:))`
figés) comme une des grandes surfaces Dynamic Type restantes. Surface **orthogonale** aux 20 PRs
iOS ouvertes au moment du run (vérifié via `list_pull_requests`) : le composer de feed (#1182) et
le sheet de commentaires (#1139) sont des **fichiers distincts** (`FeedComposerView` / `FeedCommentsSheet`).
`FeedView+Attachments.swift` héberge l'extension `FeedView` (tuiles de pièces jointes en attente)
+ la struct `FeedComposerSheet` (composer plein écran fullscreen depuis `ThemedFeedOverlay`).

## Diagnostic

30 sites `.font(.system(size:))` figés → migration mécanique 1:1 selon la doctrine établie
(55i / 71i / 74i) : **texte de lecture + glyphes inline appariés à du texte** → `MeeshyFont.relative(size, weight:)`
(weight préservé) ; **glyphes décoratifs / boutons-icônes en géométrie fixe** gardés figés.

### Convertis (16) — texte de lecture + glyphes inline appariés

| Site | Avant | Rôle |
|---|---|---|
| tuile `feedAttachmentTile` label | `10, .medium` | libellé sous vignette |
| tuile `sheetAttachmentTile` label | `10, .medium` | libellé sous vignette |
| `reelTypeToggle` icône | `10` | glyphe inline chip Réel/Post |
| `reelTypeToggle` texte | `12` | texte chip Réel/Post |
| header « Annuler » | `15, .medium` | bouton |
| header « Nouveau post » | `16, .bold` | titre |
| header « Publier » | `15, .bold` | bouton |
| menu visibilité icône | `10` | glyphe inline (globe/person/lock) |
| menu visibilité texte | `12` | Public/Amis/Privé |
| placeholder composer | `17` | « Qu'avez-vous en tête ? » |
| `TextEditor` composer | `17` | texte de saisie |
| quoted author | `13, .semibold` | nom auteur cité |
| quoted timestamp | `11` | horodatage relatif |
| quoted content | `14` | corps du post cité |
| bouton langue icône | `14` | globe inline |
| bouton langue texte | `13, .semibold` | nom de langue |

### Gardés figés (14) — décoratifs / géométrie fixe (documentés ici, pas de commentaire inline
conformément à la règle « no comments » et au précédent 74i)

| Site | Taille | Raison |
|---|---|---|
| bouton suppression tuile (`xmark`) | `12, .bold` | glyphe dans cercle **fixe 28×28** — scaler déborderait |
| overlay vidéo `play.circle.fill` (tuile) | `20` | glyphe décoratif sur vignette **56×56** |
| icône location `mappin.circle.fill` (tuile) | `22` | glyphe décoratif sur vignette 56×56 |
| icône type-fichier (tuile) | `22` | glyphe décoratif sur vignette 56×56 |
| barre d'outils composer (×6) | `20` | **rangée d'icônes-boutons** en layout fixe (parité barres système) |
| overlay vidéo `play.circle.fill` (sheet) | `22` | glyphe décoratif sur vignette **72×72** |
| icône location `mappin.circle.fill` (sheet) | `26` | glyphe décoratif sur vignette 72×72 |
| icône type-fichier (sheet) | `26` | glyphe décoratif sur vignette 72×72 |
| bouton suppression sheet (`xmark`) | `9, .bold` | glyphe dans cercle **fixe 20×20** |

## Implémentation

- 1 fichier touché : `apps/ios/Meeshy/Features/Main/Views/FeedView+Attachments.swift`
- `MeeshyFont.relative` déjà accessible (fichier importe `MeeshyUI` ligne 7 ; helper `public` dans
  `MeeshyUI/Theme/Accessibility.swift`).
- Aucune modification de logique, de layout, de couleur, de state. Swaps littéraux uniquement.
- Pas de test neuf : couverture structurelle CI existante (comportement inchangé, parité 55i/74i/78i).

## Vérification

- `grep` : 16 `MeeshyFont.relative`, 14 `.font(.system(size:))` restants = exactement les décoratifs listés.
- Build/tests iOS non reproductibles localement (environnement Linux) → gate = CI `iOS Tests`.

## Différés (report vers 80i+)

Inchangés par rapport au pointeur 78i, moins `FeedView+Attachments` (soldé ici) :
- Dynamic Type grandes surfaces restantes : `StoryViewerView+Content` (attention collision i18n #1174), `CallView` (11 sites, léger).
- Glass adoption reste (`MessageOverlayMenu` via `AdaptiveGlassContainer` — lot dédié ; i18n déjà pris par #1172).
- i18n continu (`"Story expirée…"` — déjà pris par #1174 ; audit `Text/Button` FR figés hors surfaces prises).
- Palette hexes proches-mais-non-exacts (`#4ADE80`→success ? `#3B82F6`→info ?) — audit un par un avec vérif visuelle.

**NE PAS re-flagger** : les 14 sites figés de `FeedView+Attachments` (décisions design ci-dessus).
