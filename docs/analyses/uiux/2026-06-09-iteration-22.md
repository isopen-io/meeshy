# UI/UX Analysis — Iteration 22 (2026-06-09)

## Scope
Remaining hardcoded placeholder strings in user-settings, and last two unharvested strings in AudioEffectsPanel.

## Findings

### Partial (existing useI18n but unharvested strings)
- `components/settings/user-settings.tsx` — 5 hardcoded placeholders:
  - `placeholder="nouveau@email.com"` → key already exists: `profile.account.email.newEmailPlaceholder`
  - `placeholder="6 12 34 56 78"` → new key: `profile.phone.newPhonePlaceholder`
  - `placeholder="000000"` (×2) → new key: `profile.verification.codePlaceholder`
  - `placeholder="nouveau_username"` → key already exists: `profile.account.username.newUsernamePlaceholder`
  - `placeholder="••••••••"` → key already exists: `security.password.currentPasswordPlaceholder`
- `components/video-calls/AudioEffectsPanel.tsx` — 13 hardcoded strings:
  - `aria-label="Close audio effects panel"` → new key: `closePanel`
  - 12 musical note names (`Do (C)` … `Si (B)`) → new keys under `voiceCoder.key.notes.*`

## Changes

### Locale files
- `settings.json` (en/fr/es/pt): add `profile.phone.newPhonePlaceholder`, `profile.verification.codePlaceholder`
- `audioEffects.json` (en/fr/es/pt): add `closePanel`, `voiceCoder.key.notes.{C,Cs,D,Ds,E,F,Fs,G,Gs,A,As,B}`

### Components
- `user-settings.tsx`: wire 5 placeholders via existing + new t() keys
- `AudioEffectsPanel.tsx`: wire aria-label + 12 note SelectItems via t() keys
