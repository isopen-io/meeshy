# UI/UX Audit — Iteration 24 (2026-06-09)

> Base: dev after PR #534 (perf/ios-iter71). First iteration to include Android.
> Previous iterations 1–23 addressed: color tokens, aria-labels, Dynamic Type (MessageDetailSheet, DetailTab, PostDetailView, UploadProgressBar, ReportMessageSheet, Contacts tabs), media-viewer i18n (Text/PPTX/PDF/Audio/Video/Image lightboxes), toast i18n (Admin, AgentTimeline, AttachmentGallery, groups-layout, invite-modal, create-link, ScanControlPanel, AgentArchetypesTab, AgentConfigDialog, AgentConversationsTab, TriggerSchedulingModal, UserSecuritySection, TextViewer, CallManager), settings i18n (video, voiceQuality, audio formats, draft days, document, privacy, ConversationSettings v2, ConversationDrawer v2, AgentTopicsTab, user-settings placeholders, AudioEffectsPanel notes), iOS magic link keys.

---

## Part 1 — Web

### W1 — HIGH — `conversation-participants-drawer.tsx` — French toasts + role labels + aria-labels

**File:** `apps/web/components/conversations/conversation-participants-drawer.tsx`

- L304: `` toast.success(`Rôle mis à jour avec succès: ${roleNames[newRole]}`) `` → `t('participants.roleUpdated', { role: t('participants.roles.'+newRole) })`
- L329: `` toast.success(`${user.displayName} a été invité`) `` → `t('participants.invited', { name: user.displayName })`
- L364: `` toast.success(`${user.displayName} ajouté comme ${roleLabels[role]}`) `` → `t('participants.added', { name: user.displayName, role: t('participants.roles.'+role) })`
- L337–348: `getAssignableRoles()` returns hardcoded `{ label: 'Membre'/'Modérateur'/'Administrateur' }` — replace with i18n
- L483: `aria-label="Promouvoir"` → `t('participants.promote')`
- L503: `aria-label="Rétrograder"` → `t('participants.demote')`
- L599: `aria-label="Effacer la recherche"` → `t('common.clearSearch')`
- L613: `aria-label="Rafraîchir les statuts"` → `t('participants.refreshStatuses')`

### W2 — HIGH — `app/v2/(protected)/settings/page.tsx` — All toasts hardcoded French

**File:** `apps/web/app/v2/(protected)/settings/page.tsx`

No `useI18n` in file. Lines 196/199/203/224/226/235/238/526/625/647:
- `addToast('Langue de traduction mise a jour', ...)` → `t('v2settings.translationLangUpdated')`
- `addToast('Langue de l\'interface mise a jour', ...)` → `t('v2settings.uiLangUpdated')`
- `addToast('Mot de passe modifie avec succes', ...)` → `t('v2settings.passwordChanged')`
- `'Erreur lors du changement de mot de passe'` → `t('v2settings.passwordChangeError')`
- `addToast('Compte supprime', ...)` → `t('v2settings.accountDeleted')`
- `addToast('Erreur lors de la suppression du compte', ...)` → `t('v2settings.accountDeleteError')`
- `'Supprimer mon compte'` / `'Supprimer votre compte ?'` / `'Supprimer'` → locale keys

### W3 — HIGH — `app/v2/(protected)/me/page.tsx` — All toasts hardcoded French

**File:** `apps/web/app/v2/(protected)/me/page.tsx`

- L187: `'Deconnexion reussie'` → `t('v2me.logoutSuccess')`
- L190: `'Erreur lors de la deconnexion'` → `t('v2me.logoutError')`
- L198: `'Profil mis a jour avec succes'` → `t('v2me.profileUpdated')`
- L201: `'Erreur lors de la mise a jour du profil'` → `t('v2me.profileUpdateError')`
- L71/307: `'Modifier le profil'` → `t('v2me.editProfile')`

### W4 — HIGH — `app/v2/(protected)/chats/page.tsx` — Core chat view hardcoded French

**File:** `apps/web/app/v2/(protected)/chats/page.tsx`

- L483: `'Chargement...' : 'Charger plus de messages'` → `t('chats.loading') : t('chats.loadMore')`
- L492: `'Aucun message dans cette conversation'` → `t('chats.emptyState')`
- L493: `'Envoyez le premier message !'` → `t('chats.emptyStateCta')`

### W5 — HIGH — `components/common/Breadcrumb.tsx` — 16 French labels + 2 French aria-labels

**File:** `apps/web/components/common/Breadcrumb.tsx`

- L22–44: `DEFAULT_LABEL_MAP` contains 16 hardcoded French strings → replace with `t()` calls
- L69: `aria-label="Fil d'Ariane"` → `t('common.breadcrumb')`
- L75: `aria-label="Accueil"` → `t('common.home')`

### W6 — HIGH — `components/admin/user-detail/{3 files}` — French toasts, no i18n

**Files:**
- `UserContactInfoSection.tsx` L99/104: `'Informations de contact mises à jour'`, `'Erreur …'`
- `UserPersonalInfoSection.tsx` L64/69: `'Informations personnelles mises à jour'`, `'Erreur …'`

### W7 — HIGH — `components/common/message-composer/index.tsx` — French aria + visible text

- L276: `Réponse à {sender}` → `t('composer.replyTo', { sender })`
- L306: `aria-label="Annuler la réponse"` → `t('composer.cancelReply')`
- L413: `aria-label="Message input"` → `t('composer.messageInput')`
- L531: `aria-label="Sélectionner des fichiers …"` → `t('composer.attachFiles')`

### W8 — HIGH — `components/conversations/conversation-links-section.tsx` — No i18n

- L222: `'Aucune description'` → `t('conversations.links.noDescription')`
- L325: `'Chargement des liens...'` → `t('conversations.links.loading')`
- L330: `'Aucun lien de partage'` → `t('conversations.links.empty')`

### W9 — HIGH — `v2/layout` back buttons — French aria-labels

- `SplitViewLayout.tsx` L22: `aria-label="Retour"` → `t('common.back')`
- `PageHeader.tsx` L38: `aria-label="Retour à la liste"` → `t('common.backToList')`
- `RightPanelHeader.tsx` L42: `aria-label="Retour aux conversations"` → `t('common.backToConversations')`

### W10 — HIGH — `components/conversations/conversation-item/ConversationItem.tsx` — keyboard accessibility + French fallback

- L200: `'Anonyme' / 'Utilisateur'` → `t('common.anonymous') / t('common.user')`
- L207–215: Root `<div onClick>` missing `role="button"`, `tabIndex={0}`, `onKeyDown`

### W11 — MEDIUM — `components/v2/StoryComposer.tsx` — template literals + getCategoryLabel()

- L159/166: French toast template literals → `t()` with interpolation
- `getCategoryLabel()` returns French category names → i18n

### W12 — MEDIUM — `components/v2/StoryViewer.tsx` L900 — `aria-label="Envoyer"` slipped through

### W13 — MEDIUM — `invite-user-modal.tsx` L207 — `'Aucun utilisateur trouvé'`

### W14 — MEDIUM — `message-composer/ToolbarButtons.tsx` — English aria-labels (L71/98)

### W15 — MEDIUM — Missing `consentDialog` + `emailChange` keys in fr/es/pt settings.json

### W16 — MEDIUM — `MessageActionsBar.tsx` L398 — `aria-label={\`Réagir avec ${emoji}\`}` → `t('reactWith', { emoji })`

### W17 — LOW — `ConversationItemActions.tsx` L63 — `aria-label="Actions de la conversation"`

### W18 — LOW — `PushPermissionBanner.tsx` L75 — `aria-label="Dismiss"`

### W19 — LOW — `user-selector.tsx` + `DeliveryQueuePanel.tsx` + `trending-section.tsx` — French loading/empty states

---

## Part 2 — iOS

### I1 — HIGH — `BubbleFooter.swift` — 11 fixed fonts on every message bubble

**File:** `apps/ios/Meeshy/Features/Main/Views/BubbleFooter.swift`
Lines 39, 48, 92, 101, 124, 155, 177, 186, 244, 262, 264
- `size: 10/11` → `.caption2` / `.caption`
- `size: 12/13` → `.caption` / `.footnote`

### I2 — HIGH — `BubbleQuotedReply.swift` — fonts as small as size 6 on every quoted message

**File:** `apps/ios/Meeshy/Features/Main/Views/BubbleQuotedReply.swift`
Lines 113, 126, 221, 226, 233, 237, 280, 283, 288–325
- `size: 6/8/9` → `.caption2`
- `size: 10/11` → `.caption`
- `size: 13` → `.footnote`

### I3 — HIGH — `EmailVerificationView.swift` — auth-critical fixed fonts

**File:** `apps/ios/Meeshy/Features/Auth/Views/EmailVerificationView.swift`
Lines 66, 75, 98, 128, 130, 217, 222
- OTP `size: 32 monospaced` → `.title.monospaced`
- Success icon `size: 72` → `.system(.largeTitle)` scale

### I4 — HIGH — `ShareLinkDetailView.swift` — French hardcoded strings

**File:** `apps/ios/Meeshy/Features/Main/Views/ShareLinkDetailView.swift`
- `confirmationDialog` title `"Supprimer ce lien ?"` + buttons `"Supprimer"`, `"Annuler"`, destructive message
- Action labels: `"Copier"`, `"Partager"`, `"Désactiver"`, `"Activer"`
→ wrap all in `String(localized:)`

### I5 — HIGH — `CommunityLinkDetailView.swift` — French strings + fixed font

**File:** `apps/ios/Meeshy/Features/Main/Views/CommunityLinkDetailView.swift`
- `"Copier"`, `"Partager"`, `"Identifier"` → `String(localized:)`
- `Text("INFORMATIONS")` with `.font(.system(size: 12, weight: .semibold))` → `.font(.caption.weight(.semibold))`

### I6 — HIGH — `SecurityView.swift` — 61 fixed fonts + 21 unlabeled buttons

**File:** `apps/ios/Meeshy/Features/Main/Views/SecurityView.swift`
- Systematic pass: all `.font(.system(size: N))` → semantic styles
- Icon-only buttons: add `.accessibilityLabel(String(localized:))`

### I7 — HIGH — `BubbleSecondaryContent.swift` — Prisme translation UI, 5 fixed fonts

**File:** `apps/ios/Meeshy/Features/Main/Views/Bubble/BubbleSecondaryContent.swift`
Lines 36, 38, 73, 98, 249 → `.caption2` / `.caption`

### I8 — MEDIUM — `ReportUserView.swift` — safety-critical, 11 fixed fonts

### I9 — MEDIUM — `PrivacyPolicyView.swift` — legal content, 7 fixed fonts

### I10 — MEDIUM — `FloatingCallPillView.swift` — below 44pt touch targets + 5 fixed fonts

**File:** `apps/ios/Meeshy/Features/Main/Views/FloatingCallPillView.swift`
- Button frames `36×36` → `frame(minWidth: 44, minHeight: 44)`

### I11 — MEDIUM — `StatusBarView.swift` — 8 fixed fonts (size: 10 badge labels)

### I12 — MEDIUM — `TrackingLinksView.swift` + `TrackingLinkDetailView.swift` — `Color(hex: "A855F7")` bypasses MeeshyColors

### I13 — MEDIUM — `BubbleAttachmentView.swift` L131 — fixed `frame(width: 200, height: 120)` → adaptive

### I14 — MEDIUM — `MessageDaySeparator.swift` L19 — `.font(.system(size: 12, weight: .semibold))`

### I15 — MEDIUM — `FeedPostCard.swift` + `FeedView.swift` — 30+ fixed pixel fonts

### I16 — LOW — `ShareLinkDetailView.swift` + `CommunityLinkDetailView.swift` — hardcoded hex colors

### I17 — LOW — `BubbleCallNoticeView.swift` — 9 fixed fonts (size: 9 min)

### I18 — LOW — `RequestsTab.swift` L149/160 — accept/reject buttons 36×36 (< 44pt HIG minimum)

---

## Part 3 — Android (first iteration)

### A1 — HIGH/COMPILE — `FeedScreen.kt` — `MeeshySkeleton` does not exist (should be `MeeshySkeletonBox`)

**File:** `apps/android/feature/feed/src/main/kotlin/me/meeshy/app/feed/FeedScreen.kt`
- L47 import: `me.meeshy.ui.component.MeeshySkeleton` → `me.meeshy.ui.component.MeeshySkeletonBox`
- L156: `MeeshySkeleton(` → `MeeshySkeletonBox(`

### A2 — HIGH/COMPILE — `ProfileScreen.kt` + `NotificationsScreen.kt` — wrong param name `displayName`

- `ProfileScreen.kt` L111: `displayName = …` → `name = …`
- `NotificationsScreen.kt` L120: `displayName = …` → `name = …`

### A3 — HIGH — `MeeshyApp.kt` — unstable `startDestination` re-creates NavHost on auth change

- L99: `val startDestination = if (authState.isAuthenticated) Routes.CONVERSATIONS else Routes.LOGIN`
  → fix to: `val startDestination = remember { if (authState.isAuthenticated) Routes.CONVERSATIONS else Routes.LOGIN }`
  + add `LaunchedEffect(authState.isAuthenticated)` to navigate programmatically

### A4 — HIGH — `MeeshyApp.kt` L64–83 — null contentDescription on all 4 bottom nav icons

- All `Icon(Icons.*, null)` → `Icon(Icons.*, contentDescription = tab.label)`

### A5 — HIGH — Zero `stringResource` usage — full i18n bootstrap

Create `app/src/main/res/values/strings.xml` with all app-level strings, then replace literals in:
- `MeeshyApp.kt` (tab labels)
- `LoginScreen.kt`
- `ChatScreen.kt`
- `ConversationListScreen.kt`
- `NotificationsScreen.kt`
- `FeedScreen.kt`
- `ProfileScreen.kt`
- `ChatScreen.kt`

### A6 — HIGH — `FeedScreen.kt` L111 — author avatar `contentDescription = null`

- `contentDescription = null` → `contentDescription = post.author?.displayName ?: post.author?.username`

### A7 — MEDIUM — `ProfileScreen.kt` + `FeedScreen.kt` + `NotificationsScreen.kt` — missing `containerColor`

Add `containerColor = MeeshyTheme.tokens.backgroundPrimary` to Scaffolds.

### A8 — MEDIUM — `MeeshyPrimaryButton.kt` L44 — `Indigo200` disabled color breaks dark mode

- `SolidColor(MeeshyPalette.Indigo200)` → `SolidColor(MeeshyTheme.tokens.backgroundTertiary)`

### A9 — MEDIUM — `MeeshyAvatar.kt` L45 + `BrandLogo.kt` L42 — font scale ignores accessibility

- `fontSize = (size.value * N).sp` → `with(LocalDensity.current) { (size * N).toSp() }`

### A10 — MEDIUM — Feature screens — raw dp bypasses `MeeshySpacing` tokens

Replace `12.dp`→`MeeshySpacing.md`, `16.dp`→`MeeshySpacing.lg`, `24.dp`→`MeeshySpacing.xl`, `8.dp`→`MeeshySpacing.sm` across all feature screens.

### A11 — MEDIUM — `MeeshyApp.kt` L112 — `popUpTo(Routes.CONVERSATIONS)` hardcoded

→ `popUpTo(navController.graph.startDestinationId)`

### A12 — MEDIUM — No deep link configuration — FCM cannot open specific screens

Add `navDeepLink { uriPattern = "meeshy://chat/{conversationId}" }` to `Routes.CHAT` composable.

### A13 — MEDIUM — `ConversationListScreen.kt` L93–95 — clickable Row missing semantics role

Add `.semantics { role = Role.Button; contentDescription = conversation.displayTitle() }`

### A14 — MEDIUM — `FeedScreen.kt` L135 — static "Like" contentDescription

→ `contentDescription = if ((post.likeCount ?: 0) > 0) stringResource(R.string.unlike) else stringResource(R.string.like)`

### A15 — MEDIUM — `MeeshyPrimaryButton.kt` L56 — button label missing `MaterialTheme.typography.labelLarge`

### A16 — LOW — `ChatScreen.kt` L63 — hardcoded title "Messages", no conversation name

### A17 — LOW — `FeedScreen.kt` L103/158 — `RoundedCornerShape(16.dp)` not in radius token scale

→ `RoundedCornerShape(MeeshyRadius.xl)` (20dp) or `MeeshyRadius.lg` (14dp)

### A18 — LOW — `ProfileScreen.kt` L3 — unused `background` import

---

## Summary

| Platform | HIGH | MEDIUM | LOW | Total |
|----------|------|--------|-----|-------|
| Web | 10 | 6 | 3 | 19 |
| iOS | 7 | 8 | 3 | 18 |
| Android | 6 | 8 | 3 | 17 |
| **Total** | **23** | **22** | **9** | **54** |

---

## Status (annotated 2026-06-12, iteration 40)

Majority implemented across iter-24 commits (b6980975, 394bbd33, bdb8fced, b3e51ac6, c5c24c21).
Verified leftovers were closed in **iteration 40** (see `2026-06-12-iteration-40.md`):
W5 (Breadcrumb label map), W11 (StoryComposer), W19 (user-selector, DeliveryQueuePanel),
W6 remnants (user-detail card titles/buttons), W8 remnants (links-section labels),
I1 (BubbleFooter fonts), I11 (StatusBarView fonts).
Still deferred (tracked in iteration 40 plan — do not re-analyze): I12/I16 hex-color token
refactor, I15 FeedPostCard/FeedView fonts. iter-24 is otherwise CLOSED.

## ✅ Status : itération soldée — PR #543 mergée dans main
Annoté le 2026-06-12 (iter-46w). Reliquats (I12/I16 hex tokens, I15 fonts) repris par les itérations 40+ et tracés dans branch-tracking.md. NE PAS retraiter les findings ci-dessus.
