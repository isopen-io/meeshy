# UI/UX Audit — Iteration 19 (2026-06-09)

> Base: main after PR #434 (iteration 18). All findings verified against current code.
> Previous iterations 1–18 addressed: color tokens, aria-labels, Dynamic Type, accessibility labels, media controls i18n, TextLightbox/PPTXLightbox/SimpleAudioPlayer/PDFViewer/AudioRecorderCard (partial)/UserSecuritySection, iOS magic link keys, VideoLightbox/ImageLightbox, ScanControlPanel/AgentArchetypesTab/AgentConfigDialog/AgentConversationsTab/TriggerSchedulingModal/TextViewer/CallManager.

---

## Web Issues

### HIGH — French/English hardcoded toasts in AdminLayout (2 instances)

**File:** `apps/web/components/admin/AdminLayout.tsx`
No `useI18n` in file.

| Line | Before | After |
|------|--------|-------|
| 69 | `'Déconnexion réussie'` | `t('logoutSuccess')` NEW in admin.json |
| 73 | `'Erreur lors de la déconnexion'` | `t('logoutError')` NEW in admin.json |

---

### HIGH — French toasts in AgentScheduleTimeline (3 instances)

**File:** `apps/web/components/admin/agent/AgentScheduleTimeline.tsx`
No `useI18n` in file. All 3 keys already exist in admin.json.

| Line | Before | After |
|------|--------|-------|
| 82 | `'Scan déclenché'` | `t('agent.toasts.scanTriggered')` ✅ key exists |
| 85 | `'Erreur lors du déclenchement'` | `t('agent.toasts.scanTriggerError')` ✅ key exists |
| 88 | `'Erreur réseau'` | `t('agent.toasts.networkError')` ✅ key exists |

---

### HIGH — French toasts in AttachmentGallery (2 instances + 1 title)

**File:** `apps/web/components/attachments/AttachmentGallery.tsx`
File already has `useI18n('attachments')` → `t`.

| Line | Before | After |
|------|--------|-------|
| 104 | `'Fichier supprimé avec succès'` | `t('gallery.deleteSuccess')` NEW |
| 108 | `'Impossible de supprimer le fichier'` | `t('gallery.deleteError')` NEW |
| 300 | `title="Supprimer"` | `title={t('gallery.delete')}` NEW |

---

### HIGH — French toasts in useAttachmentDeletion hook (2 instances)

**File:** `apps/web/components/attachments/hooks/useAttachmentDeletion.ts`
No `useI18n` — duplicates AttachmentGallery toast logic.

| Line | Before | After |
|------|--------|-------|
| 44 | `'Fichier supprimé avec succès'` | `t('gallery.deleteSuccess')` ✅ same new key |
| 48 | `'Impossible de supprimer le fichier'` | `t('gallery.deleteError')` ✅ same new key |

---

### HIGH — French toasts in groups-layout-responsive (2 instances)

**File:** `apps/web/components/groups/groups-layout-responsive.tsx`
File already has `useI18n('groups')` → `tGroups`. Keys already exist.

| Line | Before | After |
|------|--------|-------|
| 142 | `'Erreur lors du chargement des groupes'` | `tGroups('groups.errors.loadError')` ✅ key exists |
| 169/173 | `'Erreur lors du chargement du groupe'` | `tGroups('groups.errors.loadingGroupError')` ✅ key exists |

---

### HIGH — French toasts in invite-user-modal (4 instances)

**File:** `apps/web/components/conversations/invite-user-modal.tsx`
File already has `useI18n('conversations')` → `t`.

| Line | Before | After |
|------|--------|-------|
| 64/68 | `'Erreur lors de la recherche d\'utilisateurs'` | `t('conversations.inviteModal.searchError')` NEW |
| 113 | `` `${n} utilisateur(s) invité(s) avec succès` `` | `t('conversations.inviteModal.inviteSuccess', { count: n })` NEW |
| 126 | `` `${n} invitation(s) ont échoué` `` | `t('conversations.inviteModal.partialError', { count: n })` NEW |
| 131 | `'Erreur lors de l\'invitation des utilisateurs'` | `t('conversations.inviteModal.inviteError')` NEW |

---

### HIGH — French toast in create-link-button (1 instance)

**File:** `apps/web/components/conversations/create-link-button.tsx`
File already has `useI18n('conversations')` → `t`.

| Line | Before | After |
|------|--------|-------|
| 248 | `'Erreur lors de la récupération de la conversation'` | `t('conversations.createLinkButton.fetchError')` NEW |

---

### HIGH — Missed French/English toasts in AudioRecorderCard (5 instances)

**File:** `apps/web/components/audio/AudioRecorderCard.tsx`
File already has `useI18n('audioEffects')` → `t`. Keys mostly exist.

| Line | Before | After |
|------|--------|-------|
| 196 | `'Audio recording requires HTTPS.'` | `t('audioEffects.recorder.errors.httpsRequired')` ✅ key exists |
| 203 | `'Your browser does not support audio recording.'` | `t('audioEffects.recorder.errors.browserNotSupported')` ✅ key exists |
| 303 | `'Accès au microphone refusé.'` | `t('audioEffects.recorder.errors.microphoneAccessDenied')` ✅ key exists |
| 336 | `'Erreur lors de la lecture'` | `t('audioEffects.recorder.errors.playbackError')` NEW |
| 486 | `'Erreur de lecture audio'` | `t('audioEffects.recorder.errors.playbackError')` ✅ same new key |

---

## iOS Issues

### MEDIUM — Dynamic Type violations in Contacts empty states (4 instances)

Each file uses `.font(.system(size: 48/32, weight: .light))` for empty-state SF Symbol icons.
These should scale with Dynamic Type.

| File | Line | Before | After |
|------|------|--------|-------|
| `ContactsListTab.swift` | 202 | `.font(.system(size: 48, weight: .light))` | `.font(.system(.largeTitle).weight(.light))` |
| `DiscoverTab.swift` | 173 | `.font(.system(size: 32, weight: .light))` | `.font(.system(.title).weight(.light))` |
| `BlockedTab.swift` | 114 | `.font(.system(size: 48, weight: .light))` | `.font(.system(.largeTitle).weight(.light))` |
| `RequestsTab.swift` | 263 | `.font(.system(size: 48, weight: .light))` | `.font(.system(.largeTitle).weight(.light))` |

---

## New locale keys summary

### admin.json (en/fr/es/pt):
- Top-level: `logoutSuccess`, `logoutError`

### attachments.json (en/fr/es/pt) — under `gallery`:
- `deleteSuccess`, `deleteError`, `delete`

### conversations.json (en/fr/es/pt) — under `conversations`:
- New `inviteModal` section: `searchError`, `inviteSuccess`, `partialError`, `inviteError`
- Under `createLinkButton`: `fetchError`

### audioEffects.json (en/fr/es/pt) — under `audioEffects.recorder.errors`:
- `playbackError`

---

## Summary

| ID | Severity | File | Issue |
|----|----------|------|-------|
| W1 | HIGH | admin.json ×4 | 2 new top-level keys |
| W2 | HIGH | attachments.json ×4 | 3 new gallery keys |
| W3 | HIGH | conversations.json ×4 | 5 new keys (inviteModal + createLinkButton) |
| W4 | HIGH | audioEffects.json ×4 | 1 new recorder.errors key |
| W5 | HIGH | AdminLayout.tsx | 2× French toasts → t() |
| W6 | HIGH | AgentScheduleTimeline.tsx | 3× French toasts → t() (keys exist) |
| W7 | HIGH | AttachmentGallery.tsx | 2× French toasts + 1 title → t() |
| W8 | HIGH | useAttachmentDeletion.ts | 2× French toasts → t() |
| W9 | HIGH | groups-layout-responsive.tsx | 2× French toasts → t() (keys exist) |
| W10 | HIGH | invite-user-modal.tsx | 4× French toasts → t() |
| W11 | HIGH | create-link-button.tsx | 1× French toast → t() |
| W12 | HIGH | AudioRecorderCard.tsx | 5× missed French/English toasts → t() |
| I1 | MEDIUM | ContactsListTab.swift | Dynamic Type fix |
| I2 | MEDIUM | DiscoverTab.swift | Dynamic Type fix |
| I3 | MEDIUM | BlockedTab.swift | Dynamic Type fix |
| I4 | MEDIUM | RequestsTab.swift | Dynamic Type fix |
