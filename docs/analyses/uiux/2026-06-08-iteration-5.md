# UI/UX Analysis — Iteration 5 (2026-06-08)

## Context
Builds on iterations 1-4. Previous work covered: MeeshyColors brand migration, Dynamic Type,
viewer i18n (PDF/PPTX/Markdown), admin panel i18n, MarkdownViewer i18n, AdminLayout dark mode,
UniversalComposerBar clipboard preview colors.

## Findings

### 1. Web — Hardcoded French Strings (High Priority)
Components still containing French text literals not wired through `useI18n()`:

| File | French String(s) |
|------|-----------------|
| `components/join/AnonymousForm.tsx:116,154` | `"Ce pseudo est déjà utilisé"` |
| `components/groups/groups-layout-responsive.tsx:402,403,576,590,592` | Empty state, date label, prompts |
| `components/admin/agent/AgentOverviewTab.tsx:312` | `"Aucune activité récente"` |
| `components/admin/agent/AgentLiveTab.tsx:451` | `"Sélectionnez une conversation..."` |
| `components/admin/ranking/RankingFilters.tsx:69,106,151` | Select placeholder strings (3×) |
| `components/admin/user-detail/UserLanguageSection.tsx:96` | `"Préférences de Langue"` |
| `components/translation/translation-monitor.tsx:156,270` | Loading/section headers |
| `components/ui/language-select.tsx:103` | `"Aucune langue trouvée"` |

### 2. Web — Accessibility Gaps (High Priority)
Icon-only `<Button>` elements missing `aria-label`:

| File | Line | Icon | Suggested Label |
|------|------|------|----------------|
| `components/admin/agent/AgentConversationsTab.tsx:328` | `<Settings />` | `"Edit agent configuration"` |
| `components/admin/agent/AgentConversationsTab.tsx:335` | `<Trash2 />` | `"Delete configuration"` |
| `components/admin/agent/TriggerSchedulingModal.tsx:298` | `<X />` | `"Cancel schedule"` |
| `components/admin/user-detail/UserPicker.tsx:51` | `<Plus />` | `"Add user"` |
| `components/admin/user-detail/ConversationPicker.tsx:120` | `<X />` | `"Clear selection"` |
| `components/audio/AudioEffectsCarousel.tsx:104` | `<X />` | `"Close audio effects"` |
| `components/admin/CustomizationManager.tsx:143,211` | `<X />` | `"Cancel edit"` |

### 3. iOS — Accessibility Gaps (High Priority)
Button + Image(systemName:) combos without `.accessibilityLabel()`:

| File | Line | Suggested Label |
|------|------|----------------|
| `OnboardingStepViews.swift:43` | eye/eye.slash toggle | `"Toggle password visibility"` |
| `OnboardingStepViews.swift:326` | arrow.right.circle skip | `"Skip step"` |
| `OnboardingStepViews.swift:771` | xmark.circle.fill clear | `"Clear search"` |
| `BubbleFooter.swift:153` | translate icon | `"Translate message"` |
| `MagicLinkView.swift:236` | arrow.clockwise retry | `"Resend magic link"` |
| `MiniAudioPlayerBar.swift:161` | forward.fill skip | `"Skip to next audio"` |

### 4. Web — Dark Mode Gap (Low Priority)
`components/admin/ranking/RankingFilters.tsx:109` — `bg-white` missing `dark:bg-gray-950`
on the sticky filter header.

### 5. iOS — Color(hex:) Literals (Medium Priority, Categorize First)
170+ remaining occurrences across:
- `UniversalComposerBar.swift` (47×) — recording states, ephemeral message gradients
- `ConversationInfoSheet.swift` (24×) — member role colors, error states
- `ConversationDashboardView.swift` (24×) — sentiment analytics score colors
- `ConversationPreferencesTab.swift` (22×) — feature flag icon colors
- `UniversalComposerBar+Recording.swift` (18×) — recording gradients

Many use `accentColor` variable (intentional). Separate the static hex literals
(→ MeeshyColors) from the dynamic ones (→ keep).

## Priority Order for Iteration 5
1. Web accessibility: aria-label on 7 icon-only buttons (quick, high WCAG impact)
2. iOS accessibility: .accessibilityLabel on 6 buttons (quick, VoiceOver)
3. RankingFilters dark mode (1-line fix)
4. Web i18n: groups-layout, AgentOverviewTab, AgentLiveTab, RankingFilters, language-select
