# Itération 82i — Dynamic Type `AudioFullscreenView`

**Date** : 2026-07-01
**Piste** : iOS (suffixe `i`)
**Branche** : `claude/upbeat-euler-l50chc` (base = `main` HEAD post-78i)
**Surface** : `apps/ios/Meeshy/Features/Main/Views/AudioFullscreenView.swift`

## Contexte

Poursuite du différé prioritaire iOS « Dynamic Type grandes surfaces restantes,
une par itération » (soldé jusqu'ici : `ConversationDashboardView` 74i,
`VoiceProfileManageView` 75i #1150, `FeedCommentsSheet` 72i, `2FA` 71i,
`InviteFriendsSheet` 76i). `AudioFullscreenView` (lecteur audio plein écran :
transport, seek bar, transcription, sélecteur de langue Prisme) portait **26**
sites `.font(.system(size:))` figés — 2e plus grosse surface encore non prise
et hors de toute PR ouverte au moment de l'analyse (vérifié via
`list_pull_requests` : 81i ConversationLockSheet, 80i CountryPicker, 79i
story-viewer/router, 78i feed-composer/PrivacySettings/link-preview/colors —
aucune ne touche `AudioFullscreenView`).

## Diagnostic

26 sites `.font(.system(size:))`. Grille de décision identique à 74i :
**texte-de-lecture → `MeeshyFont.relative(size, weight:, design:)`** (scale avec
Dynamic Type, poids + design monospaced préservés) ; **glyphes SF Symbol dans un
cadre fixe + emoji → figés** (un glyphe qui grossit dans un cercle de taille
fixe déborderait ; les drapeaux emoji ne scalent pas utilement).

### Migrés (13 — texte-de-lecture)
| Ligne | Élément | Note |
|---|---|---|
| 315 | Compteur de page `N / M` | monospaced préservé |
| 325 | Durée `attachment.durationFormatted` | monospaced |
| 329 | Badge codec (uppercased) | monospaced |
| 367 | Nom expéditeur | |
| 372 | Date du message | |
| 385 | Taille de fichier | |
| 576, 582 | Chrono temps courant / durée (time row) | monospaced, ×2 identiques |
| 597 | Label vitesse de lecture (`1x`…) | monospaced |
| 639 | Texte état-vide « Aucune transcription » | |
| 655 | Label bouton « Transcrire » | |
| 751 | Label pastille de langue (weight conditionnel préservé) | |
| 786 | Nom de langue dans le picker (weight conditionnel préservé) | |

### Figés (13 — documentés, NE PAS re-flagger)
- **Glyphes de contrôle en cadre fixe** : `xmark` close 36×36 (304), bouton
  download 36×36 (408), transport `gobackward.10`/`goforward.10` 28 (491, 524),
  `play.fill`/`pause.fill` dans cercle 64×64 (512), `translate` 26×26 (722).
- **Icônes décoratives / listes** : `waveform` 10 inline taille-fichier (381),
  icône état-vide `text.word.spacing` 28 (635, parité 74i empty-state icon 24),
  `waveform.and.mic` 13 dans le bouton transcrire (652), `checkmark.circle.fill`
  16 (793) + `speaker.wave.2.fill` 14 (799) dans les lignes du picker.
- **Emoji** : drapeau de pastille (749), drapeau de ligne picker 20 (783)
  (parité 74i émoji sentiment).

## Implémentation

- Swap mécanique littéral → helper existant `MeeshyFont.relative` (déjà utilisé
  par 74i dans le target app ; `import MeeshyUI` déjà présent ligne 4 — aucun
  import neuf).
- 0 logique modifiée, 0 test neuf (sweep typographique pur, parité 55i/71i/72i/74i).
- Gate = CI `iOS Tests`.

## Vérification
- `grep MeeshyFont.relative` → 13 ; `grep .font(.system(size:)` restants → 13
  (tous glyphes/emoji classés ci-dessus). Total conservé = 26.
- Poids/design monospaced préservés site par site.

## Annotation de clôture
✅ **Dynamic Type `AudioFullscreenView` soldé (82i).** NE PLUS re-flagger cette
surface ni ses 13 sites figés (décisions design documentées ci-dessus). Prochaine
grande surface Dynamic Type candidate : `StoryViewerView+Content` (38, attention
i18n 79i en vol) ou `FeedView+Attachments` (30).
